---
alwaysApply: false
---

import AVFoundation
import AudioToolbox
import Combine
import CoreGraphics
import CoreTransferable
import CryptoKit
import DeveloperToolsSupport
import Foundation
import GameKit
import KeyboardKit.Swift
import Speech
import SwiftUI
import UIKit
import UniformTypeIdentifiers

//! Project version number for KeyboardKit.
public var KeyboardKitVersionNumber: Double

//! Project version string for KeyboardKit.
public let KeyboardKitVersionString: <<error type>>

/// This namespace contains autocomplete-related types.
public struct Autocomplete {
}

extension Autocomplete {

    /// This view mimics a native autocomplete separator.
    ///
    /// You can style this component with the style modifier
    /// ``autocompleteToolbarSeparatorStyle(_:)``.
    @MainActor @preconcurrency public struct ToolbarSeparator : View {

        /// Create an autocomplete toolbar item separator.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This style can be used to modify the visual style of
    /// the ``Autocomplete/ToolbarSeparator`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/autocompleteToolbarSeparatorStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ToolbarSeparatorStyle : KeyboardKit.KeyboardModel {

        /// Create a custom toolbar separator style.
        ///
        /// - Parameters:
        ///   - color: The separator color, by default `.secondary.opacity(0.5)`.
        ///   - width: The separator width, by default `1`.
        ///   - height: The separator height, by default `30`.
        public init(color: Color = .secondary.opacity(0.5), width: CGFloat = 1, height: CGFloat = 30)

        /// The separator color.
        public var color: Color

        /// The separator width.
        public var height: CGFloat

        /// The separator height.
        public var width: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Autocomplete.ToolbarSeparatorStyle, b: KeyboardKit.Autocomplete.ToolbarSeparatorStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Autocomplete {

    /// This toolbar can be added above the keyboard to show
    /// autocomplete suggestions as the user types.
    ///
    /// The toolbar will separate all emoji suggestions from
    /// the provided suggestions and drop the last non-emoji
    /// suggestion if emojis exist. It shows max three emoji
    /// suggestions, and only one if there are more than two
    /// visible non-emoji suggestions.
    ///
    /// You can style this component with the style modifier
    /// ``autocompleteToolbarStyle(_:)``.
    @MainActor @preconcurrency public struct Toolbar<ItemView, SeparatorView> : View where ItemView : View, SeparatorView : View {

        /// Create a toolbar with standard views.
        ///
        /// - Parameters:
        ///   - suggestions: The suggestions to display.
        ///   - suggestionAction: The action to run when tapping a suggestion.
        @MainActor @preconcurrency public init(suggestions: [KeyboardKit.Autocomplete.Suggestion], suggestionAction: @escaping KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.SuggestionAction) where ItemView == KeyboardKit.Autocomplete.ToolbarItem, SeparatorView == KeyboardKit.Autocomplete.ToolbarSeparator

        /// Create a toolbar with custom views.
        ///
        /// Just return `{ $0.view }` in any view builder to
        /// return the standard view.
        ///
        /// - Parameters:
        ///   - suggestions: The suggestions to display.
        ///   - itemView: The suggestion view builder to use.
        ///   - separatorView: The separator view builder to use.
        ///   - suggestionAction: The action to run when tapping a suggestion.
        @MainActor @preconcurrency public init(suggestions: [KeyboardKit.Autocomplete.Suggestion], itemView: @escaping KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.ItemViewBuilder, separatorView: @escaping KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.SeparatorViewBuilder, suggestionAction: @escaping KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.SuggestionAction)

        public typealias Item = KeyboardKit.Autocomplete.ToolbarItem

        public typealias ItemViewBuilder = (KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.ItemParams) -> ItemView

        public typealias Separator = KeyboardKit.Autocomplete.ToolbarSeparator

        public typealias SeparatorViewBuilder = (KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.SeparatorParams) -> SeparatorView

        public typealias Suggestion = KeyboardKit.Autocomplete.Suggestion

        public typealias SuggestionAction = (KeyboardKit.Autocomplete.Toolbar<ItemView, SeparatorView>.Suggestion) -> Void

        public struct ItemParams {
        }

        public struct SeparatorParams {
        }

        public typealias StandardItem = KeyboardKit.Autocomplete.ToolbarItem

        public typealias StandardSeparator = KeyboardKit.Autocomplete.ToolbarSeparator

        public typealias Style = KeyboardKit.Autocomplete.ToolbarStyle

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This style can be used to modify the visual style of
    /// the ``Autocomplete/Toolbar`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/autocompleteToolbarStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ToolbarStyle : KeyboardKit.KeyboardModel {

        /// Create a custom autocomplete toolbar style.
        ///
        /// - Parameters:
        ///   - height: An optional, fixed height, by default ``Keyboard/ToolbarStyle/standardHeight``.
        ///   - padding: An optional, fixed edge padding, by default `4`.
        ///   - item: The style to apply to the toolbar items, by default `.standard`.
        ///   - separator: The style to apply to autocorrect items `.standardAutocorrect`.
        ///   - autocorrectItem: The autocorrect background style, by default `.standard`.
        public init(height: CGFloat? = nil, padding: CGFloat = 4, item: KeyboardKit.Autocomplete.ToolbarItemStyle = .standard, autocorrectItem: KeyboardKit.Autocomplete.ToolbarItemStyle = .standardAutocorrect, separator: KeyboardKit.Autocomplete.ToolbarSeparatorStyle = .standard)

        /// An optional, fixed toolbar height.
        public var height: CGFloat?

        /// The toolbar's edge padding.
        public var padding: CGFloat

        /// The style to apply to the toolbar items.
        public var item: KeyboardKit.Autocomplete.ToolbarItemStyle

        /// The style to apply to autocorrect toolbar items.
        public var autocorrectItem: KeyboardKit.Autocomplete.ToolbarItemStyle

        /// The style to apply to toolbar separators.
        public var separator: KeyboardKit.Autocomplete.ToolbarSeparatorStyle

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Autocomplete.ToolbarStyle, b: KeyboardKit.Autocomplete.ToolbarStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Autocomplete {

    /// This screen can be used as the autocomplete settings
    /// screen in your app or keyboard extension.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``autocompleteSettingsScreenLocalization(_:)``
    /// * ``autocompleteSettingsScreenVisibility(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen<Header, AutocompleteSection, NextWordPredictionSection, Footer> : View where Header : View, AutocompleteSection : View, NextWordPredictionSection : View, Footer : View {

        /// Create a custom screen.
        ///
        /// - Parameters:
        ///   - header: The view to show at the top of the list.
        ///   - autocompleteSection: The autocomplete section content.
        ///   - nextWordPredictionSection: The next word prediction section content.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(@ViewBuilder header: @escaping () -> Header, @ViewBuilder autocompleteSection: @escaping KeyboardKit.Autocomplete.SettingsScreen<Header, AutocompleteSection, NextWordPredictionSection, Footer>.AutocompleteSectionBuilder, @ViewBuilder nextWordPredictionSection: @escaping KeyboardKit.Autocomplete.SettingsScreen<Header, AutocompleteSection, NextWordPredictionSection, Footer>.NextWordSectionBuilder, @ViewBuilder footer: @escaping () -> Footer)

        public typealias Sections = KeyboardKit.Autocomplete.SettingsScreenSections

        public typealias AutocompleteSectionBuilder = (KeyboardKit.Autocomplete.SettingsScreen<Header, AutocompleteSection, NextWordPredictionSection, Footer>.Sections.AutocompleteParams) -> AutocompleteSection

        public typealias NextWordSectionBuilder = (KeyboardKit.Autocomplete.SettingsScreen<Header, AutocompleteSection, NextWordPredictionSection, Footer>.Sections.NextWordPredictionParams) -> NextWordPredictionSection

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Autocomplete {

    /// This type contains setting section views and builder
    /// parameters that are used by the settings screen.
    ///
    /// You can use these sections as stand-alone views that
    /// can be added to any other screen.
    public struct SettingsScreenSections {
    }
}

extension Autocomplete {

    /// This view mimics a native autocomplete toolbar item.
    ///
    /// You can style this component with the style modifier
    /// ``autocompleteToolbarItemStyle(_:)``.
    @MainActor @preconcurrency public struct ToolbarItem : View {

        /// Create an autocomplete toolbar item.
        ///
        /// - Parameters:
        ///   - suggestion: The suggestion to display.
        @MainActor @preconcurrency public init(suggestion: KeyboardKit.Autocomplete.Suggestion)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This style can be used to modify the visual style of
    /// the ``Autocomplete/ToolbarItem`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/autocompleteToolbarItemStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ToolbarItemStyle : KeyboardKit.KeyboardModel {

        /// Create a custom autocomplete toolbar item style.
        ///
        /// - Parameters:
        ///   - titleColor: The title color to use, by default `.primary`.
        ///   - titleFont: The title font to use, by default `.body`.
        ///   - subtitleColor: The subtitle color to use, by default `.primary`.
        ///   - subtitleFont: The subtitle font to use, by default `.footnote`.
        ///   - horizontalPadding: The horizontal padding to apply, by default `4`.
        ///   - verticalPadding: The vertical padding to apply, by default `10`.
        ///   - backgroundColor: The background color to use, by default `.clear`.
        ///   - backgroundCornerRadius: The background color to use, by default `6`.
        public init(titleColor: Color = .primary, titleFont: KeyboardKit.KeyboardFont = .body, subtitleColor: Color = .primary, subtitleFont: KeyboardKit.KeyboardFont = .footnote, horizontalPadding: Double = 2, verticalPadding: Double = 10, backgroundColor: Color = .clear, backgroundCornerRadius: CGFloat = 4)

        /// The title font to use.
        public var titleFont: KeyboardKit.KeyboardFont

        /// The title color to use.
        public var titleColor: Color

        /// The subtitle font to use.
        public var subtitleFont: KeyboardKit.KeyboardFont

        /// The subtitle color to use.
        public var subtitleColor: Color

        /// The horizontal padding to apply.
        public var horizontalPadding: Double

        /// The vertical padding to apply.
        public var verticalPadding: Double

        public var backgroundColor: Color

        public var backgroundCornerRadius: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Autocomplete.ToolbarItemStyle, b: KeyboardKit.Autocomplete.ToolbarItemStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Autocomplete {

    /// This type represents an autocomplete suggestion that
    /// is returned by an ``AutocompleteService``.
    ///
    /// Suggestions can either be standard ones that require
    /// users to manually apply them, or autocorrecting ones
    /// that are auto-applied when a word delimiter is typed.
    ///
    /// Native keyboards typically present an autocorrecting
    /// suggestion with a semi-white background, and unknown
    /// or current words with locale-specific quotation.
    public struct Suggestion : KeyboardKit.KeyboardModel {

        /// Create an autocomplete suggestion.
        ///
        /// - Parameters:
        ///   - text: The text that should be sent to the proxy.
        ///   - type: The type of suggestion this is, by default `.regular`.
        ///   - title: The text that should be displayed, by default `text`.
        ///   - subtitle: An optional subtitle that can complete the title, by default `nil`.
        ///   - source: An optional source to describe where the suggestion is from, by default `nil`.
        ///   - additionalDeleteCount: An optional delete count, to account for trigger characters, by default `0`.
        ///   - additionalInfo: An optional info dictionary, by default `empty`.
        public init(text: String, type: KeyboardKit.Autocomplete.SuggestionType = .regular, title: String? = nil, subtitle: String? = nil, source: String? = nil, additionalDeleteCount: Int = 0, additionalInfo: [String : String] = [:])

        /// The text that should be sent to the proxy.
        public var text: String

        /// The type of suggestion this is.
        public var type: KeyboardKit.Autocomplete.SuggestionType

        /// The text that should be displayed.
        public var title: String

        /// An optional subtitle that can complete the title.
        public var subtitle: String?

        /// An optional source to describe where the suggestion is from.
        public var source: String?

        /// An optional, additional delete count, to account for trigger characters.
        public var additionalDeleteCount: Int

        /// An optional info dictionary.
        public var additionalInfo: [String : String]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Autocomplete.Suggestion, b: KeyboardKit.Autocomplete.Suggestion) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Autocomplete {

    /// This dictionary can be used to define localized text
    /// replacements, for instance custom autocorrections.
    public struct TextReplacementDictionary {

        public init(_ initialValue: KeyboardKit.Autocomplete.TextReplacementDictionary.Dictionary = .init())

        public typealias Dictionary = Locale.Dictionary<[String : String]>
    }
}

extension Autocomplete {

    /// This enum defines every autocomplete suggestion type
    /// that can be returned by an ``AutocompleteService``.
    public enum SuggestionType : String, CaseIterable, KeyboardKit.KeyboardModel {

        /// A regular suggestion is applied when it's tapped.
        ///
        /// Native keyboards display all regular suggestions
        /// as plain text, but you can customize this.
        case regular

        /// An autocorrect suggestion is applied when a user
        /// types a word or sentence delimiter.
        ///
        /// Native keyboards display autocorrect suggestions
        /// with a rounded white background.
        case autocorrect

        /// An emoji suggestion can be grouped into a single
        /// slot in the autocomplete toolbar.
        ///
        /// Native keyboards display these emoji suggestions
        /// like reular suggestions, but in a tighter space.
        case emoji

        /// These suggestions can be used when the currently
        /// typed word is unknown, to automatically learn it.
        ///
        /// Native keyboards display unknown suggestions and
        /// the current word although known, with quotes.
        case unknown

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Autocomplete.SuggestionType]

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Autocomplete.SuggestionType] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Autocomplete {

    /// This type can be used to configure the localizations
    /// of a ``Autocomplete/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/autocompleteSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Autocomplete", autocompleteSection: String? = nil, autocompleteSectionAutocomplete: String = "Auto-Complete", autocompleteSectionAutocorrect: String = "Auto-Correction", autocompleteSectionAutolearn: String = "Auto-Learn Words", autocompleteSectionEmojis: String = "Emoji Suggestions", autocompleteSectionSuggestions: String = "Suggestions", autocompleteSectionFooter: String? = "Emoji autocomplete requires that the word starts with a colon.", nextWordPredictionSection: String = "Next Word Prediction", nextWordPredictionSectionApiKeyPlaceholder: String = "Enter API Key...", nextWordPredictionSectionApiKeyLink: String = "Get an API Key...", nextWordPredictionSectionApiKeyFooter: String? = "Enter an API key to enable Next Word Prediction.", nextWordPredictionAlertTitle: String = "Important!", nextWordPredictionAlertMessage: String = "Next word prediction will send the text you type to the service you select, using the API key you specify. Do you want to enable this feature?", nextWordPredictionAlertOk: String = "Enable")

        public var title: String

        public var autocompleteSection: String?

        public var autocompleteSectionAutocomplete: String

        public var autocompleteSectionAutocorrect: String

        public var autocompleteSectionAutolearn: String

        public var autocompleteSectionEmojis: String

        public var autocompleteSectionSuggestions: String

        public var autocompleteSectionFooter: String?

        public var nextWordPredictionSection: String

        public var nextWordPredictionSectionApiKeyPlaceholder: String

        public var nextWordPredictionSectionApiKeyLink: String

        public var nextWordPredictionSectionApiKeyFooter: String?

        public var nextWordPredictionAlertTitle: String

        public var nextWordPredictionAlertMessage: String

        public var nextWordPredictionAlertOk: String
    }
}

extension Autocomplete {

    /// This picker can be used to let a user pick next word
    /// prediction request types and specify an API key.
    @MainActor @preconcurrency public struct NextWordPredictionTypePicker : View {

        @MainActor @preconcurrency public init(_ title: String, apiKeyPlaceholder: String, apiKeyLinkTitle: String, isTextFieldFocused: FocusState<Bool>.Binding? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Autocomplete {

    /// This type represents an autocomplete result with all
    /// potential values.
    public struct Result {

        /// Create an autocomplete result for an `inputText`.
        ///
        /// - Parameters:
        ///   - inputText: The originally provided text.
        ///   - suggestions: Resulting autocomplete suggestions.
        ///   - emojiSuggestions: Emoji completion matches, if any.
        ///   - nextCharacterPredictions: Next character predictions, if any.
        ///   - isOutdated: Whether the result is outdated and should be ignored.
        public init(inputText: String, suggestions: [KeyboardKit.Autocomplete.Suggestion], emojiSuggestions: [KeyboardKit.Autocomplete.Suggestion]? = nil, nextCharacterPredictions: [Character : Double]? = nil, isOutdated: Bool = false)

        /// The originally provided text.
        public let inputText: String

        /// Suggestions for the provided texts.
        public let suggestions: [KeyboardKit.Autocomplete.Suggestion]

        /// Emoji suggestions, if any.
        public let emojiSuggestions: [KeyboardKit.Autocomplete.Suggestion]?

        /// Next character predictions for the provided text, if any.
        public let nextCharacterPredictions: [Character : Double]?

        /// Whether the result is outdated and should be ignored.
        public let isOutdated: Bool
    }
}

extension Autocomplete {

    /// This view modifier can be applied to a view, to make
    /// it honor `.autocorrectionDisabled()`.
    @MainActor @preconcurrency public struct AutocorrectionDisabledToContextModifer : ViewModifier {

        /// Create a modifier instance for a certain context.
        @MainActor @preconcurrency public init(context: KeyboardKit.AutocompleteContext)

        /// Gets the current body of the caller.
        ///
        /// `content` is a proxy for the view that will have the modifier
        /// represented by `Self` applied to it.
        @MainActor @preconcurrency public func body(content: KeyboardKit.Autocomplete.AutocorrectionDisabledToContextModifer.Content) -> some View


        /// The type of view representing the body.
        public typealias Body = some View
    }
}

extension Autocomplete {

    /// This enum defines next word prediction request types
    /// that are provided by KeyboardKit Pro.
    public enum NextWordPredictionRequestType : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Perform next word prediction with the Claude API.
        case claude

        /// Perform next word prediction with the OpenAI API.
        case openAI

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Autocomplete.NextWordPredictionRequestType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Autocomplete.NextWordPredictionRequestType] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Autocomplete {

    /// This service is used as a default service, until you
    /// setup KeyboardKit Pro or register a custom service.
    ///
    /// This service can also be resolved with the shorthand
    /// ``AutocompleteService/disabled(suggestions:)``.
    public class DisabledAutocompleteService : KeyboardKit.AutocompleteService {

        /// Create a disabled autocomplete service.
        ///
        /// - Parameters:
        ///   - suggestions: The suggestions to present.
        public init(suggestions: [KeyboardKit.Autocomplete.Suggestion] = [])

        /// The currently applied locale.
        open var locale: Locale

        open var suggestions: [KeyboardKit.Autocomplete.Suggestion] { get }

        /// Get autocomplete suggestions for the provided `text`.
        open func autocomplete(_ text: String) async throws -> KeyboardKit.Autocomplete.Result

        /// Whether the service can ignore words.
        open var canIgnoreWords: Bool { get }

        /// Whether the service can lean words.
        open var canLearnWords: Bool { get }

        /// The service's currently ignored words.
        open var ignoredWords: [String]

        /// The service's currently learned words.
        open var learnedWords: [String]

        /// Whether the service has ignored a certain word.
        open func hasIgnoredWord(_ word: String) -> Bool

        /// Whether the service has learned a certain word.
        open func hasLearnedWord(_ word: String) -> Bool

        /// Make the service ignore a certain word.
        open func ignoreWord(_ word: String)

        /// Make the service learn a certain word.
        open func learnWord(_ word: String)

        /// Remove a certain ignored word from the service.
        open func removeIgnoredWord(_ word: String)

        /// Make the service unlearn a certain word.
        open func unlearnWord(_ word: String)

        @objc deinit
    }
}

extension Autocomplete {

    /// This type can be used to customize the visibility of
    /// any part of a ``SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/autocompleteSettingsScreenVisibility(_:)``.
    public struct SettingsScreenVisibility {

        public init(nextWordPredictionSection: Bool = false)

        public var nextWordPredictionSection: Bool
    }
}

extension Autocomplete {

    /// This type can be used to perform remote autocomplete
    /// requests of various types.
    ///
    /// To create a custom remote prediction request, define
    /// a request builder that can create a URLRequest value
    /// for the text to predict, as well as a result builder
    /// that parses the data that is returned by the request.
    ///
    /// KeyboardKit Pro unlocks some pre-configured requests:
    ///
    /// - ``claude(apiKey:apiUrl:anthropicVersion:model:maxTokens:systemPrompt:)``
    /// - ``openAI(apiKey:apiUrl:apiKeyHeader:apiKeyValuePrefix:model:maxTokens:systemPrompt:)``
    ///
    /// You have to provide your own private API keys to use
    /// these requests.
    public struct RemotePredictionRequest {

        /// Create a custom remote prediction request.
        ///
        /// - Parameters:
        ///   - request: A request builder that creates a URLRequest for a text.
        ///   - result: A result builder that parses response Data into a result.
        public init(request: @escaping KeyboardKit.Autocomplete.RemotePredictionRequest.PredictionRequest, result: @escaping KeyboardKit.Autocomplete.RemotePredictionRequest.PredictionParser)

        public typealias PredictionText = String

        public typealias PredictionRequest = (KeyboardKit.Autocomplete.RemotePredictionRequest.PredictionText) throws -> URLRequest

        public typealias PredictionParser = (Data) throws -> KeyboardKit.Autocomplete.Result
    }
}

extension Autocomplete {

    /// This service can perform autocomplete operations and
    /// additional autocomplete-related operations.
    ///
    /// The service performs local autocompletion by default.
    /// You can override its many functions to customize any
    /// part of the autocomplete logic.
    ///
    /// For instance, ``autocomplete(_:)`` is defined by the
    /// ``AutocompleteService`` protocol and performs a full
    /// autocomplete operation for a text, which includes an
    /// ``autocompleteSuggestions(for:)`` operation to get a
    /// list of suggestions, and many other operations, like
    /// ``nextWordPredictionResult(for:)`` and ``emojiColonSuggestions(for:)``.
    ///
    /// You can override ``autocomplete(_:)`` to replace the
    /// autocomplete logic or just tweak the standard result,
    /// or override any of the other functions to generate a
    /// custom value for that part of the end result.
    ///
    /// You can use a ``RemotePredictionRequest`` to perform
    /// a remote prediction in any overridden function. Just
    /// call ``RemotePredictionRequest/performRequest(for:with:)``
    /// to perform it asynchronously, then return the result.
    ///
    /// You can set ``autocompleteRequest`` to enable remote
    /// autocomplete without having to create a custom class
    /// and override ``autocompleteSuggestions(for:)``. This
    /// makes it easy to integrate with a remote server. You
    /// can also set ``nextWordPredictionRequest`` to enable
    /// remote next word prediction in the same way.
    ///
    /// You can ``learn(_:)`` any suggestions to permanently
    /// add them to the device dictionary, and ``unlearn(_:)``
    /// suggestions to remove them from that dictionary. You
    /// can temporarily ``ignore(_:)`` suggestions, and undo
    /// it with ``removeIgnoredWord(_:)``.
    ///
    /// Local autocomplete supports a limited set of locales.
    /// See <doc:Autocomplete-Article> for the list. You can
    /// add support for more locales by overriding functions
    /// and either interact with custom local models, or use
    /// a ``RemotePredictionRequest`` to get a remote result.
    ///
    /// See <doc:Autocomplete-Article> for more information.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/autocomplete`` feature.
    open class StandardAutocompleteService : KeyboardKit.AutocompleteService {

        /// Create a local autocomplete service.
        ///
        /// > Important: This requires that your Pro license
        /// unlocks the ``ProFeature/autocomplete`` feature.
        ///
        /// - Parameters:
        ///   - autocompleteContext: The autocomplete context to use.
        ///   - keyboardContext: The keyboard context to use.
        ///   - locale: The locale to use, by default `.current`.
        ///   - maxCount: The max number of suggestions to return, by default `1_000`.
        ///   - remoteAutocompleteRequest: A remote autocomplete context to use for all requests, if any.
        ///   - remoteNextWordPredictionRequest: A remote next word prediction context to use for all requests, if any.
        public init(autocompleteContext: KeyboardKit.AutocompleteContext, keyboardContext: KeyboardKit.KeyboardContext, locale: Locale = .current, maxCount: Int = 1_000, remoteAutocompleteRequest: KeyboardKit.Autocomplete.RemotePredictionRequest? = nil, remoteNextWordPredictionRequest: KeyboardKit.Autocomplete.RemotePredictionRequest? = nil) throws

        /// The autocomplete context to use.
        public var autocompleteContext: KeyboardKit.AutocompleteContext

        /// The autocomplete settings to use.
        public var autocompleteSettings: KeyboardKit.AutocompleteSettings { get }

        /// The keyboard context to use.
        public var keyboardContext: KeyboardKit.KeyboardContext

        /// The currently applied locale.
        public var locale: Locale

        /// The max number of suggestions to return.
        public var maxCount: Int

        /// A remote autocomplete context.
        ///
        /// If you set this request, then ``autocomplete(_:)``
        /// will use it to fetch the result remotely.
        public var remoteAutocompleteRequest: KeyboardKit.Autocomplete.RemotePredictionRequest?

        /// A remote next word prediction request.
        ///
        /// If you set this, ``nextWordPredictionResult(for:)``
        /// will use it to fetch the result remotely.
        public var remoteNextWordPredictionRequest: KeyboardKit.Autocomplete.RemotePredictionRequest?

        /// Whether or not the service has a registered lexicon.
        open var hasLexicon: Bool { get }

        /// Register a lexicon use to improve the results.
        open func registerLexicon(_ lexicon: UILexicon)

        /// Get autocomplete suggestions for the provided `text`.
        public func autocomplete(_ text: String) async throws -> KeyboardKit.Autocomplete.Result

        open func autocompleteSuggestions(for text: String) async throws -> [KeyboardKit.Autocomplete.Suggestion]

        /// Whether autocorrect is enabled, based on several
        /// factors like keyboard state, settings, etc.
        open var isAutocorrectEnabled: Bool { get }

        /// Whether the service can ignore words.
        open var canIgnoreWords: Bool { get }

        /// Whether the service can lean words.
        open var canLearnWords: Bool { get }

        /// The service's currently ignored words.
        open var ignoredWords: [String] { get }

        /// The service's currently learned words.
        open var learnedWords: [String] { get }

        /// Whether the service has ignored a certain word.
        open func hasIgnoredWord(_ word: String) -> Bool

        /// Whether the service has learned a certain word.
        open func hasLearnedWord(_ word: String) -> Bool

        /// Make the service ignore a certain word.
        open func ignoreWord(_ word: String)

        /// Make the service learn a certain word.
        open func learnWord(_ word: String)

        /// Remove a certain ignored word from the service.
        open func removeIgnoredWord(_ word: String)

        /// Make the service unlearn a certain word.
        open func unlearnWord(_ word: String)

        /// Whether to autocorrect a `word`.
        open func shouldAutocorrect(_ word: String) -> Bool

        /// Get emoji colon suggestions for a certain text.
        ///
        /// This returns nil if ``autocompleteSettings`` has
        /// disabled emoji autocomplete, or if the last word
        /// segment doesn't start with a colon.
        open func emojiColonSuggestions(for text: String) async throws -> [KeyboardKit.Autocomplete.Suggestion]?

        /// Whether to perform next character predictions for the provided text.
        open func shouldPerformEmojiColonPredictions(for text: String) -> Bool

        /// Get next character predictions for the provided text.
        open func nextCharacterPredictions(for text: String, _ suggestions: [KeyboardKit.Autocomplete.Suggestion]) async throws -> [Character : Double]?

        /// Whether to perform next character predictions for the provided text.
        open func shouldPerformNextCharacterPredictions(for text: String) -> Bool

        /// Get next word predictions for the provided text.
        open func nextWordPredictionResult(for text: String) async throws -> KeyboardKit.Autocomplete.Result?

        /// Whether to perform next word predictions for the provided text.
        open func shouldPerformNextWordPredictions(for text: String) -> Bool

        @objc deinit
    }
}

extension Autocomplete.ToolbarSeparator : Sendable {
}

extension Autocomplete.ToolbarSeparatorStyle {

    /// The standard autocomplete toolbar separator style.
    public static var standard: KeyboardKit.Autocomplete.ToolbarSeparatorStyle { get }
}

extension Autocomplete.Toolbar : Sendable {
}

extension Autocomplete.ToolbarStyle {

    /// The standard autocomplete toolbar style.
    public static var standard: KeyboardKit.Autocomplete.ToolbarStyle { get }
}

extension Autocomplete.SettingsScreen where Header == EmptyView, AutocompleteSection == KeyboardKit.Autocomplete.SettingsScreenSections.AutocompleteContent, NextWordPredictionSection == KeyboardKit.Autocomplete.SettingsScreenSections.NextWordPredictionContent, Footer == EmptyView {

    /// Create a custom screen.
    @MainActor @preconcurrency public init()
}

extension Autocomplete.SettingsScreen : Sendable {
}

extension Autocomplete.SettingsScreenSections {

    /// This struct defines view builder parameters that are
    /// used to create an autocomplete settings section.
    public struct AutocompleteParams {

        public let view: KeyboardKit.Autocomplete.SettingsScreenSections.AutocompleteContent
    }

    /// This is a standard autocomplete settings section.
    @MainActor @preconcurrency public struct AutocompleteContent : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Autocomplete.SettingsScreenSections {

    /// This struct defines view builder parameters that are
    /// used to create the prediction section.
    public struct NextWordPredictionParams {

        public let view: KeyboardKit.Autocomplete.SettingsScreenSections.NextWordPredictionContent
    }

    /// This is a standard prediction settings section, that
    /// can be localized in the same way as the main screen. 
    @MainActor @preconcurrency public struct NextWordPredictionContent : View {

        @MainActor @preconcurrency public init(isExpanded: Binding<Bool>? = nil, isTextFieldFocused: FocusState<Bool>.Binding? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Autocomplete.ToolbarItem : Sendable {
}

extension Autocomplete.ToolbarItemStyle {

    /// The standard autocomplete toolbar item style.
    public static var standard: KeyboardKit.Autocomplete.ToolbarItemStyle { get }

    /// The standard autocomplete toolbar autocorrect style.
    public static var standardAutocorrect: KeyboardKit.Autocomplete.ToolbarItemStyle { get }
}

extension Autocomplete.Suggestion {

    /// Whether the suggestion is an autocorrect suggestion.
    public var isAutocorrect: Bool { get }

    /// Whether the suggestion is a regular suggestion.
    public var isRegular: Bool { get }

    /// Whether the suggestion is an unknown suggestion.
    public var isUnknown: Bool { get }

    /// Adjust the ``text`` casing to match a certain word.
    public func autocompleteCased(for word: String) -> KeyboardKit.Autocomplete.Suggestion
}

extension Autocomplete.TextReplacementDictionary {

    /// This predefined dictionary can be used as a starting
    /// point when defining a custom list of autocorrections.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/autocomplete`` feature.
    public static var additionalAutocorrections: KeyboardKit.Autocomplete.TextReplacementDictionary { get throws }
}

extension Autocomplete.TextReplacementDictionary {

    /// Insert a text replacement for a certain locale.
    public mutating func addTextReplacement(for text: String, with replacement: String, locale: Locale)

    /// Insert a text replacement for a certain locale.
    public mutating func addTextReplacements(_ dict: [String : String], for locale: Locale)

    /// Set the text replacements for a certain locale.
    public mutating func setTextReplacements(_ dict: [String : String], for locale: Locale)

    /// Get a text replacement for a certain text and locale.
    public func textReplacement(for text: String, locale: Locale) -> String?

    /// Get all text replacements for a certain locale.
    public func textReplacements(for locale: Locale) -> [String : String]?
}

extension Autocomplete.SuggestionType : RawRepresentable {
}

extension Autocomplete.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Autocomplete.SettingsScreenLocalization { get }
}

extension Autocomplete.NextWordPredictionTypePicker : Sendable {
}

extension Autocomplete.AutocorrectionDisabledToContextModifer : Sendable {
}

extension Autocomplete.NextWordPredictionRequestType {

    /// The request type's unique ID.
    public var id: String { get }

    /// The request type's display name
    public var displayName: String { get }

    /// The service provider's website URL.
    public var url: URL? { get }

    /// The service provider's website URL.
    public var urlString: String { get }

    /// Get the standard request for a certain API key.
    public func standardRequest(for apiKey: String) -> KeyboardKit.Autocomplete.RemotePredictionRequest
}

extension Autocomplete.NextWordPredictionRequestType : RawRepresentable {
}

extension Autocomplete.SettingsScreenVisibility {

    public static var standard: KeyboardKit.Autocomplete.SettingsScreenVisibility { get }
}

extension Autocomplete.RemotePredictionRequest {

    /// This type can be used to setup custom system prompts
    /// for AI-based remote prediction request.
    public struct SystemPrompt {

        /// Create a custom next word prediction prompt.
        public init(prompt: String)
    }
}

extension Autocomplete.RemotePredictionRequest {

    /// Create an OpenAI-based remote prediction request.
    ///
    /// This request integrates with the Claude API to fetch
    /// predictions based on the provided system prompt. You
    /// can customize the request by replacing any parameter.
    ///
    /// You must provide your own API key to use the request,
    /// and pay for your consumption to the service provider.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/remoteAutocomplete`` feature.
    ///
    /// - Parameters:
    ///   - apiKey: Your personal OpenAI API key.
    ///   - apiUrl: The API URL to use, by default `https://api.openai.com/v1/chat/completions`.
    ///   - apiKeyHeader: The API key header name, by default `Authorization`.
    ///   - apiKeyValuePrefix: The API key value prefix, by default `Bearer `.
    ///   - model: The AI model to use, by default `gpt-4o-mini`.
    ///   - maxTokens: The max number of tokens, by default `100`.
    ///   - systemPrompt: The system prompt to use.
    public static func openAI(apiKey: String, apiUrl: String = "https://api.openai.com/v1/chat/completions", apiKeyHeader: String = "Authorization", apiKeyValuePrefix: String = "Bearer ", model: String = "gpt-4o-mini", maxTokens: Int = 100, systemPrompt: KeyboardKit.Autocomplete.RemotePredictionRequest.SystemPrompt) -> KeyboardKit.Autocomplete.RemotePredictionRequest
}

extension Autocomplete.RemotePredictionRequest {

    /// Perform the request for a certain text.
    public func performRequest(for text: KeyboardKit.Autocomplete.RemotePredictionRequest.PredictionText, with session: URLSession = .shared) async throws -> KeyboardKit.Autocomplete.Result
}

extension Autocomplete.RemotePredictionRequest {

    /// Create a Claude-based remote prediction request.
    ///
    /// This request integrates with the Claude API to fetch
    /// predictions based on the provided system prompt. You
    /// can customize the request by replacing any parameter.
    ///
    /// You must provide your own API key to use the request,
    /// and pay for your consumption to the service provider.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/remoteAutocomplete`` feature.
    ///
    /// - Parameters:
    ///   - apiKey: Your personal Claude API key.
    ///   - apiUrl: The API URL to use, by default `https://api.anthropic.com/v1/messages`.
    ///   - anthropicVersion: The API version to use, by default `2023-06-01`.
    ///   - model: The AI model to use, by default `claude-sonnet-4-20250514`.
    ///   - maxTokens: The max number of tokens, by default `100`.
    ///   - systemPrompt: The system prompt to use.
    public static func claude(apiKey: String, apiUrl: String = "https://api.anthropic.com/v1/messages", anthropicVersion: String = "2023-06-01", model: String = "claude-sonnet-4-20250514", maxTokens: Int = 100, systemPrompt: KeyboardKit.Autocomplete.RemotePredictionRequest.SystemPrompt) -> KeyboardKit.Autocomplete.RemotePredictionRequest
}

extension Autocomplete.SettingsScreenSections.AutocompleteContent : Sendable {
}

extension Autocomplete.SettingsScreenSections.NextWordPredictionContent : Sendable {
}

extension Autocomplete.RemotePredictionRequest.SystemPrompt {

    /// This is a next word prediction prompt for OpenAI.
    public static let openAINextWordPrediction: KeyboardKit.Autocomplete.RemotePredictionRequest.SystemPrompt
}

extension Autocomplete.RemotePredictionRequest.SystemPrompt {

    /// This is a next word prediction prompt for Claude.
    public static let claudeNextWordPrediction: KeyboardKit.Autocomplete.RemotePredictionRequest.SystemPrompt
}

/// This class has observable states and persistent settings
/// for keyboard autocomplete.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings,
/// like ``AutocompleteSettings/isAutocompleteEnabled``.
final public class AutocompleteContext : ObservableObject {

    /// Create an autocomplete context instance.
    public init()

    /// Auto-persisted autocomplete settings.
    @Published final public var settings: KeyboardKit.AutocompleteSettings

    final public var $settings: Published<KeyboardKit.AutocompleteSettings>.Publisher

    /// The last received autocomplete error.
    final public var lastError: (any Error)?

    /// This dictionary can be used to define autocorrection
    /// replacements for various locales.
    ///
    /// KeyboardKit Pro initializes the dictionary with many
    /// replacements that the system is missing.
    final public var autocorrectDictionary: KeyboardKit.Autocomplete.TextReplacementDictionary

    /// Whether or not suggestions are being fetched.
    @Published final public var isLoading: Bool

    final public var $isLoading: Published<Bool>.Publisher

    /// The characters that are more likely to be typed next.
    @Published final public var emojiSuggestions: [KeyboardKit.Autocomplete.Suggestion]

    final public var $emojiSuggestions: Published<[KeyboardKit.Autocomplete.Suggestion]>.Publisher

    /// The characters that are more likely to be typed next.
    @Published final public var nextCharacterPredictions: [Character : Double]

    final public var $nextCharacterPredictions: Published<[Character : Double]>.Publisher

    /// The suggestions that were returned by the service.
    @Published final public var suggestionsFromService: [KeyboardKit.Autocomplete.Suggestion]

    final public var $suggestionsFromService: Published<[KeyboardKit.Autocomplete.Suggestion]>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension AutocompleteContext {

    /// The suggestions that should be presented to the user,
    /// which is an aggregate of the ``suggestionsFromService``.
    final public var suggestions: [KeyboardKit.Autocomplete.Suggestion] { get }
}

extension AutocompleteContext {

    /// Reset the context.
    final public func reset()

    /// Update the context with a certain result.
    final public func update(with result: KeyboardKit.Autocomplete.Result)

    /// Update the context with a certain error.
    final public func update(with error: any Error)
}

extension AutocompleteContext {

    /// Get a 0-1 next character prediction for a `char`.
    final public func nextCharacterPrediction(for char: String) -> Double

    /// Get a 0-1 next character prediction for an `action`.
    final public func nextCharacterPrediction(for action: KeyboardKit.KeyboardAction) -> Double
}

extension AutocompleteContext {

    public static var preview: KeyboardKit.AutocompleteContext { get }
}

/// This protocol can be implemented by any type that can be
/// used perform autocomplete as the user types.
///
/// Simply call ``autocomplete(_:)`` to perform autocomplete
/// for the provided `text`. Pass in as much text as you can,
/// to give the service as much context as possible.
///
/// Words can be learned using ``learnWord(_:)`` and ignored
/// with ``ignoreWord(_:)``. Although it's up to the service,
/// learned words should be persisted and ignored words only
/// apply to the current service instance.
///
/// Not every service supports ignoring/learning words. Make
/// sure to check the ``canLearnWords`` and ``canIgnoreWords``
/// properties before exposing any such features to the user.
///
/// KeyboardKit doesn't have a standard autocomplete service
/// as it has for other services. Instead, a disabled one is
/// used until you setup KeyboardKit Pro or a custom service.
public protocol AutocompleteService : AnyObject {

    /// The currently applied locale.
    var locale: Locale { get set }

    /// Get autocomplete suggestions for the provided `text`.
    func autocomplete(_ text: String) async throws -> KeyboardKit.Autocomplete.Result

    /// Whether the service can ignore words.
    var canIgnoreWords: Bool { get }

    /// Whether the service can lean words.
    var canLearnWords: Bool { get }

    /// The service's currently ignored words.
    var ignoredWords: [String] { get }

    /// The service's currently learned words.
    var learnedWords: [String] { get }

    /// Whether the service has ignored a certain word.
    func hasIgnoredWord(_ word: String) -> Bool

    /// Whether the service has learned a certain word.
    func hasLearnedWord(_ word: String) -> Bool

    /// Make the service ignore a certain word.
    func ignoreWord(_ word: String)

    /// Make the service learn a certain word.
    func learnWord(_ word: String)

    /// Remove a certain ignored word from the service.
    func removeIgnoredWord(_ word: String)

    /// Make the service unlearn a certain word.
    func unlearnWord(_ word: String)
}

extension AutocompleteService where Self == KeyboardKit.KeyboardPreviews.AutocompleteService {

    public static var preview: any KeyboardKit.AutocompleteService { get }

    public static func preview(suggestions: [KeyboardKit.Autocomplete.Suggestion] = .preview) -> any KeyboardKit.AutocompleteService
}

extension AutocompleteService {

    /// Perform a background autocomplete operation and sync
    /// the result or error to the provided context.
    public func autocomplete(_ text: String, updating context: KeyboardKit.AutocompleteContext)

    /// Make the service ignore a certain suggestion.
    public func ignore(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

    /// Make the service ignore a collection of words.
    public func ignoreWords(_ words: [String])

    /// Make the service learn a certain suggestion.
    public func learn(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

    /// Remove a certain ignored suggested from the service.
    public func removeIgnoredSuggestion(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

    /// Make the service unlearn a certain suggestion.
    public func unlearn(_ suggestion: KeyboardKit.Autocomplete.Suggestion)
}

extension AutocompleteService where Self == KeyboardKit.Autocomplete.DisabledAutocompleteService {

    /// Create a disabled service instance.
    public static var disabled: KeyboardKit.Autocomplete.DisabledAutocompleteService { get }

    /// Create a disabled service instance.
    ///
    /// - Parameters:
    ///   - suggestions: The suggestions to present.
    public static func disabled(suggestions: [KeyboardKit.Autocomplete.Suggestion] = []) -> Self
}

/// This type has auto-persisted autocomplete settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with an `autocomplete` prefix.
///
/// The ``isNextWordPredictionEnabled`` property is false by
/// default, since users should approve that text is sent to
/// a 3rd party service. When it's enabled, you can define a
/// request type with ``nextWordPredictionRequestType``, and
/// define API key using ``nextWordPredictionRequestApiKey``.
///
/// You can also expose these settings to your users, to let
/// them choose their own service accounts. This is a way to
/// let your users pay for their own data.
public struct AutocompleteSettings : Sendable {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }

    /// Whether autocomplete is enabled, by default `true`.
    @AppStorage public var isAutocompleteEnabled: Bool { get nonmutating set }

    public var $isAutocompleteEnabled: Binding<Bool> { get }

    /// Whether autocorrect is enabled, by default `true`.
    @AppStorage public var isAutocorrectEnabled: Bool { get nonmutating set }

    public var $isAutocorrectEnabled: Binding<Bool> { get }

    /// Whether to autolearn unknown suggestions, by default `false`.
    @AppStorage public var isAutolearnEnabled: Bool { get nonmutating set }

    public var $isAutolearnEnabled: Binding<Bool> { get }

    /// Whether to automatically ignore adjusted suggestions, by default `true`.
    @AppStorage public var isAutoIgnoreEnabled: Bool { get nonmutating set }

    public var $isAutoIgnoreEnabled: Binding<Bool> { get }

    /// Whether emoji autocomplete is enabled, by default `true`.
    @AppStorage public var isEmojiAutocompleteEnabled: Bool { get nonmutating set }

    public var $isEmojiAutocompleteEnabled: Binding<Bool> { get }

    /// Whether next character prediction is enabled, by default `true`.
    @AppStorage public var isNextCharacterPredictionEnabled: Bool { get nonmutating set }

    public var $isNextCharacterPredictionEnabled: Binding<Bool> { get }

    /// Whether next word prediction is enabled, by default `true`.
    @AppStorage public var isNextWordPredictionEnabled: Bool { get nonmutating set }

    public var $isNextWordPredictionEnabled: Binding<Bool> { get }

    /// A custom, user-specified next word predicton request API key.
    @AppStorage public var nextWordPredictionRequestApiKey: String { get nonmutating set }

    public var $nextWordPredictionRequestApiKey: Binding<String> { get }

    /// A custom, user-specified next word predicton request type.
    @AppStorage public var nextWordPredictionRequestType: KeyboardKit.Autocomplete.NextWordPredictionRequestType { get nonmutating set }

    public var $nextWordPredictionRequestType: Binding<KeyboardKit.Autocomplete.NextWordPredictionRequestType> { get }

    /// The number of autocomplete suggestions to display, by default `3`.
    @AppStorage public var suggestionsDisplayCount: Int { get nonmutating set }

    public var $suggestionsDisplayCount: Binding<Int> { get }
}

extension AutocompleteSettings {

    /// The trimmed next word prediction request API key.
    public var trimmedNextWordPredictionRequestApiKey: String { get }

    /// Whether the user has provided a next word prediction
    /// request API key.
    public var hasNextWordPredictionRequestApiKey: Bool { get }

    /// Try to build a next word prediction request from the
    /// selected request type and provided API key, if any.
    public var nextWordPredictionRequest: KeyboardKit.Autocomplete.RemotePredictionRequest? { get }
}

/// This context has observable callout-related state and is
/// used for both input and action callouts.
///
/// KeyboardKit will create an instance of this context, and
/// inject into the environment, when you set up KeyboardKit
/// as shown in <doc:Getting-Started-Article>.
final public class CalloutContext : ObservableObject {

    /// Create a keyboard callout context.
    public init()

    /// The coordinate space to use for callout.
    final public let coordinateSpace: String

    /// The scale to apply if the items must be compressed.
    final public var compressedWidthScale: Double

    /// The scale to apply to drag selection index changes.
    final public var dragIndexScaleFactor: Double

    /// The last time an input was updated.
    final public var lastInputUpdate: Date

    /// The minimum input callout duration.
    final public var minimumInputDuration: Double

    /// The action to use when tapping actions.
    final public var actionHandler: (KeyboardKit.KeyboardAction) -> Void

    /// The action to use when changing action selection.
    final public var selectionFeedbackHandler: () -> Void

    /// The currently active button frame.
    @Published final public var buttonFrame: CGRect { get }

    final public var $buttonFrame: Published<CGRect>.Publisher { get }

    /// The current input action, if any.
    @Published final public var inputAction: KeyboardKit.KeyboardAction? { get }

    final public var $inputAction: Published<KeyboardKit.KeyboardAction?>.Publisher { get }

    /// The current secondary actions.
    @Published final public var secondaryActions: [KeyboardKit.KeyboardAction] { get }

    final public var $secondaryActions: Published<[KeyboardKit.KeyboardAction]>.Publisher { get }

    /// The current secondary action callout alignment.
    @Published final public var secondaryActionsAlignment: HorizontalAlignment { get }

    final public var $secondaryActionsAlignment: Published<HorizontalAlignment>.Publisher { get }

    /// The current secondary action index.
    @Published final public var secondaryActionsIndex: Int { get }

    final public var $secondaryActionsIndex: Published<Int>.Publisher { get }

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension CalloutContext {

    final public var selectedSecondaryAction: KeyboardKit.KeyboardAction? { get }

    @discardableResult
    final public func handleSelectedSecondaryAction() -> Bool

    /// Reset the input action. This will remove the callout.
    final public func resetInputAction()

    /// Reset the context with a slight delay, since we want
    /// the callout to be visible for a short while, even if
    /// the user immediately releases the button.
    final public func resetInputActionWithDelay()

    /// Reset the context. This will dismiss the callout.
    final public func resetSecondaryActions()

    /// Update the current input for a certain action.
    final public func updateInputAction(_ action: KeyboardKit.KeyboardAction?, in geo: GeometryProxy)

    /// Update the secondary actions for a certain action.
    final public func updateSecondaryActions(_ actions: [KeyboardKit.KeyboardAction]?, for action: KeyboardKit.KeyboardAction, in geo: GeometryProxy, alignment: HorizontalAlignment? = nil)

    /// Update the secondary action selection with a drag gesture value.
    final public func updateSecondaryActionsSelection(with value: DragGesture.Value)
}

extension CalloutContext {

    public static var preview: KeyboardKit.CalloutContext { get }
}

/// This namespace contains keyboard callout-related types.
public struct Callouts {
}

extension Callouts {

    /// This type can be used to define callout actions that
    /// can be shown in an ``Callouts/ActionCallout``.
    ///
    /// You can use standard, ``english`` callout actions or
    /// upgrade to KeyboardKit Pro to unlock callout actions
    /// for all supported locales.
    ///
    /// You can customize the callout actions for any action
    /// with ``SwiftUICore/View/keyboardCalloutActions(_:)``.
    public struct Actions : KeyboardKit.KeyboardModel {

        /// Create callout actions from an action dictionary.
        ///
        /// This initializer applies the provided actions as
        /// they are, without any modifications.
        public init(actions: [KeyboardKit.KeyboardAction : [KeyboardKit.KeyboardAction]])

        /// Create callout actions from a string dictionary.
        ///
        /// This initializer applies the provided actions as
        /// they are, without any modifications, and appends
        /// the additional action/actions strings dictionary,
        /// where the actions for each action are defined in
        /// a single string without any separator.
        public init(actions: [KeyboardKit.KeyboardAction : [KeyboardKit.KeyboardAction]] = [:], characters: [String : String])

        /// An action mapping dictionary.
        public var actionsDictionary: [KeyboardKit.KeyboardAction : [KeyboardKit.KeyboardAction]]

        /// Whether diacritic key match is enabled.
        public var isDiacriticInsensitive: Bool

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Callouts.Actions, b: KeyboardKit.Callouts.Actions) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Callouts {

    /// This callout can show secondary callout actions when
    /// long pressing input keys with secondary actions.
    ///
    /// This callout will adjust the button corner radius to
    /// fit the style's or the keyboard layout configuration.
    @MainActor @preconcurrency public struct ActionCallout : View {

        /// Create an action callout.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Callouts {

    /// This style can be used to modify the visual style of
    /// the various callout components.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/keyboardCalloutStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct CalloutStyle : KeyboardKit.KeyboardModel {

        /// Create a custom callout style.
        ///
        /// When creating custom styles, note that the style
        /// has components that are meant to fit the overall
        /// style. For instance, the corner and shadow radii
        /// should probably be the same as the corner radius
        /// of presenting button.
        ///
        /// Only customize the parameters you need, then use
        /// the default values for all other parameters.
        ///
        /// - Parameters:
        ///   - actionItemFont: The action item font, by default `.title3`.
        ///   - actionItemMaxSize: The max action item size, by default `50x50` points.
        ///   - actionItemPadding: The intrinsic action item padding, by default `0x6` points.
        ///   - backgroundColor: The callout background color, by default `.keyboardButtonBackground`.
        ///   - borderColor: The callout border color, by default transparent `.black`.
        ///   - buttonOverlayCornerRadius: A custom button overlay corner radius, if any.
        ///   - buttonOverlayInset: The button overlay inset, by default `.zero`.
        ///   - cornerRadius: The callout corner radius, by default `10`.
        ///   - curveSize: The callout to button curve size, by default `8x15` points.
        ///   - foregroundColor: The callout foreground color, by default `.primary`.
        ///   - inputItemFont: The input item font, by default `.largeTitle .light`.
        ///   - inputItemMinSize: The minimum input item size, by default `55` height.
        ///   - offset: A custom callout offset, if any.
        ///   - selectedBackgroundColor: The selected item background, by default `.blue`.
        ///   - selectedForegroundColor: The selected item foreground, by default `.white`.
        ///   - shadowColor: The callout shadow color, by default 0.1 `.black`.
        ///   - shadowRadius: The callout shadow radius, by default `5`.
        public init(actionItemFont: KeyboardKit.KeyboardFont? = nil, actionItemMaxSize: CGSize? = nil, actionItemPadding: CGSize? = nil, backgroundColor: Color = .keyboardButtonBackground, borderColor: Color? = nil, buttonOverlayCornerRadius: CGFloat? = nil, buttonOverlayInset: CGSize? = nil, cornerRadius: CGFloat? = nil, curveSize: CGSize? = nil, foregroundColor: Color? = nil, inputItemFont: KeyboardKit.KeyboardFont? = nil, inputItemMinSize: CGSize? = nil, offset: CGPoint? = nil, selectedBackgroundColor: Color? = nil, selectedForegroundColor: Color? = nil, shadowColor: Color? = nil, shadowRadius: CGFloat? = nil)

        /// The action item font.
        public var actionItemFont: KeyboardKit.KeyboardFont

        /// The max action item size.
        public var actionItemMaxSize: CGSize

        /// The intrinsic action item padding.
        public var actionItemPadding: CGSize

        /// The callout background color.
        public var backgroundColor: Color

        /// The callout border color.
        public var borderColor: Color

        /// The button overlay corner radius.
        public var buttonOverlayCornerRadius: CGFloat?

        /// The button overlay inset.
        public var buttonOverlayInset: CGSize

        /// The callout corner radius.
        public var cornerRadius: CGFloat

        /// The callout to button overlay curve size.
        public var curveSize: CGSize

        /// The callout foreground color.
        public var foregroundColor: Color

        /// The action item font.
        public var inputItemFont: KeyboardKit.KeyboardFont

        /// The minimum input item size.
        public var inputItemMinSize: CGSize

        /// A custom callout offset, if any.
        public var offset: CGPoint?

        /// The selected item background.
        public var selectedBackgroundColor: Color

        /// The selected item foreground.
        public var selectedForegroundColor: Color

        /// The callout shadow color.
        public var shadowColor: Color

        /// The callout shadow radius.
        public var shadowRadius: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Callouts.CalloutStyle, b: KeyboardKit.Callouts.CalloutStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Callouts {

    /// This callout can show the currenly pressed key, when
    /// typing on an iPhone.
    ///
    /// This callout will adjust the button corner radius to
    /// fit the style's or the keyboard layout configuration.
    @MainActor @preconcurrency public struct InputCallout : View {

        /// Create a custom input callout.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Callouts {

    /// This type is used to customize callout actions using
    /// ``SwiftUICore/View/keyboardCalloutActions(_:)``.
    public typealias ActionsBuilder = (KeyboardKit.Callouts.ActionsBuilderParams) -> [KeyboardKit.KeyboardAction]?

    /// This type is used to customize callout actions using
    /// ``SwiftUICore/View/keyboardCalloutActions(_:)``.
    public struct ActionsBuilderParams {

        /// Create a callout actions builder parameter value.
        public init(action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext)

        /// The action to get callout actions for.
        public let action: KeyboardKit.KeyboardAction

        /// The context to base the callout actions on.
        public let context: KeyboardKit.KeyboardContext

        /// Get the standard callout actions for the context.
        public func standardActions() -> [KeyboardKit.KeyboardAction]?
    }
}

extension Callouts.Actions {

    /// The standard callout actions to use for the provided
    /// keyboard action and context.
    public static func standard(for action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext) -> [KeyboardKit.KeyboardAction]?
}

extension Callouts.Actions {

    /// Base callout actions, without any alphabetic actions.
    public static let base: KeyboardKit.Callouts.Actions

    /// Base numeric and symbolic callout actions characters.
    public static var baseNumericAndSymbolicChars: [String : String] { get }

    /// English callout actions.
    public static let english: KeyboardKit.Callouts.Actions

    /// English, alphabetic callout actions characters.
    public static var englishAlphabeticCharacters: [String : String]
}

extension Callouts.Actions {

    /// Get keyboard callout actions for the provided action.
    public func actions(for action: KeyboardKit.KeyboardAction) -> [KeyboardKit.KeyboardAction]?
}

extension Callouts.Actions {

    public static let albanian: KeyboardKit.Callouts.Actions?

    public static func albanianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let armenian: KeyboardKit.Callouts.Actions?

    public static func armenianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let azerbaijani: KeyboardKit.Callouts.Actions?

    public static func azerbaijaniBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let belarusian: KeyboardKit.Callouts.Actions?

    public static func belarusianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let bengali: KeyboardKit.Callouts.Actions?

    public static func bengaliBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let bulgarian: KeyboardKit.Callouts.Actions?

    public static func bulgarianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let catalan: KeyboardKit.Callouts.Actions?

    public static func catalanBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let cherokee: KeyboardKit.Callouts.Actions?

    public static func cherokeeBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let chuvash: KeyboardKit.Callouts.Actions?

    public static func chuvashBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let croatian: KeyboardKit.Callouts.Actions?

    public static func croatianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let czech: KeyboardKit.Callouts.Actions?

    public static func czechBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let danish: KeyboardKit.Callouts.Actions?

    public static func danishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var danishCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let dutch: KeyboardKit.Callouts.Actions?

    public static func dutchBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let dutch_belgium: KeyboardKit.Callouts.Actions?

    public static func dutch_belgiumBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let english_us: KeyboardKit.Callouts.Actions?

    public static func english_usBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let english_gb: KeyboardKit.Callouts.Actions?

    public static func english_gbBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let english_canada: KeyboardKit.Callouts.Actions?

    public static func english_canadaBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let english_australia: KeyboardKit.Callouts.Actions?

    public static func english_australiaBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let estonian: KeyboardKit.Callouts.Actions?

    public static func estonianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let faroese: KeyboardKit.Callouts.Actions?

    public static func faroeseBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let filipino: KeyboardKit.Callouts.Actions?

    public static func filipinoBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let finnish: KeyboardKit.Callouts.Actions?

    public static func finnishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var finnishCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let french: KeyboardKit.Callouts.Actions?

    public static func frenchBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let french_belgium: KeyboardKit.Callouts.Actions?

    public static func french_belgiumBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let french_canada: KeyboardKit.Callouts.Actions?

    public static func french_canadaBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let french_switzerland: KeyboardKit.Callouts.Actions?

    public static func french_switzerlandBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let georgian: KeyboardKit.Callouts.Actions?

    public static func georgianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let german: KeyboardKit.Callouts.Actions?

    public static func germanBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let german_austria: KeyboardKit.Callouts.Actions?

    public static func german_austriaBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let german_switzerland: KeyboardKit.Callouts.Actions?

    public static func german_switzerlandBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let greek: KeyboardKit.Callouts.Actions?

    public static func greekBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let hawaiian: KeyboardKit.Callouts.Actions?

    public static func hawaiianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let hungarian: KeyboardKit.Callouts.Actions?

    public static func hungarianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let icelandic: KeyboardKit.Callouts.Actions?

    public static func icelandicBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var icelandicCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let inari_sami: KeyboardKit.Callouts.Actions?

    public static func inari_samiBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var inari_samiCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let indonesian: KeyboardKit.Callouts.Actions?

    public static func indonesianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let irish: KeyboardKit.Callouts.Actions?

    public static func irishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let italian: KeyboardKit.Callouts.Actions?

    public static func italianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let kazakh: KeyboardKit.Callouts.Actions?

    public static func kazakhBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let latvian: KeyboardKit.Callouts.Actions?

    public static func latvianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let lithuanian: KeyboardKit.Callouts.Actions?

    public static func lithuanianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let macedonian: KeyboardKit.Callouts.Actions?

    public static func macedonianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let malay: KeyboardKit.Callouts.Actions?

    public static func malayBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let maltese: KeyboardKit.Callouts.Actions?

    public static func malteseBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let mongolian: KeyboardKit.Callouts.Actions?

    public static func mongolianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let northern_sami: KeyboardKit.Callouts.Actions?

    public static func northern_samiBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var northern_samiCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let norwegian: KeyboardKit.Callouts.Actions?

    public static func norwegianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let norwegian_nynorsk: KeyboardKit.Callouts.Actions?

    public static func norwegian_nynorskBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var norwegianCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let polish: KeyboardKit.Callouts.Actions?

    public static func polishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let portuguese: KeyboardKit.Callouts.Actions?

    public static func portugueseBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let portuguese_brazil: KeyboardKit.Callouts.Actions?

    public static func portuguese_brazilBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let romanian: KeyboardKit.Callouts.Actions?

    public static func romanianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let russian: KeyboardKit.Callouts.Actions?

    public static func russianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let serbian: KeyboardKit.Callouts.Actions?

    public static func serbianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let serbian_latin: KeyboardKit.Callouts.Actions?

    public static func serbian_latinBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let shughni_tajik: KeyboardKit.Callouts.Actions?

    public static func shughni_tajikBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let slovak: KeyboardKit.Callouts.Actions?

    public static func slovakBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let slovenian: KeyboardKit.Callouts.Actions?

    public static func slovenianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let spanish: KeyboardKit.Callouts.Actions?

    public static func spanishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let spanish_latinAmerica: KeyboardKit.Callouts.Actions?

    public static func spanish_latinAmericaBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let spanish_mexico: KeyboardKit.Callouts.Actions?

    public static func spanish_mexicoBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let swahili: KeyboardKit.Callouts.Actions?

    public static func swahiliBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let swedish: KeyboardKit.Callouts.Actions?

    public static func swedishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static var swedishCurrencyActions: [KeyboardKit.KeyboardAction] { get }

    public static let turkish: KeyboardKit.Callouts.Actions?

    public static func turkishBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let ukrainian: KeyboardKit.Callouts.Actions?

    public static func ukrainianBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let uzbek: KeyboardKit.Callouts.Actions?

    public static func uzbekBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let vietnamese: KeyboardKit.Callouts.Actions?

    public static func vietnameseBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let welsh: KeyboardKit.Callouts.Actions?

    public static func welshBuilder() throws -> KeyboardKit.Callouts.Actions
}

extension Callouts.Actions {

    public static let arabic: KeyboardKit.Callouts.Actions?

    public static func arabicBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let hebrew: KeyboardKit.Callouts.Actions?

    public static func hebrewBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let kurdish_sorani: KeyboardKit.Callouts.Actions?

    public static func kurdish_soraniBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let kurdish_sorani_arabic: KeyboardKit.Callouts.Actions?

    public static func kurdish_sorani_arabicBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let kurdish_sorani_pc: KeyboardKit.Callouts.Actions?

    public static func kurdish_sorani_pcBuilder() throws -> KeyboardKit.Callouts.Actions

    public static let persian: KeyboardKit.Callouts.Actions?

    public static func persianBuilder() throws -> KeyboardKit.Callouts.Actions
}

extension Callouts.ActionCallout {

    /// This view is used to cover the part of a button that
    /// was tapped or pressed to trigger the callout.
    ///
    /// This view requires a button corner radius, since the
    /// style can't provide a dynamic radius.
    @MainActor @preconcurrency public struct ButtonArea : View {

        /// Create a callout button area.
        ///
        /// - Parameters:
        ///   - frame: The button area frame.
        ///   - buttonCornerRadius: The button corner radius.
        @MainActor @preconcurrency public init(frame: CGRect, buttonCornerRadius: Double)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Callouts.ActionCallout : Sendable {
}

extension Callouts.CalloutStyle {

    /// The corner radius to use for the provided context.
    public func buttonCornerRadius(for context: KeyboardKit.KeyboardContext) -> Double

    /// The standard vertical offset for the provided device.
    public func standardVerticalOffset(for device: KeyboardKit.DeviceType) -> CGFloat
}

extension Callouts.CalloutStyle {

    /// A standard callout style.
    public static var standard: KeyboardKit.Callouts.CalloutStyle { get }
}

extension Callouts.InputCallout {

    /// This view is used to cover the part of a button that
    /// was tapped or pressed to trigger the callout.
    ///
    /// This view requires a button corner radius, since the
    /// style can't provide a dynamic radius.
    @MainActor @preconcurrency public struct ButtonArea : View {

        /// Create a callout button area.
        ///
        /// - Parameters:
        ///   - frame: The button area frame.
        ///   - buttonCornerRadius: The button corner radius.
        @MainActor @preconcurrency public init(frame: CGRect, buttonCornerRadius: Double)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Callouts.InputCallout : Sendable {
}

extension Callouts.ActionCallout.ButtonArea : Sendable {
}

extension Callouts.InputCallout.ButtonArea : Sendable {
}

/// This is a namespace for clipboard related types.
public struct Clipboard {
}

extension Clipboard {

    /// This screen can be used to list the system clipboard
    /// content and ``Clipboard/Clip`` items.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``clipboardClipsScreenLocalization(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/clipboardAddons`` feature.
    @MainActor @preconcurrency public struct ClipsScreen<Header, Footer> : View where Header : View, Footer : View {

        /// Create a custom screen.
        ///
        /// - Parameters:
        ///   - tapAction: The action to trigger when a clip is tapped.
        ///   - header: The view to show at the top of the list.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(tapAction: @escaping (KeyboardKit.Clipboard.Clip) -> Void, @ViewBuilder header: @escaping () -> Header, @ViewBuilder footer: @escaping () -> Footer)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Clipboard {

    /// This struct represents a custom clipboard clip.
    public struct Clip : KeyboardKit.KeyboardModel {

        /// Create a new clip
        public init(text: String)

        /// The clip text.
        public let text: String

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Clipboard.Clip, b: KeyboardKit.Clipboard.Clip) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Clipboard {

    /// This type can be used to configure the localizations
    /// of a ``Clipboard/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/clipboardClipsScreenLocalization(_:)``.
    public struct ClipsScreenLocalization {

        public init(title: String = "Clipboard", save: String = "Save")

        public var title: String

        public var save: String
    }
}

extension Clipboard {

    /// This view can be used as a clipboard keyboard and be
    /// used as ``Keyboard/KeyboardType/clipboard`` keyboard.
    ///
    /// The keyboard will render a ``Clipboard/ClipsScreen``,
    /// and a ``Keyboard/BottomRow``. See each view for more
    /// info on how you can configure them.
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/clipboardAddons`` feature.
    @MainActor @preconcurrency public struct Keyboard : View {

        /// Create a clipboard keyboard view.
        @MainActor @preconcurrency public init(services: KeyboardKit.Keyboard.Services)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Clipboard {

    public typealias KeyboardLocalization = KeyboardKit.Clipboard.ClipsScreenLocalization
}

extension Clipboard {

    /// This style can be used to customize the parts of the
    /// ``Clipboard/SettingsScreen`` that can't be styled by
    /// applying standard view modifiers to the view.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/clipboardClipsScreenStyle(_:)``.
    public struct ClipsScreenStyle {

        public init(listSeparatorTint: Color? = nil, saveButtonTint: Color? = nil, saveButtonForegroundColor: Color? = nil)

        public var listSeparatorTint: Color?

        public var saveButtonTint: Color?

        public var saveButtonForegroundColor: Color?
    }
}

extension Clipboard {

    /// This screen can be used as clipboard settings screen
    /// in a keyboard app, to manage the user's custom clips.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``clipboardSettingsScreenLocalization(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen<Header, Footer> : View where Header : View, Footer : View {

        /// Create a custom screen.
        ///
        /// - Parameters:
        ///   - header: The view to show at the top of the list.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(@ViewBuilder header: @escaping () -> Header, @ViewBuilder footer: @escaping () -> Footer)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Clipboard {

    /// This type can be used to configure the localizations
    /// of a ``Clipboard/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/clipboardSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Clipboard", add: String = "Add", cancel: String = "Cancel", addNewClip: String = "Add New Clip", clipSection: String? = "Clips")

        public var title: String

        public var add: String

        public var addNewClip: String

        public var cancel: String

        public var clipSection: String?
    }
}

extension Clipboard.ClipsScreen where Header == EmptyView, Footer == EmptyView {

    /// Create a custom screen.
    ///
    /// - Parameters:
    ///   - tapAction: The action to trigger when a clip is tapped.
    @MainActor @preconcurrency public init(tapAction: @escaping (KeyboardKit.Clipboard.Clip) -> Void)
}

extension Clipboard.ClipsScreen : Sendable {
}

extension Clipboard.ClipsScreenLocalization {

    public static var standard: KeyboardKit.Clipboard.ClipsScreenLocalization { get }
}

extension Clipboard.Keyboard : Sendable {
}

extension Clipboard.ClipsScreenStyle {

    public static var standard: KeyboardKit.Clipboard.ClipsScreenStyle { get }
}

extension Clipboard.SettingsScreen where Header == EmptyView, Footer == EmptyView {

    /// Create a custom screen.
    @MainActor @preconcurrency public init()
}

extension Clipboard.SettingsScreen : Sendable {
}

extension Clipboard.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Clipboard.SettingsScreenLocalization { get }
}

/// This class has observable and persisted clipboard states.
///
/// This class can be used to manage clips, via the settings,
/// and provide you with the system clipboard content.
///
/// KeyboardKit will create an instance of this context, and
/// inject into the environment, when you set up KeyboardKit
/// as shown in <doc:Getting-Started-Article>.
final public class ClipboardContext : ObservableObject {

    /// Create a new context instance.
    public init()

    /// Auto-persisted clipboard settings.
    @Published final public var settings: KeyboardKit.ClipboardSettings

    final public var $settings: Published<KeyboardKit.ClipboardSettings>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension ClipboardContext {

    public static var preview: KeyboardKit.ClipboardContext { get }
}

extension ClipboardContext {

    /// Get the current string from the system clipboard.
    ///
    /// This function will show a prompt to the user, asking
    /// for permission to access the clipboard. You can link
    /// users to Settings, to permanently allow this.
    final public func getClipFromSystemClipboard() -> KeyboardKit.Clipboard.Clip?
}

/// This type has auto-persisted clipboard settings.
///
/// The ``clips`` property can be used to manage a list with
/// custom text clip. You can let users add, remove, and 
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `clipboard` prefix.
public struct ClipboardSettings : Sendable {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }

    /// A list of custom clips stored by the user.
    public var clips: [KeyboardKit.Clipboard.Clip]
}

extension ClipboardSettings {

    public static var preview: KeyboardKit.ClipboardSettings { get }
}

/// This enum defines various supported device types.
///
/// The static ``current`` property will resolve the current
/// device type.
public enum DeviceType : String, CaseIterable, KeyboardKit.KeyboardModel {

    case phone

    case pad

    case watch

    case mac

    case tv

    case vision

    case other

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.DeviceType]

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String

    /// A collection of all values of this type.
    nonisolated public static var allCases: [KeyboardKit.DeviceType] { get }

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }
}

extension DeviceType {

    public var isMac: Bool { get }

    public var isPad: Bool { get }

    public var isPhone: Bool { get }

    public var isTv: Bool { get }

    public var isWatch: Bool { get }

    public var isVisionPro: Bool { get }
}

extension DeviceType {

    /// The keyboard device type to prefer for a device type.
    ///
    /// This property maps device types to their appropriate
    /// keyboard rendering styles, where iPadOS, macOS, tvOS,
    /// and visionOS prefer an iPad keyboard.
    public var preferredKeyboardDeviceType: KeyboardKit.DeviceType { get }

    /// Whether the device type prefers swipe down actions.
    public var prefersSecondarySwipeDownActions: Bool { get }
}

extension DeviceType {

    /// Get the current device type.
    public static var current: KeyboardKit.DeviceType { get }
}

extension DeviceType : RawRepresentable {
}

/// This namespace contains dictation-related types.
public struct Dictation {
}

extension Dictation {

    /// This class is used as the default service, until you
    /// register a custom implementation or register a valid
    /// KeyboardKit Pro license key.
    ///
    /// This service can also be resolved with the shorthand
    /// ``DictationService/disabled``.
    public class DisabledDictationService : KeyboardKit.DictationService {

        public init()

        public typealias AuthStatus = KeyboardKit.Dictation.AuthorizationStatus

        public var context: KeyboardKit.DictationContext { get }

        /// The current dictation authorization status.
        open var authorizationStatus: KeyboardKit.Dictation.DisabledDictationService.AuthStatus { get }

        /// A list of supported locales.
        open var supportedLocales: [Locale] { get }

        /// Start a dictation operation from the keyboard.
        open func startDictationFromKeyboard() async throws

        /// Start a dictation operation in the main app.
        open func startDictationInApp() async throws

        /// Abort an active dictation operation in the main app.
        open func abortDictationInApp() async throws

        /// Finish an active dictation operation in the main app.
        open func finishDictationInApp() async throws

        /// Try to return to the keyboard from the main app.
        open func returnToKeyboardFromApp() throws

        /// Handle any dictation result in the keyboard.
        open func handleDictationResultInKeyboard() async throws

        /// Request dictation authorization.
        open func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.DisabledDictationService.AuthStatus

        /// Undo the last performed dictation operation, if any.
        open func undoLastDictation()

        @objc deinit
    }
}

extension Dictation {

    /// This type contains setting section views and builder
    /// parameters that are used by the settings screen.
    ///
    /// You can use these sections as stand-alone views that
    /// can be added to any other screen.
    public struct SettingsScreenSections {
    }
}

extension Dictation {

    /// This type can be used to configure the localizations
    /// of a ``Dictation/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/dictationSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Dictation", dictationSection: String? = nil, dictationSectionSilenceLimit: String = "Silence Limit (sec)", dictationSectionFooter: String? = "Dictation will automatically stop after this many seconds of silence.")

        public var title: String

        public var dictationSection: String?

        public var dictationSectionSilenceLimit: String

        public var dictationSectionFooter: String?
    }
}

extension Dictation {

    /// This screen can be used as dictation settings screen
    /// in your app or keyboard extension.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``dictationSettingsScreenLocalization(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen<Header, DictationSection, Footer> : View where Header : View, DictationSection : View, Footer : View {

        /// Create a custom screen.
        ///
        /// - Parameters:
        ///   - header: The view to show at the top of the list.
        ///   - dictationSection: The dictation section content.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(@ViewBuilder header: @escaping () -> Header, @ViewBuilder dictationSection: @escaping KeyboardKit.Dictation.SettingsScreen<Header, DictationSection, Footer>.DictationSectionBuilder, @ViewBuilder footer: @escaping () -> Footer)

        public typealias Sections = KeyboardKit.Dictation.SettingsScreenSections

        public typealias DictationSectionBuilder = (KeyboardKit.Dictation.SettingsScreen<Header, DictationSection, Footer>.Sections.DictationParams) -> DictationSection

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Dictation {

    /// This enum defines various dictation statuses.
    public enum AuthorizationStatus : Int, KeyboardKit.KeyboardModel {

        case disabledService

        case notDetermined

        case denied

        case restricted

        case authorized

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: Int)

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = Int

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: Int { get }
    }
}

extension Dictation {

    /// This enum defines dictation service errors.
    ///
    /// These errors can be thrown by a ``DictationService``.
    public enum ServiceError : Error, KeyboardKit.KeyboardModel {

        /// The service is disabled.
        case disabledService

        /// The service is setup with an invalid deep link.
        case invalidDeepLink(String)

        /// The service hasn't got the proper authorization.
        case invalidDictationAuthorizationStatus(KeyboardKit.Dictation.AuthorizationStatus)

        /// The current keyboard back navigation host has an invalid URL.
        case invalidKeyboardBackNavigationHostUrl(String)

        /// The service is missing required app information.
        case missingAppInfo

        /// The service is missing a required deep link.
        case missingDictationDeepLink

        /// The service is missing a required keyboard back navigation host.
        case missingKeyboardBackNavigationHost

        /// The service is missing a keyboard configuration.
        case missingKeyboardConfiguration

        /// The service is missing a main app configuration.
        case missingMainAppConfiguration

        /// The service failed to setup a speech recognizer.
        case missingSpeechRecognizer

        /// The service failed to setup a speech request.
        case missingSpeechRecognitionRequest

        /// The service doesn't know how to navigate to the current keyboard back navigation host.
        case unknownKeyboardBackNavigationHost(String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Dictation.ServiceError, b: KeyboardKit.Dictation.ServiceError) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Dictation {

    /// This standard dictation engine can perform dictation
    /// where microphone access is available.
    ///
    /// This engine can also be resolved with this shorthand
    /// ``DictationEngine/standard(context:speechRecognizer:result:)``.
    ///
    /// See <doc:Dictation-Article> for more information.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/dictation`` feature. 
    open class StandardDictationEngine : KeyboardKit.DictationEngine {

        /// Create a standard dictation engine.
        ///
        /// - Parameters:
        ///   - context: The context to use to sync dictation state.
        ///   - speechRecognizer: The speech recognizer to use.
        ///   - result: The function to call when dictation finishes.
        public init(context: KeyboardKit.DictationContext, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer, result: @escaping (String) -> Void) throws

        /// The context to use to sync dictation state.
        final public let context: KeyboardKit.DictationContext

        /// The speech recognizer to use.
        final public let speechRecognizer: any KeyboardKit.DictationSpeechRecognizer

        /// The function to call when dictation finishes.
        final public let result: (String) -> Void

        /// The current dictation authorization status.
        open var authorizationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }

        /// A list of supported locales.
        open var supportedLocales: [Locale] { get }

        /// Whether or not the service is dictating.
        open var isDictating: Bool { get }

        /// Request dictation authorization.
        open func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.AuthorizationStatus

        /// Reset any previously set dictation result.
        open func resetDictationResult() async throws

        /// Start dictating with the provided configuration.
        public func startDictation(with locale: Locale) async throws

        /// Stop dictating.
        open func stopDictation() async throws

        /// Verify that the service is authorized for dictation.
        open func verifyAuthorizationStatus() throws

        @objc deinit
    }
}

extension Dictation {

    /// This service can be used to start dictation from the
    /// keyboard and perform it in the main app.
    ///
    /// This service can also be resolved with the shorthand
    /// ``DictationService/standardInKeyboard(dictationContext:keyboardContext:actionHandler:)``
    /// and ``DictationService/standardInApp(dictationContext:keyboardContext:openUrl:speechRecognizer:)``.
    ///
    /// See <doc:Dictation-Article> for more information.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/dictation`` feature.
    open class StandardDictationService : KeyboardKit.DictationService {

        /// Create a service instance that is intended to be
        /// used within a keyboard extension.
        ///
        /// - Parameters:
        ///   - config: The configuration to use.
        public init(keyboardConfiguration config: KeyboardKit.Dictation.StandardDictationService.KeyboardConfiguration) throws

        /// Create a service instance that is intended to be
        /// used within a main app target.
        ///
        /// - Parameters:
        ///   - config: The configuration to use.
        ///   - dictationEngine: A custom dictation engine, if any.
        public init(mainAppConfiguration config: KeyboardKit.Dictation.StandardDictationService.MainAppConfiguration, dictationEngine: (any KeyboardKit.DictationEngine)? = nil) throws

        /// A configuration for use in a keyboard extension.
        public struct KeyboardConfiguration {

            public init(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, actionHandler: any KeyboardKit.KeyboardActionHandler)

            public let dictationContext: KeyboardKit.DictationContext

            public let keyboardContext: KeyboardKit.KeyboardContext

            public let actionHandler: any KeyboardKit.KeyboardActionHandler
        }

        /// A configuration for use in the main app target.
        public struct MainAppConfiguration {

            public init(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, openUrl: OpenURLAction, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer)

            public let dictationContext: KeyboardKit.DictationContext

            public let keyboardContext: KeyboardKit.KeyboardContext

            public let openUrl: OpenURLAction

            public let speechRecognizer: any KeyboardKit.DictationSpeechRecognizer
        }

        /// The configuration to use in a keyboard extension.
        final public let keyboardConfiguration: KeyboardKit.Dictation.StandardDictationService.KeyboardConfiguration?

        /// The configuration to use in a main app target.
        final public let mainAppConfiguration: KeyboardKit.Dictation.StandardDictationService.MainAppConfiguration?

        /// The dictation context to use.
        public var dictationContext: KeyboardKit.DictationContext

        /// The engine to use to perform dictation.
        public var dictationEngine: any KeyboardKit.DictationEngine { get }

        /// The keyboard context to use.
        public var keyboardContext: KeyboardKit.KeyboardContext

        /// The current dictation authorization status.
        open var authorizationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }

        /// A list of supported locales.
        open var supportedLocales: [Locale] { get }

        /// Request dictation authorization.
        open func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.AuthorizationStatus

        /// Start dictation from a keyboard extension.
        open func startDictationFromKeyboard() async throws

        /// Start dictation within the main app.
        open func startDictationInApp() async throws

        /// Abort an ongoing dictation in the main app.
        open func abortDictationInApp() async throws

        /// Finish an active dictation in the main app.
        open func finishDictationInApp() async throws

        /// Try to return to the keyboard from the main app.
        ///
        /// > Important: Since the host application approach
        /// just supports some apps and relies on deep links,
        /// this approach is brittle. You can override it to
        /// customize this default behavior, and observe the
        /// ``DictationContext/returnToKeyboardFromAppError``
        /// to tell users to tap the top-leading system back
        /// button to navigate to the keyboard.
        open func returnToKeyboardFromApp() throws

        /// Handle a dictation result in the keyboard extension.
        open func handleDictationResultInKeyboard() async throws

        /// Try to handle a certain dictation result.
        open func handleDictationResult(text: String)

        /// Undo the last performed dictation operation, if any.
        open func undoLastDictation()

        @objc deinit
    }
}

extension Dictation {

    /// This style can be used to modify the visual style of
    /// the ``Dictation/BarVisualizer`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/dictationBarVisualizerStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct BarVisualizerStyle {

        /// Create a dictation loading indicator style.
        public init(bars: [Color] = ((0...100).map { _ in .primary }), barWidth: Double = 1, barHeight: Double = 64, barHeightScaleRange: ClosedRange<Double> = 0.1...0.7, barCornerRadius: Double = 3, barSpacing: Double = 5, barAlignment: Alignment = .center)

        public var bars: [Color]

        public var barWidth: Double

        public var barHeight: Double

        public var barHeightScaleRange: ClosedRange<Double>

        public var barCornerRadius: Double

        public var barSpacing: Double

        public var barAlignment: Alignment
    }
}

extension Dictation {

    /// This type represents the result that's returned when
    /// performing speech recognition.
    public struct SpeechRecognizerResult {

        /// Create a speech recognizer result value.
        public init(dictatedText: String?, error: (any Error)?, isFinal: Bool)

        /// The currently dictated text, if any.
        public let dictatedText: String?

        /// The last error, if any.
        public let error: (any Error)?

        /// Whether or not the result is the final result.
        public let isFinal: Bool
    }
}

extension Dictation {

    /// This class can be used to disable dictation.
    ///
    /// This service can also be resolved with the shorthand
    /// ``DictationEngine/disabled()``.
    ///
    /// See <doc:Dictation-Article> for more information.
    public class DisabledDictationEngine : KeyboardKit.DictationEngine {

        public init()

        /// The current dictation authorization status.
        open var authorizationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }

        /// A list of supported locales.
        open var supportedLocales: [Locale] { get }

        /// Request dictation authorization.
        open func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.AuthorizationStatus

        /// Reset any previously set dictation result.
        open func resetDictationResult() async throws

        /// Start dictating with the provided configuration.
        public func startDictation(with locale: Locale) async throws

        /// Stop dictating.
        open func stopDictation() async throws

        @objc deinit
    }
}

extension Dictation {

    /// This visualizer visualizes dictation progress with a
    /// horizontal list of animating bars.
    ///
    /// These visualizer bars do not use real frequency data,
    /// but animate randomly when `isDictating` is `true`. 
    ///
    /// You can style this component with the style modifier
    /// ``dictationBarVisualizerStyle(_:)``.
    @MainActor @preconcurrency public struct BarVisualizer : View {

        /// Create a bar visualizer.
        ///
        /// - Parameters:
        ///   - isAnimating: Whether or not to enable animations.
        @MainActor @preconcurrency public init(isAnimating: Bool)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Dictation {

    /// This style can be used to modify the visual style of
    /// the ``Dictation/Screen`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/dictationScreenStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ScreenStyle {

        /// Create a dictation screen style.
        ///
        /// For now, you can apply a foreground color to the
        /// entire view if you want to tint the content.
        public init(backgroundColor: Color = .clear, contentPadding: Double = 16, contentSpacing: Double = 30, showDictatedText: Bool = true, dictatedTextFont: Font = .body.bold())

        public var backgroundColor: Color

        public var contentPadding: Double

        public var contentSpacing: Double

        public var showDictatedText: Bool

        public var dictatedTextFont: Font
    }
}

extension Dictation {

    /// This is a disabled speech recognizer.
    ///
    /// This class is used as a placeholder, to avoid having
    /// to provide a real protocol implementation.
    ///
    /// See <doc:Dictation-Article> for more information.
    public class DisabledSpeechRecognizer : KeyboardKit.Dictation.DisabledDictationEngine, KeyboardKit.DictationSpeechRecognizer {

        /// Start dictating for the provided locale and handler.
        public func startDictation(with locale: Locale, resultHandler: ((KeyboardKit.Dictation.SpeechRecognizerResult) -> Void)?) async throws

        /// Setup a audio engine PCM buffer.
        public func setupAudioEngineBuffer(_ buffer: AVAudioPCMBuffer)

        override public init()

        @objc deinit
    }
}

extension Dictation {

    /// This view can be shown when dictation is in progress.
    ///
    /// The screen supports visualizing the dictation with a
    /// custom visualizer, e.g. a ``Dictation/BarVisualizer``.
    ///
    /// You can style this component with the style modifier
    /// ``dictationScreenStyle(_:)``.
    @MainActor @preconcurrency public struct Screen<TitleView, Visualizer, DoneButton> : View where TitleView : View, Visualizer : View, DoneButton : View {

        /// Create a standard dictation view.
        ///
        /// - Parameters:
        ///   - titleView: The title view to show above the indicator, if any.
        ///   - visualizer: The dictation indicator to show, e.g. ``Dictation/BarVisualizer``.
        ///   - doneButton: The done button to show bottommost, if any.
        @MainActor @preconcurrency public init(@ViewBuilder titleView: @escaping () -> TitleView, @ViewBuilder visualizer: @escaping (_ isLoading: Bool) -> Visualizer, @ViewBuilder doneButton: @escaping (@escaping KeyboardKit.Dictation.Screen<TitleView, Visualizer, DoneButton>.DoneButtonAction) -> DoneButton)

        public typealias DoneButtonAction = () -> Void

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Dictation.SettingsScreenSections {

    /// This struct defines view builder parameters that are
    /// used to create a dictation settings section.
    public struct DictationParams {

        public let view: KeyboardKit.Dictation.SettingsScreenSections.DictationContent
    }

    /// This is a standard dictation settings section.
    @MainActor @preconcurrency public struct DictationContent : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Dictation.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Dictation.SettingsScreenLocalization { get }
}

extension Dictation.SettingsScreen where Header == EmptyView, DictationSection == KeyboardKit.Dictation.SettingsScreenSections.DictationContent, Footer == EmptyView {

    /// Create a custom screen.
    @MainActor @preconcurrency public init()
}

extension Dictation.SettingsScreen : Sendable {
}

extension Dictation.AuthorizationStatus : RawRepresentable {
}

extension Dictation.BarVisualizerStyle {

    /// A standard bar visualizer style.
    public static var standard: KeyboardKit.Dictation.BarVisualizerStyle { get }
}

extension Dictation.BarVisualizer : Sendable {
}

extension Dictation.ScreenStyle {

    /// A standard dictation screen style.
    public static var standard: KeyboardKit.Dictation.ScreenStyle { get }
}

extension Dictation.Screen : Sendable {
}

extension Dictation.SettingsScreenSections.DictationContent : Sendable {
}

/// This class has observable states and persistent settings
/// for keyboard-based dictation.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings,
/// like the ``DictationSettings/silenceLimit``.
final public class DictationContext : ObservableObject {

    /// Create a dictation context instance.
    public init()

    /// Dictation-specific, auto-persisted settings.
    @Published final public var settings: KeyboardKit.DictationSettings

    final public var $settings: Published<KeyboardKit.DictationSettings>.Publisher

    /// The currently dictated text, which will auto-persist.
    @Published final public var dictatedText: String

    final public var $dictatedText: Published<String>.Publisher

    /// Whether dictation is currently in progress.
    @Published final public var isDictating: Bool

    final public var $isDictating: Published<Bool>.Publisher

    /// The last applied dictation error.
    @Published final public var lastError: (any Error)?

    final public var $lastError: Published<(any Error)?>.Publisher

    /// The last inserted dictated text.
    @Published final public var lastInsertedText: String?

    final public var $lastInsertedText: Published<String?>.Publisher

    /// The last navigate back error.
    @Published final public var returnToKeyboardFromAppError: (any Error)?

    final public var $returnToKeyboardFromAppError: Published<(any Error)?>.Publisher

    /// A strong reference to the current dictation service.
    final public var service: (any KeyboardKit.DictationService)?

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension DictationContext {

    /// The bundle ID of the app that is using the keyboard.
    ///
    /// This is automatically set by the keyboard to let the
    /// main app navigate back to the keyboard.
    final public var hostApplicationBundleId: String?

    /// Whether dictation has been started by a keyboard.
    ///
    /// This is automatically set by the keyboard to let the
    /// main app start dictation if it cannot use deep links.
    final public var isDictationStartedByKeyboard: Bool
}

extension DictationContext {

    /// Reset the context.
    final public func reset()

    /// Set ``isDictating`` with an animation.
    final public func setIsDictating(_ value: Bool)
}

extension DictationContext : KeyboardKit.KeyboardHostApplicationProvider {
}

extension DictationContext {

    public static var preview: KeyboardKit.DictationContext { get }
}

/// This protocol can be implemented by any classes that can
/// perform dictation by accessing the microphone.
///
/// See <doc:Dictation-Article> for more information.
public protocol DictationEngine : AnyObject {

    /// The current dictation authorization status.
    var authorizationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }

    /// A list of supported locales.
    var supportedLocales: [Locale] { get }

    /// Request dictation authorization.
    func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.AuthorizationStatus

    /// Reset any previously set dictation result.
    func resetDictationResult() async throws

    /// Start dictating with the provided configuration.
    func startDictation(with locale: Locale) async throws

    /// Stop dictating.
    func stopDictation() async throws
}

extension DictationEngine where Self == KeyboardKit.Dictation.StandardDictationEngine {

    /// Create a ``Dictation/StandardDictationEngine`` instance.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/dictation`` feature.
    ///
    /// - Parameters:
    ///   - context: The context to sync dictation state to.
    ///   - speechRecognizer: The speech recognizer to use.
    ///   - result: The function to call when dictation finishes.
    public static func standard(context: KeyboardKit.DictationContext, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer, result: @escaping (String) -> Void) throws -> Self
}

extension DictationEngine where Self == KeyboardKit.Dictation.DisabledDictationEngine {

    /// Create a ``Dictation/DisabledDictationEngine`` instance.
    public static func disabled() throws -> Self
}

/// This protocol can be implemented by any classes that can
/// perform dictation from a keyboard extension or in an app.
///
/// Since keyboard extensions can't access the microphone, a
/// dictation service must open the main app to let it start
/// dictation, write the dictation results to a shared state,
/// then navigate back to the keyboard and handle the result.
///
/// KeyboardKit doesn't have a standard dictation service as
/// it has for other services. Instead, a disabled dictation
/// service will be used until you setup KeyboardKit Pro, or
/// register a custom service.
public protocol DictationService : AnyObject {

    /// The current dictation authorization status.
    var authorizationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }

    /// A list of supported locales.
    var supportedLocales: [Locale] { get }

    /// Request dictation authorization.
    func requestDictationAuthorization() async throws -> KeyboardKit.Dictation.AuthorizationStatus

    /// Start a dictation operation from the keyboard.
    func startDictationFromKeyboard() async throws

    /// Start a dictation operation in the main app.
    func startDictationInApp() async throws

    /// Abort an active dictation operation in the main app.
    func abortDictationInApp() async throws

    /// Finish an active dictation operation in the main app.
    func finishDictationInApp() async throws

    /// Try to return to the keyboard from the main app.
    func returnToKeyboardFromApp() throws

    /// Handle any dictation result in the keyboard.
    func handleDictationResultInKeyboard() async throws

    /// Undo the last performed dictation operation, if any.
    func undoLastDictation()
}

extension DictationService where Self == KeyboardKit.Dictation.DisabledDictationService {

    /// Create a ``Dictation/DisabledDictationService`` instance.
    public static var disabled: KeyboardKit.Dictation.DisabledDictationService { get }
}

extension DictationService where Self == KeyboardKit.Dictation.DisabledDictationService {

    public static var preview: KeyboardKit.Dictation.DisabledDictationService { get }
}

extension DictationService where Self == KeyboardKit.Dictation.StandardDictationService {

    /// Create a standard dictation service that is meant to
    /// be used within the main app target.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/dictation`` feature.
    ///
    /// - Parameters:
    ///   - dictationContext: The dictation context to use.
    ///   - keyboardContext: The keyboard context to use.
    ///   - openUrl: The URL open action to use.
    ///   - speechRecognizer: The speech recognizer to use.
    public static func standardInApp(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, openUrl: OpenURLAction, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer) throws -> Self

    /// Create a standard dictation service that is meant to
    /// be used within the keyboard extension.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/dictation`` feature.
    ///
    /// - Parameters:
    ///   - dictationContext: The dictation context to use.
    ///   - keyboardContext: The keyboard context to use.
    ///   - actionHandler: The keyboard action handler to use.
    public static func standardInKeyboard(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, actionHandler: any KeyboardKit.KeyboardActionHandler) throws -> Self
}

/// This type has auto-persisted dictation settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `dictation` prefix.
public struct DictationSettings : Sendable {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use for the namespace.
    public static var settingsPrefix: String { get }

    /// The max number of seconds of silence, after which dictation automatically ends.
    @AppStorage public var silenceLimit: TimeInterval { get nonmutating set }

    public var $silenceLimit: Binding<TimeInterval> { get }
}

extension DictationSettings {

    public static var preview: KeyboardKit.DictationSettings { get }
}

/// This protocol can be implemented by any classes that can
/// perform speech recognition.
///
/// KeyboardKit doesn't include a standard speech recognizer
/// implementation, since referring to `Speech` will require
/// all apps to add speech permissions to their `Info.plist`.
///
/// Instead, you can copy a speech recognizer implementation
/// from the <doc:Dictation-Article> article or the demo app.
public protocol DictationSpeechRecognizer : KeyboardKit.DictationEngine {

    /// Start dictating for the provided locale and handler.
    func startDictation(with locale: Locale, resultHandler: ((KeyboardKit.Dictation.SpeechRecognizerResult) -> Void)?) async throws

    /// Setup a audio engine PCM buffer.
    func setupAudioEngineBuffer(_ buffer: AVAudioPCMBuffer)
}

extension DictationSpeechRecognizer where Self == KeyboardKit.Dictation.DisabledSpeechRecognizer {

    public static var disabled: KeyboardKit.Dictation.DisabledSpeechRecognizer { get }
}

/// This protocol can be implemented by any type that can be
/// used to handle drag gestures.
public protocol DragGestureHandler {

    /// Handle a drag gesture from start to current location.
    func handleDragGesture(from startLocation: CGPoint, to currentLocation: CGPoint)
}

extension DragGestureHandler where Self == KeyboardKit.Gestures.SpacebarDragGestureHandler {

    /// Create a ``Gestures/SpacebarDragGestureHandler``.
    ///
    /// - Parameters:
    ///   - sensitivity: The drag sensitivity, by default `.medium`.
    ///   - verticalThreshold: The vertical threshold in points, by default `50`.
    ///   - action: The action to perform when the offset changes.
    public static func spacebarDrag(sensitivity: KeyboardKit.Keyboard.SpacebarDragSensitivity? = nil, verticalThreshold: Double? = nil, action: @escaping (Int) -> Void) -> Self
}

/// This type represents an emoji character and is used as a
/// namespace for emoji-related types and functionality.
public struct Emoji : Equatable, Codable, Hashable, Identifiable, Sendable {

    /// Create an emoji from a certain character.
    public init(_ char: Character)

    /// Create an emoji from a certain string.
    public init(_ char: String)

    /// The emoji character string.
    public let char: String

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.Emoji, b: KeyboardKit.Emoji) -> Bool

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = String

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension Emoji : KeyboardKit.Localizable {

    /// The `Localizable.strings` key to use when localizing.
    public var localizationKey: String { get }
}

extension Emoji {

    /// Whether the emoji matches a certain query.
    public func matches(_ query: String, in locale: Locale = .current) -> Bool
}

extension Emoji {

    /// This class can be used to set up emoji search, where
    /// `:smile` can be used to get a smiling emoji.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/emojiAddons`` feature.
    public class ColonSearch : ObservableObject {

        /// Create a colon search instance, with an optional
        /// additional dictionary.
        public init(additional: [String : String] = [:]) throws

        /// The type of publisher that emits before the object has changed.
        public typealias ObjectWillChangePublisher = ObservableObjectPublisher

        @objc deinit
    }
}

extension Emoji {

    /// The emoji's unique identifier with a category prefix.
    ///
    /// This can be used to get a unique identifier for each
    /// category, e.g. when listing multiple categories that
    /// can contain the same emoji.
    public func id(in category: KeyboardKit.EmojiCategory) -> String
}

extension Emoji {

    /// If the emoji is available in the current runtime.
    public var isAvailableInCurrentRuntime: Bool { get }

    /// If the emoji is unavailable in the current runtime.
    public var isUnavailableInCurrentRuntime: Bool { get }
}

extension Emoji {

    /// The emoji's unique identifier.
    public var id: String { get }
}

extension Emoji : Transferable {

    /// The representation used to import and export the item.
    ///
    /// A ``transferRepresentation`` can contain multiple representations
    /// for different content types.
    public static var transferRepresentation: some TransferRepresentation { get }

    /// The type of the representation used to import and export the item.
    ///
    /// Swift infers this type from the return value of the
    /// ``transferRepresentation`` property.
    public typealias Representation = some TransferRepresentation
}

extension Emoji {

    /// Get all emojis from all standard categories.
    ///
    /// This property only returns emojis that are available
    /// the current runtime.
    public static let all: [KeyboardKit.Emoji]
}

extension Emoji {

    /// The emoji's unique identifier.
    public var unicodeIdentifier: String? { get }

    /// The emoji's full, readable unicode name.
    ///
    /// Note that this name may not always be what you want to display to users. For that, use the localized name instead.
    public var unicodeName: String { get }
}

extension Emoji {

    /// This emoji keyboard menu can be used to select emoji
    /// categories in an ``EmojiKeyboard``.
    @MainActor @preconcurrency public struct KeyboardSearchField : View {

        /// Create an emoji keyboard search field.
        @MainActor @preconcurrency public init(didSelectEmoji: ((KeyboardKit.Emoji) -> Void)? = nil) throws

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Emoji {

    /// All available skin tone variants as keyboard actions.
    public var skinToneVariantActions: [KeyboardKit.KeyboardAction] { get }
}

extension Emoji {

    /// Whether or not the emoji has any skin tone variants.
    public var hasSkinToneVariants: Bool { get }

    /// The emoji's neutral skin tone variant.
    public var neutralSkinToneVariant: KeyboardKit.Emoji { get }

    /// All skin tone variants for the emoji, if any.
    public var skinToneVariants: [KeyboardKit.Emoji] { get }
}

extension Emoji.KeyboardSearchField : Sendable {
}

/// This enum defines the standard emoji categories, as well
/// as their emojis.
///
/// The ``persisted(_:)`` category uses a ``Persisted`` type
/// that defines special categories to which you can add and
/// remove emojis, like ``Persisted/recent``.
///
/// Use the ``custom(id:name:emojis:iconName:)`` to create a
/// custom category with static emojis. You can use a custom
/// name and SF Symbol to define how it's presented.
///
/// You can also create a custom ``Persisted`` category when
/// you want to be able to add and remove emojis.
public enum EmojiCategory : Codable, Equatable, Hashable, Identifiable, Sendable {

    case smileysAndPeople

    case animalsAndNature

    case foodAndDrink

    case activity

    case travelAndPlaces

    case objects

    case symbols

    case flags

    case custom(id: String, name: String, emojis: [KeyboardKit.Emoji], iconName: String = "")

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.EmojiCategory, b: KeyboardKit.EmojiCategory) -> Bool

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = String

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension EmojiCategory : KeyboardKit.Localizable {

    /// The `Localizable.strings` key to use when localizing.
    public var localizationKey: String { get }
}

extension EmojiCategory {

    /// This type can be used to handle persisted categories,
    /// where emojis can be customzied and persisted.
    ///
    /// You can use the predefined emoji categories, such as
    /// ``favorites``, and ``recent`` and set up custom ones.
    public struct Persisted : Codable, Equatable, Hashable, Identifiable, Sendable {

        /// Create a custom persisted emoji category.
        ///
        /// - Parameters:
        ///   - id: The category ID.
        ///   - name: The category name.
        ///   - iconName: The category icon name.
        ///   - initialEmojis: The emojis to initialize the category with, if any.
        ///   - insertionStrategy: The insertion strategy to use when adding emojis.
        public init(id: String, name: String, iconName: String, initialEmojis: [KeyboardKit.Emoji] = [], insertionStrategy: KeyboardKit.EmojiCategory.Persisted.InsertionStrategy)

        /// The category ID.
        public let id: String

        /// The category name.
        public var name: String { get }

        /// The category icon name.
        public let iconName: String

        /// The default emojis to return if no data is saved.
        public let initialEmojis: [KeyboardKit.Emoji]

        /// The insertion strategy to use.
        public let insertionStrategy: KeyboardKit.EmojiCategory.Persisted.InsertionStrategy

        /// The store to use.
        public var store: UserDefaults { get }

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.EmojiCategory.Persisted, b: KeyboardKit.EmojiCategory.Persisted) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension EmojiCategory : Transferable {

    /// The representation used to import and export the item.
    ///
    /// A ``transferRepresentation`` can contain multiple representations
    /// for different content types.
    public static var transferRepresentation: some TransferRepresentation { get }

    /// The type of the representation used to import and export the item.
    ///
    /// Swift infers this type from the return value of the
    /// ``transferRepresentation`` property.
    public typealias Representation = some TransferRepresentation
}

extension EmojiCategory {

    /// A persisted favorites category.
    public static var favorites: KeyboardKit.EmojiCategory { get }

    /// A persisted frequent category.
    public static var frequent: KeyboardKit.EmojiCategory { get }

    /// A persisted recent category.
    public static var recent: KeyboardKit.EmojiCategory { get }

    /// A custom, persisted category.
    public static func persisted(_ category: KeyboardKit.EmojiCategory.Persisted) -> KeyboardKit.EmojiCategory

    /// A custom category with an emoji search result.
    public static func search(query: String) -> KeyboardKit.EmojiCategory
}

extension EmojiCategory {

    /// Get an ordered list of all standard categories, with
    /// no ``EmojiCategory/frequent`` emojis firstmost.
    public static let standardCategories: [KeyboardKit.EmojiCategory]
}

extension EmojiCategory {

    /// The category's unique identifier.
    public var id: String { get }

    /// An SF Symbol-based icon for the category.
    public var symbolIcon: Image { get }

    /// A list of all available emojis in the category.
    public var emojis: [KeyboardKit.Emoji] { get }

    /// A list of all available emojis in the category, as a
    /// concatenated string.
    public var emojisString: String { get }

    /// Whether or not the category has any emojis.
    public var hasEmojis: Bool { get }
}

extension EmojiCategory {

    /// A list of all available emoji actions in a category.
    public var emojiActions: [KeyboardKit.KeyboardAction] { get }

    /// The emoji category icon.
    public var icon: Image { get throws }
}

extension EmojiCategory.Persisted : KeyboardKit.Localizable {

    /// The `Localizable.strings` key to use when localizing.
    public var localizationKey: String { get }
}

extension EmojiCategory.Persisted {

    /// A strategy that defines how new emojis are added.
    public enum InsertionStrategy : Codable, Equatable, Sendable {

        /// Append new emojis last in the category.
        case append

        /// Insert new emojis with a frequent strategy.
        case frequent

        /// Insert new emojis first in the category.
        case insertFirst

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.EmojiCategory.Persisted.InsertionStrategy, b: KeyboardKit.EmojiCategory.Persisted.InsertionStrategy) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension EmojiCategory.Persisted {

    /// A persisted category for favorite emojis.
    public static let favorites: KeyboardKit.EmojiCategory.Persisted

    /// A persisted category for frequently used emojis.
    public static let frequent: KeyboardKit.EmojiCategory.Persisted

    /// A persisted category for recent emojis.
    public static let recent: KeyboardKit.EmojiCategory.Persisted
}

extension EmojiCategory.Persisted {

    /// Add an emoji to the category.
    public func addEmoji(_ emoji: KeyboardKit.Emoji)

    /// Get the persisted emojis for the category.
    public func getEmojis() -> [KeyboardKit.Emoji]

    /// Get the max number of emojis to persist.
    public func getEmojisMaxCount() -> Int

    /// Remove an emoji from the category.
    public func removeEmoji(_ emoji: KeyboardKit.Emoji)

    /// Reset the category.
    public func reset()

    /// Reset the category emojis to its initial value.
    public func resetEmojis()

    /// Reset the category max count.
    public func resetEmojisMaxCount()

    /// Set the persisted emojis for the category.
    public func setEmojis(_ emojis: [KeyboardKit.Emoji]?)

    /// Set the max number of emojis to persist.
    public func setEmojisMaxCount(_ val: Int)
}

extension EmojiCategory.Persisted.InsertionStrategy : Hashable {
}

/// This class has observable states and persistent settings
/// for emoji-related functionality.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings,
/// like the ``EmojiSettings/preferredSkinTones`` for emojis.
final public class EmojiContext : ObservableObject {

    /// Create an autocomplete context instance.
    public init()

    /// Auto-persisted emoji settings.
    @Published final public var settings: KeyboardKit.EmojiSettings

    final public var $settings: Published<KeyboardKit.EmojiSettings>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension EmojiContext {

    public static var preview: KeyboardKit.EmojiContext { get }
}

@MainActor @preconcurrency public struct EmojiKeyboard : View {

    @MainActor @preconcurrency public init(didPressBackspace: (() -> Void)? = nil, didPressChangeKeyboard: (() -> Void)? = nil, didSelectEmoji: ((KeyboardKit.Emoji) -> Void)? = nil, didSelectEmojiCategory: ((KeyboardKit.EmojiCategory) -> Void)? = nil)

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension EmojiKeyboard {

    final public class State : ObservableObject {

        public init()

        @AppStorage final public var selectedCategoryIndex: Int

        final public var $selectedCategoryIndex: Binding<Int> { get }

        /// The type of publisher that emits before the object has changed.
        public typealias ObjectWillChangePublisher = ObservableObjectPublisher

        @objc deinit
    }
}

extension EmojiKeyboard {

    /// This struct defines emoji keyboard-specific sizes.
    ///
    /// The ``standard`` sizes correspond to iPhone portrait
    /// and landscape, while
    public struct Sizes : Sendable {

        public init(emojiFontSize: Double = 40, emojiFrameWidth: Double = 55, emojiFrameHeight: Double = 43, emojiSelectedScale: Double = 1.5, menuButtonWidth: Double = 45, menuButtonHeight: Double = 40, menuFontSize: Double = 16, menuFontSizeBackspace: Double = 24, menuHeight: Double = 44, menuHighlightSize: Double = 35, menuIconSize: Double = 17, popoverEmojiFontSize: Double = 30, popoverEmojiFrameWidth: Double = 45, popoverEmojiFrameHeight: Double = 35, popoverEmojiSelectedCornerRadius: Double = 8, popoverEmojiSelectedScale: Double = 1.2, popoverSwipeDownCancelThreshold: Double = 100, popoverVerticalOffset: Double = -15)

        public var emojiFontSize: Double

        public var emojiFrameWidth: Double

        public var emojiFrameHeight: Double

        public var emojiSelectedScale: Double

        public var menuButtonWidth: Double

        public var menuButtonHeight: Double

        public var menuFontSize: Double

        public var menuFontSizeBackspace: Double

        public var menuIconSize: Double

        public var menuHeight: Double

        public var menuHighlightSize: Double

        public var popoverEmojiFontSize: Double

        public var popoverEmojiFrameWidth: Double

        public var popoverEmojiFrameHeight: Double

        public var popoverEmojiSelectedCornerRadius: Double

        public var popoverEmojiSelectedScale: Double

        public var popoverSwipeDownCancelThreshold: Double

        public var popoverVerticalOffset: Double
    }
}

extension EmojiKeyboard {

    /// This struct defines emoji keyboard-specific configs.
    public struct Configuration : Sendable {

        public init(isSkinTonePopoverEnabled: Bool = true, selectedEmojiAnimationDuration: Double = 0.1, selectedEmojiOpacity: Double = 0.75, showBottomRowControlKeys: Bool = true)

        public var isSkinTonePopoverEnabled: Bool

        public var showBottomRowControlKeys: Bool

        public var selectedEmojiAnimationDuration: Double

        public var selectedEmojiOpacity: Double

        public var preferredSkinTines: [KeyboardKit.Emoji : KeyboardKit.Emoji]

        public var preferredSkinTones: [KeyboardKit.Emoji : KeyboardKit.Emoji]
    }
}

extension EmojiKeyboard {

    /// This struct defines emoji keyboard-specific styles.
    public struct Style : Sendable {

        public init(foregroundColor: Color = .primary.opacity(0.75), menuSelectedBackgroundColor: Color = .secondary.opacity(0.15), menuSelectedForegroundColor: Color = .primary.opacity(0.75), popoverSelectedBackgroundColor: Color = .secondary.opacity(0.15))

        public var foregroundColor: Color

        public var menuSelectedBackgroundColor: Color

        public var menuSelectedForegroundColor: Color

        public var popoverSelectedBackgroundColor: Color
    }
}

extension EmojiKeyboard : Sendable {
}

extension EmojiKeyboard.Sizes {

    public static var standard: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var iPad: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var iPhone: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var iPhoneLandscape: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var small: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var medium: KeyboardKit.EmojiKeyboard.Sizes { get }

    public static var large: KeyboardKit.EmojiKeyboard.Sizes { get }
}

extension EmojiKeyboard.Sizes {

    public var emojiFrameSize: CGSize { get }

    public var popoverEmojiFrameSize: CGSize { get }

    public var popoverTopPartSize: CGSize { get }
}

extension EmojiKeyboard.Configuration {

    public static var standard: KeyboardKit.EmojiKeyboard.Configuration { get }
}

extension EmojiKeyboard.Style {

    public var uiForegroundColor: UIColor { get }

    public var uiMenuSelectedBackgroundColor: UIColor { get }

    public var uiMenuSelectedForegroundColor: UIColor { get }

    public var uiPopoverSelectedBackgroundColor: UIColor { get }
}

extension EmojiKeyboard.Style {

    public static var standard: KeyboardKit.EmojiKeyboard.Style { get }
}

/// This type has auto-persisted emoji settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with an `emojis` prefix.
public struct EmojiSettings : Sendable {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }

    /// The preferred skin tone index for all emojis.
    @AppStorage public var preferredSkinToneIndex: Int { get }

    public var $preferredSkinToneIndex: Binding<Int> { get }

    /// The preferred skin tones for various emojis.
    @AppStorage public var preferredSkinTones: [KeyboardKit.Emoji : KeyboardKit.Emoji] { get nonmutating set }

    public var $preferredSkinTones: Binding<[KeyboardKit.Emoji : KeyboardKit.Emoji]> { get }
}

extension EmojiSettings {

    /// Display the ``preferredSkinToneIndex`` with an emoji.
    ///
    /// This will use that index instead of the current skin
    /// tone, in case it has changed for the emoji.
    ///
    /// If you provide an emoji that has no skin tones, then
    /// the thumbs up emoji will be used.
    public func preferredSkinToneForAllEmojis(with emoji: KeyboardKit.Emoji = Emoji("👍")) -> KeyboardKit.Emoji

    /// Get the preferred skin tone for a certain emoji.
    ///
    /// > Important: This ONLY works for a neutral skin tone,
    /// to keep performance in the emoji grid. We could make
    /// changes to hard-code this information to avoid using
    /// unicode calculations, since they slow down the emoji
    /// keyboard when being called excessively.
    public func preferredSkinTone(for emoji: KeyboardKit.Emoji) -> KeyboardKit.Emoji

    /// Register the preferred skin tone for a certain emoji.
    public func setPreferredSkinTone(_ emoji: KeyboardKit.Emoji)

    /// Register a preferred skin tone for all emojis.
    public func setPreferredSkinToneForAllEmojis(using emoji: KeyboardKit.Emoji)

    /// Rest the preferred skin tone for a certain emoji.
    public func resetPreferredSkinTone(for emoji: KeyboardKit.Emoji)

    /// Rest the preferred skin tone for all emojis.
    public func resetPreferredSkinToneForAllEmojis()
}

extension EmojiSettings {

    public static var preview: KeyboardKit.EmojiSettings { get }
}

/// This enum defines all supported Emoji versions, that are
/// currently available to macOS.
///
/// The static ``EmojiVersion/all`` property will return all
/// versions while ``EmojiVersion/allAvailable`` will return
/// all versions available to the current runtime. This will
/// be used to let ``EmojiCategory`` just return emojis that
/// are available to the current runtime.
public struct EmojiVersion : CaseIterable, Equatable, Identifiable {

    /// The latest version for a certain iOS version.
    public init?(iOS version: Double)

    /// The latest version for a certain macOS version.
    public init?(macOS version: Double)

    /// The latest version for a certain tvOS version.
    public init?(tvOS version: Double)

    /// The latest version for a certain watchOS version.
    public init?(watchOS version: Double)

    /// The unique version ID.
    public var id: Double { get }

    /// The emojis to that were added in this version.
    public var emojis: [KeyboardKit.Emoji]

    /// The emojis to that were added in this version.
    public var emojiString: String

    /// The emoji version in which emojis became available.
    public let version: Double

    /// The iOS version in which emojis became available.
    public let iOS: Double

    /// The macOS version in which emojis became available.
    public let macOS: Double

    /// The tvOS version in which emojis became available.
    public let tvOS: Double

    /// The watchOS version in which emojis became available.
    public let watchOS: Double

    /// An optional comment, which can be used to discuss if
    /// the version made any changes that isn't reflected in
    /// the emojis it provides.
    public let comment: String?

    /// The version's display name, e.g. `15.1`.
    public var displayName: String { get }

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.EmojiVersion, b: KeyboardKit.EmojiVersion) -> Bool

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.EmojiVersion]

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = Double
}

extension EmojiVersion {

    /// All emoji versions that are defined in the library.
    public static var all: [KeyboardKit.EmojiVersion] { get }

    /// All emoji versions that are defined in the library.
    public static var allCases: [KeyboardKit.EmojiVersion] { get }

    public static var v16_0: KeyboardKit.EmojiVersion { get }

    public static var v15_1: KeyboardKit.EmojiVersion { get }

    public static var v15: KeyboardKit.EmojiVersion { get }

    public static var v14: KeyboardKit.EmojiVersion { get }

    public static var v13_1: KeyboardKit.EmojiVersion { get }

    public static var v13: KeyboardKit.EmojiVersion { get }

    public static var v12_1: KeyboardKit.EmojiVersion { get }

    public static var v12: KeyboardKit.EmojiVersion { get }

    public static var v11: KeyboardKit.EmojiVersion { get }
}

extension EmojiVersion {

    /// The ``EmojiVersion`` that is used by the current OS.
    public static var current: KeyboardKit.EmojiVersion { get }

    /// All currently unavailable emojis.
    public static let currentUnavailableEmojis: [KeyboardKit.Emoji]

    /// All currently unavailable emojis, performance cached.
    public static let currentUnavailableEmojisDictionary: [String : KeyboardKit.Emoji]

    /// All emoji versions that are available to the runtime.
    public static var allAvailable: [KeyboardKit.EmojiVersion] { get }

    /// All emoji versions that are available for a runtime.
    public static func allAvailableIn(_ version: Double = 1_000, iOS: Double = 1_000, macOS: Double = 1_000, tvOS: Double = 1_000, watchOS: Double = 1_000) -> [KeyboardKit.EmojiVersion]

    /// Get all earlier Emoji versions for this version.
    public var earlierVersions: [KeyboardKit.EmojiVersion] { get }

    /// Get all later Emoji versions for this version.
    public var laterVersions: [KeyboardKit.EmojiVersion] { get }

    /// Get all older Emoji versions specified by this type.
    public var olderVersions: [KeyboardKit.EmojiVersion] { get }

    /// Get all emojis that are unavailable in this version.
    public var unavailableEmojis: [KeyboardKit.Emoji] { get }
}

/// This enum defines available experiments.
///
/// The ``isActive`` flag shows if an experiment is actively
/// used by the SDK to toggle features and changes on, while
/// ``isEnabled`` can be used to get the enabled state of an
/// experiment and ``setIsEnabled(_:)`` to enable or disable
/// an experiment.
///
/// Use ``ExperimentContext`` to enable and disable features.
public enum Experiment : String, CaseIterable, KeyboardKit.KeyboardModel {

    /// This feature can be used to enable layout caching to
    /// avoid unnecessary recalculations of the same layouts.
    case layoutCaching

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.Experiment]

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String

    /// A collection of all values of this type.
    nonisolated public static var allCases: [KeyboardKit.Experiment] { get }

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }
}

extension Experiment {

    /// Whether the experiment is active.
    public var isActive: Bool { get }
}

extension Experiment {

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }
}

extension Experiment {

    public static var activeExperiments: [KeyboardKit.Experiment] { get }

    /// Whether the experiment is enabled.
    public var isEnabled: Bool { get }

    /// Set whether the experiment is enabled.
    public func setIsEnabled(_ value: Bool)
}

extension Experiment : RawRepresentable {
}

/// This context can be used to keep track of if an external
/// keyboard is connected to the device.
///
/// KeyboardKit Pro automatically keeps this context updated
/// as external keyboards are connected and disconnected.
///
/// You can set the ``isEnabledOnSimulator`` setting to true
/// to enable external keyboard observation on Simulator. It
/// is disabled by default, since the computer keyboard will
/// otherwise be detected as an external keyboard.
///
/// > Important: The Apple Smart Keyboard Folio keyboard can
/// be connected to a device without being actively used. It
/// should be fixed to only be considered connected when the
/// keyboard is actively being used.
final public class ExternalKeyboardContext : ObservableObject {

    /// Create an external keyboard context.
    ///
    /// - Parameters:
    ///   - notificationCenter: The notification center to use.
    public init(notificationCenter: NotificationCenter = .default)

    /// The notification center to use for observations.
    final public let notificationCenter: NotificationCenter

    /// Whether the context is enabled on simulator.
    @Published final public var isEnabledOnSimulator: Bool

    final public var $isEnabledOnSimulator: Published<Bool>.Publisher

    /// Whether an external keyboard is connected.
    @Published final public var isExternalKeyboardConnected: Bool

    final public var $isExternalKeyboardConnected: Published<Bool>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

/// This namespace contains keyboard feedback-related types.
public struct Feedback {
}

extension Feedback {

    /// This view can toggle audio & haptic feedback on/off.
    @MainActor @preconcurrency public struct Toggle<ViewType> : View where ViewType : View {

        @MainActor @preconcurrency public init(type: KeyboardKit.Feedback.Toggle<ViewType>.FeedbackType, enabledView: ViewType, disabledView: ViewType) throws

        public enum FeedbackType {

            case audio

            case haptic

            /// Returns a Boolean value indicating whether two values are equal.
            ///
            /// Equality is the inverse of inequality. For any values `a` and `b`,
            /// `a == b` implies that `a != b` is `false`.
            ///
            /// - Parameters:
            ///   - lhs: A value to compare.
            ///   - rhs: Another value to compare.
            public static func == (a: KeyboardKit.Feedback.Toggle<ViewType>.FeedbackType, b: KeyboardKit.Feedback.Toggle<ViewType>.FeedbackType) -> Bool

            /// Hashes the essential components of this value by feeding them into the
            /// given hasher.
            ///
            /// Implement this method to conform to the `Hashable` protocol. The
            /// components used for hashing must be the same as the components compared
            /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
            /// with each of these components.
            ///
            /// - Important: In your implementation of `hash(into:)`,
            ///   don't call `finalize()` on the `hasher` instance provided,
            ///   or replace it with a different instance.
            ///   Doing so may become a compile-time error in the future.
            ///
            /// - Parameter hasher: The hasher to use when combining the components
            ///   of this instance.
            public func hash(into hasher: inout Hasher)

            /// The hash value.
            ///
            /// Hash values are not guaranteed to be equal across different executions of
            /// your program. Do not save hash values to use during a future execution.
            ///
            /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
            ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
            ///   The compiler provides an implementation for `hashValue` for you.
            public var hashValue: Int { get }
        }

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Feedback {

    /// This standard service can be used to trigger various
    /// audio and haptic feedback, using system capabilities.
    ///
    /// KeyboardKit automatically creates an instance of the
    /// class when the keyboard is launched, then injects it
    /// into ``KeyboardInputViewController/services``.
    ///
    /// You can inherit this class to get base functionality,
    /// then override any open parts that you want to change.
    ///
    /// This service can also be resolved with the shorthand
    /// ``FeedbackService/standard``.
    open class StandardFeedbackService : KeyboardKit.FeedbackService {

        /// Create a standard feedback service.
        public init()

        /// The audio feedback engine to use.
        public var audioEngine: KeyboardKit.Feedback.AudioEngine

        /// The haptic feedback engine to use.
        public var hapticEngine: KeyboardKit.Feedback.HapticEngine

        /// Trigger the provided audio feedback.
        open func triggerAudioFeedback(_ feedback: KeyboardKit.Feedback.Audio)

        /// Trigger the provided haptic feedback.
        open func triggerHapticFeedback(_ feedback: KeyboardKit.Feedback.Haptic)

        @objc deinit
    }
}

extension Feedback {

    /// This service can be used to disable all feedback.
    ///
    /// This service can also be resolved with the shorthand
    /// ``FeedbackService/disabled``.
    open class DisabledFeedbackService : KeyboardKit.FeedbackService {

        /// Create a disabled feedback service.
        public init()

        /// Trigger the provided audio feedback.
        open func triggerAudioFeedback(_ audio: KeyboardKit.Feedback.Audio)

        /// Trigger the provided haptic feedback.
        open func triggerHapticFeedback(_ feedback: KeyboardKit.Feedback.Haptic)

        @objc deinit
    }
}

extension Feedback {

    /// This enum defines standard haptic feedback types.
    public enum Haptic : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Represents feedback for an error event.
        case error

        /// Represents feedback for a successful event.
        case success

        /// Represents feedback for a warning event.
        case warning

        /// Represents light impact feedback.
        case lightImpact

        /// Represents medium impact feedback.
        case mediumImpact

        /// Represents heavy impact feedback.
        case heavyImpact

        /// Represents feedback when a selection changes.
        case selectionChanged

        /// Can be used to disable feedback.
        case none

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Feedback.Haptic]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Feedback.Haptic] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Feedback {

    /// This struct can be used to configure audio feedback.
    ///
    /// You can use any of the standard configurations, like
    /// ``standard`` and ``disabled``, or create custom ones.
    public struct AudioConfiguration : KeyboardKit.KeyboardModel {

        /// Create a custom audio feedback configuration.
        ///
        /// - Parameters:
        ///   - input: The feedback to use for input keys, by default `.input`.
        ///   - delete: The feedback to use for delete keys, by default `.delete`.
        ///   - system: The feedback to use for system keys, by default `.system`.
        ///   - custom: A list of custom feedback, by default `empty`.
        public init(input: KeyboardKit.Feedback.Audio = .input, delete: KeyboardKit.Feedback.Audio = .delete, system: KeyboardKit.Feedback.Audio = .system, custom: [KeyboardKit.Feedback.AudioConfiguration.CustomFeedback] = [])

        /// The audio to play when a delete key is pressed.
        public var delete: KeyboardKit.Feedback.Audio

        /// The audio to play when an input key is pressed.
        public var input: KeyboardKit.Feedback.Audio

        /// The audio to play when a system key is pressed.
        public var system: KeyboardKit.Feedback.Audio

        /// A list of custom audio feedback.
        public var custom: [KeyboardKit.Feedback.AudioConfiguration.CustomFeedback]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Feedback.AudioConfiguration, b: KeyboardKit.Feedback.AudioConfiguration) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Feedback {

    /// This engine can be used to trigger haptic feedback.
    ///
    /// The engine uses UIKit functionality that require iOS.
    /// Other platforms have no haptic feedback.
    public class HapticEngine {

        public init()

        /// Prepare a certain haptic feedback type.
        open func prepare(_ feedback: KeyboardKit.Feedback.Haptic)

        /// Trigger a certain haptic feedback type.
        open func trigger(_ feedback: KeyboardKit.Feedback.Haptic)

        @objc deinit
    }
}

extension Feedback {

    /// This engine can be used to trigger audio feedback.
    ///
    /// The engine uses `AudioToolbox`, which is unavailable
    /// on watchOS. Therefore, watchOS has no audio feedback.
    public class AudioEngine {

        public init()

        @objc deinit
    }
}

extension Feedback {

    /// This struct can be used to configure haptic feedback.
    ///
    /// You can use any of the standard configurations, like
    /// ``standard`` and ``disabled``, or create custom ones.
    public struct HapticConfiguration : KeyboardKit.KeyboardModel {

        /// Create a custom haptic feedback configuration.
        ///
        /// - Parameters:
        ///   - press: The feedback to use for presses, by default `.selectionChanged`.
        ///   - release: The feedback to use for releases, by default `.selectionChanged`.
        ///   - doubleTap: The feedback to use for double taps, by default `.none`.
        ///   - longPress: The feedback to use for long presses, by default `.mediumImpact`.
        ///   - repeat: The feedback to use for repeat, by default `.selectionChanged`.
        ///   - custom: A list of custom feedback, by default `empty`.
        public init(press: KeyboardKit.Feedback.Haptic = .selectionChanged, release: KeyboardKit.Feedback.Haptic = .selectionChanged, doubleTap: KeyboardKit.Feedback.Haptic = .none, longPress: KeyboardKit.Feedback.Haptic = .mediumImpact, repeat: KeyboardKit.Feedback.Haptic = .selectionChanged, custom: [KeyboardKit.Feedback.HapticConfiguration.CustomFeedback] = [])

        /// The feedback to use for presses.
        public var press: KeyboardKit.Feedback.Haptic

        /// The feedback to use for releases.
        public var release: KeyboardKit.Feedback.Haptic

        /// The feedback to use for double taps.
        public var doubleTap: KeyboardKit.Feedback.Haptic

        /// The feedback to use for long presses.
        public var longPress: KeyboardKit.Feedback.Haptic

        /// The feedback to use for repeat.
        public var `repeat`: KeyboardKit.Feedback.Haptic

        /// A list of custom haptic feedback.
        public var custom: [KeyboardKit.Feedback.HapticConfiguration.CustomFeedback]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Feedback.HapticConfiguration, b: KeyboardKit.Feedback.HapticConfiguration) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Feedback {

    /// This enum defines standard audio feedback types.
    ///
    /// Audio feedback has a unique system id that refers to
    /// system sounds. You can use ``customId(_:)`` to add a
    /// custom ID-based feedback type, and ``customUrl(_:)``
    /// to add custom audio feedback from any URL-based file.
    public enum Audio : Identifiable, KeyboardKit.KeyboardModel {

        /// Represents the sound of an input key.
        case input

        /// Represents the sound of a system key.
        case system

        /// Represents the sound of a delete key.
        case delete

        /// Represents a custom system sound.
        case customId(UInt32)

        /// Represents a custom sound at a certain URL.
        case customUrl(URL?)

        /// Can be used to disable feedback.
        case none

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Feedback.Audio, b: KeyboardKit.Feedback.Audio) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UInt32?

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Feedback {

    /// This screen can be used as a feedback setting screen
    /// in your app or keyboard extension.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``feedbackSettingsScreenLocalization(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen<Header, FeedbackSection, Footer> : View where Header : View, FeedbackSection : View, Footer : View {

        /// Create a custom screen.
        ///
        /// - Parameters:
        ///   - header: The view to show at the top of the list.
        ///   - feedbackSection: The feedback section content.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(@ViewBuilder header: @escaping () -> Header, @ViewBuilder feedbackSection: @escaping KeyboardKit.Feedback.SettingsScreen<Header, FeedbackSection, Footer>.FeedbackSectionBuilder, @ViewBuilder footer: @escaping () -> Footer)

        public typealias Sections = KeyboardKit.Feedback.SettingsScreenSections

        public typealias FeedbackSectionBuilder = (KeyboardKit.Feedback.SettingsScreen<Header, FeedbackSection, Footer>.Sections.FeedbackParams) -> FeedbackSection

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Feedback {

    /// This type contains setting section views and builder
    /// parameters that are used by the settings screen.
    ///
    /// You can use these sections as stand-alone views that
    /// can be added to any other screen.
    public struct SettingsScreenSections {
    }
}

extension Feedback {

    /// This type can be used to configure the localizations
    /// of a ``Feedback/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/feedbackSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Feedback", feedbackSection: String? = nil, feedbackSectionAudioFeedback: String = "Audio Feedback", feedbackSectionHapticFeedback: String = "Haptic Feedback", feedbackSectionFooter: String? = nil, feedbackSectionFooterPhoneFallback: String = "Haptic Feedback requires Full Access.")

        public var title: String

        public var feedbackSection: String?

        public var feedbackSectionAudioFeedback: String

        public var feedbackSectionHapticFeedback: String

        public var feedbackSectionFooter: String?

        public var feedbackSectionFooterPhoneFallback: String
    }
}

extension Feedback.Toggle where ViewType == Image {

    @MainActor @preconcurrency public init(type: KeyboardKit.Feedback.Toggle<ViewType>.FeedbackType, enabledImage: Image? = nil, disabledImage: Image? = nil) throws
}

extension Feedback.Toggle : Sendable {
}

extension Feedback.Haptic {

    /// The unique feedback identifier.
    public var id: String { get }
}

extension Feedback.Haptic : RawRepresentable {
}

extension Feedback.AudioConfiguration {

    /// This struct is used for custom audio feedback.
    public struct CustomFeedback : KeyboardKit.KeyboardModel {

        public init(action: KeyboardKit.KeyboardAction, gesture: KeyboardKit.Keyboard.Gesture, feedback: KeyboardKit.Feedback.Audio)

        public let action: KeyboardKit.KeyboardAction

        public let gesture: KeyboardKit.Keyboard.Gesture

        public let feedback: KeyboardKit.Feedback.Audio

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Feedback.AudioConfiguration.CustomFeedback, b: KeyboardKit.Feedback.AudioConfiguration.CustomFeedback) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Feedback.AudioConfiguration {

    /// A standard, enabled audio configuration.
    public static let standard: KeyboardKit.Feedback.AudioConfiguration

    /// This configuration disables all audio feedback.
    public static let disabled: KeyboardKit.Feedback.AudioConfiguration
}

extension Feedback.AudioConfiguration {

    /// Get a custom registered feedback, if any.
    public func customFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Audio?

    /// Whether a custom feedback has been registered.
    public func hasCustomFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

    /// Get the feedback to use for a certain action.
    public func feedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Audio?

    /// Register feedback for a certain action gesture.
    public mutating func registerCustomFeedback(_ feedback: KeyboardKit.Feedback.AudioConfiguration.CustomFeedback)
}

extension Feedback.HapticConfiguration {

    /// A standard, enabled haptic configuration.
    public static let standard: KeyboardKit.Feedback.HapticConfiguration

    /// This configuration disables all haptic feedback.
    public static let disabled: KeyboardKit.Feedback.HapticConfiguration
}

extension Feedback.HapticConfiguration {

    /// This struct is used for custom haptic feedback.
    public struct CustomFeedback : KeyboardKit.KeyboardModel {

        public init(action: KeyboardKit.KeyboardAction, gesture: KeyboardKit.Keyboard.Gesture, feedback: KeyboardKit.Feedback.Haptic)

        public let action: KeyboardKit.KeyboardAction

        public let gesture: KeyboardKit.Keyboard.Gesture

        public let feedback: KeyboardKit.Feedback.Haptic

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Feedback.HapticConfiguration.CustomFeedback, b: KeyboardKit.Feedback.HapticConfiguration.CustomFeedback) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Feedback.HapticConfiguration {

    /// Get a custom registered feedback, if any.
    public func customFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Haptic?

    /// Whether a custom feedback has been registered.
    public func hasCustomFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

    /// Get the feedback to use for a certain action.
    public func feedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Haptic?

    /// Register feedback for a certain action gesture.
    public mutating func registerCustomFeedback(_ feedback: KeyboardKit.Feedback.HapticConfiguration.CustomFeedback)
}

extension Feedback.Audio {

    /// The unique system sound identifier.
    public var id: UInt32? { get }
}

extension Feedback.SettingsScreen where Header == EmptyView, FeedbackSection == KeyboardKit.Feedback.SettingsScreenSections.FeedbackContent, Footer == EmptyView {

    /// Create a custom screen.
    @MainActor @preconcurrency public init()
}

extension Feedback.SettingsScreen : Sendable {
}

extension Feedback.SettingsScreenSections {

    /// This struct defines view builder parameters that are
    /// used to create a feedback settings section.
    public struct FeedbackParams {

        public let view: KeyboardKit.Feedback.SettingsScreenSections.FeedbackContent
    }

    /// This is a standard feedback settings section. It can
    /// be modified with the same modifiers as the screen.
    @MainActor @preconcurrency public struct FeedbackContent : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Feedback.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Feedback.SettingsScreenLocalization { get }

    public func feedbackSectionFooter(for device: KeyboardKit.DeviceType) -> String?
}

extension Feedback.Toggle.FeedbackType : Equatable {
}

extension Feedback.Toggle.FeedbackType : Hashable {
}

extension Feedback.AudioConfiguration.CustomFeedback {

    /// Create a custom audio feedback configuration.
    public static func audio(_ feedback: KeyboardKit.Feedback.Audio, for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.AudioConfiguration.CustomFeedback
}

extension Feedback.HapticConfiguration.CustomFeedback {

    /// Create a custom haptic feedback configuration.
    public static func haptic(_ feedback: KeyboardKit.Feedback.Haptic, for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.HapticConfiguration.CustomFeedback
}

extension Feedback.SettingsScreenSections.FeedbackContent : Sendable {
}

/// This class has observable states and persistent settings
/// for keyboard feedback.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings,
/// like ``FeedbackSettings/isAudioFeedbackEnabled``.
///
/// Use the ``audioConfiguration`` and ``hapticConfiguration``
/// to configure which feedback to trigger when the feedback
/// type is enabled. The enabled states for audio and haptic
/// feedback can be found in ``settings``.
final public class FeedbackContext : ObservableObject {

    public init()

    /// Feedback-specific, auto-persisted settings.
    @Published final public var settings: KeyboardKit.FeedbackSettings

    final public var $settings: Published<KeyboardKit.FeedbackSettings>.Publisher

    public typealias AudioConfiguration = KeyboardKit.Feedback.AudioConfiguration

    public typealias HapticConfiguration = KeyboardKit.Feedback.HapticConfiguration

    /// The audio configuration to use when enabled.
    final public var audioConfiguration: KeyboardKit.FeedbackContext.AudioConfiguration

    /// The haptic configuration to use when enabled.
    final public var hapticConfiguration: KeyboardKit.FeedbackContext.HapticConfiguration

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension FeedbackContext {

    /// Register custom audio feedback.
    final public func registerCustomFeedback(_ feedback: KeyboardKit.FeedbackContext.AudioConfiguration.CustomFeedback)

    /// Register custom haptic feedback.
    final public func registerCustomFeedback(_ feedback: KeyboardKit.FeedbackContext.HapticConfiguration.CustomFeedback)
}

extension FeedbackContext {

    public static var preview: KeyboardKit.FeedbackContext { get }
}

/// This protocol can be implemented by any type that can be
/// used to trigger audio and haptic feedback.
///
/// KeyboardKit will automatically setup a standard protocol
/// implementation in ``KeyboardInputViewController/services``
/// when the keyboard is launched.
public protocol FeedbackService : AnyObject {

    /// Trigger the provided audio feedback.
    func triggerAudioFeedback(_ feedback: KeyboardKit.Feedback.Audio)

    /// Trigger the provided haptic feedback.
    func triggerHapticFeedback(_ feedback: KeyboardKit.Feedback.Haptic)
}

extension FeedbackService where Self == KeyboardKit.Feedback.StandardFeedbackService {

    /// Create a standard feedback service instance.
    public static var standard: KeyboardKit.Feedback.StandardFeedbackService { get }
}

extension FeedbackService where Self == KeyboardKit.Feedback.DisabledFeedbackService {

    /// Create a disabled feedback service instance.
    public static var disabled: KeyboardKit.Feedback.DisabledFeedbackService { get }
}

extension FeedbackService where Self == KeyboardKit.Feedback.DisabledFeedbackService {

    public static var preview: KeyboardKit.Feedback.DisabledFeedbackService { get }
}

/// This type has auto-persisted feedback settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `feedback` prefix.
public struct FeedbackSettings : Sendable {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use for the namespace.
    public static var settingsPrefix: String { get }

    /// Whether audio feedback is enabled, by default `true`.
    @AppStorage public var isAudioFeedbackEnabled: Bool { get nonmutating set }

    public var $isAudioFeedbackEnabled: Binding<Bool> { get }

    /// Whether haptic feedback is enabled, by default `true`.
    @AppStorage public var isHapticFeedbackEnabled: Bool { get nonmutating set }

    public var $isHapticFeedbackEnabled: Binding<Bool> { get }
}

extension FeedbackSettings {

    /// Toggle audio feedback enabled state.
    public func toggleIsAudioFeedbackEnabled()

    /// Toggle haptic feedback enabled state.
    public func toggleIsHapticFeedbackEnabled()
}

extension FeedbackSettings {

    public static var preview: KeyboardKit.FeedbackSettings { get }
}

/// This class has observable states and persistent settings
/// for font-related functionality.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings.
final public class FontContext : ObservableObject {

    /// Create a custom context instance.
    public init()

    /// Auto-persisted font settings.
    @Published final public var settings: KeyboardKit.FontSettings

    final public var $settings: Published<KeyboardKit.FontSettings>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension FontContext {

    public static var preview: KeyboardKit.FontContext { get }
}

/// This type has auto-persisted font settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `fonts` prefix.
public struct FontSettings {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }

    /// The currently selected font.
    @AppStorage public var font: KeyboardKit.Fonts.UnicodeFont? { get nonmutating set }

    public var $font: Binding<KeyboardKit.Fonts.UnicodeFont?> { get }

    /// Whether the selected font should be underlined.
    @AppStorage public var isFontUnderlined: Bool { get nonmutating set }

    public var $isFontUnderlined: Binding<Bool> { get }
}

extension FontSettings {

    /// Check whether a certain font is currently selected.
    public func isFontSelected(_ font: KeyboardKit.Fonts.UnicodeFont) -> Bool

    /// Reset the currently selected font.
    public func resetSelectedFont()

    /// Select a certain font.
    public func selectFont(_ font: KeyboardKit.Fonts.UnicodeFont)

    /// Toggle the selected state for a certain font.
    public func toggleSelection(for font: KeyboardKit.Fonts.UnicodeFont)
}

extension FontSettings {

    public static var preview: KeyboardKit.FontSettings { get }
}

/// This is a namespace for font-related types.
///
/// The namespace has utilities for mapping plain characters
/// to Unicode font variants, which lets users type in other
/// fonts than the regular system font.
public struct Fonts {
}

extension Fonts {

    /// This screen can be used as a font settings screen in
    /// which users can select a custom Unicode font.
    ///
    /// The screen will by list all ``Fonts/UnicodeFont`` by
    /// default, but you can customize the font list to show
    /// a custom set of fonts.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``fontSettingsScreenFonts(_:)``
    /// * ``fontSettingsScreenLocalization(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen : View {

        /// Create a custom settings screen.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Fonts {

    /// This type can be used to configure the localizations
    /// of a ``Fonts/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/fontSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Fonts", reset: String = "Reset")

        public var title: String

        public var reset: String
    }
}

extension Fonts {

    /// This enum defines all supported Unicode fonts.
    ///
    /// > Important: This feature requires a KeyboardKit Pro
    /// license with the ``ProFeature/fonts`` feature, which
    /// is NOT part of the ``License/Tier-swift.struct/gold``
    /// tier. The feature must be explicitly enabled as part
    /// of a Business License Agreement.
    public enum UnicodeFont : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        case normal

        case circled

        case circledNegative

        case fullWidth

        case mathBold

        case mathBoldItalic

        case mathItalic

        case mathFraktur

        case mathMonospace

        case mathSansSerif

        case mathSansSerifBold

        case mathSansSerifBoldItalic

        case mathSansSerifItalic

        case mathScript

        case parenthesized

        case squared

        case squaredNegative

        case block

        case broken

        case brush

        case cursive

        case dot

        case gothic

        case handwritten

        case medieval

        case outline

        case retro

        case script

        case smallCaps

        case stencil

        case typewriter

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Fonts.UnicodeFont]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Fonts.UnicodeFont] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }

    /// This struct defines Unicode font options.
    public enum UnicodeFontOption : KeyboardKit.KeyboardModel {

        case underlined

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Fonts.UnicodeFontOption, b: KeyboardKit.Fonts.UnicodeFontOption) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Fonts.SettingsScreen : Sendable {
}

extension Fonts.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Fonts.SettingsScreenLocalization { get }
}

extension Fonts.UnicodeFont {

    /// The stable identity of the entity associated with this instance.
    public var id: String { get }

    public var displayName: String { get }
}

extension Fonts.UnicodeFont {

    public func transformCharacters(_ characters: [[String]], options: [KeyboardKit.Fonts.UnicodeFontOption] = []) throws -> [[String]]

    /// Transform a keyboard layout for the Unicode font.
    ///
    /// - Parameters:
    ///   - layout: The keyboard layout to transform.
    ///   - options: The Unicode font options to apply.
    /// - Returns: A new keyboard layout with transformed character actions.
    /// - Throws: If the license validation fails or character transformation fails.
    public func transformKeyboardLayout(_ layout: KeyboardKit.KeyboardLayout, options: [KeyboardKit.Fonts.UnicodeFontOption] = []) throws -> KeyboardKit.KeyboardLayout
}

extension Fonts.UnicodeFont : RawRepresentable {
}

/// This button can be used to trigger gesture-based actions.
///
/// A `cancelDelay` can be specified to make a button cancel
/// its gesture if no values are registered during the delay.
/// This can be used to avoid a button from getting stuck in
/// a pressed state, which for instance can happen when it's
/// placed next to a scroll view and touched by accident.
///
/// > Important: Make sure to use ``GestureButtonScrollState``
/// if the button is within a `ScrollView`, otherwise it may
/// block the scroll view gestures in iOS 17 and earlier and
/// trigger unwanted actions in iOS 18 and later.
@MainActor @preconcurrency public struct GestureButton<Label> : View where Label : View {

    /// Create a gesture button.
    ///
    /// - Parameters:
    ///   - config: A custom config, if any.
    ///   - isPressed: A custom, optional binding to track pressed state, if any.
    ///   - scrollState: The scroll state to use, if any.
    ///   - pressAction: The action to trigger when the button is pressed, if any.
    ///   - releaseInsideAction: The action to trigger when the button is released inside, if any.
    ///   - releaseOutsideAction: The action to trigger when the button is released outside of its bounds, if any.
    ///   - longPressAction: The action to trigger when the button is long pressed, if any.
    ///   - doubleTapAction: The action to trigger when the button is double tapped, if any.
    ///   - repeatTimer: A custom repeat timer to use for the repeating action, if any.
    ///   - repeatAction: The action to repeat while the button is being pressed, if any.
    ///   - dragStartAction: The action to trigger when a drag gesture starts, if any.
    ///   - dragAction: The action to trigger when a drag gesture changes, if any.
    ///   - dragEndAction: The action to trigger when a drag gesture ends, if any.
    ///   - endAction: The action to trigger when a button gesture ends, if any.
    ///   - accessibilityTraits: The accessibility traits to apply, by default `.isButton`.
    ///   - label: The button label.
    @MainActor @preconcurrency public init(config: KeyboardKit.GestureButtonConfiguration? = nil, isPressed: Binding<Bool>? = nil, scrollState: KeyboardKit.GestureButtonScrollState? = nil, pressAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, releaseInsideAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, releaseOutsideAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, longPressAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, doubleTapAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, repeatAction: KeyboardKit.GestureButton<Label>.Action? = nil, dragStartAction: KeyboardKit.GestureButton<Label>.DragAction? = nil, dragAction: KeyboardKit.GestureButton<Label>.DragAction? = nil, dragEndAction: KeyboardKit.GestureButton<Label>.DragAction? = nil, endAction: KeyboardKit.GestureButton<Label>.GeoAction? = nil, accessibilityTraits: AccessibilityTraits = .isButton, label: @escaping KeyboardKit.GestureButton<Label>.LabelBuilder)

    public typealias Action = () -> Void

    public typealias GeoAction = (GeometryProxy) -> Void

    public typealias DragAction = (DragGesture.Value, GeometryProxy) -> Void

    public typealias LabelBuilder = (_ isPressed: Bool) -> Label

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension GestureButton : Sendable {
}

/// This configuration can be used to setup a gesture button.
public struct GestureButtonConfiguration : Codable, Equatable, Hashable, Sendable {

    /// Create a gesture button configuration.
    ///
    /// - Parameters:
    ///   - cancelDelay: The time it takes for a cancelled press to cancel, if any.
    ///   - doubleTapTimeout: The max time between two taps to count as a double tap, by default `0.2` seconds.
    ///   - longPressDelay: The time it takes for a press to count as a long press, by default `0.5` seconds.
    ///   - longPressMaxDragDistance: The max drag distance for a press to trigger a long press, by default `50` points.
    ///   - repeatDelay: The time it takes for a press to start a repeating action, by default `0.5` seconds.
    public init(cancelDelay: TimeInterval? = nil, doubleTapTimeout: TimeInterval? = nil, longPressDelay: TimeInterval? = nil, longPressMaxDragDistance: Double? = nil, repeatDelay: TimeInterval? = nil)

    public var cancelDelay: TimeInterval?

    public var longPressDelay: TimeInterval

    public var doubleTapTimeout: TimeInterval

    public var longPressMaxDragDistance: Double

    public var repeatDelay: TimeInterval

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.GestureButtonConfiguration, b: KeyboardKit.GestureButtonConfiguration) -> Bool

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension GestureButtonConfiguration {

    /// A standard gesture button configuration.
    public static let standard: KeyboardKit.GestureButtonConfiguration
}

/// This class can be used to coordinate gesture state for a
/// scroll view and any nested ``GestureButton``.
///
/// The state will be used to avoid the gesture buttons from
/// triggering their gesture actions when scrolling. It will
/// also disable scrolling for the scroll view when a button
/// gesture is active.
///
/// To use this class, create a `@StateValue` instance of it
/// within the view that contains your scroll view, apply it
/// to the scroll view with `.scrollGestureState(state)` and
/// pass the state into the ``GestureButton``.
public class GestureButtonScrollState : ObservableObject {

    /// Create a scroll state value.
    public init()

    @Published public var isScrolling: Bool

    public var $isScrolling: Published<Bool>.Publisher

    @Published public var isScrollGestureDisabled: Bool

    public var $isScrollGestureDisabled: Published<Bool>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

/// This internal class can be used to repeat an action when
/// a button is kept pressed.
public class GestureButtonTimer : ObservableObject {

    /// Create a custom gesture button timer.
    ///
    /// - Parameters:
    ///   - interval: The trigger interval, by default `0.1`.
    public init(interval: TimeInterval = 0.1)

    @objc deinit

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher
}

/// This namespace contains gesture-related types.
public struct Gestures {
}

extension Gestures {

    /// This custom gesture handler can handle drag gestures
    /// on the spacebar, that moves the input cursor.
    ///
    /// KeyboardKit will create an instance of the class and
    /// inject it into ``KeyboardInputViewController/state``.
    ///
    /// This service can also be resolved with the shorthand
    /// ``DragGestureHandler/spacebarDrag(sensitivity:verticalThreshold:action:)``.
    public class SpacebarDragGestureHandler : KeyboardKit.DragGestureHandler {

        /// Create a spacebar drag gesture handler instance.
        ///
        /// - Parameters:
        ///   - sensitivity: The drag sensitivity, by default `.medium`.
        ///   - verticalThreshold: The vertical threshold in points, by default `50`.
        ///   - action: The action to perform when the offset changes.
        public init(sensitivity: KeyboardKit.Keyboard.SpacebarDragSensitivity? = nil, verticalThreshold: Double? = nil, action: @escaping (Int) -> Void)

        /// The drag sensitivity.
        public var sensitivity: KeyboardKit.Keyboard.SpacebarDragSensitivity

        /// The vertical threshold in points.
        public var verticalThreshold: Double

        /// The action to perform when the offset changes.
        public var action: (Int) -> Void

        /// The location where the current drag stated.
        public var currentDragStartLocation: CGPoint?

        /// The currently applied drag text position offset.
        public var currentDragTextPositionOffset: Int

        /// Handle a spacebar drag gesture, which by default
        /// should activate after a long press.
        public func handleDragGesture(from startLocation: CGPoint, to currentLocation: CGPoint)

        @objc deinit
    }
}

/// This enum defines supported interface orientations.
///
/// The static ``current`` property will resolve the current
/// interface orientation.
public enum InterfaceOrientation : String, CaseIterable, KeyboardKit.KeyboardModel {

    case portrait

    case portraitUpsideDown

    case landscape

    case landscapeLeft

    case landscapeRight

    case unknown

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.InterfaceOrientation]

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String

    /// A collection of all values of this type.
    nonisolated public static var allCases: [KeyboardKit.InterfaceOrientation] { get }

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }
}

extension InterfaceOrientation {

    /// Get the current interface orientation.
    public static var current: KeyboardKit.InterfaceOrientation { get }

    /// Whether or not the orientation is landscape.
    public var isLandscape: Bool { get }

    /// Whether or not the orientation is portrait.
    public var isPortrait: Bool { get }
}

extension InterfaceOrientation : RawRepresentable {
}

/// This enum defines localized keyboard texts.
///
/// You can use the ``text`` property to translate texts for
/// the current locale and ``text(forLocale:)`` to translate
/// text for a certain locale.
public enum KKL10n : String, CaseIterable, Identifiable {

    case `continue`

    case capsLock

    case done

    case emergencyCall

    case go

    case join

    case next

    case ok

    case `return`

    case route

    case search

    case searchEmoji

    case send

    case space

    case switcherAlphabetic

    case switcherNumeric

    case switcherSymbolic

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.KKL10n]

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = String

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String

    /// A collection of all values of this type.
    nonisolated public static var allCases: [KeyboardKit.KKL10n] { get }

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }
}

extension KKL10n {

    /// The item's unique identifier.
    public var id: String { get }

    /// The item's localization key.
    public var key: String { get }

    /// The item's localized text.
    public var text: String { get }

    /// Get the localized text for a certain `Locale`.
    public func text(for locale: Locale) -> String

    /// Get the localized text for a certain `Locale`.
    public func text(forLocale locale: Locale) -> String

    /// Get a localized text for a certain locale.
    public static func text(forKey key: String, locale: Locale) -> String
}

extension KKL10n : Equatable {
}

extension KKL10n : Hashable {
}

extension KKL10n : RawRepresentable {
}

/// This namespace contains essential keyboard-related types.
///
/// See the <doc:Essentials-Article> article for information
/// on this essential part of the KeyboardKit SDK.
public struct Keyboard {
}

extension Keyboard {

    /// This type can be used to customize the visibility of
    /// any part of a ``Keyboard/LocaleSettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/localeSettingsScreenVisibility(_:)``.
    public struct LocaleSettingsScreenVisibility {

        /// Create a custom home screen visibility configuration.
        ///
        /// - Parameters:
        ///   - keyboardSection: Whether to show the keyboard section, by default `true`.
        public init(keyboardSection: Bool = true)

        /// Whether to show the keyboard section.
        public var keyboardSection: Bool
    }
}

extension Keyboard {

    /// This struct contains keyboard settings picker types.
    public struct LocaleSettingsScreenPickers {
    }
}

extension Keyboard {

    /// This struct contains keyboard settings picker types.
    public struct SettingsScreenPickers {
    }
}

extension Keyboard {

    /// This type contains setting section views and builder
    /// parameters that are used by the settings screen.
    ///
    /// You can use these sections as stand-alone views that
    /// can be added to any other screen.
    public struct SettingsScreenSections {
    }
}

extension Keyboard {

    /// This enum defines spacebar context menus that can be
    /// added as leading and trailing items to the spacebar.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardSpacebarMenuLeading(_:)``,
    /// or its trailing variant.
    public enum SpacebarMenuType : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Add a locale menu, if multiple locales are added.
        case locale

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.SpacebarMenuType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.SpacebarMenuType] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }

    /// This view renders a spacebar menu title.
    @MainActor @preconcurrency public struct SpacebarMenuTitle : View {

        @MainActor @preconcurrency public init(_ title: String, alignment: Alignment, style: KeyboardKit.Keyboard.ButtonStyle)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This type can be used to define metadata for locales
    /// that have been added to a keyboard.
    ///
    /// Every new value gets a unique ``id`` on creation. Do
    /// not confuse this ID with the ``localeIdentifier``.
    public struct AddedLocale : Identifiable, KeyboardKit.KeyboardModel {

        public init(_ locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType? = nil)

        /// The unique identifier, defined at creation.
        public let id: UUID

        /// The ``locale`` identifier.
        public let localeIdentifier: String

        /// The ``layoutType`` identifier, if any.
        public var layoutTypeIdentifier: KeyboardKit.Keyboard.LayoutType.ID?

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.AddedLocale, b: KeyboardKit.Keyboard.AddedLocale) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This view creates a keyboard bottom row with leading
    /// and trailing actions around the spacebar.
    ///
    /// The view can be used to keep the bottom row when you
    /// replace the rest of the keyboard. It's meant to be a
    /// basic replacement.
    ///
    /// If you want full control over your bottom row layout,
    /// use a ``KeyboardView`` with a ``KeyboardLayout`` and
    /// use the layout ``KeyboardLayout/bottomRowLayout`` to
    /// just keep the bottom row.
    @MainActor @preconcurrency public struct BottomRow<ButtonContent, ButtonView> : View where ButtonContent : View, ButtonView : View {

        /// Create a bottom row based on state and services.
        ///
        /// - Parameters:
        ///   - leadingActions: The actions to add before the spacebar.
        ///   - trailingActions: The actions to add after the spacebar.
        ///   - services: The keyboard services to use.
        ///   - buttonContent: The content view to use for buttons.
        ///   - buttonView: The button view to use for an buttons.
        @MainActor @preconcurrency public init(leadingActions: [KeyboardKit.KeyboardAction], trailingActions: [KeyboardKit.KeyboardAction], services: KeyboardKit.Keyboard.Services, @ViewBuilder buttonContent: @escaping KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.ButtonContentBuilder, @ViewBuilder buttonView: @escaping KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.ButtonViewBuilder)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This struct can be used to manage a swipe down state
    /// for a keyboard button that has a secondary action.
    ///
    /// For now, swipe down actions aren't apploed to iPhone.
    public struct SwipeDownActionState {

        /// Create a custom swipe down state.
        ///
        /// - Parameters:
        ///   - action: The action to apply, if any.
        ///   - activeRatio: How much of the swipe down that is completed.
        ///   - buttonHeight: The height of the button that uses this state.
        public init?(action: KeyboardKit.KeyboardAction?, activeRatio: Double = 0, buttonHeight: Double = 45)

        /// The action to apply, if any.
        public let action: KeyboardKit.KeyboardAction

        /// How much of the swipe down that is completed.
        public var activeRatio: Double

        /// The height of the button that uses this state.
        public let buttonHeight: Double

        /// Whether the state has been fully activated.
        public var isActive: Bool { get }
    }
}

extension Keyboard {

    /// This type can be used to replace character(s) with a
    /// diacritic variant.
    ///
    /// The ``KeyboardAction/StandardActionHandler`` will do
    /// this by replacing any last typed, matching character.
    public struct Diacritic : KeyboardKit.KeyboardModel {

        /// Create a custom diacritic value.
        ///
        /// - Parameters:
        ///   - char: The character to display.
        ///   - replacements: All possible replacements.
        public init(char: String, replacements: [String : String])

        /// The character to display.
        public let char: String

        /// All possible replacements.
        public let replacements: [String : String]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.Diacritic, b: KeyboardKit.Keyboard.Diacritic) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This type describes how to apply a diacritic.
    public struct DiacriticInsertionResult {

        /// The number of backwards deletion to apply.
        public let deleteBackwardsCount: Int

        /// The text to insert after deleting backwards.
        public let textInsertion: String
    }
}

extension Keyboard {

    /// This enum defines spacebar long press behaviors.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardSpacebarLongPressBehavior(_:)``.
    public enum SpacebarLongPressBehavior : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Long pressing space moves the input cursor.
        case moveInputCursor

        /// Long pressing space opens a locale context menu.
        case openLocaleContextMenu

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.SpacebarLongPressBehavior]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.SpacebarLongPressBehavior] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This view mimics a native keyboard button key shape.
    ///
    /// The view will render the button shape, corner radius,
    /// shadow etc. and act on an injected pressed state.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardButtonStyle(_:)``.
    @MainActor @preconcurrency public struct ButtonKey : View {

        /// Create a keyboard button key.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This enum defines various keyboard types for various
    /// kinds of inputs.
    ///
    /// While ``KeyboardView`` automatically renders most of
    /// these keyboard types by varying the standard layouts,
    /// some types like ``images`` and ``custom(named:)`` do
    /// require custom handling.
    public enum KeyboardType : CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// A keyboard with alphabetic input keys.
        case alphabetic

        /// A keyboard with numeric input keys.
        case numeric

        /// A keyboard with symbolic input keys.
        case symbolic

        /// A clipboard keyboard.
        case clipboard

        /// An emoji keyboard with emojis and categories.
        case emojis

        /// An image keyboard, not currently implemented.
        case images

        /// A number pad keyboard.
        case numberPad

        /// A custom keyboard type, if you need to use one.
        case custom(named: String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.KeyboardType, b: KeyboardKit.Keyboard.KeyboardType) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.KeyboardType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This enum defines spacebar drag sensitivities.
    ///
    /// The spacebar drag sensitivity model defines how many
    /// points a spacebar must be dragged for the text input
    /// cursor to be moved one step.
    ///
    /// A higher sensitivity means fewer points.
    public enum SpacebarDragSensitivity : KeyboardKit.KeyboardModel {

        case low

        case medium

        case high

        case custom(points: Int)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.SpacebarDragSensitivity, b: KeyboardKit.Keyboard.SpacebarDragSensitivity) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This view mimics a native keyboard button shadow.
    ///
    /// Instead of being a shadow, the view is an overlay to
    /// support opaque shadows with semi-transparent buttons.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardButtonStyle(_:)``. 
    @MainActor @preconcurrency public struct ButtonShadow : View {

        /// Create a keyboard button shadow.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This view mimics a native number pad keyboard.
    ///
    /// This view is greedy and will let its keys take up as
    /// much space as they can. You can restrict its size by
    /// applying a `frame` modifier, or adding it as overlay
    /// above e.g. a ``KeyboardView``.
    @MainActor @preconcurrency public struct NumberPad : View {

        @MainActor @preconcurrency public init(actions: KeyboardKit.KeyboardAction.Rows = [
                .init(characters: "123"),
                .init(characters: "456"),
                .init(characters: "789"),
                [.keyboardType(.alphabetic), .character("0"), .backspace]
            ], actionHandler: any KeyboardKit.KeyboardActionHandler, keyboardContext: KeyboardKit.KeyboardContext)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This view can be used to render a toolbar with a min
    /// height, to ensure that callouts are not cut off.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardToolbarStyle(_:)``.
    @MainActor @preconcurrency public struct Toolbar<Content> : View where Content : View {

        @MainActor @preconcurrency public init(@ViewBuilder content: @escaping () -> Content)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This style can be used to modify the visual style of
    /// the ``Keyboard/Toolbar`` component.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ToolbarStyle : KeyboardKit.KeyboardModel {

        /// Create a custom keyboard toolbar style.
        ///
        /// - Parameters:
        ///   - backgroundColor: The toolbar color, by default `.clear`.
        ///   - height: An optional fixed height, by default `nil`..
        ///   - minHeight: The toolbar's minimum height, by default ``standardHeight``.
        ///   - maxHeight: An optional max height, by default `nil`.
        public init(backgroundColor: Color = .clear, height: CGFloat? = nil, minHeight: Double? = nil, maxHeight: CGFloat? = nil)

        /// The default toolbar height.
        public static var standardHeight: CGFloat { get }

        /// The toolbar's background color.
        public var backgroundColor: Color

        /// An optional fixed height.
        public var height: CGFloat?

        /// The toolbar's minimum height.
        public var minHeight: Double

        /// An optional max height.
        public var maxHeight: CGFloat?

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.ToolbarStyle, b: KeyboardKit.Keyboard.ToolbarStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This enum defines various keyboard return key types.
    ///
    /// Return keys are used as ``KeyboardAction/primary(_:)``
    /// actions and insert a new line or perform the primary
    /// action when they are tapped.
    public enum ReturnKeyType : CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// A return key that uses a return text and not a ⏎.
        case `return`

        /// An continue key (native use unknown).
        case `continue`

        /// A done key (used when adding a Calendar location).
        case done

        /// An emergency call key (native use unknown).
        case emergencyCall

        /// A go key (used when entering a URL in Safari).
        case go

        /// A join key (used when joining wifi with password).
        case join

        /// A return key that uses a ⏎ and not a return text.
        case newLine

        /// A next key (used when joining wifi with uid/pwd).
        case next

        /// An ok key, which isn't actually used in native.
        case ok

        /// A route key (native use unknown).
        case route

        /// A search key (used for Google search in Safari).
        case search

        /// A send key (used when typing in WeChat, QQ, etc.).
        case send

        /// A custom key with a custom title.
        case custom(title: String)

        /// All primary return button types with a known use.
        public static var allCases: [KeyboardKit.Keyboard.ReturnKeyType] { get }

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.ReturnKeyType, b: KeyboardKit.Keyboard.ReturnKeyType) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.ReturnKeyType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This enum defines keyboard auto-capitalization types.
    ///
    /// The ``KeyboardContext/autocapitalizationTypeOverride``
    /// property can be set to override the default behavior
    /// that is defined by the active text-field.
    public enum AutocapitalizationType : String, CaseIterable, KeyboardKit.KeyboardModel {

        case allCharacters

        case sentences

        case words

        case none

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.AutocapitalizationType]

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.AutocapitalizationType] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This view can be used to create keyboard buttons for
    /// various keyboard actions.
    ///
    /// The view will apply the proper keyboard gestures and
    /// adapt its content to the provided action and values.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardButtonStyle(builder:)``.
    ///
    /// > Important: Some keyboard actions use gestures that
    /// live for a long time, for instance backspace deletes
    /// as long as the button is pressed. These buttons must
    /// not be given an explicit `id`, since this will cause
    /// some gesture handlers to not be cancelled.
    @MainActor @preconcurrency public struct Button<Content> : View where Content : View {

        /// Create a keyboard button.
        ///
        /// - Parameters:
        ///   - action: The keyboard action to apply.
        ///   - actionHandler: The action handler to use.
        ///   - repeatTimer: The repeat gesture timer to use, if any.
        ///   - calloutContext: The callout context to affect, if any.
        ///   - edgeInsets: The edge insets to apply to the interactable area, if any.
        ///   - isPressed: An external boolean binding for the pressed state, if any.
        ///   - content: An optional content builder that can be used to customize or replace the standard button content.
        @MainActor @preconcurrency public init(action: KeyboardKit.KeyboardAction, actionHandler: any KeyboardKit.KeyboardActionHandler, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, calloutContext: KeyboardKit.CalloutContext?, edgeInsets: EdgeInsets = .init(), isPressed: Binding<Bool>? = nil, @ViewBuilder content: @escaping KeyboardKit.Keyboard.Button<Content>.ContentBuilder)

        /// Create a keyboard button.
        ///
        /// - Parameters:
        ///   - action: The keyboard action to apply.
        ///   - actionHandler: The action handler to use.
        ///   - repeatTimer: The repeat gesture timer to use, if any.
        ///   - calloutContext: The callout context to affect, if any.
        ///   - edgeInsets: The edge insets to apply to the interactable area, if any.
        ///   - isPressed: An external boolean binding for the pressed state, if any.
        @MainActor @preconcurrency public init(action: KeyboardKit.KeyboardAction, actionHandler: any KeyboardKit.KeyboardActionHandler, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, calloutContext: KeyboardKit.CalloutContext?, edgeInsets: EdgeInsets = .init(), isPressed: Binding<Bool>? = nil) where Content == KeyboardKit.Keyboard.ButtonContent

        public typealias ContentBuilder = (_ content: KeyboardKit.Keyboard.ButtonContent) -> Content

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This view can be used to render a collapsed keyboard,
    /// e.g. when an external keyboard is connected.
    ///
    /// The content view is rendered in an `HStack`, since a
    /// collapsed keyboard view is more like a toolbar.
    @MainActor @preconcurrency public struct CollapsedView<Content> : View where Content : View {

        /// Create a coll
        @MainActor @preconcurrency public init(openKeyboardAction: @escaping () -> Void, @ViewBuilder content: @escaping () -> Content)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This view renders the content for a certain keyboard
    /// button, based on a provided action and style service.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardButtonStyle(builder:)``.
    @MainActor @preconcurrency public struct ButtonContent : View {

        /// Create a keyboard button content view.
        ///
        /// - Parameters:
        ///   - action: The keyboard action to use.
        ///   - secondaryActionState: An optional secondary action state.
        @MainActor @preconcurrency public init(action: KeyboardKit.KeyboardAction, secondaryActionState: KeyboardKit.Keyboard.SwipeDownActionState? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This enum defines various input toolbar modes, which
    /// can be used to set how an input toolbar is displayed.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardInputToolbarDisplayMode(_:)``.
    public enum InputToolbarDisplayMode : KeyboardKit.KeyboardModel {

        /// Display a toolbar based on the native behavior.
        case automatic

        /// Display a fixed set of characters.
        case characters([String])

        /// Display contextual numbers or symbols.
        case numbers

        /// Remove the input toolbar altogether.
        case none

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.InputToolbarDisplayMode, b: KeyboardKit.Keyboard.InputToolbarDisplayMode) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This type specifies global keyboard state.
    ///
    /// This lets us decouple the input view controller from
    /// any views that require settings, services, and state
    /// from the controller.
    ///
    /// You can adjust any state value at any time to adjust
    /// the global behavior of the keyboard.
    public class State {

        /// Create state instances.
        public init()

        /// Observable autocomplete-related state.
        public var autocompleteContext: KeyboardKit.AutocompleteContext

        /// Observable clipboard-related state.
        public var clipboardContext: KeyboardKit.ClipboardContext

        /// Observable callout-related state.
        public var calloutContext: KeyboardKit.CalloutContext

        /// Observable dictation-related state.
        public var dictationContext: KeyboardKit.DictationContext

        /// Observable emoji-related state.
        public var emojiContext: KeyboardKit.EmojiContext

        /// Observable external keyboard-related state.
        public var externalKeyboardContext: KeyboardKit.ExternalKeyboardContext

        /// Observable feedback-related state.
        public var feedbackContext: KeyboardKit.FeedbackContext

        /// Observable font-related state.
        public var fontContext: KeyboardKit.FontContext

        /// Observable keyboard-related state.
        public var keyboardContext: KeyboardKit.KeyboardContext

        /// Observable theme-related state.
        public var themeContext: KeyboardKit.KeyboardThemeContext

        @objc deinit
    }
}

extension Keyboard {

    /// This view renders keyboard a button title text.
    ///
    /// The text is limited to a single line with a vertical
    /// offset for input actions with lowercased text.
    @MainActor @preconcurrency public struct ButtonTitle : View {

        /// Create a keyboard button title view.
        ///
        /// - Parameters:
        ///   - text: The text to display.
        ///   - action: The action bound to the button.
        @MainActor @preconcurrency public init(text: String, action: KeyboardKit.KeyboardAction)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This background style can define the background of a
    /// keyboard, or any component within it.
    ///
    /// This style has many optional layers, which are added
    /// from the bottom to the top.
    ///
    /// Since the style implements `View`, you can render it
    /// directly into any view hierarchy.
    ///
    /// You can use the ``standard`` style or your own style.
    @MainActor @preconcurrency public struct Background : Codable, Equatable, View {

        /// Create a keyboard button background style.
        ///
        /// - Parameters:
        ///   - backgroundColor: A background color to apply, by default `nil`.
        ///   - backgroundGradient: A background gradient color set, by default `nil`.
        ///   - imageData: Optional image data for a background image, by default `nil`.
        ///   - imageContentMode: The background image content mode, by default `.fill`.
        ///   - overlayColor: An overlay color to apply, by default `nil`.
        ///   - overlayGradient: An overlay gradient color set, by default `nil`.
        @MainActor @preconcurrency public init(backgroundColor: Color? = nil, backgroundGradient: [Color]? = nil, imageData: Data? = nil, imageContentMode: KeyboardKit.Keyboard.Background.ImageContentMode = .fill, overlayColor: Color? = nil, overlayGradient: [Color]? = nil)

        /// This enum contains codable mode representations.
        public enum ImageContentMode : String, Codable {

            case stretch

            case fill

            case fit

            /// Creates a new instance with the specified raw value.
            ///
            /// If there is no value of the type that corresponds with the specified raw
            /// value, this initializer returns `nil`. For example:
            ///
            ///     enum PaperSize: String {
            ///         case A4, A5, Letter, Legal
            ///     }
            ///
            ///     print(PaperSize(rawValue: "Legal"))
            ///     // Prints "Optional(PaperSize.Legal)"
            ///
            ///     print(PaperSize(rawValue: "Tabloid"))
            ///     // Prints "nil"
            ///
            /// - Parameter rawValue: The raw value to use for the new instance.
            public init?(rawValue: String)

            /// The raw type that can be used to represent all values of the conforming
            /// type.
            ///
            /// Every distinct value of the conforming type has a corresponding unique
            /// value of the `RawValue` type, but there may be values of the `RawValue`
            /// type that don't have a corresponding value of the conforming type.
            public typealias RawValue = String

            /// The corresponding value of the raw type.
            ///
            /// A new instance initialized with `rawValue` will be equivalent to this
            /// instance. For example:
            ///
            ///     enum PaperSize: String {
            ///         case A4, A5, Letter, Legal
            ///     }
            ///
            ///     let selectedSize = PaperSize.Letter
            ///     print(selectedSize.rawValue)
            ///     // Prints "Letter"
            ///
            ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
            ///     // Prints "true"
            public var rawValue: String { get }
        }

        /// A background color to apply.
        @MainActor @preconcurrency public var backgroundColor: Color?

        /// A background gradient color set.
        @MainActor @preconcurrency public var backgroundGradient: [Color]?

        /// Optional image data for a background image.
        @MainActor @preconcurrency public var imageData: Data?

        /// An optional image content mode.
        @MainActor @preconcurrency public var imageContentMode: KeyboardKit.Keyboard.Background.ImageContentMode

        /// An overlay color to apply.
        @MainActor @preconcurrency public var overlayColor: Color?

        /// An overlay gradient color set.
        @MainActor @preconcurrency public var overlayGradient: [Color]?

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        nonisolated public static func == (a: KeyboardKit.Keyboard.Background, b: KeyboardKit.Keyboard.Background) -> Bool

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        nonisolated public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        nonisolated public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This type specifies global keyboard services.
    ///
    /// This lets us decouple the input view controller from
    /// any views that require its states and services. This
    /// reduces the risk of memory leaks.
    ///
    /// You can adjust or replace any service at any time to
    /// adjust your global keyboard behavior. Just make sure
    /// to replace a service before other services that have
    /// a dependency to that service are resolved.
    public class Services {

        /// Create service instances based on keyboard state.
        ///
        /// - Parameters:
        ///   - state: The state to base the services on.
        public init(state: KeyboardKit.Keyboard.State)

        /// The keyboard action handler to use.
        public var actionHandler: any KeyboardKit.KeyboardActionHandler

        /// The autocomplete service to use.
        public var autocompleteService: any KeyboardKit.AutocompleteService

        /// The dictation service to use.
        public var dictationService: any KeyboardKit.DictationService

        /// The feedback service to use.
        public var feedbackService: any KeyboardKit.FeedbackService

        /// The keyboard behavior to use.
        public var keyboardBehavior: any KeyboardKit.KeyboardBehavior

        /// The shared repeat gesture timer.
        public var repeatGestureTimer: KeyboardKit.GestureButtonTimer

        /// The spacebar drag gesture handler to use.
        public var spacebarDragGestureHandler: KeyboardKit.Gestures.SpacebarDragGestureHandler

        @objc deinit
    }
}

extension Keyboard {

    /// This class defines the standard keyboard behavior of
    /// a standard western keyboard, with some locale tweaks.
    ///
    /// Note that this class handles `shift` a bit different,
    /// since it must handle double taps to enable caps lock.
    ///
    /// KeyboardKit automatically creates an instance of the
    /// class when the keyboard is launched, then injects it
    /// into ``KeyboardInputViewController/services``.
    ///
    /// You can inherit this class to get base functionality,
    /// then override any open parts that you want to change.
    open class StandardKeyboardBehavior : KeyboardKit.KeyboardBehavior {

        /// Create a standard keyboard behavior instance.
        ///
        /// - Parameters:
        ///   - keyboardContext: The keyboard context to use.
        ///   - doubleTapThreshold: The douple tap threshold to use, by default `0.5` seconds.
        ///   - endSentenceText: The text to use to end sentences with, by default `. `.
        ///   - endSentenceThreshold: The end sentence auto-close threshold to use, by default `1` seconds.
        ///   - repeatGestureTimer: The repease gesture timer to use, by default a new one.
        public init(keyboardContext: KeyboardKit.KeyboardContext, doubleTapThreshold: TimeInterval? = nil, endSentenceText: String? = nil, endSentenceThreshold: TimeInterval? = nil, repeatGestureTimer: KeyboardKit.GestureButtonTimer? = nil)

        /// The keyboard context to use.
        final public let keyboardContext: KeyboardKit.KeyboardContext

        /// The douple tap threshold to use.
        final public let doubleTapThreshold: TimeInterval

        /// The text to use to end sentences with.
        public var endSentenceText: String

        /// The end sentence auto-close threshold to use.
        final public let endSentenceThreshold: TimeInterval

        /// The repease gesture timer to use.
        final public let repeatGestureTimer: KeyboardKit.GestureButtonTimer

        /// An internal state to keep track of shift checks.
        public var lastShiftCheck: Date { get }

        /// An internal state to keep track of the last spacebar tap.
        public var lastSpacebarTap: Date? { get }

        /// The range that backspace should delete.
        open var backspaceRange: KeyboardKit.Keyboard.BackspaceRange { get }

        /// The preferred keyboard case after a certain action gesture.
        open func preferredKeyboardCase(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardCase

        /// The preferred keyboard type after a certain action gesture.
        open func preferredKeyboardType(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardType

        /// Whether to end the current sentence after a certain action gesture.
        open func shouldEndCurrentSentence(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to register an emoji after a certain action gesture.
        open func shouldRegisterEmoji(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        @objc deinit
    }
}

/// This type can wrap any `Codable` type to make it able to
/// store the value in `AppStorage` and `SceneStorage`.
///
/// This type uses `JSONEncoder` and `JSONDecoder` to encode
/// the value to data then decode it back to the source type.
///
/// > Important: JSON encoding may cause important type data
/// to disappear. For instance, JSON encoding a `Color` will
/// not include any information about alternate color values
/// for light and dark mode, high constrasts, etc.
extension Keyboard {

    public struct StorageValue<Value> : RawRepresentable where Value : Decodable, Value : Encodable {

        public var value: Value

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String
    }
}

extension Keyboard {

    /// This enum defines various keyboard shift states.
    public enum KeyboardCase : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        case capsLocked

        case lowercased

        case uppercased

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.KeyboardCase]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.KeyboardCase] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This view makes any view behave as a native keyboard
    /// switcher button, which switches to the next keyboard
    /// when tapped, and opens a switcher menu on long press.
    @MainActor @preconcurrency public struct NextKeyboardButton<Content> : View where Content : View {

        @MainActor @preconcurrency public init(@ViewBuilder content: @escaping () -> Content)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This enum defines supported keyboard dock edges.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardDockEdge(_:)``.
    public enum DockEdge : String, Identifiable, CaseIterable, KeyboardKit.KeyboardModel {

        /// Dock the keyboard to the leading edge.
        case leading

        /// Dock the keyboard to the trailing edge.
        case trailing

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.DockEdge]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.DockEdge] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This enum defines how much the backspace key deletes.
    public enum BackspaceRange : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Delete a single char at a time.
        case character

        /// Delete an entire word at a time.
        case word

        /// Delete an entire sentence at a time.
        case sentence

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.BackspaceRange]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.BackspaceRange] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This button style can be used for plain buttons that are
    /// used in keyboard extensions.
    ///
    /// This is needed because the transparent background is not
    /// responding to touches, which this style fixes.
    @MainActor @preconcurrency public struct PlainButtonStyle : ButtonStyle {

        /// Creates a view that represents the body of a button.
        ///
        /// The system calls this method for each ``Button`` instance in a view
        /// hierarchy where this style is the current button style.
        ///
        /// - Parameter configuration : The properties of the button.
        @MainActor @preconcurrency public func makeBody(configuration: KeyboardKit.Keyboard.PlainButtonStyle.Configuration) -> some View


        /// A view that represents the body of a button.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This style can be used to style a ``Keyboard/Button``.
    ///
    /// The ``keyboardFont`` property can be used to apply a
    /// codable font that is serialized with the rest of the
    /// style, while the optional ``font`` can be used for a
    /// non-codable, custom font that will not be serialized.
    ///
    /// Apply the ``SwiftUICore/View/keyboardButtonStyle(_:)``
    /// view modifier to apply a static button style, or use
    /// the ``SwiftUICore/View/keyboardButtonStyle(builder:)``
    /// modifier to apply a dynamic, parameter-based builder.
    public struct ButtonStyle : Codable, Equatable, Sendable {

        /// Create a custom keyboard button style.
        ///
        /// - Parameters:
        ///   - background: The background style to apply, by default `nil`.
        ///   - backgroundColor: The background color to apply, by default `nil`.
        ///   - foregroundColor: The foreground color to apply, by default `nil`.
        ///   - foregroundSecondaryOpacity: The opacity to apply to secondary titles, by default `nil`.
        ///   - font: A custom, native font to apply, by default `nil`.
        ///   - fontWeight: A weight to apply, regardless of the resolved font, by default `nil`.
        ///   - keyboardFont: The keyboard font to apply, by default `nil`.
        ///   - cornerRadius: The corner radius to apply, by default `nil`.
        ///   - border: The border style to apply, by default `nil`.
        ///   - shadow: The shadow style to apply, by default `nil`.
        ///   - contentInsets: The content insets to apply, by default `nil`.
        ///   - pressedOverlayColor: The overlay color to apply on press, by default `nil`.
        public init(background: KeyboardKit.Keyboard.Background? = nil, backgroundColor: Color? = nil, backgroundOpacity: Double? = nil, foregroundColor: Color? = nil, foregroundSecondaryOpacity: Double? = nil, font: Font? = nil, fontWeight: KeyboardKit.KeyboardFont.FontWeight? = nil, keyboardFont: KeyboardKit.KeyboardFont? = nil, cornerRadius: CGFloat? = nil, border: KeyboardKit.Keyboard.ButtonStyle.BorderStyle? = nil, shadow: KeyboardKit.Keyboard.ButtonStyle.ShadowStyle? = nil, contentInsets: EdgeInsets? = nil, pressedOverlayColor: Color? = nil)

        public typealias BorderStyle = KeyboardKit.Keyboard.ButtonBorderStyle

        public typealias ShadowStyle = KeyboardKit.Keyboard.ButtonShadowStyle

        /// The background style to apply, if any.
        public var background: KeyboardKit.Keyboard.Background?

        /// The background color to apply, if any.
        public var backgroundColor: Color?

        /// The background opacity.
        public var backgroundOpacity: Double?

        /// The foreground color to apply, if any.
        public var foregroundColor: Color?

        /// The opacity to apply to secondary titles, if any.
        public var foregroundSecondaryOpacity: Double?

        /// The font to apply, if any.
        ///
        /// This will return ``nativeFont`` if set, else the
        /// ``keyboardFont``'s computed font value.
        public var font: Font? { get }

        /// A weight to apply, regardless of the resolved font, if any.
        public var fontWeight: KeyboardKit.KeyboardFont.FontWeight?

        /// The native font to apply, if any.
        public var nativeFont: Font?

        /// The keyboard font to apply, if any.
        public var keyboardFont: KeyboardKit.KeyboardFont?

        /// The content insets to apply, if any.
        public var contentInsets: EdgeInsets?

        /// The corner radius to apply, if any.
        public var cornerRadius: CGFloat?

        /// The border style to apply, if any.
        public var border: KeyboardKit.Keyboard.ButtonStyle.BorderStyle?

        /// The border color to apply, if any.
        public var borderColor: Color?

        /// The border size to apply, if any.
        public var borderSize: CGFloat?

        /// The shadow style to apply, if any.
        public var shadow: KeyboardKit.Keyboard.ButtonStyle.ShadowStyle?

        /// The shadow color to apply, if any.
        public var shadowColor: Color?

        /// The shadow size to apply, if any.
        public var shadowSize: CGFloat?

        /// The overlay color to apply on press, if any.
        public var pressedOverlayColor: Color?

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.ButtonStyle, b: KeyboardKit.Keyboard.ButtonStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This style defines the border of a keyboard button.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ButtonBorderStyle : KeyboardKit.KeyboardModel {

        /// Create a custom keyboard button border style.
        ///
        /// - Parameters:
        ///   - color: The border color, by default `.clear`.
        ///   - size: The border size, by default `0`.
        public init(color: Color = .clear, size: CGFloat = 0)

        /// The border color.
        public var color: Color

        /// The border size.
        public var size: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.ButtonBorderStyle, b: KeyboardKit.Keyboard.ButtonBorderStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This style defines the shadow of a keyboard button.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ButtonShadowStyle : KeyboardKit.KeyboardModel {

        /// Create a custom keyboard button shadow style.
        ///
        /// - Parameters:
        ///   - color: The shadow color, by default `.keyboardButtonShadow`.
        ///   - size: The shadow size, by default `1`.
        public init(color: Color = .keyboardButtonShadow, size: CGFloat = 1)

        /// The shadow color.
        public var color: Color

        /// The shadow size.
        public var size: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.ButtonShadowStyle, b: KeyboardKit.Keyboard.ButtonShadowStyle) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This type can be used to customize the related style
    /// with ``SwiftUICore/View/keyboardButtonStyle(builder:)``.
    public typealias ButtonStyleBuilder = (KeyboardKit.Keyboard.ButtonStyleBuilderParams) -> KeyboardKit.Keyboard.ButtonStyle

    /// This type can be used to customize the related style
    /// with ``SwiftUICore/View/keyboardButtonStyle(builder:)``.
    public struct ButtonStyleBuilderParams {

        /// Create a style builder parameter value.
        public init(action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext, isPressed: Bool)

        /// The action to create a style for.
        public let action: KeyboardKit.KeyboardAction

        /// The context to base the button style on.
        public let context: KeyboardKit.KeyboardContext

        /// Whether the button is pressed.
        public let isPressed: Bool

        /// The standard button style.
        public func standardStyle() -> KeyboardKit.Keyboard.ButtonStyle
    }
}

extension Keyboard {

    /// This enum defines various keyboard gestures that can
    /// be used to interact with a keyboard.
    public enum Gesture : String, CaseIterable, KeyboardKit.KeyboardModel {

        /// Triggers when a button is double tapped.
        case doubleTap

        /// Triggers when a button is pressed down.
        case press

        /// Triggers when a button is released.
        case release

        /// Triggers when a button is long pressed.
        case longPress

        /// Triggers repeatedly while a button is pressed.
        case repeatPress

        /// Triggers when a button gesture ends.
        case end

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.Gesture]

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.Gesture] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This enum defines supported keyboard layout types.
    ///
    /// This is used to allow ``AddedLocale`` to also define
    /// an optional layout type. This means that you can add
    /// English with QWERTY, or English with AZERTY, without
    /// having to apply to layout, which happens later.
    ///
    /// This may be refactored in the future, since it mixes
    /// the concept of locales, layouts and input methods. A
    /// better name should at least be found.
    public enum LayoutType : CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        public init?(id: String)

        /// A AZERTY keyboard layout type.
        case azerty

        /// A Colemak keyboard layout type.
        case colemak

        /// A Dvorak keyboard layout type.
        case dvorak

        /// A Turkish F keyboard layout type.
        case turkishF

        /// A Turkish Q keyboard layout type.
        case turkishQ

        /// A QWERTY keyboard layout type.
        case qwerty

        /// A QWERTY keyboard in Catalan layout type.
        case qwerty_catalan

        /// A QWERTZ keyboard layout type.
        case qwertz

        /// A Vietnamese Telex layout type.
        case vietnamese_telex(KeyboardKit.Keyboard.LayoutType.NestedReference)

        /// A Vietnamese VIQR layout type.
        case vietnamese_viqr(KeyboardKit.Keyboard.LayoutType.NestedReference)

        /// A Vietnamese VNI layout type.
        case vietnamese_vni(KeyboardKit.Keyboard.LayoutType.NestedReference)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.LayoutType, b: KeyboardKit.Keyboard.LayoutType) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.LayoutType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Keyboard {

    /// This view can be used to preview a ``Keyboard/Button``
    /// with various actions, themes and services.
    ///
    /// The view renders a single button without interaction.
    /// It's greedy and will take up as much space as it can.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/proPreviews`` feature.
    @MainActor @preconcurrency public struct ButtonPreview : View {

        /// Create a keyboard theme button preview.
        ///
        /// - Parameters:
        ///   - action: The action to preview, by default `.character("A")`.
        @MainActor @preconcurrency public init(action: KeyboardKit.KeyboardAction = .character("A"))

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This view can be used to toggle between two toolbars.
    ///
    /// The toolbar has a customizable leading `toggle` that
    /// toggles between the `toolbar` and `toggledToolbar`.
    ///
    /// This view uses a ``Keyboard/ToggleToolbarToggle`` as
    /// the default toggle, and will by deafult animate with
    /// a ``Keyboard/ToggleToolbarAnimation/slideUp``. These
    /// defaults can be replaced by custom values.
    ///
    /// > Important: This requires a KeyboardKit Pro license.
    @MainActor @preconcurrency public struct ToggleToolbar<Toggle, Toolbar, ToggledToolbar> : View where Toggle : View, Toolbar : View, ToggledToolbar : View {

        /// Create a toggle toolbar with custom views.
        ///
        /// - Parameters:
        ///   - isToggled: A custom toggled binding.
        ///   - animation: The animation to use, by default `.slideUp`.
        ///   - toolbar: The standard toolbar to display.
        ///   - toggledToolbar: The toolbar to display when the toggle state is active.
        ///   - toggle: The leading view that toggles between the two toolbars.
        @MainActor @preconcurrency public init(isToggled: Binding<Bool>? = nil, animation: KeyboardKit.Keyboard.ToggleToolbarAnimation = .slideUp, toolbar: Toolbar, toggledToolbar: ToggledToolbar, toggle: @escaping (_ isToggled: Bool) -> Toggle) throws

        /// Create a toggle toolbar with custom views, and a
        /// plus symbol toggle.
        ///
        /// - Parameters:
        ///   - toolbar: The standard toolbar to display.
        ///   - toggledToolbar: The toolbar to display when the toggle state is active.
        ///   - isToggled: A custom toggled binding.
        ///   - animation: The animation to use, by default `.slideUp`.
        @MainActor @preconcurrency public init(isToggled: Binding<Bool>? = nil, animation: KeyboardKit.Keyboard.ToggleToolbarAnimation = .slideUp, toolbar: Toolbar, toggledToolbar: ToggledToolbar) throws where Toggle == KeyboardKit.Keyboard.ToggleToolbarToggle

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This enum defines animations that can be used with a
    /// ``Keyboard/ToggleToolbar``.
    public enum ToggleToolbarAnimation : String, CaseIterable, Codable, Equatable, Identifiable {

        /// The toolbars fade in and out when toggled.
        case opacity

        /// The toolbar slides up and the toggled toolbar down.
        case flipUp

        /// The toolbar slides down and the toggled toolbar up.
        case flipDown

        /// The toolbars slides up when toggled.
        case slideUp

        /// The toolbars slides down when toggled.
        case slideDown

        /// The unique animation id.
        public var id: String { get }

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.ToggleToolbarAnimation]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.ToggleToolbarAnimation] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This is a standard toggle toolbar toggle, which uses
    /// a custom `Image` as toggle view.
    @MainActor @preconcurrency public struct ToggleToolbarToggle : View {

        /// Create a toggle toolbar toggle.
        ///
        /// - Parameters:
        ///   - isToggled: Whether the toggle is toggled.
        ///   - imageName: The icon name, by default `plus`.
        @MainActor @preconcurrency public init(imageName: String = "plus", isToggled: Bool)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This enum defines all supported text input types.
    ///
    /// Splitting up the native keyboard type into this enum
    /// and ``KeyboardType`` lets us change input type while
    /// keeping the same keyboard type.
    public enum InputType : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        /// Plain text.
        case text

        /// E-mail address.
        case emailAddress

        /// Emoji search.
        case emojiSearch

        /// URL.
        case url

        /// Web search.
        case webSearch

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.InputType]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.InputType] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard {

    /// This enum can be used to define when a screen should
    /// show a certain section or part of the user interface.
    public enum DisplayMode {

        case always

        case never

        case ifNeeded

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Keyboard.DisplayMode, b: KeyboardKit.Keyboard.DisplayMode) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }
}

extension Keyboard {

    /// This screen can be used as the main keyboard locales
    /// screen in your app or keyboard extension.
    ///
    /// The screen lists all added and available locales and
    /// can customize the spacebar behavior.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``localeSettingsScreenLocalization(_:)``
    /// * ``localeSettingsScreenVisibility(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct LocaleSettingsScreen : View {

        /// Create a keyboard app locale screen.
        ///
        /// - Parameters:
        ///   - displayLocale: The locale to display locales in.
        @MainActor @preconcurrency public init(displayLocale: Locale = .current)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This screen can be used as the main keyboard setting
    /// screen in your app or keyboard extension.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``keyboardSettingsScreenLocalization(_:)``
    /// * ``keyboardSettingsScreenVisibility(_:)``.
    /// 
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer> : View where Header : View, TopSection : View, SettingsSection : View, SpacebarSection : View, ToolbarsSection : View, Footer : View {

        /// Create a keyboard app settings screen.
        ///
        /// - Parameters:
        ///   - header: The view to show at the top of the list.
        ///   - topSection: The top section content.
        ///   - settingsSection: The settings section content.
        ///   - spacebarSection: The spacebar section content.
        ///   - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(@ViewBuilder header: @escaping () -> Header, @ViewBuilder topSection: @escaping KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.TopSectionBuilder, @ViewBuilder settingsSection: @escaping KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.SettingsSectionBuilder, @ViewBuilder spacebarSection: @escaping KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.SpacebarSectionBuilder, @ViewBuilder toolbarsSection: @escaping KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.ToolbarsSectionBuilder, @ViewBuilder footer: @escaping () -> Footer)

        public typealias Sections = KeyboardKit.Keyboard.SettingsScreenSections

        public typealias TopSectionBuilder = (KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.Sections.TopParams) -> TopSection

        public typealias SettingsSectionBuilder = (KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.Sections.SettingsParams) -> SettingsSection

        public typealias SpacebarSectionBuilder = (KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.Sections.SpacebarParams) -> SpacebarSection

        public typealias ToolbarsSectionBuilder = (KeyboardKit.Keyboard.SettingsScreen<Header, TopSection, SettingsSection, SpacebarSection, ToolbarsSection, Footer>.Sections.ToolbarsParams) -> ToolbarsSection

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard {

    /// This type can be used to customize the visibility of
    /// any part of a ``Keyboard/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardSettingsScreenVisibility(_:)``.
    public struct SettingsScreenVisibility {

        public init(topSection: Bool = true, settingsSection: Bool = true, spacebarSection: Bool = true, toolbarsSection: Bool = true)

        public var topSection: Bool

        public var settingsSection: Bool

        public var spacebarSection: Bool

        public var toolbarsSection: Bool
    }
}

extension Keyboard {

    /// This type can be used to configure the localizations
    /// of a ``Keyboard/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Keyboard Settings", topSection: String? = nil, topSectionFooter: String? = nil, settingsSection: String? = nil, settingsSectionAutocapitalization: String = "Auto-Capitalization", settingsSectionAutocollapse: String = "Auto-Collapse", settingsSectionEnableCapsLock: String = "Enable Caps Lock", settingsSectionSwipeDownActions: String = "Swipe Down Actions", settingsSectionPeriodShortcut: String = "\".\" Shortcut", settingsSectionFooter: String? = nil, spacebarSection: String? = "Spacebar", spacebarSectionLongPressBehavior: String = "Long Press", spacebarSectionMenuLeading: String = "Leading Menu", spacebarSectionMenuTrailing: String = "Trailing Menu", spacebarSectionFooter: String? = nil, toolbarsSection: String? = "Toolbars", toolbarsSectionInputToolbar: String = "Input Toolbar", toolbarsSectionFooter: String? = nil, dockEdgePicker: String = "One-Handed Keyboard", dockEdgePickerOff: String = "Off", dockEdgePickerLeading: String = "Leading", dockEdgePickerTrailing: String = "Trailing", inputToolbarTypePicker: String = "Input Toolbar", inputToolbarTypePickerAutomatic: String = "Automatic", inputToolbarTypePickerCharacters: String = "Characters", inputToolbarTypePickerCharactersPlaceholder: String = "Enter 1-10 Characters...", inputToolbarTypePickerNumbers: String = "Numbers", inputToolbarTypePickerRecentEmojis: String = "Recent Emojis", inputToolbarTypePickerNone: String = "None", spacebarLongPressBehaviorPicker: String = "Spacebar Long Press Behavior", spacebarLongPressBehaviorPickerMoveCursor: String = "Move Input Cursor", spacebarLongPressBehaviorPickerOpenLanguagePicker: String = "Open Language Picker", spacebarMenuPicker: String = "Spacebar Menu", spacebarMenuPickerNone: String = "None", spacebarMenuPickerLanguage: String = "Language")

        public var title: String

        public var topSection: String?

        public var topSectionFooter: String?

        public var settingsSection: String?

        public var settingsSectionAutocapitalization: String

        public var settingsSectionAutocollapse: String

        public var settingsSectionEnableCapsLock: String

        public var settingsSectionSwipeDownActions: String

        public var settingsSectionPeriodShortcut: String

        public var settingsSectionFooter: String?

        public var spacebarSection: String?

        public var spacebarSectionLongPressBehavior: String

        public var spacebarSectionMenuLeading: String

        public var spacebarSectionMenuTrailing: String

        public var spacebarSectionFooter: String?

        public var toolbarsSection: String?

        public var toolbarsSectionInputToolbar: String

        public var toolbarsSectionFooter: String?

        public var dockEdgePicker: String

        public var dockEdgePickerOff: String

        public var dockEdgePickerLeading: String

        public var dockEdgePickerTrailing: String

        public var inputToolbarTypePicker: String

        public var inputToolbarTypePickerAutomatic: String

        public var inputToolbarTypePickerCharacters: String

        public var inputToolbarTypePickerCharactersPlaceholder: String

        public var inputToolbarTypePickerNumbers: String

        public var inputToolbarTypePickerRecentEmojis: String

        public var inputToolbarTypePickerNone: String

        public var spacebarLongPressBehaviorPicker: String

        public var spacebarLongPressBehaviorPickerMoveCursor: String

        public var spacebarLongPressBehaviorPickerOpenLanguagePicker: String

        public var spacebarMenuPicker: String

        public var spacebarMenuPickerNone: String

        public var spacebarMenuPickerLanguage: String
    }
}

extension Keyboard {

    /// This type can be used to configure the localizations
    /// of a ``Keyboard/LocaleSettingsScreen``.
    ///
    /// Although the screen is technically a `locale` screen,
    /// it uses the terminology "languages" when it presents
    /// them to the user, which is why these keys use it too.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/localeSettingsScreenLocalization(_:)``.
    public struct LocaleSettingsScreenLocalization {

        public init(title: String = "Languages", addedLanguagesSection: String? = "Added languages", addedLanguagesSelectedSubtitle: String = "Selected", addedLanguagesSectionFooter: String? = "The keyboard will use the first language in this list.", keyboardSection: String? = "Keyboard", keyboardSectionSpacebarLocaleMenu: String = "Spacebar Locale Menu", keyboardSectionFooter: String? = nil, moreLanguagesSection: String? = "More languages", moreLanguagesSectionFooter: String? = nil)

        public var title: String

        public var addedLanguagesSection: String?

        public var addedLanguagesSelectedSubtitle: String

        public var addedLanguagesSectionFooter: String?

        public var keyboardSection: String?

        public var keyboardSectionSpacebarLocaleMenu: String

        public var keyboardSectionFooter: String?

        public var moreLanguagesSection: String?

        public var moreLanguagesSectionFooter: String?
    }
}

extension Keyboard.LocaleSettingsScreenVisibility {

    public static var standard: KeyboardKit.Keyboard.LocaleSettingsScreenVisibility { get }
}

extension Keyboard.LocaleSettingsScreenPickers {

    /// This picker is used to pick keyboard layout types in
    /// ``Keyboard/LocaleSettingsScreen``.
    ///
    /// The view can also be used as a standalone picker and
    /// be localized with the same modifier as the screen.
    ///
    /// This picker will modify the passed in locales, which
    /// is why it uses a locales binding and a static locale.
    @MainActor @preconcurrency public struct LayoutItemPicker : View {

        @MainActor @preconcurrency public init(locales: Binding<[KeyboardKit.Keyboard.AddedLocale]>, locale addedLocale: KeyboardKit.Keyboard.AddedLocale)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard.SettingsScreenPickers {

    /// This picker can be used to pick dock edges.
    ///
    /// The view can also be used as a standalone picker and
    /// be localized with the same modifier as the screen.
    @MainActor @preconcurrency public struct DockEdgePicker : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This picker can pick an input toolbar type.
    ///
    /// The view can also be used as a standalone picker and
    /// be localized with the same modifier as the screen.
    @MainActor @preconcurrency public struct InputToolbarTypePicker : View {

        @MainActor @preconcurrency public init(isTextFieldFocused: FocusState<Bool>.Binding? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This picker can pick a spacebar long press behavior.
    ///
    /// The view can also be used as a standalone picker and
    /// be localized with the same modifier as the screen.
    @MainActor @preconcurrency public struct SpacebarLongPressBehaviorPicker : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This picker can pick a spacebar menu.
    ///
    /// The view can also be used as a standalone picker and
    /// be localized with the same modifier as the screen. 
    @MainActor @preconcurrency public struct SpacebarMenuPicker : View {

        @MainActor @preconcurrency public init(selection: Binding<KeyboardKit.Keyboard.SpacebarMenuType?>)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension Keyboard.SettingsScreenSections {

    @MainActor @preconcurrency public struct Top : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    public struct TopParams {

        public let view: KeyboardKit.Keyboard.SettingsScreenSections.Top
    }
}

extension Keyboard.SettingsScreenSections {

    @MainActor @preconcurrency public struct Settings : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    public struct SettingsParams {

        public let view: KeyboardKit.Keyboard.SettingsScreenSections.Settings
    }
}

extension Keyboard.SettingsScreenSections {

    @MainActor @preconcurrency public struct Spacebar : View {

        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    public struct SpacebarParams {

        public let view: KeyboardKit.Keyboard.SettingsScreenSections.Spacebar
    }
}

extension Keyboard.SettingsScreenSections {

    @MainActor @preconcurrency public struct Toolbars : View {

        @MainActor @preconcurrency public init(isInputToolbarTextFieldFocused: FocusState<Bool>.Binding? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    public struct ToolbarsParams {

        public let view: KeyboardKit.Keyboard.SettingsScreenSections.Toolbars
    }
}

extension Keyboard.SpacebarMenuType {

    /// The unique behavior ID.
    public var id: String { get }
}

extension Keyboard.SpacebarMenuType : RawRepresentable {
}

extension Keyboard.SpacebarMenuTitle : Sendable {
}

extension Keyboard.AddedLocale {

    /// A list of all supported locales.
    public static var keyboardKitSupported: [KeyboardKit.Keyboard.AddedLocale] { get }

    /// The related locale to use.
    ///
    /// This is optional, since the ``localeIdentifier`` can
    /// be invalid, even though this should never happen.
    public var locale: Locale? { get }

    /// The related keyboard layout to use, if any.
    public var layoutType: KeyboardKit.Keyboard.LayoutType? { get }
}

extension Keyboard.BottomRow {

    /// This typealias defines a button content builder.
    public typealias ButtonContentBuilder = (KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.ButtonContentParams) -> ButtonContent

    /// This typealias defines button content builder params.
    public typealias ButtonContentParams = (item: KeyboardKit.KeyboardLayout.Item, view: KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.StandardButtonContent)

    /// The standard button content view type.
    public typealias StandardButtonContent = KeyboardKit.Keyboard.ButtonContent

    /// This typealias defines a button view builder.
    public typealias ButtonViewBuilder = (KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.ButtonViewParams) -> ButtonView

    /// This typealias defines button view builder params.
    public typealias ButtonViewParams = (item: KeyboardKit.KeyboardLayout.Item, view: KeyboardKit.Keyboard.BottomRow<ButtonContent, ButtonView>.StandardButtonView)

    /// The standard button view type.
    public typealias StandardButtonView = KeyboardKit.KeyboardViewItem<ButtonContent>
}

extension Keyboard.BottomRow : Sendable {
}

extension Keyboard.Diacritic {

    /// A Czech `´`.
    public static var czechAccent: KeyboardKit.Keyboard.Diacritic { get }

    /// A Czech `ˇ`.
    public static var czechCaron: KeyboardKit.Keyboard.Diacritic { get }
}

extension Keyboard.Diacritic {

    /// The sorted keys to check when performing insertions.
    public var sortedReplacementKeys: [String] { get }

    /// Determine how the diacritic should be applied a text.
    public func insertionResult(whenAppendedTo text: String) -> KeyboardKit.Keyboard.DiacriticInsertionResult
}

extension Keyboard.DiacriticInsertionResult {

    public static func noMatch(for char: String) -> KeyboardKit.Keyboard.DiacriticInsertionResult
}

extension Keyboard.SpacebarLongPressBehavior {

    /// The unique behavior ID.
    public var id: String { get }

    /// Whether space should move the input cursor.
    public var shouldMoveInputCursor: Bool { get }

    /// Whether space should open a locale context menu.
    public var shouldOpenLocaleContextMenu: Bool { get }
}

extension Keyboard.SpacebarLongPressBehavior : RawRepresentable {
}

extension Keyboard.ButtonKey {

    @MainActor @preconcurrency public var backgroundColor: Color { get }

    @MainActor @preconcurrency public var backgroundColorRaw: Color { get }

    @MainActor @preconcurrency public var backgroundOpacity: Double { get }

    @MainActor @preconcurrency public var borderColor: Color { get }

    @MainActor @preconcurrency public var borderLineWidth: CGFloat { get }

    @MainActor @preconcurrency public var cornerRadius: CGFloat { get }
}

extension Keyboard.ButtonKey : Sendable {
}

extension Keyboard.KeyboardType {

    /// Get a list of all standard available keyboard types.
    public static var allCases: [KeyboardKit.Keyboard.KeyboardType] { get }
}

extension Keyboard.KeyboardType {

    /// The type's unique identifier.
    public var id: String { get }

    /// Whether the type is numeric or symbolic.
    public var isNumericOrSymbolic: Bool { get }

    /// Whether the type should sync with keyboard context.
    ///
    /// This should only be true for
    public var shouldSyncWithKeyboardContext: Bool { get }
}

extension Keyboard.KeyboardType {

    /// The keyboard type's standard button image.
    public var standardButtonImage: Image? { get }

    /// The keyboard type's standard button text.
    public func standardButtonText(for context: KeyboardKit.KeyboardContext) -> String?
}

extension Keyboard.KeyboardType {

    /// Whether or not this type prefers autocomplete.
    ///
    /// Use both ``Keyboard/KeyboardType`` & `UIKeyboardType`
    /// to compute this, since many native keyboard types do
    /// not map to a corresponding ``Keyboard/KeyboardType``.
    public var prefersAutocomplete: Bool { get }
}

extension Keyboard.SpacebarDragSensitivity {

    public var id: Int { get }

    public var points: Int { get }
}

extension Keyboard.ButtonShadow : Sendable {
}

extension Keyboard.NumberPad : Sendable {
}

extension Keyboard.Toolbar : Sendable {
}

extension Keyboard.ToolbarStyle {

    /// The standard keyboard toolbar style.
    public static var standard: KeyboardKit.Keyboard.ToolbarStyle { get }
}

extension Keyboard.ReturnKeyType {

    /// The type's unique identifier.
    public var id: String { get }

    /// Whether or not this type is a system action.
    public var isSystemAction: Bool { get }
}

extension Keyboard.ReturnKeyType {

    /// The native UIReturnKeyType that this type represents.
    public var nativeType: UIReturnKeyType { get }
}

extension Keyboard.ReturnKeyType {

    /// The standard button image for a certain locale.
    public func standardButtonImage(for context: KeyboardKit.KeyboardContext) -> Image?
}

extension Keyboard.ReturnKeyType {

    /// The standard button to text for a certain locale.
    public func standardButtonText(for locale: Locale) -> String?
}

extension Keyboard.AutocapitalizationType {

    public var id: String { get }
}

extension Keyboard.AutocapitalizationType : RawRepresentable {
}

extension Keyboard.Button : Sendable {
}

extension Keyboard.CollapsedView : Sendable {
}

extension Keyboard.ButtonContent : Sendable {
}

extension Keyboard.InputToolbarDisplayMode {

    /// Display a fixed set of characters.
    public static func characters(_ inputs: String) -> KeyboardKit.Keyboard.InputToolbarDisplayMode
}

extension Keyboard.State {

    public func setup(for app: KeyboardKit.KeyboardApp)
}

@MainActor extension Keyboard.State {

    @MainActor public func setup(for controller: KeyboardKit.KeyboardInputViewController)
}

extension Keyboard.State {

    public static var preview: KeyboardKit.Keyboard.State { get }
}

extension Keyboard.ButtonTitle : Sendable {
}

extension Keyboard.Background {

    /// The standard keyboard background style.
    @MainActor @preconcurrency public static var standard: KeyboardKit.Keyboard.Background { get }

    /// A background style with a single color.
    @MainActor @preconcurrency public static func color(_ color: Color) -> KeyboardKit.Keyboard.Background

    /// A background style with a single image.
    @MainActor @preconcurrency public static func image(data: Data, contentMode: KeyboardKit.Keyboard.Background.ImageContentMode = .fill) -> KeyboardKit.Keyboard.Background

    /// A background style with a single image.
    @MainActor @preconcurrency public static func image(_ image: UIImage?, contentMode: KeyboardKit.Keyboard.Background.ImageContentMode = .fill) -> KeyboardKit.Keyboard.Background

    /// A background style with a single image.
    @MainActor @preconcurrency public static func image(systemName: String, contentMode: KeyboardKit.Keyboard.Background.ImageContentMode = .fill) -> KeyboardKit.Keyboard.Background

    /// A background style with a vertical gradient.
    @MainActor @preconcurrency public static func verticalGradient(_ colors: [Color]) -> KeyboardKit.Keyboard.Background
}

extension Keyboard.Background {

    /// The background color to use to an accesory view that
    /// is docked just above this background.
    @MainActor @preconcurrency public var topAccessoryBackgroundColor: some View { get }

    /// The background color to use to an accesory view that
    /// is docked just below this background.
    @MainActor @preconcurrency public var bottomAccessoryBackgroundColor: some View { get }
}

extension Keyboard.Background : Sendable {
}

@MainActor extension Keyboard.Services {

    @MainActor public func setup(for controller: KeyboardKit.KeyboardInputViewController)

    @MainActor public func setupActionHandler(for controller: KeyboardKit.KeyboardInputViewController)

    @MainActor public func setupSpacebarGesture(for controller: KeyboardKit.KeyboardInputViewController)
}

extension Keyboard.Services {

    public static var preview: KeyboardKit.Keyboard.Services { get }
}

extension Keyboard.StandardKeyboardBehavior {

    /// Is the type currently registering a double shift tap.
    public var isDoubleShiftTap: Bool { get }
}

extension Keyboard.StorageValue {

    public init(_ value: Value)

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }
}

extension Keyboard.KeyboardCase {

    /// The stable identity of the entity associated with this instance.
    public var id: String { get }

    /// Whether the case is uppercased or caps locked.
    public var isUppercasedOrCapslocked: Bool { get }
}

extension Keyboard.KeyboardCase : RawRepresentable {
}

extension Keyboard.NextKeyboardButton : Sendable {
}

extension Keyboard.DockEdge {

    /// The unique edge ID.
    public var id: String { get }

    /// The corresponding keyboard alignment.
    public var alignment: Alignment { get }

    /// The corresponding horizontal edge.
    public var edge: HorizontalEdge { get }
}

extension Keyboard.DockEdge : RawRepresentable {
}

extension Keyboard.BackspaceRange {

    /// The stable identity of the entity associated with this instance.
    public var id: String { get }
}

extension Keyboard.BackspaceRange : RawRepresentable {
}

extension Keyboard.PlainButtonStyle : Sendable {
}

extension Keyboard.ButtonStyle {

    /// Extend this style with set values from another style.
    public func extended(with style: KeyboardKit.Keyboard.ButtonStyle) -> KeyboardKit.Keyboard.ButtonStyle

    /// A spacer button style means that the button will not
    /// be visually detectable, but still rendered.
    public static let spacer: KeyboardKit.Keyboard.ButtonStyle
}

extension Keyboard.ButtonBorderStyle {

    /// A button border style with no borders.
    public static var noBorder: KeyboardKit.Keyboard.ButtonBorderStyle { get }

    /// The standard button border style.
    public static var standard: KeyboardKit.Keyboard.ButtonBorderStyle { get }
}

extension Keyboard.ButtonShadowStyle {

    /// A button shadow style with no shadow.
    public static var noShadow: KeyboardKit.Keyboard.ButtonShadowStyle { get }

    /// The standard button shadow style.
    public static var standard: KeyboardKit.Keyboard.ButtonShadowStyle { get }
}

extension Keyboard.Gesture {

    /// A ``Keyboard/Gesture/repeatPress`` shorthand.
    public static let `repeat`: KeyboardKit.Keyboard.Gesture
}

extension Keyboard.Gesture {

    /// The gesture's unique identifier.
    public var id: String { get }
}

extension Keyboard.Gesture : RawRepresentable {
}

extension Keyboard.LayoutType {

    /// All possible layout combinations
    public static var allCases: [KeyboardKit.Keyboard.LayoutType] { get }

    /// The layout type's display name.
    public var displayName: String { get }

    /// The layout type's unique ID.
    public var id: String { get }
}

extension Keyboard.LayoutType {

    /// This enum defines references to main layout types to
    /// allow layout types to refer other main types.
    public enum NestedReference : String, CaseIterable, Identifiable, KeyboardKit.KeyboardModel {

        case azerty

        case colemak

        case dvorak

        case qwerty

        case qwertz

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Keyboard.LayoutType.NestedReference]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Keyboard.LayoutType.NestedReference] { get }

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension Keyboard.LayoutType {

    /// The layout type's alphabetic input set.
    ///
    /// > Important: Some input sets require KeyboardKit Pro.
    public var alphabeticInputSet: KeyboardKit.KeyboardLayout.InputSet { get throws }
}

extension Keyboard.ButtonPreview : Sendable {
}

extension Keyboard.ToggleToolbar : Sendable {
}

extension Keyboard.ToggleToolbarAnimation : Hashable {
}

extension Keyboard.ToggleToolbarAnimation : RawRepresentable {
}

extension Keyboard.ToggleToolbarToggle : Sendable {
}

extension Keyboard.InputType {

    /// The type's unique identifier.
    public var id: String { get }

    /// Whether the type should sync with keyboard context.
    public var shouldSyncWithKeyboardContext: Bool { get }
}

extension Keyboard.InputType {

    /// The input type's standard button text.
    public func standardButtonText(for context: KeyboardKit.KeyboardContext) -> String?
}

extension Keyboard.InputType {

    /// Whether or not this type prefers autocomplete.
    public var prefersAutocomplete: Bool { get }
}

extension Keyboard.InputType : RawRepresentable {
}

extension Keyboard.DisplayMode {

    /// Whether or not to display a preferred display value.
    public func shouldDisplay(_ preferred: Bool = true) -> Bool
}

extension Keyboard.DisplayMode : Equatable {
}

extension Keyboard.DisplayMode : Hashable {
}

extension Keyboard.LocaleSettingsScreen : Sendable {
}

extension Keyboard.SettingsScreen where Header == EmptyView, TopSection == KeyboardKit.Keyboard.SettingsScreenSections.Top, SettingsSection == KeyboardKit.Keyboard.SettingsScreenSections.Settings, SpacebarSection == KeyboardKit.Keyboard.SettingsScreenSections.Spacebar, ToolbarsSection == KeyboardKit.Keyboard.SettingsScreenSections.Toolbars, Footer == EmptyView {

    /// Create a keyboard app settings screen.
    @MainActor @preconcurrency public init()
}

extension Keyboard.SettingsScreen : Sendable {
}

extension Keyboard.SettingsScreenVisibility {

    public static var standard: KeyboardKit.Keyboard.SettingsScreenVisibility { get }
}

extension Keyboard.SettingsScreenLocalization {

    public func title(for edge: KeyboardKit.Keyboard.DockEdge?) -> String

    public func title(for value: KeyboardKit.Keyboard.SpacebarLongPressBehavior) -> String

    public func title(for value: KeyboardKit.Keyboard.SpacebarMenuType?) -> String

    public func title(for value: KeyboardKit.KeyboardSettings.InputToolbarType) -> String
}

extension Keyboard.SettingsScreenLocalization {

    public static var standard: KeyboardKit.Keyboard.SettingsScreenLocalization { get }
}

extension Keyboard.LocaleSettingsScreenLocalization {

    public static var standard: KeyboardKit.Keyboard.LocaleSettingsScreenLocalization { get }
}

extension Keyboard.LocaleSettingsScreenPickers.LayoutItemPicker : Sendable {
}

extension Keyboard.SettingsScreenPickers.DockEdgePicker : Sendable {
}

extension Keyboard.SettingsScreenPickers.InputToolbarTypePicker : Sendable {
}

extension Keyboard.SettingsScreenPickers.SpacebarLongPressBehaviorPicker : Sendable {
}

extension Keyboard.SettingsScreenPickers.SpacebarMenuPicker : Sendable {
}

extension Keyboard.SettingsScreenSections.Top : Sendable {
}

extension Keyboard.SettingsScreenSections.Settings : Sendable {
}

extension Keyboard.SettingsScreenSections.Spacebar : Sendable {
}

extension Keyboard.SettingsScreenSections.Toolbars : Sendable {
}

extension Keyboard.Background.ImageContentMode : Equatable {
}

extension Keyboard.Background.ImageContentMode : Hashable {
}

extension Keyboard.Background.ImageContentMode : RawRepresentable {
}

extension Keyboard.LayoutType.NestedReference {

    public var displayName: String { get }

    /// The stable identity of the entity associated with this instance.
    public var id: String { get }
}

extension Keyboard.LayoutType.NestedReference : RawRepresentable {
}

/// This enum defines keyboard-specific actions, and is also
/// a namespace for action-related types.
///
/// Keyboard actions can be used to trigger keyboard-related
/// operation, for instance when tapping a button, when some
/// event happens or programatically by triggering an action
/// with a ``KeyboardActionHandler``.
///
/// The documentation for each action describes the standard
/// behavior when using a ``StandardActionHandler``. Actions
/// that don't have a standard behavior must be handled with
/// a custom ``KeyboardActionHandler``.
public enum KeyboardAction : KeyboardKit.KeyboardModel {

    /// Deletes backwards when pressed, and repeats until released.
    case backspace

    /// Switch to a caps-lock keyboard when pressed.
    case capsLock

    /// Inserts a text character when released.
    case character(String)

    /// Inserts a text character when released, rendered as empty space.
    case characterMargin(String)

    /// Represents a command (⌘) key.
    case command

    /// Represents a control (⌃) key.
    case control

    /// A custom action that you can handle in any custom way.
    case custom(named: String)

    /// A diacritic action, that will replace any previous match when release.
    case diacritic(KeyboardKit.Keyboard.Diacritic)

    /// Represents a dictation key.
    case dictation

    /// Dismisses the keyboard when released.
    case dismissKeyboard

    /// Inserts an emoji when released.
    case emoji(KeyboardKit.Emoji)

    /// Represents an escape (esc) key.
    case escape

    /// Represents a function (fn) key.
    case function

    /// Can be used to refer to an image asset.
    case image(description: String, keyboardImageName: String, imageName: String)

    /// Changes the keyboard input type when pressed.
    case keyboardInputType(KeyboardKit.Keyboard.InputType)

    /// Changes the keyboard type when pressed.
    case keyboardType(KeyboardKit.Keyboard.KeyboardType)

    /// Moves the input cursor back one step when released.
    case moveCursorBackward

    /// Moves the input cursor forward one step when released.
    case moveCursorForward

    /// Triggers the system keyboard switcher on release and long press.
    case nextKeyboard

    /// Triggers a locale switcher action on release and menu on long press.
    case nextLocale

    /// A placeholder action that does nothing and should be rendered as empty space.
    case none

    /// Represents an option (⌥) key.
    case option

    /// Pastes from the system clipboard when tapped.
    case pasteFromClipboard

    /// A primary key, e.g. `return`, `search` etc. that inserts a new line on release.
    case primary(KeyboardKit.Keyboard.ReturnKeyType)

    /// Represents a settings (⚙️) key.
    case settings

    /// Changes keyboard case when released and double tapped.
    ///
    /// > Note: We currently need the current case, since it
    /// is used for uniqueness. Without it, the keyboard key
    /// isn't properly updated when the context case changes.
    /// We should however try to find a way around it, since
    /// the action should just be `shift`.
    case shift(KeyboardKit.Keyboard.KeyboardCase)

    /// Inserts a space when released and can perform custom actions when long pressed.
    case space

    /// Can be used to refer to a system image (SF Symbol).
    case systemImage(description: String, keyboardImageName: String, imageName: String)

    /// Open system settings for the app when released.
    case systemSettings

    /// Inserts a tab when released.
    case tab

    /// Inserts a text string when released.
    case text(String)

    /// Open an url when released, using a custom id for identification.
    case url(URL?, id: String?)

    /// Inserts a period or a URL domain.
    case urlDomain

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.KeyboardAction, b: KeyboardKit.KeyboardAction) -> Bool

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension KeyboardAction {

    /// Whether an action has a standard image.
    public func hasStandardButtonImage(for context: KeyboardKit.KeyboardContext) -> Bool

    /// The standard button image.
    public func standardButtonImage(for context: KeyboardKit.KeyboardContext) -> Image?

    /// The standard button image scale.
    public func standardButtonImageScale(for context: KeyboardKit.KeyboardContext) -> CGFloat
}

extension KeyboardAction {

    /// The standard keyboard button background color.
    public func standardButtonBackgroundColor(for context: KeyboardKit.KeyboardContext, isPressed: Bool = false) -> Color

    /// The standard keyboard button background opacity.
    public func standardButtonBackgroundColorOpacity(for context: KeyboardKit.KeyboardContext, isPressed: Bool = false) -> Double

    /// The standard keyboard button background color in its
    /// raw form, without any opacity applied.
    public func standardButtonBackgroundColorValue(for context: KeyboardKit.KeyboardContext, isPressed: Bool) -> Color

    /// The standard keyboard button style foreground color.
    public func standardButtonForegroundColor(for context: KeyboardKit.KeyboardContext, isPressed: Bool = false) -> Color
}

extension KeyboardAction {

    /// Whether an action has a standard text.
    public func hasStandardButtonText(for context: KeyboardKit.KeyboardContext) -> Bool

    /// The standard button text.
    public func standardButtonText(for context: KeyboardKit.KeyboardContext) -> String?
}

extension KeyboardAction {

    /// The standard button style.
    ///
    /// You can use individual style builders to calculate a
    /// single values instead of the entire style.
    public func standardButtonStyle(for context: KeyboardKit.KeyboardContext, isPressed: Bool = false) -> KeyboardKit.Keyboard.ButtonStyle
}

extension KeyboardAction {

    /// The standard button border style.
    public func standardButtonBorderStyle(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.Keyboard.ButtonBorderStyle

    /// The standard button style content insets.
    public func standardButtonContentInsets(for context: KeyboardKit.KeyboardContext) -> EdgeInsets

    /// The standard button style corner radius.
    public func standardButtonCornerRadius(for context: KeyboardKit.KeyboardContext) -> CGFloat

    /// The standard button layout configuration.
    public func standardButtonLayoutConfiguration(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// The standard button shadow style.
    public func standardButtonShadowStyle(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.Keyboard.ButtonShadowStyle
}

extension KeyboardAction {

    /// The standard keyboard font.
    public func standardButtonFont(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardFont
}

extension KeyboardAction {

    /// The standard keyboard font size.
    public func standardButtonFontSize(for context: KeyboardKit.KeyboardContext) -> CGFloat

    /// The standard keyboard font weight.
    public func standardButtonFontWeight(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardFont.FontWeight?
}

extension KeyboardAction {

    /// This typealias defines a controller gesture action.
    public typealias GestureAction = ((any KeyboardKit.KeyboardController)?) -> Void

    /// The standard controller action.
    public var standardAction: KeyboardKit.KeyboardAction.GestureAction? { get }

    /// The standard controller action for a certain gesture.
    public func standardAction(for gesture: KeyboardKit.Keyboard.Gesture) -> KeyboardKit.KeyboardAction.GestureAction?

    /// The standard double tap action, if any.
    public var standardDoubleTapAction: KeyboardKit.KeyboardAction.GestureAction? { get }

    /// The standard long press action, if any.
    public var standardLongPressAction: KeyboardKit.KeyboardAction.GestureAction? { get }

    /// The standard press action, if any.
    public var standardPressAction: KeyboardKit.KeyboardAction.GestureAction? { get }

    /// The standard release action, if any.
    public var standardReleaseAction: KeyboardKit.KeyboardAction.GestureAction? { get }

    /// The standard repeat action, if any.
    public var standardRepeatAction: KeyboardKit.KeyboardAction.GestureAction? { get }
}

extension KeyboardAction {

    /// This is a typealias for a keyboard action array.
    public typealias Row = [KeyboardKit.KeyboardAction]

    /// This is a typealias for a keyboard action row array.
    public typealias Rows = [KeyboardKit.KeyboardAction.Row]
}

extension KeyboardAction {

    /// The action's accessibility label.
    public var accessibilityLabel: String? { get }
}

extension KeyboardAction {

    /// An `character(_:)` shorthand.
    public static func character(char: Character) -> KeyboardKit.KeyboardAction

    /// An `emoji(_:)` shorthand.
    public static func emoji(_ char: String) -> KeyboardKit.KeyboardAction

    /// An ``KeyboardAction/url(_:id:)`` shorthand.
    public static func url(_ url: URL?) -> KeyboardKit.KeyboardAction

    /// An ``KeyboardAction/url(_:id:)`` shorthand.
    public static func url(_ url: String?) -> KeyboardKit.KeyboardAction

    /// A list of URL domain-related actions.
    public static var urlDomainActions: [KeyboardKit.KeyboardAction] { get }
}

extension KeyboardAction {

    /// A character margin action can add an extra tap area.
    public var characterMarginAction: KeyboardKit.KeyboardAction { get }

    /// Whether or not the action is an alphabetic type.
    public var isAlphabeticKeyboardTypeAction: Bool { get }

    /// Whether or not the action is a character action.
    public var isCharacterAction: Bool { get }

    /// Whether or not the action is a character action.
    public func isCharacterAction(for character: String) -> Bool

    /// Whether or not the action is a character margin.
    public var isCharacterMarginAction: Bool { get }

    /// Whether or not the action is a character margin.
    public func isCharacterMarginAction(for character: String) -> Bool

    /// Whether or not the action is an emoji action.
    public var isEmojiAction: Bool { get }

    /// Whether or not the action is an input action.
    ///
    /// Input actions insert content into the text proxy and
    /// are by default rendered as light buttons.
    public var isInputAction: Bool { get }

    /// Whether or not the action is a primary action.
    ///
    /// Primary actions insert new lines into the proxy, but
    /// can be rendered differently to express intent.
    public var isPrimaryAction: Bool { get }

    /// Whether or not the action is a shift action.
    public var isShiftAction: Bool { get }

    /// Whether or not the action primary serves as a spacer.
    public var isSpacer: Bool { get }

    /// Whether or not the action is a dark system action.
    public var isSystemAction: Bool { get }

    /// Whether or not the action is a URL action.
    public var isUrlAction: Bool { get }

    /// Whether or not the action is a keyboard type action.
    public func isKeyboardTypeAction(_ keyboardType: KeyboardKit.Keyboard.KeyboardType) -> Bool
}

extension KeyboardAction {

    /// The theme button type.
    public var keyboardThemeButtonType: KeyboardKit.KeyboardTheme.ButtonType { get }
}

extension KeyboardAction {

    /// This class provides a standard way to handle actions
    /// that are triggered by the keyboard.
    ///
    /// KeyboardKit automatically creates an instance of the
    /// class when the keyboard is launched, then injects it
    /// into ``KeyboardInputViewController/services``.
    ///
    /// The handler implements ``KeyboardBehavior`` by using
    /// the ``behavior`` by default, but it can override any
    /// of the standard behaviors. It has `should` functions
    /// and corresponding `try` functions to let is override
    /// *if* something should be done, and if so, *how*.
    ///
    /// You can inherit this class to get base functionality,
    /// then override any open parts that you want to change.
    ///
    /// This service can also be resolved with the shorthand
    /// ``KeyboardActionHandler/standard(for:)``.
    @objc open class StandardActionHandler : NSObject, KeyboardKit.KeyboardActionHandler, KeyboardKit.KeyboardBehavior {

        /// Create a standard keyboard action handler for an
        /// explicit controller.
        ///
        /// - Parameters:
        ///   - controller: The keyboard controller to use.
        public convenience init(controller: any KeyboardKit.KeyboardController)

        /// Create a standard keyboard action handler for an
        /// optional controller and explicit dependencies.
        ///
        /// The `controller` parameter is optional, to allow
        /// you to set up later.
        ///
        /// - Parameters:
        ///   - controller: The keyboard controller to use, if any.
        ///   - keyboardContext: The keyboard context to use.
        ///   - keyboardBehavior: The keyboard behavior to use.
        ///   - autocompleteContext: The autocomplete context to use.
        ///   - autocompleteService: The autocomplete service to use.
        ///   - emojiContext: The emoji context to use.
        ///   - feedbackContext: The feedback context to use.
        ///   - feedbackService: The feedback service to use.
        ///   - spacebarDragGestureHandler: The spacebar drag gesture handler to use.
        public init(controller: (any KeyboardKit.KeyboardController)?, keyboardContext: KeyboardKit.KeyboardContext, keyboardBehavior: any KeyboardKit.KeyboardBehavior, autocompleteContext: KeyboardKit.AutocompleteContext, autocompleteService: any KeyboardKit.AutocompleteService, emojiContext: KeyboardKit.EmojiContext, feedbackContext: KeyboardKit.FeedbackContext, feedbackService: any KeyboardKit.FeedbackService, spacebarDragGestureHandler: KeyboardKit.Gestures.SpacebarDragGestureHandler)

        /// The controller to which this handler applies.
        weak public var keyboardController: (any KeyboardKit.KeyboardController)?

        /// The autocomplete context to use.
        public var autocompleteContext: KeyboardKit.AutocompleteContext

        /// The autocomplete service to use.
        public var autocompleteService: (any KeyboardKit.AutocompleteService)?

        /// The keyboard behavior to use.
        public var behavior: any KeyboardKit.KeyboardBehavior

        /// The emoji context to use.
        public var emojiContext: KeyboardKit.EmojiContext

        /// The feedback context to use.
        public var feedbackContext: KeyboardKit.FeedbackContext

        /// The feedback service to use.
        public var feedbackService: any KeyboardKit.FeedbackService

        /// The keyboard context to use.
        public var keyboardContext: KeyboardKit.KeyboardContext

        /// The spacebar drag gesture handler to use.
        public var spacebarDragGestureHandler: KeyboardKit.Gestures.SpacebarDragGestureHandler

        /// The start point of the last spacebar drag.
        public var spacebarDragGestureStartPoint: CGPoint?

        /// This property can be resolved in KeyboardKit Pro.
        public var vietnameseInputEngine: KeyboardKit.VietnameseInputEngine?

        /// Whether the handler can handle an action gesture.
        open func canHandle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Handle a certain keyboard action.
        open func handle(_ action: KeyboardKit.KeyboardAction)

        /// Handle a certain keyboard action gesture.
        open func handle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Handle a certain action gesture, with replace logic.
        open func handle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction, replaced: Bool)

        /// Handle a certain autocomplete suggestion.
        open func handle(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

        /// Handle a certain keyboard action drag gesture.
        open func handleDrag(on action: KeyboardKit.KeyboardAction, from startLocation: CGPoint, to currentLocation: CGPoint)

        /// The range that backspace should delete.
        open var backspaceRange: KeyboardKit.Keyboard.BackspaceRange { get }

        /// The text to use to end sentences with.
        open var endSentenceText: String

        /// The preferred keyboard case after a certain action gesture.
        open func preferredKeyboardCase(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardCase

        /// The preferred keyboard type after a certain action gesture.
        open func preferredKeyboardType(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardType

        /// Whether to end the current sentence after a certain action gesture.
        open func shouldEndCurrentSentence(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to register an emoji after a certain action gesture.
        open func shouldRegisterEmoji(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Try to change keyboard case after a certain action gesture.
        open func tryChangeKeyboardCase(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to change keyboard type after a certain action gesture.
        open func tryChangeKeyboardType(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to end the current sentence after a certain action gesture.
        open func tryEndCurrentSentence(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to register an emoji after a certain action gesture.
        open func tryRegisterEmoji(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// The standard action to use for a gesture action.
        open func action(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.KeyboardAction.GestureAction?

        /// An optional keyboard action gesture replacement.
        open func replacementAction(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.KeyboardAction?

        /// Try to handle a replacement action for a certain
        /// action, which then replaces this original action.
        ///
        /// The caller shouldn't handle the action when this
        /// returns `true`, since this means that the action
        /// has been replaced.
        open func tryHandleReplacementAction(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to apply autocorrect before a certain gesture action.
        open func shouldApplyAutocorrectSuggestion(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to ignore autocorrections for the current word before a certain gesture action.
        open func shouldAutoIgnoreCurrentWord(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to auto-learn a certain suggestion.
        open func shouldAutolearnSuggestion(_ suggestion: KeyboardKit.Autocomplete.Suggestion) -> Bool

        /// Whether to perform autocomplete after a certain gesture action.
        open func shouldPerformAutocomplete(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to reinsert an autocomplete removed space after a certain gesture action.
        open func shouldReinsertAutocompleteRemovedSpace(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to remove an autocomplete inserted space after a certain gesture action.
        open func shouldRemoveAutocompleteInsertedSpace(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Try to apply autocorrect before a certain gesture action.
        open func tryApplyAutocorrectSuggestion(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to ignore autocorrections for the current word before a certain gesture action.
        open func tryAutocompleteIgnoreCurrentWord(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to auto-learn a certain suggestion.
        open func tryAutolearnSuggestion(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

        /// Try to perform autocomplete after a certain gesture action.
        open func tryPerformAutocomplete(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to reinsert an autocomplete removed space after a certain gesture action.
        open func tryReinsertAutocompleteRemovedSpace(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to remove inserted space before a certain gesture action.
        open func tryRemoveAutocompleteInsertedSpace(before gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// The audio feedback to use for an action gesture.
        open func audioFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Audio?

        /// The haptic feedback to use for an action gesture.
        open func hapticFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Feedback.Haptic?

        /// Whether to trigger feedback for an action.
        ///
        /// This always returns `true` to allow the class to
        /// perform more granular checks later.
        open func shouldTriggerFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to trigger audio feedback for an action.
        ///
        /// This returns true if the ``feedbackContext`` has
        /// audio feedback enabled in it's settings.
        open func shouldTriggerAudioFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Whether to trigger haptic feedback for an action.
        ///
        /// This returns true if the ``feedbackContext`` has
        /// haptic feedback enabled in it's settings, and if
        /// certain other gesture conditions are `true`. The
        /// function also always returns true for long press
        /// on space by default.
        open func shouldTriggerHapticFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

        /// Trigger a certain audio feedback.
        open func triggerAudioFeedback(_ feedback: KeyboardKit.Feedback.Audio)

        /// Trigger a certain haptic feedback.
        open func triggerHapticFeedback(_ feedback: KeyboardKit.Feedback.Haptic)

        /// Try to trigger audio and haptic feedback for the
        /// provided gesture action.
        open func tryTriggerFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to trigger audio feedback for the action.
        open func tryTriggerAudioFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Try to trigger haptic feedback for the action.
        open func tryTriggerHapticFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        /// Trigger feedback for a certain action gesture.
        open func triggerFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

        @objc deinit
    }
}

extension KeyboardAction {

    /// Whether the action should apply autocorrect suggestions.
    public var shouldApplyAutocorrectSuggestion: Bool { get }

    /// Whether the action should re-insert autocomplete inserted spaces.
    public var shouldReinsertAutocompleteInsertedSpace: Bool { get }

    /// Whether the action should remove autocomplete inserted spaces.
    public var shouldRemoveAutocompleteInsertedSpace: Bool { get }
}

extension KeyboardAction {

    /// The image to which the action refers, if any.
    public var image: Image? { get }
}

extension KeyboardAction {

    /// The input callout text to present for the action.
    public var inputCalloutText: String? { get }
}

extension KeyboardAction {

    /// The standard layout item insets.
    public func standardLayoutItem(for config: KeyboardKit.KeyboardLayout.DeviceConfiguration, secondaryAction: KeyboardKit.KeyboardAction? = nil, width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, alignment: Alignment = .center) -> KeyboardKit.KeyboardLayout.Item

    /// The standard layout item insets.
    public func standardLayoutItemInsets(for config: KeyboardKit.KeyboardLayout.DeviceConfiguration) -> EdgeInsets

    /// The standard layout item size.
    public func standardLayoutItemSize(for config: KeyboardKit.KeyboardLayout.DeviceConfiguration, width: KeyboardKit.KeyboardLayout.ItemWidth? = nil) -> KeyboardKit.KeyboardLayout.ItemSize

    /// The standard layout item height.
    public func standardLayoutItemSizeHeight(for config: KeyboardKit.KeyboardLayout.DeviceConfiguration) -> Double

    /// The standard layout item width.
    public func standardLayoutItemSizeWidth(for config: KeyboardKit.KeyboardLayout.DeviceConfiguration) -> KeyboardKit.KeyboardLayout.ItemWidth
}

/// This protocol can be implemented by any classes that can
/// handle triggered ``KeyboardAction`` events.
///
/// Call the various `handle` functions to handle any action,
/// action gesture and autocomplete suggestion. You can also
/// use ``triggerFeedback(for:on:)`` to trigger audio/haptic
/// feedback at any time.
///
/// KeyboardKit will automatically setup a standard protocol
/// implementation in ``KeyboardInputViewController/services``
/// when the keyboard is launched. You can change or replace
/// it at any time to customize the keyboard action handling.
public protocol KeyboardActionHandler : KeyboardKit.FeedbackService {

    /// Whether the handler can handle an action gesture.
    func canHandle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

    /// Handle a certain keyboard action.
    func handle(_ action: KeyboardKit.KeyboardAction)

    /// Handle a certain keyboard action gesture.
    func handle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

    /// Handle a certain autocomplete suggestion.
    func handle(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

    /// Handle a certain keyboard action drag gesture.
    func handleDrag(on action: KeyboardKit.KeyboardAction, from startLocation: CGPoint, to currentLocation: CGPoint)

    /// Trigger feedback for a certain action gesture.
    func triggerFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)
}

extension KeyboardActionHandler {

    /// Handle a certain keyboard action.
    public func handle(action: KeyboardKit.KeyboardAction)

    /// Handle a certain autocomplete suggestion.
    public func handle(suggestion: KeyboardKit.Autocomplete.Suggestion)
}

extension KeyboardActionHandler {

    /// Try open a certain host application.
    ///
    /// You can check ``KeyboardHostApplication/canBeOpened`` in
    /// advance to see if the app will be opened.
    public func tryOpen(_ app: KeyboardKit.KeyboardHostApplication)
}

extension KeyboardActionHandler where Self == KeyboardKit.KeyboardAction.StandardActionHandler {

    /// Create a standard action handler.
    ///
    /// - Parameters:
    ///   - controller: The keyboard controller to use.
    public static func standard(for controller: any KeyboardKit.KeyboardController) -> Self

    /// Create a standard action handler.
    ///
    /// The `controller` parameter is optional, to allow you
    /// to set up later.
    ///
    /// - Parameters:
    ///   - controller: The keyboard controller to use, if any.
    ///   - keyboardContext: The keyboard context to use.
    ///   - keyboardBehavior: The keyboard behavior to use.
    ///   - autocompleteContext: The autocomplete context to use.
    ///   - autocompleteService: The autocomplete service to use.
    ///   - emojiContext: The emoji context to use.
    ///   - feedbackContext: The feedback context to use.
    ///   - feedbackService: The feedback service to use.
    ///   - spacebarDragGestureHandler: The spacebar drag gesture handler to use.
    public static func standard(for controller: (any KeyboardKit.KeyboardController)?, keyboardContext: KeyboardKit.KeyboardContext, keyboardBehavior: any KeyboardKit.KeyboardBehavior, autocompleteContext: KeyboardKit.AutocompleteContext, autocompleteService: any KeyboardKit.AutocompleteService, emojiContext: KeyboardKit.EmojiContext, feedbackContext: KeyboardKit.FeedbackContext, feedbackService: any KeyboardKit.FeedbackService, spacebarDragGestureHandler: KeyboardKit.Gestures.SpacebarDragGestureHandler) -> Self
}

extension KeyboardActionHandler where Self == KeyboardKit.KeyboardPreviews.ActionHandler {

    public static var preview: any KeyboardKit.KeyboardActionHandler { get }
}

/// This type can be used to define important properties for
/// your app, and is also a namespace for app-based features.
///
/// You can create a static app value and add it to both the
/// app and its keyboard, to be able to use it in both, e.g.:
///
/// ```swift
/// extension KeyboardApp {
///     static var keyboardKitDemo: Self {
///         .init(
///             name: "KeyboardKit",
///             licenseKey: "abc123",
///             appGroupId: "group.com.keyboardkit.demo",
///             locales: [.english, .swedish, .persian],
///             autocomplete: .init(...),
///             deepLinks: .init(app: "keyboardkit://")
///         )
///     }
/// }
/// ```
///
/// The ``licenseKey`` can be used to unlock KeyboardKit Pro.
/// You can omit this parameter if you use a license file.
///
/// The ``appGroupId`` can be used to make KeyboardKit setup
/// automatic settings sync between the app and its keyboard.
///
/// The ``locales`` list specifies which locales you want to
/// use. You don't have to provide this list if you are on a
/// Gold license, since it has all locales enabled.
public struct KeyboardApp {

    /// Create a custom keyboard app value.
    ///
    /// - Parameters:
    ///   - name: The name of the app.
    ///   - licenseKey: Your license key, if any.
    ///   - appGroupId: The app's App Group identifier, if any.
    ///   - locales: The locales to use in the app, by default `.all`.
    ///   - autocomplete: The autocomplete configuration to use.
    ///   - deepLinks: App-specific deep links, if any.
    ///   - keyboardSettingsKeyPrefix: A custom keyboard settings key prefix, if any.
    public init(name: String, licenseKey: String? = nil, appGroupId: String? = nil, locales: [Locale] = .keyboardKitSupported, autocomplete: KeyboardKit.KeyboardApp.AutocompleteConfiguration = .init(), deepLinks: KeyboardKit.KeyboardApp.DeepLinks? = nil, keyboardSettingsKeyPrefix: String? = nil)

    /// The name of the app.
    public let name: String

    /// Your license key, if any.
    public let licenseKey: String?

    /// The app's App Group identifier, if any.
    public let appGroupId: String?

    /// The locales to use in the app.
    public let locales: [Locale]

    /// App-specific deep links, if any.
    public let deepLinks: KeyboardKit.KeyboardApp.DeepLinks?

    /// The autocomplete configuration to use.
    public let autocomplete: KeyboardKit.KeyboardApp.AutocompleteConfiguration

    /// A custom keyboard settings key prefix, if any.
    public let keyboardSettingsKeyPrefix: String?
}

extension KeyboardApp {

    /// This type can define app-specific deep links.
    ///
    /// You only have to provide an `app` url, and can leave
    /// the rest blank to use standard URLs.
    public struct AutocompleteConfiguration {

        /// Create a custom autocomplete configuration.
        ///
        /// - Parameters:
        ///   - nextWordPredictionRequest: The next word prediction request to use, if any.
        public init(nextWordPredictionRequest: KeyboardKit.Autocomplete.RemotePredictionRequest? = nil)

        /// The next word prediction request to use, if any.
        public let nextWordPredictionRequest: KeyboardKit.Autocomplete.RemotePredictionRequest?
    }

    /// This type can define app-specific deep links.
    ///
    /// You only have to provide an `app` url, and can leave
    /// the rest blank, if you want to use standard patterns.
    ///
    /// > Important: You must register your `app` URL scheme
    /// within the app project, for these deep links to work.
    public struct DeepLinks : KeyboardKit.KeyboardModel {

        /// Create a custom keyboard deep links value.
        ///
        /// - Parameters:
        ///   - app: A deep link for opening the app, e.g. `myapp://`.
        ///   - dictation: A deep link for opening the app and starting dictation, by default `myapp://dictation`.
        ///   - keyboardSettings: A deep link for opening the app's keyboard settings screen, by default `myapp://keyboardSettings`.
        ///   - languageSettings: A deep link for opening the app's language settings screen, by default `myapp://languageSettings`.
        ///   - themeSettings: A deep link for opening the app's theme settings screen, by default `myapp://themeSettings`.
        public init(app: String, dictation: String? = nil, keyboardSettings: String? = nil, languageSettings: String? = nil, themeSettings: String? = nil)

        /// A deep link for opening the app.
        public let app: String

        /// A deep link for opening the app and starting dictation.
        public let dictation: String

        /// A deep link for opening the app's keyboard settings screen.
        public let keyboardSettings: String

        /// A deep link for opening the app's language settings screen.
        public let languageSettings: String

        /// A deep link for opening the app's theme settings screen.
        public let themeSettings: String

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardApp.DeepLinks, b: KeyboardKit.KeyboardApp.DeepLinks) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardApp {

    /// This type can be used to customize the visibility of
    /// any part of a ``KeyboardApp/HomeScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardAppHomeScreenVisibility(_:)``.
    public struct HomeScreenVisibility {

        /// Create a custom home screen visibility configuration.
        ///
        /// - Parameters:
        ///   - appIcon: Whether to show the app icon header.
        ///   - settingsSection: Whether to show the settings section, by default `true`.
        ///   - settingsSectionKeyboard: Whether to show a keyboard settings link, by default `true`.
        ///   - settingsSectionLanguages: Whether to show a language settings link, by default `true`.
        ///   - settingsSectionAutocomplete: Whether to show an autocomplete settings link, by default `true`.
        ///   - settingsSectionClipboard: Whether to show a clipboard settings link, by default `true`.
        ///   - settingsSectionDictation: Whether to show a dictation settings link, by default `true`.
        ///   - settingsSectionFeedback: Whether to show a feedback settings link, by default `true`.
        ///   - settingsSectionFonts: Whether to show a fonts settings link, by default `true`.
        ///   - settingsSectionThemes: Whether to show a theme settings link, by default `false`.
        ///   - keyboardStatusSection: Whether to show the keyboard status section, by default `true`.
        public init(appIcon: Bool = true, settingsSection: Bool = true, settingsSectionKeyboard: Bool = true, settingsSectionLanguages: Bool = true, settingsSectionAutocomplete: Bool = true, settingsSectionClipboard: Bool = true, settingsSectionDictation: Bool = true, settingsSectionFeedback: Bool = true, settingsSectionFonts: Bool = true, settingsSectionThemes: Bool = false, keyboardStatusSection: Bool = true)

        /// Whether to show the app icon header.
        public var appIcon: Bool

        /// Whether to show the keyboard settings section.
        public var settingsSection: Bool

        /// Whether to show a keyboard settings link.
        public var settingsSectionKeyboard: Bool

        /// Whether to show a language settings link.
        public var settingsSectionLanguages: Bool

        /// Whether to show an autocomplete settings link.
        public var settingsSectionAutocomplete: Bool

        /// Whether to show a clipboard settings link.
        public var settingsSectionClipboard: Bool

        /// Whether to show a dictation settings link.
        public var settingsSectionDictation: Bool

        /// Whether to show a feedback settings link.
        public var settingsSectionFeedback: Bool

        /// Whether to show a fonts settings link.
        public var settingsSectionFonts: Bool

        /// Whether to show a theme settings link.
        public var settingsSectionThemes: Bool

        /// Whether to show the keyboard status section.
        public var keyboardStatusSection: Bool
    }
}

extension KeyboardApp {

    /// This screen can be used as the main home screen of a
    /// keyboard app that should look like iOS Settings.
    ///
    /// The screen has an app icon header, a keyboard status
    /// section, and links to to other screens.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``keyboardAppHomeScreenLocalization(_:)``
    /// * ``keyboardAppHomeScreenStyle(_:)``
    /// * ``keyboardAppHomeScreenVisibility(_:)``
    ///
    /// This screen can navigate to these following screens:
    ///
    /// * ``Keyboard``.``Keyboard/SettingsScreen``
    /// * ``Keyboard``.``Keyboard/LocaleSettingsScreen``
    /// * ``Autocomplete``.``Autocomplete/SettingsScreen``
    /// * ``Clipboard``.``Clipboard/SettingsScreen``
    /// * ``Dictation``.``Dictation/SettingsScreen``
    /// * ``Feedback``.``Feedback/SettingsScreen``
    /// * ``Fonts``.``Fonts/SettingsScreen``
    /// * ``KeyboardTheme``.``KeyboardTheme/SettingsScreen``
    ///
    /// If you don't want to use this screen as the app home
    /// screen of your app, you can use these various screen
    /// views as stand-alone screens. You can also use their
    /// nested sections as standalone-views, and embed these
    /// sections into other screens.
    ///
    /// > Important: This screen will only show settings for
    /// features that are included in your license. Hide the
    /// settings section with the visibility value if you do
    /// not have any settings.
    @MainActor @preconcurrency public struct HomeScreen<Header, Footer> : View where Header : View, Footer : View {

        /// Create a keyboard app home screen.
        ///
        /// - Parameters:
        ///  - app: The app to use with the view.
        ///  - appIcon: The app icon to use as list header.
        ///  - header: The view to show topmost.
        ///  - footer: The view to show bottommost.
        @MainActor @preconcurrency public init(app: KeyboardKit.KeyboardApp, appIcon: Image, @ViewBuilder header: @escaping () -> Header, @ViewBuilder footer: @escaping () -> Footer)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardApp {

    /// This style can be used to customize the style of the
    /// ``KeyboardApp/HomeScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardAppHomeScreenStyle(_:)``.
    public struct HomeScreenStyle {

        public init(appIconSize: Double = 100, appIconCornerRadius: Double? = nil, appIconShadowColor: Color = .black.opacity(0.4), keyboardSettingsIcon: Image = .keyboard, keyboardSettingsLanguageSettingsIcon: Image = .keyboardGlobe, keyboardSettingsAutocompleteSettingsIcon: Image = .keyboardAutocomplete, keyboardSettingsClipboardSettingsIcon: Image = .keyboardClipboard, keyboardSettingsDictationSettingsIcon: Image = .keyboardDictation, keyboardSettingsFeedbackSettingsIcon: Image = .keyboardFeedback, keyboardSettingsFontSettingsIcon: Image = .keyboardFont, keyboardSettingsThemeSettingsIcon: Image = .keyboardTheme, keyboardStatusSection: KeyboardKit.KeyboardStatus.SectionStyle? = nil)

        public var appIconSize: Double

        public var appIconCornerRadius: Double

        public var appIconShadowColor: Color

        public var keyboardSettingsIcon: Image

        public var keyboardSettingsLanguageSettingsIcon: Image

        public var keyboardSettingsAutocompleteSettingsIcon: Image

        public var keyboardSettingsClipboardSettingsIcon: Image

        public var keyboardSettingsDictationSettingsIcon: Image

        public var keyboardSettingsFeedbackSettingsIcon: Image

        public var keyboardSettingsFontSettingsIcon: Image

        public var keyboardSettingsThemeSettingsIcon: Image

        public var keyboardStatusSection: KeyboardKit.KeyboardStatus.SectionStyle?
    }
}

extension KeyboardApp {

    /// This type can be used to configure the localizations
    /// of a ``KeyboardApp/HomeScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/keyboardAppHomeScreenLocalization(_:)``.
    public struct HomeScreenLocalization {

        /// Create a custom home screen localization.
        public init(settingsSection: String = "Settings", settingsSectionKeyboard: String = "Keyboard", settingsSectionLanguages: String = "Languages", settingsSectionAutocomplete: String = "Autocomplete", settingsSectionClipboard: String = "Clipboard", settingsSectionDictation: String = "Dictation", settingsSectionFeedback: String = "Feedback", settingsSectionFonts: String = "Fonts", settingsSectionTheme: String = "Theme", settingsSectionFooter: String? = nil, statusSection: KeyboardKit.KeyboardStatus.SectionLocalization = .standard)

        public var settingsSection: String

        public var settingsSectionKeyboard: String

        public var settingsSectionLanguages: String

        public var settingsSectionAutocomplete: String

        public var settingsSectionClipboard: String

        public var settingsSectionDictation: String

        public var settingsSectionFeedback: String

        public var settingsSectionFonts: String

        public var settingsSectionTheme: String

        public var settingsSectionFooter: String?

        public var statusSection: KeyboardKit.KeyboardStatus.SectionLocalization
    }
}

extension KeyboardApp.DeepLinks {

    /// A deep link for opening the app.
    public var appURL: URL? { get }

    /// A deep link for opening the app and starting dictation.
    public var dictationURL: URL? { get }

    /// A deep link for opening the app's keyboard settings screen.
    public var keyboardSettingsURL: URL? { get }

    /// A deep link for opening the app's language settings screen.
    public var languageSettingsURL: URL? { get }

    /// A deep link for opening the app's theme settings screen.
    public var themeSettingsURL: URL? { get }
}

extension KeyboardApp.HomeScreenVisibility {

    public static var standard: KeyboardKit.KeyboardApp.HomeScreenVisibility { get }
}

extension KeyboardApp.HomeScreen where Header == EmptyView, Footer == EmptyView {

    /// Create a keyboard app home screen.
    ///
    /// - Parameters:
    ///  - app: The app to use with the view.
    ///  - appIcon: The app icon to use as list header.
    @MainActor @preconcurrency public init(app: KeyboardKit.KeyboardApp, appIcon: Image)
}

extension KeyboardApp.HomeScreen : Sendable {
}

extension KeyboardApp.HomeScreenStyle {

    public static var standard: KeyboardKit.KeyboardApp.HomeScreenStyle { get }
}

extension KeyboardApp.HomeScreenLocalization {

    public static var standard: KeyboardKit.KeyboardApp.HomeScreenLocalization { get }
}

/// This view can be used as the root view of a keyboard app,
/// to set up KeyboardKit for a ``KeyboardApp``.
///
/// To use the view, just add it around your app's root view:
///
/// ```swift
/// @main
/// struct MyApp: App {
///
///     var body: some Scene {
///         WindowGroup {
///             KeyboardAppView(for: .myApp) {
///                 ContentView()
///             }
///         }
///     }
/// }
/// ```
///
/// This will set up your app, register your Pro license (if
/// needed), inject keyboard state into the environment, etc.
@MainActor @preconcurrency public struct KeyboardAppView<Content> : View where Content : View {

    /// Create a KeyboardKit app wrapper, that injects state
    /// into the view hierarchy, registers
    @MainActor @preconcurrency public init(for app: KeyboardKit.KeyboardApp, @ViewBuilder content: @escaping () -> Content)

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension KeyboardAppView : Sendable {
}

/// This protocol can be implemented by any classes that can
/// define keyboard-specific behaviors.
///
/// KeyboardKit will automatically setup a standard protocol
/// implementation in ``KeyboardInputViewController/services``
/// when the keyboard is launched. You can change or replace
/// it at any time to customize the keyboard behavior.
public protocol KeyboardBehavior {

    /// The range that backspace should delete.
    var backspaceRange: KeyboardKit.Keyboard.BackspaceRange { get }

    /// The text to use to end sentences with.
    var endSentenceText: String { get set }

    /// The preferred keyboard case after a certain action gesture.
    func preferredKeyboardCase(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardCase

    /// The preferred keyboard type after a certain action gesture.
    func preferredKeyboardType(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> KeyboardKit.Keyboard.KeyboardType

    /// Whether to end the current sentence after a certain action gesture.
    func shouldEndCurrentSentence(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

    /// Whether to register an emoji after a certain action gesture.
    func shouldRegisterEmoji(after gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool
}

extension KeyboardBehavior where Self == KeyboardKit.Keyboard.StandardKeyboardBehavior {

    /// Create a ``Keyboard/StandardKeyboardBehavior`` instance.
    ///
    /// - Parameters:
    ///   - keyboardContext: The keyboard context to use.
    ///   - doubleTapThreshold: The douple tap threshold to use, by default `0.5` seconds.
    ///   - endSentenceText: The text to use to end sentences with, by default `. `.
    ///   - endSentenceThreshold: The end sentence auto-close threshold to use, by default `3.0` seconds.
    ///   - repeatGestureTimer: The repease gesture timer to use, by default a new one.
    public static func standard(keyboardContext: KeyboardKit.KeyboardContext, doubleTapThreshold: TimeInterval? = nil, endSentenceText: String? = nil, endSentenceThreshold: TimeInterval? = nil, repeatGestureTimer: KeyboardKit.GestureButtonTimer? = nil) -> Self
}

/// This class has observable states and persistent settings
/// for the core keyboard functionality.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings.
///
/// You can use ``locale`` to get and set the current locale.
/// This affects the primary language and will also localize
/// the ``KeyboardView`` if the locale is unlocked.
///
/// You can use ``locales`` to set the locales to enable and
/// ``KeyboardSettings/addedLocales`` to select which of the
/// locales to use. The ``enabledLocales`` property will use
/// the added locales, if any, else the full list of locales.
/// Use ``selectNextLocale()`` to loop through locales.
///
/// You can use ``keyboardType`` to set the current keyboard
/// type, and ``keyboardCase`` to set the current input case.
///
/// You can use ``keyboardLayoutType`` to apply a layout for
/// special needs, like ``Keyboard/LayoutType/dvorak``. This
/// should be handled with ``Keyboard/AddedLocale`` to avoid
/// applying layout types that can't be used with the locale.
final public class KeyboardContext : ObservableObject {

    public init(hasFullAccess: Bool? = nil)

    /// Auto-persisted keyboard settings.
    @Published final public var settings: KeyboardKit.KeyboardSettings

    final public var $settings: Published<KeyboardKit.KeyboardSettings>.Publisher

    /// Set this to override ``autocapitalizationType``.
    @Published final public var autocapitalizationTypeOverride: KeyboardKit.Keyboard.AutocapitalizationType?

    final public var $autocapitalizationTypeOverride: Published<KeyboardKit.Keyboard.AutocapitalizationType?>.Publisher

    /// Set this to override ``returnKeyType``.
    @Published final public var returnKeyTypeOverride: KeyboardKit.Keyboard.ReturnKeyType?

    final public var $returnKeyTypeOverride: Published<KeyboardKit.Keyboard.ReturnKeyType?>.Publisher

    /// The app for which the context is set up, if any.
    @Published final public var app: KeyboardKit.KeyboardApp?

    final public var $app: Published<KeyboardKit.KeyboardApp?>.Publisher

    /// The color scheme to use.
    @Published final public var colorScheme: ColorScheme

    final public var $colorScheme: Published<ColorScheme>.Publisher

    /// The device type that is currently being used.
    @Published final public var deviceType: KeyboardKit.DeviceType

    final public var $deviceType: Published<KeyboardKit.DeviceType>.Publisher

    /// The device type to use for keyboard rendering.
    @Published final public var deviceTypeForKeyboard: KeyboardKit.DeviceType

    final public var $deviceTypeForKeyboard: Published<KeyboardKit.DeviceType>.Publisher

    @available(*, deprecated, message: "This is never true in iOS. It will therefore be removed in 10.1 to avoid confusion.")
    @Published final public var hasDictationKey: Bool

    final public var $hasDictationKey: Published<Bool>.Publisher

    @Published final public var hasFullAccess: Bool

    final public var $hasFullAccess: Published<Bool>.Publisher

    /// The current interface orientation.
    @Published final public var interfaceOrientation: KeyboardKit.InterfaceOrientation

    final public var $interfaceOrientation: Published<KeyboardKit.InterfaceOrientation>.Publisher

    /// Whether the keyboard is collapsed.
    @Published final public var isKeyboardCollapsed: Bool

    final public var $isKeyboardCollapsed: Published<Bool>.Publisher

    /// Whether the keyboard is in floating mode.
    @Published final public var isKeyboardFloating: Bool

    final public var $isKeyboardFloating: Published<Bool>.Publisher

    /// Whether liquid glass is available.
    final public var isLiquidGlassAvailable: Bool { get }

    /// Whether liquid glass is available.
    public static var isLiquidGlassAvailable: Bool { get }

    /// Whether liquid glass is enabled.
    ///
    /// You can set this to `true` to enable liquid glass on
    /// all devices and operating systems.
    @Published final public var isLiquidGlassEnabled: Bool

    final public var $isLiquidGlassEnabled: Published<Bool>.Publisher

    /// Whether a spacebar drag gesture is active.
    @Published final public var isSpacebarDragGestureActive: Bool

    final public var $isSpacebarDragGestureActive: Published<Bool>.Publisher

    /// The keyboard case that is currently used.
    @Published final public var keyboardCase: KeyboardKit.Keyboard.KeyboardCase

    final public var $keyboardCase: Published<KeyboardKit.Keyboard.KeyboardCase>.Publisher

    @available(*, deprecated, message: "This is no longer used. It will therefore be removed in 10.1 to avoid confusion.")
    @Published final public var keyboardDictationReplacement: KeyboardKit.KeyboardAction?

    final public var $keyboardDictationReplacement: Published<KeyboardKit.KeyboardAction?>.Publisher

    /// The current keyboard layout type, if any.
    @Published final public var keyboardLayoutType: KeyboardKit.Keyboard.LayoutType?

    final public var $keyboardLayoutType: Published<KeyboardKit.Keyboard.LayoutType?>.Publisher

    /// The keyboard type that is currently used.
    @Published final public var keyboardType: KeyboardKit.Keyboard.KeyboardType

    final public var $keyboardType: Published<KeyboardKit.Keyboard.KeyboardType>.Publisher

    /// The keyboard input type that is currently used.
    @Published final public var keyboardInputType: KeyboardKit.Keyboard.InputType

    final public var $keyboardInputType: Published<KeyboardKit.Keyboard.InputType>.Publisher

    /// The current locale, by default `.current`.
    @Published final public var locale: Locale

    final public var $locale: Published<Locale>.Publisher

    /// The locales that are enabled for the keyboard.
    @Published final public var locales: [Locale]

    final public var $locales: Published<[Locale]>.Publisher

    /// The locale to use to localize other locale names. If
    /// this is `nil`, each locale will use its own language.
    @Published final public var localePresentationLocale: Locale?

    final public var $localePresentationLocale: Published<Locale?>.Publisher

    /// Whether to add an input mode switch key.
    @Published final public var needsInputModeSwitchKey: Bool

    final public var $needsInputModeSwitchKey: Published<Bool>.Publisher

    /// The primary language that is currently being used.
    @Published final public var primaryLanguage: String?

    final public var $primaryLanguage: Published<String?>.Publisher

    /// The current screen size (avoid using this).
    @Published final public var screenSize: CGSize

    final public var $screenSize: Published<CGSize>.Publisher

    /// The original text document proxy.
    @Published final public var originalTextDocumentProxy: any UITextDocumentProxy

    final public var $originalTextDocumentProxy: Published<any UITextDocumentProxy>.Publisher

    /// The text document proxy that is currently active.
    final public var textDocumentProxy: any UITextDocumentProxy { get }

    /// A custom text proxy to which text can be routed.
    @Published final public var textInputProxy: (any UITextDocumentProxy)?

    final public var $textInputProxy: Published<(any UITextDocumentProxy)?>.Publisher

    /// The text input mode of the input controller.
    @Published final public var textInputMode: UITextInputMode?

    final public var $textInputMode: Published<UITextInputMode?>.Publisher

    /// The current trait collection.
    ///
    /// Setting this will automatically update ``colorScheme``.
    @Published final public var traitCollection: UITraitCollection

    final public var $traitCollection: Published<UITraitCollection>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension KeyboardContext {

    /// This enum defines various enabled locale datasources.
    public enum EnabledLocalesDataSource : KeyboardKit.KeyboardModel {

        case added

        case context

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardContext.EnabledLocalesDataSource, b: KeyboardKit.KeyboardContext.EnabledLocalesDataSource) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// The ``KeyboardSettings/addedLocales``, or the static
    /// ``locales`` if there are no added ones.
    final public var enabledLocales: [Locale] { get }

    /// The ``enabledLocales`` data source.
    final public var enabledLocalesDataSource: KeyboardKit.KeyboardContext.EnabledLocalesDataSource { get }

    /// Whether the context has multiple ``enabledLocales``.
    final public var hasMultipleEnabledLocales: Bool { get }

    /// Select a locale with an optional layout type.
    final public func selectLocale(_ locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType? = nil)

    /// Select a locale from ``enabledLocales``.
    final public func selectLocale(at index: Int)

    /// Select a locale from ``KeyboardSettings/addedLocales``.
    final public func selectLocaleFromAddedLocales(at index: Int)

    /// Select a locale from ``locales``.
    final public func selectLocaleFromContextLocales(at index: Int)

    /// Select the next locale from ``enabledLocales``.
    final public func selectNextLocale()

    /// Select the next locale from ``KeyboardSettings/addedLocales``.
    final public func selectNextLocaleFromAddedLocales()

    /// Select the next locale from ``enabledLocales``.
    final public func selectNextLocaleFromContextLocales()
}

extension KeyboardContext {

    /// The bundle ID of the keyboard host application.
    ///
    /// The property is persisted, and can be used to see in
    /// which app the keyboard was last used. You should use
    /// ``hostApplicationBundleIdSyncDate`` to check when it
    /// was last written, to see if it's still relevant.
    ///
    /// > Note: Future versions may reset this property when
    /// the app is launched without the keyboard.
    final public var hostApplicationBundleId: String?

    /// A date when ``hostApplicationBundleId`` was last set.
    final public var hostApplicationBundleIdSyncDate: Date? { get }
}

extension KeyboardContext {

    /// The autocapitalization type to use.
    ///
    /// This value comes from the ``textDocumentProxy``, but
    /// ``autocapitalizationTypeOverride`` can override it.
    final public var autocapitalizationType: KeyboardKit.Keyboard.AutocapitalizationType? { get }

    /// Whether to use a dark color scheme.
    final public var hasDarkColorScheme: Bool { get }

    /// Whether the context has multiple locales.
    final public var hasMultipleLocales: Bool { get }

    /// The current keyboard appearance.
    final public var keyboardAppearance: UIKeyboardAppearance { get }

    /// Whether the context prefers autocomplete.
    final public var prefersAutocomplete: Bool { get }

    /// The return key type type to use.
    ///
    /// You can use ``returnKeyTypeOverride`` to override it.
    final public var returnKeyType: KeyboardKit.Keyboard.ReturnKeyType { get }
}

extension KeyboardContext {

    /// Sync the context with the provided input controller.
    final public func sync(with controller: KeyboardKit.KeyboardInputViewController)

    /// Sync the ``keyboardType`` with the provided proxy.
    final public func syncKeyboardType(with proxy: any UITextDocumentProxy)

    /// Sync the ``originalTextDocumentProxy``.
    final public func syncTextDocumentProxy(with controller: KeyboardKit.KeyboardInputViewController)
}

extension KeyboardContext {

    /// The currently preferred keyboard case.
    final public var preferredKeyboardCase: KeyboardKit.Keyboard.KeyboardCase { get }
}

extension KeyboardContext : KeyboardKit.KeyboardHostApplicationProvider {
}

extension KeyboardContext {

    public static var preview: KeyboardKit.KeyboardContext { get }

    public static func preview(hasFullAccess: Bool = false) -> KeyboardKit.KeyboardContext
}

/// This protocol can be implemented by any classes that can
/// be used to control a keyboard extension.
///
/// ``KeyboardInputViewController`` implements this protocol
/// by using its text document proxy, state, and services.
public protocol KeyboardController : AnyObject {

    /// Keyboard-specific services.
    var services: KeyboardKit.Keyboard.Services { get }

    /// Keyboard-specific state.
    var state: KeyboardKit.Keyboard.State { get }

    /// Adjust the text input cursor position.
    func adjustTextPosition(by characterOffset: Int)

    /// Delete backwards.
    func deleteBackward()

    /// Delete backwards a certain number of times.
    func deleteBackward(times: Int)

    /// Dismiss the keyboard.
    func dismissKeyboard()

    /// End the current sentence with the provided text.
    func endSentence(withText text: String)

    /// Insert the provided diacritic.
    func insertDiacritic(_ diacritic: KeyboardKit.Keyboard.Diacritic)

    /// Insert the provided text.
    func insertText(_ text: String)

    /// Perform an autocomplete operation.
    func performAutocomplete()

    /// Perform a keyboard-initiated dictation operation.
    func performDictation()

    /// Select the next locale, if any.
    func selectNextLocale()

    /// Set a certain keyboard case.
    func setKeyboardCase(_ case: KeyboardKit.Keyboard.KeyboardCase)

    /// Set a certain keyboard input type.
    func setKeyboardInputType(_ type: KeyboardKit.Keyboard.InputType)

    /// Set a certain keyboard type.
    func setKeyboardType(_ type: KeyboardKit.Keyboard.KeyboardType)

    /// Open a certain URL
    func openUrl(_ url: URL?)
}

/// This type represents a keyboard-specific font, which can
/// be used by `Codable` types.
///
/// You can use the ``font`` property to get the native font.
public struct KeyboardFont : KeyboardKit.KeyboardModel {

    public init(_ type: KeyboardKit.KeyboardFont.FontType, _ weight: KeyboardKit.KeyboardFont.FontWeight? = nil)

    public var type: KeyboardKit.KeyboardFont.FontType

    public var weight: KeyboardKit.KeyboardFont.FontWeight?

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.KeyboardFont, b: KeyboardKit.KeyboardFont) -> Bool

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension KeyboardFont {

    /// This enum defines various keyboard font types.
    public enum FontType : KeyboardKit.KeyboardModel {

        case body

        case callout

        case caption

        case caption2

        case custom(String, size: CGFloat)

        case customFixed(String, size: CGFloat)

        case footnote

        case headline

        case largeTitle

        case subheadline

        case system(size: CGFloat)

        case title

        case title2

        case title3

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardFont.FontType, b: KeyboardKit.KeyboardFont.FontType) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardFont {

    /// This enum defines various keyboard font weights.
    public enum FontWeight : KeyboardKit.KeyboardModel {

        case black

        case bold

        case heavy

        case light

        case medium

        case regular

        case semibold

        case thin

        case ultraLight

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardFont.FontWeight, b: KeyboardKit.KeyboardFont.FontWeight) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardFont {

    public static var body: KeyboardKit.KeyboardFont { get }

    public static var callout: KeyboardKit.KeyboardFont { get }

    public static var caption: KeyboardKit.KeyboardFont { get }

    public static var caption2: KeyboardKit.KeyboardFont { get }

    public static var footnote: KeyboardKit.KeyboardFont { get }

    public static var headline: KeyboardKit.KeyboardFont { get }

    public static var largeTitle: KeyboardKit.KeyboardFont { get }

    public static var subheadline: KeyboardKit.KeyboardFont { get }

    public static var title: KeyboardKit.KeyboardFont { get }

    public static var title2: KeyboardKit.KeyboardFont { get }

    public static var title3: KeyboardKit.KeyboardFont { get }

    public static func body(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func callout(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func caption(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func caption2(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func footnote(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func headline(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func largeTitle(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func subheadline(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func title(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func title2(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func title3(weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func custom(_ name: String, fixedSize: CGFloat) -> KeyboardKit.KeyboardFont

    public static func custom(_ name: String, fixedSize: CGFloat, weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func custom(_ name: String, size: CGFloat) -> KeyboardKit.KeyboardFont

    public static func custom(_ name: String, size: CGFloat, weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public static func system(size: CGFloat) -> KeyboardKit.KeyboardFont

    public static func system(size: CGFloat, weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    public func weight(_ weight: KeyboardKit.KeyboardFont.FontWeight) -> KeyboardKit.KeyboardFont

    /// Get the native font for the font style.
    public var font: Font { get }
}

extension KeyboardFont.FontType {

    /// Get the native font.
    public var font: Font { get }
}

extension KeyboardFont.FontWeight {

    /// Get the native font weight.
    public var fontWeight: Font.Weight { get }
}

/// This enum defines a list of known host applications that
/// can be used to identify various application.
///
/// You can implement ``KeyboardHostApplicationProvider`` to
/// make any type able to fetch the current host application.
///
/// The ``KeyboardHostApplication/allCases`` property can be
/// used to get a list of all currently known host apps.
///
/// You can open any host app with a ``KeyboardActionHandler``
/// or ``SwiftUICore/OpenURLAction``, using `open(with:)` or
/// the action handler and URL action `tryOpen(_:)` function.
/// You can use ``canBeOpened`` in advance, to see if an app
/// can be opened.
///
/// The host application concept is a best effort attempt to
/// recognize and integrate with other apps, since many APIs
/// have been deprecated by Apple. Reach out if an app stops
/// working, or if you want to add any app to the known list.
///
/// > Important: This requires that your license unlocks the
///  ``ProFeature/hostApplication`` feature.
public struct KeyboardHostApplication : Identifiable, CaseIterable, Equatable, Sendable {

    /// Create a custom host application.
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/hostApplication`` feature.
    public init(_ name: String, _ bundleId: String, _ urlScheme: String?) throws

    /// Try to find an app with a certain bundle ID.
    ///
    /// This initializer lets you match the bundle ID with a
    /// list of additional apps that will be appended to the
    /// standard ``allCases`` list.
    ///
    /// - Parameters:
    ///   - bundleId: The bundle ID of the host application.
    ///   - additionalApps: An additiona
    public init?(bundleId: String?, in additionalApps: [KeyboardKit.KeyboardHostApplication] = []) throws

    /// The application name.
    public let name: String

    /// The application's unique bundleId.
    public let bundleId: String

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.KeyboardHostApplication, b: KeyboardKit.KeyboardHostApplication) -> Bool

    /// A type that can represent a collection of all values of this type.
    public typealias AllCases = [KeyboardKit.KeyboardHostApplication]

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = String
}

extension KeyboardHostApplication {

    /// The application's unique ID.
    public var id: String { get }

    /// Whether the application can be opened.
    public var canBeOpened: Bool { get }

    /// Try to open the app with an action handler.
    public func open(with actionHandler: any KeyboardKit.KeyboardActionHandler)

    /// Try to open the app with an action handler.
    public func openWithActionHandler(_ actionHandler: any KeyboardKit.KeyboardActionHandler)
}

@MainActor extension KeyboardHostApplication {

    /// Try to open the app with a URL action.
    @MainActor public func open(with action: OpenURLAction)

    /// Try to open the app with a URL action.
    @MainActor public func openWithAction(_ action: OpenURLAction)
}

extension KeyboardHostApplication {

    /// A collection of all values of this type.
    public static let allCases: [KeyboardKit.KeyboardHostApplication]
}

/// This protocol can be implemented by any class that has a
/// host application ID that can be mapped to an application.
///
/// This can be handled differently for different types. For
/// instance, ``KeyboardInputViewController`` will check the
/// app that is currently using the keyboard extension while
/// others like ``DictationContext`` will instead resolve an
/// application based on the bundle ID that was used last.
///
/// The ``hostApplication`` property will try to resolve the
/// matching ``KeyboardHostApplication``, if any.
///
/// > Important: This requires that your license unlocks the
///  ``ProFeature/hostApplication`` feature.
public protocol KeyboardHostApplicationProvider {

    /// The host application bundle ID to resolve.
    var hostApplicationBundleId: String? { get }
}

extension KeyboardHostApplicationProvider {

    /// Whether this provider can resolve a host application
    /// from the ``hostApplicationBundleId``.
    public var hostApplicationBundleIdIsKnown: Bool { get }

    /// Get an app within ``KeyboardHostApplication/allCases``
    /// that matches the ``hostApplicationBundleId``, if any.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/hostApplication`` feature.
    public var hostApplication: KeyboardKit.KeyboardHostApplication? { get throws }

    /// Get an app within ``KeyboardHostApplication/allCases``
    /// that matches the ``hostApplicationBundleId``, if any.
    ///
    /// This function also lets you match the bundle ID with
    /// an additional list of apps. This lets you extend the
    /// host application matching logic with more apps.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/hostApplication`` feature.
    public func hostApplication(withAdditionalApps apps: [KeyboardKit.KeyboardHostApplication] = []) throws -> KeyboardKit.KeyboardHostApplication?
}

/// This is a namespace for input related types.
public struct KeyboardInput {
}

/// This is the input controller to use in KeyboardKit-based
/// keyboard extensions.
///
/// When using KeyboardKit, let `KeyboardController` inherit
/// this class instead of `UIInputViewController`. This will
/// extend thw controller with extra capabilities as well as
/// KeyboardKit-specific functionality.
///
/// You can override any functions, modify any ``state`` and
/// replace any ``services`` to tweak your keyboard behavior.
///
/// > Warning: A very important thing you MUST consider when
/// using ``setupKeyboardView(_:)`` is that the view builder
/// provides you with an unowned controller reference, which
/// you should use this instead of `self` since referring to
/// `self` can cause memory leaks. If you must use `self` to
/// reference your specific controller type, you MUST add an
/// `[unowned self]` or `[weak self]` to the builder.
///
/// See the <doc:Getting-Started-Article> article as well as
/// <doc:Essentials-Article> for more information on how you
/// should set up your keyboard extension.
@MainActor @objc @preconcurrency open class KeyboardInputViewController : UIInputViewController, KeyboardKit.KeyboardController, KeyboardKit.UrlOpener {

    @MainActor @preconcurrency override dynamic open func viewDidLoad()

    @MainActor @preconcurrency override dynamic open func viewWillAppear(_ animated: Bool)

    @MainActor @preconcurrency override dynamic open func viewDidAppear(_ animated: Bool)

    @MainActor @preconcurrency override dynamic open func viewDidLayoutSubviews()

    @MainActor @preconcurrency override dynamic open func traitCollectionDidChange(_ previousTraitCollection: UITraitCollection?)

    /// Used to keep track if the controller has been setup
    @MainActor @preconcurrency public var hasPerformedInitialSetup: Bool

    /// This function is called to handle a dictation result
    /// when returning from the main app.
    @MainActor @preconcurrency open func viewWillHandleDictationResult()

    /// This function is called when the controller is about
    /// to sync the initial keyboard type at launch.
    @MainActor @preconcurrency open func viewWillSetupInitialKeyboardType()

    /// This function is called when the controller is about
    /// to create or update the keyboard view.
    ///
    /// The function will by default create a ``KeyboardView``
    /// but you can override it to either setup a customized
    /// ``KeyboardView`` or a completely custom view.
    ///
    /// > Note: Do not call `super.viewWillSetupKeyboardView`
    /// if you override this function, since that would make
    /// an unnecessary keyboard view calculation.
    @MainActor @preconcurrency open func viewWillSetupKeyboardView()

    /// This function is called when the controller is about
    /// to sync with the various ``Keyboard/State`` contexts.
    @MainActor @preconcurrency open func viewWillSyncWithContext()

    /// Set up KeyboardKit with a custom view and an unowned
    /// ``KeyboardInputViewController`` reference.
    ///
    /// Call this in ``viewWillSetupKeyboardView()`` to make
    /// the controller use the view as the keyboard view.
    @MainActor @preconcurrency open func setupKeyboardView<Content>(_ view: @escaping (_ controller: KeyboardKit.KeyboardInputViewController) -> Content) where Content : View

    /// The original text document proxy.
    @MainActor @preconcurrency open var originalTextDocumentProxy: any UITextDocumentProxy { get }

    /// The text document proxy that is currently active.
    @MainActor @preconcurrency override dynamic open var textDocumentProxy: any UITextDocumentProxy { get }

    /// Keyboard-specific services.
    @MainActor @preconcurrency public var services: KeyboardKit.Keyboard.Services

    /// Keyboard-specific state.
    @MainActor @preconcurrency public var state: KeyboardKit.Keyboard.State

    /// Try handle a keyboard type change.
    @MainActor @preconcurrency open func tryHandleKeyboardTypeChange()

    @MainActor @preconcurrency override dynamic open func selectionWillChange(_ textInput: (any UITextInput)?)

    @MainActor @preconcurrency override dynamic open func selectionDidChange(_ textInput: (any UITextInput)?)

    @MainActor @preconcurrency override dynamic open func textWillChange(_ textInput: (any UITextInput)?)

    @MainActor @preconcurrency override dynamic open func textDidChange(_ textInput: (any UITextInput)?)

    /// This function will be called with an async delay, to
    /// give the text document proxy time to update.
    @MainActor @preconcurrency open func textDidChangeAsync(_ textInput: (any UITextInput)?)

    /// Adjust the text input cursor position.
    @MainActor @preconcurrency open func adjustTextPosition(by offset: Int)

    /// Delete backwards.
    @MainActor @preconcurrency open func deleteBackward()

    /// Delete backwards a certain number of times.
    @MainActor @preconcurrency open func deleteBackward(times: Int)

    /// End the current sentence with the provided text.
    @MainActor @preconcurrency open func endSentence(withText text: String)

    /// Insert the provided diacritic.
    @MainActor @preconcurrency open func insertDiacritic(_ diacritic: KeyboardKit.Keyboard.Diacritic)

    /// Insert the provided text.
    @MainActor @preconcurrency open func insertText(_ text: String)

    /// Select the next locale, if any.
    @MainActor @preconcurrency open func selectNextLocale()

    /// Set a certain keyboard case.
    @MainActor @preconcurrency open func setKeyboardCase(_ case: KeyboardKit.Keyboard.KeyboardCase)

    /// Set a certain keyboard input type.
    @MainActor @preconcurrency open func setKeyboardInputType(_ type: KeyboardKit.Keyboard.InputType)

    /// Set a certain keyboard type.
    @MainActor @preconcurrency open func setKeyboardType(_ type: KeyboardKit.Keyboard.KeyboardType)

    /// Try to open a URL from the keyboard extension.
    ///
    /// > Warning: This has stopped working in iOS 18. Until
    /// we make `openURL:options:completionHandler:` work, a
    /// workaround is using regular SwiftUI Links.
    @MainActor @preconcurrency open func openUrl(_ url: URL?)

    /// Whether or not context syncing is enabled.
    ///
    /// By default, syncung is enabled while a text document
    /// proxy isn't reading full the document context.
    @MainActor @preconcurrency open var isContextSyncEnabled: Bool { get }

    /// Perform a keyboard context sync.
    ///
    /// This is performed to keep the ``state`` in sync, and
    /// will abort if ``isContextSyncEnabled`` is `false`.
    @MainActor @preconcurrency open func performKeyboardContextSync()

    /// The text to use when performing autocomplete.
    ///
    /// All available text before the text input cursor will
    /// be used by default. You can override the function to
    /// change which text to use.
    @MainActor @preconcurrency open var autocompleteText: String? { get }

    /// Whether or not autocomple is enabled.
    ///
    /// The property aggregates several data sources such as
    /// checking the ``AutocompleteContext`` settings and if
    /// any ongoing operations should temporarily disable it.
    @MainActor @preconcurrency open var isAutocompleteEnabled: Bool { get }

    /// Perform an autocomplete operation.
    @MainActor @preconcurrency open func performAutocomplete()

    /// Reset the current autocomplete state.
    @MainActor @preconcurrency open func resetAutocomplete()

    /// Perform a keyboard-initiated dictation operation.
    @MainActor @preconcurrency public func performDictation()

    @MainActor @preconcurrency override dynamic public init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?)

    @MainActor @preconcurrency required dynamic public init?(coder: NSCoder)

    @objc deinit
}

extension KeyboardInputViewController {

    /// Set up KeyboardKit for the provided app.
    ///
    /// Call this in ``viewDidLoad()`` to make sure that the
    /// keyboard is properly configured as early as possible.
    ///
    /// If the ``KeyboardApp`` has a KeyboardKit Pro license
    /// key or a `KeyboardKit.license` exists in this bundle,
    /// this function will try to unlock KeyboardKit Pro and
    /// return the validated license or any license error in
    /// the `completion` block. You can then use the license
    /// to customize the app or keyboard or handle the error.
    ///
    /// See <doc:Getting-Started-Article> for information on
    /// how to properly set up an app and keyboard extension.
    ///
    /// - Parameters:
    ///   - app: The keyboard app to setup the controller for.
    ///   - completion: The function to call when the operation completes.
    @MainActor @preconcurrency public func setup(for app: KeyboardKit.KeyboardApp, completion: @escaping (Result<KeyboardKit.License?, any Error>) -> Void = { _ in })
}

extension KeyboardInputViewController : KeyboardKit.KeyboardHostApplicationProvider {
}

extension KeyboardInputViewController {

    /// The bundle ID of the host application, if any, or if
    /// within a host app, the bundle id of the main bundle.
    ///
    /// The property uses technologies that may stop working
    /// in any future iOS version. Do not rely solely on the
    /// function, and design the app to be able to work even
    /// if this feature suddenly stops working.
    @MainActor @preconcurrency public var hostApplicationBundleId: String? { get }
}

extension KeyboardInputViewController {

    @MainActor @preconcurrency public static var preview: KeyboardKit.KeyboardInputViewController { get }
}

/// This type defines the full set of keys on a keyboard and
/// is also a namespace for layout-related types.
///
/// A layout also specifies sizes, alignments, etc. which is
/// required information when rendering a keyboard.
///
/// You can use the ``itemRows`` property to modify a layout,
/// e.g. with the various insert, replace & remove functions.
public struct KeyboardLayout {

    /// Create a new layout with the provided items.
    ///
    /// - Parameters:
    ///   - rows: The items to add to the layout.
    ///   - deviceConfiguration: The device configuration to use.
    ///   - height: The ideal item height, by default picked from the first item
    ///   - insets: The ideal item inset value, by default picked from the first item.
    ///   - inputToolbarInputSet: The input set to use for a topmost input toolbar, if any.
    public init(itemRows rows: KeyboardKit.KeyboardLayout.ItemRows, deviceConfiguration: KeyboardKit.KeyboardLayout.DeviceConfiguration = .standard(for: .preview), idealItemHeight height: Double? = nil, idealItemInsets insets: EdgeInsets? = nil, inputToolbarInputSet: KeyboardKit.KeyboardLayout.InputSet? = nil)

    /// The device configuration for the layout.
    public var deviceConfiguration: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// The layout item rows to show in the keyboard.
    public var itemRows: KeyboardKit.KeyboardLayout.ItemRows

    /// The ideal item height.
    public var idealItemHeight: Double

    /// The ideal item inserts.
    public var idealItemInsets: EdgeInsets

    /// The input set to use for a top input toolbar.
    public var inputToolbarInputSet: KeyboardKit.KeyboardLayout.InputSet?

    /// A `CGFloat` typealias for the total keyboard width.
    public typealias TotalWidth = CGFloat
}

extension KeyboardLayout {

    /// The total height of the layout.
    public var totalHeight: Double { get }

    /// Calculate the input key width for a total width.
    ///
    /// This function will find the smallest, required input
    /// width in all rows, which can then be applied to each
    /// input key in the entire layout.
    public func inputWidth(for totalWidth: KeyboardKit.KeyboardLayout.TotalWidth) -> Double
}

extension KeyboardLayout {

    /// A keyboard layout items defines an action, size, and
    /// optional insets for a key on a layout-based keyboard.
    public struct Item : Equatable, Sendable {

        /// Create a new layout item.
        ///
        /// - Parameters:
        ///   - action: The keyboard action to use.
        ///   - secondaryAction: A secondary action, if any.
        ///   - size: The layout size to use.
        ///   - alignment: The content alignment, by default `.center`.
        ///   - edgeInsets: The edge insets to apply, by default none.
        public init(action: KeyboardKit.KeyboardAction, secondaryAction: KeyboardKit.KeyboardAction? = nil, size: KeyboardKit.KeyboardLayout.ItemSize, alignment: Alignment = .center, edgeInsets: EdgeInsets = .init())

        /// The keyboard action to use.
        public var action: KeyboardKit.KeyboardAction

        /// The keyboard action to use.
        public var secondaryAction: KeyboardKit.KeyboardAction?

        /// The layout size to use.
        public var size: KeyboardKit.KeyboardLayout.ItemSize

        /// The content alignment to apply.
        public var alignment: Alignment

        /// The edge insets to apply.
        public var edgeInsets: EdgeInsets

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.Item, b: KeyboardKit.KeyboardLayout.Item) -> Bool
    }

    /// This typealias represents a list of layout items.
    public typealias ItemRow = [KeyboardKit.KeyboardLayout.Item]

    /// This typealias represents a list of layout item rows.
    public typealias ItemRows = [KeyboardKit.KeyboardLayout.ItemRow]
}

extension KeyboardLayout {

    /// The standard keyboard layout to use for the provided
    /// keyboard context and input set.
    public static func standard(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    public static func albanian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func arabic(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func armenian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func azerbaijani(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func belarusian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func bengali(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func bulgarian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func catalan(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func cherokee(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func chuvash(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func croatian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func czech(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func danish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func dutch(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func dutch_belgium(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func english(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func english_us(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func english_gb(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func english_canada(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func english_australia(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func estonian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func faroese(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func filipino(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func finnish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func french(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func french_belgium(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func french_canada(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func french_switzerland(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func georgian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func german(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func german_austria(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func german_switzerland(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func greek(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func hawaiian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func hebrew(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func hungarian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func icelandic(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func inari_sami(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func indonesian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func irish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func italian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func kazakh(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func kurdish_sorani(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func kurdish_sorani_arabic(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func kurdish_sorani_pc(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func latvian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func lithuanian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func macedonian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func malay(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func maltese(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func mongolian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func northern_sami(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func norwegian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func norwegian_nynorsk(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func persian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func polish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func portuguese(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func portuguese_brazil(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func romanian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func russian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func serbian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func serbian_latin(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func shughni_tajik(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func slovak(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func slovenian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func spanish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func spanish_latinAmerica(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func spanish_mexico(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func swahili(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func swedish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func turkish(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func ukrainian(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func uzbek(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func vietnamese(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout

    public static func welsh(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    /// A keyboard layout input set defines which input keys
    /// to add to a keyboard layout.
    ///
    /// While a keyboard layout can be identical for several
    /// locales and keyboard types, the input set can change.
    ///
    /// KeyboardKit defines basic input sets like ``qwerty``,
    /// ``numeric`` and ``symbolic``, while KeyboardKit Pro
    /// unlocks extra input sets like ``qwertz``, ``azerty``,
    /// ``colemak``, ``dvorak`` and locale-specific sets for
    /// all supported locales.
    public struct InputSet : KeyboardKit.KeyboardModel {

        /// Create an input set with rows.
        public init(rows: KeyboardKit.KeyboardLayout.InputSet.ItemRows)

        /// The rows in the input set.
        public var rows: KeyboardKit.KeyboardLayout.InputSet.ItemRows

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.InputSet, b: KeyboardKit.KeyboardLayout.InputSet) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardLayout {

    /// Create an input toolbar-specific layout.
    ///
    /// - Parameters:
    ///   - chars: The characters to display.
    ///   - item: An item to base the toolbar items on.
    public init(inputToolbarCharacters chars: String, templateItem item: KeyboardKit.KeyboardLayout.Item)

    /// Adjust this layout for an input toolbar display mode.
    public func adjusted(for displayMode: KeyboardKit.Keyboard.InputToolbarDisplayMode, layoutConfiguration: KeyboardKit.KeyboardLayout.DeviceConfiguration) -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    /// Whether the layout has a key for a certain action.
    public func hasKey(for action: KeyboardKit.KeyboardAction) -> Bool

    /// Whether the layout has a certain switcher.
    public func hasKeyboardSwitcher(_ type: KeyboardKit.Keyboard.KeyboardType) -> Bool
}

extension KeyboardLayout {

    /// Convert an input keyboard layout to an iPad layout.
    ///
    /// This adds device-specific actions to the input items
    /// in the provided layout. Based on the original inputs,
    /// you may have to adjust the final layout.
    ///
    /// - Parameters:
    ///   - layout: The keyboard layout to augment.
    ///   - keyboardContext: The keyboard context to use.   
    public static func iPadLayout(from layout: KeyboardKit.KeyboardLayout, keyboardContext: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    /// This type defines layout configs for various devices.
    public struct DeviceConfiguration : Equatable, Sendable {

        /// Create a new layout configuration.
        ///
        /// - Parameters:
        ///   - buttonCornerRadius: The button corner radius.
        ///   - buttonInsets: The button edge insets.
        ///   - rowHeight: The total row height including insets.
        ///   - inputToolbarHeight: The height to use for a topmost input toolbar.
        public init(buttonCornerRadius: Double, buttonInsets: EdgeInsets, rowHeight: Double, inputToolbarHeight: Double? = nil)

        /// The keyboard button corner radius.
        public var buttonCornerRadius: CGFloat

        /// The keyboard button edge insets.
        public var buttonInsets: EdgeInsets

        /// The total row height, including insets.
        public var rowHeight: CGFloat

        /// The height to use for a topmost input toolbar.
        public var inputToolbarHeight: Double

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.DeviceConfiguration, b: KeyboardKit.KeyboardLayout.DeviceConfiguration) -> Bool
    }
}

extension KeyboardLayout {

    /// Convert an input keyboard layout to an iPhone layout.
    ///
    /// This adds device-specific actions to the input items
    /// in the provided layout. Based on the original inputs,
    /// you may have to adjust the final layout.
    ///
    /// - Parameters:
    ///   - layout: The keyboard layout to augment.
    ///   - keyboardContext: The keyboard context to use.
    public static func iPhoneLayout(from layout: KeyboardKit.KeyboardLayout, keyboardContext: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    /// Create a layout item that honors the ideal values of
    /// the keyboard layout.
    public func createIdealItem(for action: KeyboardKit.KeyboardAction, width: KeyboardKit.KeyboardLayout.ItemWidth = .available, alignment: Alignment = .center) -> KeyboardKit.KeyboardLayout.Item
}

extension KeyboardLayout {

    /// Get the bottom row, if any.
    public var bottomRow: KeyboardKit.KeyboardLayout.ItemRow? { get }

    /// Get the bottom item row index.
    public var bottomRowIndex: Int { get }

    /// Get a bottom row-only copy of the layout.
    public var bottomRowLayout: KeyboardKit.KeyboardLayout { get }

    /// Try to create a bottom row item.
    ///
    /// This uses the first ``bottomRow`` system item as the
    /// button template, if any. Otherwise the first item is
    /// returned, or `nil` if there is no bottom row.
    public func tryCreateBottomRowItem(for action: KeyboardKit.KeyboardAction, alignment: Alignment = .center, size: KeyboardKit.KeyboardLayout.ItemSize? = nil) -> KeyboardKit.KeyboardLayout.Item?

    /// Try to insert a keyboard action into the bottom row.
    ///
    /// This aborts when ``tryCreateBottomRowItem(for:size:)``
    /// returs `nil`.
    public mutating func tryInsertBottomRowAction(_ new: KeyboardKit.KeyboardAction, before action: KeyboardKit.KeyboardAction, size: KeyboardKit.KeyboardLayout.ItemSize? = nil)

    /// Try to insert a keyboard action into the bottom row.
    public mutating func tryInsertBottomRowAction(_ new: KeyboardKit.KeyboardAction, after action: KeyboardKit.KeyboardAction, size: KeyboardKit.KeyboardLayout.ItemSize? = nil)

    /// Try to insert a keyboard action into the bottom row.
    public mutating func tryInsertBottomRowAction(_ new: KeyboardKit.KeyboardAction, replacing action: KeyboardKit.KeyboardAction, size: KeyboardKit.KeyboardLayout.ItemSize? = nil)

    /// Try to replace a certain action in the bottom row.
    public mutating func tryReplaceBottomRowAction(_ action: KeyboardKit.KeyboardAction, with new: KeyboardKit.KeyboardAction, size: KeyboardKit.KeyboardLayout.ItemSize? = nil)
}

extension KeyboardLayout {

    /// A size with point-based height and declarative width.
    public struct ItemSize : KeyboardKit.KeyboardModel {

        /// Create a new layout item size.
        public init(width: KeyboardKit.KeyboardLayout.ItemWidth, height: CGFloat)

        /// The declarative width of the item.
        public var width: KeyboardKit.KeyboardLayout.ItemWidth

        /// The fixed height of the item.
        public var height: CGFloat

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.ItemSize, b: KeyboardKit.KeyboardLayout.ItemSize) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This enum specifies various layout item width types.
    public enum ItemWidth : KeyboardKit.KeyboardModel {

        /// A width that will share the available row space.
        case available

        /// A width that should be applied to all input keys.
        case input

        /// A percentual width of the available input width.
        case inputPercentage(CGFloat)

        /// A percentual width of the total available width.
        case percentage(CGFloat)

        /// A fixed width in points.
        case points(CGFloat)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.ItemWidth, b: KeyboardKit.KeyboardLayout.ItemWidth) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardLayout {

    /// Get the layout item with a certain action, if any.
    public func firstCharacterItem() -> KeyboardKit.KeyboardLayout.Item?

    /// Get the layout item with a certain action, if any.
    public func firstItem(with action: KeyboardKit.KeyboardAction) -> KeyboardKit.KeyboardLayout.Item?
}

extension KeyboardLayout {

    /// Create a base keyboard layout with just input keys.
    ///
    /// - Parameters:
    ///   - keyboardContext: The keyboard context to conform to.
    ///   - alphabetic: The alphabetic input set to use.
    ///   - numeric: The numeric input set to use.
    ///   - symbolic: The symbolic input set to use.
    ///   - config: The device config to use, by default `.standard`.
    public static func baseLayout(for keyboardContext: KeyboardKit.KeyboardContext, alphabeticInputSet alphabetic: KeyboardKit.KeyboardLayout.InputSet, numericInputSet numeric: KeyboardKit.KeyboardLayout.InputSet, symbolicInputSet symbolic: KeyboardKit.KeyboardLayout.InputSet, deviceConfiguration config: KeyboardKit.KeyboardLayout.DeviceConfiguration? = nil) -> KeyboardKit.KeyboardLayout
}

extension KeyboardLayout {

    public static var preview: KeyboardKit.KeyboardLayout { get }
}

extension KeyboardLayout {

    /// Get the row at a certain index.
    public func itemRow(at index: Int) -> KeyboardKit.KeyboardLayout.ItemRow?

    /// Insert an item before an action in all rows.
    public mutating func insert(_ item: KeyboardKit.KeyboardLayout.Item, before action: KeyboardKit.KeyboardAction)

    /// Insert an action before an action in all rows.
    public mutating func insert(_ itemAction: KeyboardKit.KeyboardAction, withWidth width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, before action: KeyboardKit.KeyboardAction)

    /// Insert an item before an action in a certain row.
    public mutating func insert(_ item: KeyboardKit.KeyboardLayout.Item, before action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Insert an action before an action in a certain row.
    public mutating func insert(_ itemAction: KeyboardKit.KeyboardAction, withWidth width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, before action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Insert an item after an action in all rows.
    public mutating func insert(_ item: KeyboardKit.KeyboardLayout.Item, after action: KeyboardKit.KeyboardAction)

    /// Insert an action after an action in all rows.
    public mutating func insert(_ itemAction: KeyboardKit.KeyboardAction, withWidth width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, after action: KeyboardKit.KeyboardAction)

    /// Insert an item after an action in a certain row.
    public mutating func insert(_ item: KeyboardKit.KeyboardLayout.Item, after action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Insert an action after an action in a certain row.
    public mutating func insert(_ itemAction: KeyboardKit.KeyboardAction, withWidth width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, after action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Remove a certain action from all rows.
    public mutating func remove(_ action: KeyboardKit.KeyboardAction)

    /// Remove items from all rows, based on a condition.
    public mutating func remove(where condition: (KeyboardKit.KeyboardLayout.Item) -> Bool)

    /// Remove a certain action from a certain row.
    public mutating func remove(_ action: KeyboardKit.KeyboardAction, fromRow rowIndex: Int)

    /// Replace an item with another item in all rows.
    public mutating func replace(_ action: KeyboardKit.KeyboardAction, with replacement: KeyboardKit.KeyboardLayout.Item)

    /// Replace an item with another item in a certain row.
    public mutating func replace(_ action: KeyboardKit.KeyboardAction, with replacement: KeyboardKit.KeyboardLayout.Item, inRow rowIndex: Int)

    /// Replace an item with another item in a certain row.
    public mutating func setWidth(_ width: KeyboardKit.KeyboardLayout.ItemWidth, for action: KeyboardKit.KeyboardAction)

    /// Set the width for an action in a specific row.
    public mutating func setWidth(_ width: KeyboardKit.KeyboardLayout.ItemWidth, for action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Get a template item for an item in a certain row.
    public func templateItem(for action: KeyboardKit.KeyboardAction, withWidth width: KeyboardKit.KeyboardLayout.ItemWidth? = nil, inRow rowIndex: Int) -> KeyboardKit.KeyboardLayout.Item?
}

extension KeyboardLayout.Item {

    /// Create a copy by replacing the item keyboard action.
    public func copy(withAction action: KeyboardKit.KeyboardAction) -> KeyboardKit.KeyboardLayout.Item

    /// Get an item width in points, given a total row width
    /// (most often the screen width) and an input width.
    public func width(forRowWidth rowWidth: Double, inputWidth: Double) -> Double?

    /// Get an item width in points, given a total row width
    /// (most often screen width), input width and insets.
    public func width(forRowWidth rowWidth: Double, inputWidth: Double, edgeInsets insets: Double) -> Double?
}

extension KeyboardLayout.InputSet {

    /// This is an AZERTY alphabetic input set.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/inputSets`` feature.
    public static var azerty: KeyboardKit.KeyboardLayout.InputSet { get throws }

    /// This is a Colemak alphabetic input set.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/inputSets`` feature.
    public static var colemak: KeyboardKit.KeyboardLayout.InputSet { get throws }

    /// This is an Dvorak alphabetic input set.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/inputSets`` feature.
    public static var dvorak: KeyboardKit.KeyboardLayout.InputSet { get throws }

    /// This is a QWERTZ alphabetic input set.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/inputSets`` feature. 
    public static var qwertz: KeyboardKit.KeyboardLayout.InputSet { get throws }
}

extension KeyboardLayout.InputSet {

    /// Get all characters from the input set rows.
    public func characters(for case: KeyboardKit.Keyboard.KeyboardCase, device: KeyboardKit.DeviceType) -> [[String]]

    /// Get all characters strings from the input set rows.
    public func characterStrings(for case: KeyboardKit.Keyboard.KeyboardCase, device: KeyboardKit.DeviceType) -> [String]

    /// Apply secondary actions from another input set.
    ///
    /// This function iterates over the input set and adds a
    /// character action from the same place in the new set.
    public func withSecondaryActions(from inputSet: KeyboardKit.KeyboardLayout.InputSet, for case: KeyboardKit.Keyboard.KeyboardCase, device: KeyboardKit.DeviceType) -> KeyboardKit.KeyboardLayout.InputSet
}

extension KeyboardLayout.InputSet {

    /// A standard QWERTY input set.
    public static var qwerty: KeyboardKit.KeyboardLayout.InputSet { get }

    /// A standard numeric input set.
    public static var numeric: KeyboardKit.KeyboardLayout.InputSet { get }

    /// A standard numeric input set.
    ///
    /// - Parameters:
    ///   - currency: The currency input to add, by default `$`.
    public static func numeric(currency: String) -> KeyboardKit.KeyboardLayout.InputSet

    /// A standard symbolic input set.
    public static var symbolic: KeyboardKit.KeyboardLayout.InputSet { get }

    /// A standard symbolic input set.
    ///
    /// - Parameters:
    ///   - currencies: The currency inputs to add.
    public static func symbolic(currencies: [String]) -> KeyboardKit.KeyboardLayout.InputSet

    /// A standard symbolic input set.
    ///
    /// - Parameters:
    ///   - currencies: The currency inputs to add.
    public static func symbolic(currencies: String) -> KeyboardKit.KeyboardLayout.InputSet
}

extension KeyboardLayout.InputSet {

    /// This type represents an input set item row.
    ///
    /// There are many convenience initializers to assist us
    /// in creating input set rows in different ways.
    public struct ItemRow : KeyboardKit.KeyboardModel {

        /// Create an input set row.
        ///
        /// - Parameters:
        ///   - items: The base row items.
        ///   - deviceVariations: Device-specific item variations, if any.
        public init(items: [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item], deviceVariations: [KeyboardKit.DeviceType : [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item]] = [:])

        /// The base row items.
        public let items: [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item]

        /// Device-specific item variations, if any.
        public let deviceVariations: [KeyboardKit.DeviceType : [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item]]

        /// This typealias represents a row device variation.
        public typealias DeviceVariations = [KeyboardKit.DeviceType : [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item]]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.InputSet.ItemRow, b: KeyboardKit.KeyboardLayout.InputSet.ItemRow) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This typealias represents a list of input set rows.
    public typealias ItemRows = [KeyboardKit.KeyboardLayout.InputSet.ItemRow]
}

extension KeyboardLayout.InputSet {

    /// This struct represents an input set item, that has a
    /// neutral, uppercased and lowercased input variant.
    ///
    /// You can create an instance with just a string, which
    /// is the default way. You can also provide casings, to
    /// fully customize input characters for certain casings.
    public struct Item : KeyboardKit.KeyboardModel {

        /// Create an input set item with a single character.
        ///
        /// - Parameters:
        ///   - char: The char to use for all casings.
        public init(_ char: String)

        /// Create an input set item with certain characters.
        ///
        /// - Parameters:
        ///   - neutral: The neutral char value.
        ///   - uppercased: The uppercased char value.
        ///   - lowercased: The lowercased char value.
        public init(neutral: String, uppercased: String, lowercased: String)

        /// The neutral char value.
        public var neutral: String

        /// The uppercased char value.
        public var uppercased: String

        /// The lowercased char value.
        public var lowercased: String

        /// An optional secondary action.
        public var secondaryAction: KeyboardKit.KeyboardAction?

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardLayout.InputSet.Item, b: KeyboardKit.KeyboardLayout.InputSet.Item) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardLayout.DeviceConfiguration {

    /// The standard config for the provided `context`.
    public static func standard(for context: KeyboardKit.KeyboardContext) -> KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// The standard config for a provided device and screen.
    public static func standard(forDevice device: KeyboardKit.DeviceType, screenSize size: CGSize, orientation: KeyboardKit.InterfaceOrientation, liquidGlass: Bool = false) -> KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// The standard pad config for the provided `screen`.
    public static func standardPad(forScreenSize size: CGSize, orientation: KeyboardKit.InterfaceOrientation, liquidGlass: Bool = false) -> KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// The standard phone config for the provided `screen`.
    public static func standardPhone(forScreenSize size: CGSize, orientation: KeyboardKit.InterfaceOrientation, liquidGlass: Bool = false) -> KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard iPad portrait configuration.
    public static let standardPad: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard iPad landscape configuration.
    public static let standardPadLandscape: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard large iPad portrait configuration.
    public static let standardPadLarge: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard large iPad landscape configuration.
    public static let standardPadLargeLandscape: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard iPhone portrait configuration.
    public static let standardPhone: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard iPhone landscape configuration.
    public static let standardPhoneLandscape: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard large iPhone portrait configuration.
    public static let standardPhoneLarge: KeyboardKit.KeyboardLayout.DeviceConfiguration

    /// A standard large iPhone Pro landscape configuration.
    public static let standardPhoneLargeLandscape: KeyboardKit.KeyboardLayout.DeviceConfiguration
}

extension KeyboardLayout.InputSet.ItemRow {

    /// An internal typealias for cleaner code.
    public typealias Item = KeyboardKit.KeyboardLayout.InputSet.Item

    /// Get all input items for a certain device.
    public func items(for device: KeyboardKit.DeviceType) -> [KeyboardKit.KeyboardLayout.InputSet.ItemRow.Item]

    /// Get all input characters for a certain keyboard case.
    public func characters(for case: KeyboardKit.Keyboard.KeyboardCase, device: KeyboardKit.DeviceType) -> [String]
}

extension KeyboardLayout.InputSet.ItemRow {

    /// Create an input row from a character string.
    ///
    /// This is convenient to use when you can pass separate
    /// characters as a single string, that will be split up.
    public init(chars: String)

    /// Create an input row from a character string.
    ///
    /// This is convenient to use when you can pass separate
    /// characters as a single string, that will be split up.
    public init(chars: String, deviceVariations: [KeyboardKit.DeviceType : String] = [:])

    /// Create an input row from a character array.
    ///
    /// This is convenient to use when any row item must use
    /// two chars in its string, like the Swedish `kr`.
    public init(chars: [String])

    /// Create an input row from a character array.
    ///
    /// This is convenient to use when any row item must use
    /// two chars in its string, like the Swedish `kr`.
    public init(chars: [String], deviceVariations: [KeyboardKit.DeviceType : [String]] = [:])

    /// Create an input row from two cased character strings.
    public init(lowercased: String, uppercased: String)

    /// Create an input row from two cased character strings.
    public init(lowercased: String, uppercased: String, deviceVariations: [KeyboardKit.DeviceType : (lowercased: String, uppercased: String)] = [:])

    /// Create an input row from cased character arrays.
    public init(lowercased: [String], uppercased: [String])

    /// Create an input row from cased character arrays.
    public init(lowercased: [String], uppercased: [String], deviceVariations: [KeyboardKit.DeviceType : (lowercased: [String], uppercased: [String])] = [:])
}

extension KeyboardLayout.InputSet.Item {

    /// Get the character to use for a certain case.
    public func character(for case: KeyboardKit.Keyboard.KeyboardCase) -> String

    /// Get the character action to use for a certain case.
    public func characterAction(for case: KeyboardKit.Keyboard.KeyboardCase) -> KeyboardKit.KeyboardAction
}

/// This protocol is an alias for other protocols that model
/// types in the framework should conform to.
public protocol KeyboardModel : Decodable, Encodable, Hashable, Sendable {
}

/// This namespace defines preview-related type variants for
/// many types and protocols.
///
/// These previews make it easier to use SwiftUI previews in
/// a KeyboardKit-based app.
public struct KeyboardPreviews {
}

extension KeyboardPreviews {

    @MainActor @objc @preconcurrency public class TextDocumentProxy : NSObject, UITextDocumentProxy {

        @MainActor @preconcurrency override dynamic public init()

        @MainActor @preconcurrency public var autocapitalizationType: UITextAutocapitalizationType

        @MainActor @preconcurrency public var documentContextBeforeInput: String?

        @MainActor @preconcurrency public var documentContextAfterInput: String?

        @MainActor @preconcurrency public var hasText: Bool

        @MainActor @preconcurrency public var selectedText: String?

        @MainActor @preconcurrency public var documentInputMode: UITextInputMode?

        @MainActor @preconcurrency public var documentIdentifier: UUID

        @MainActor @preconcurrency public var returnKeyType: UIReturnKeyType

        @MainActor @preconcurrency public func adjustTextPosition(byCharacterOffset offset: Int)

        @MainActor @preconcurrency public func deleteBackward()

        @MainActor @preconcurrency public func insertText(_ text: String)

        @MainActor @preconcurrency public func setMarkedText(_ markedText: String, selectedRange: NSRange)

        @MainActor @preconcurrency public func unmarkText()

        @objc deinit
    }
}

extension KeyboardPreviews {

    public class AutocompleteService : KeyboardKit.Autocomplete.DisabledAutocompleteService {

        /// Create a disabled autocomplete service.
        ///
        /// - Parameters:
        ///   - suggestions: The suggestions to present.
        override public init(suggestions: [KeyboardKit.Autocomplete.Suggestion] = super)

        @objc deinit
    }
}

extension KeyboardPreviews {

    @MainActor @objc @preconcurrency public class InputViewController : KeyboardKit.KeyboardInputViewController {

        @MainActor @preconcurrency override dynamic public init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?)

        @MainActor @preconcurrency required dynamic public init?(coder: NSCoder)

        @objc deinit
    }
}

extension KeyboardPreviews {

    public class ActionHandler : KeyboardKit.KeyboardActionHandler {

        public init()

        @objc deinit
    }
}

extension KeyboardPreviews.TextDocumentProxy : Sendable {
}

extension KeyboardPreviews.ActionHandler {

    /// Whether the handler can handle an action gesture.
    public func canHandle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction) -> Bool

    /// Handle a certain keyboard action.
    public func handle(_ action: KeyboardKit.KeyboardAction)

    /// Handle a certain autocomplete suggestion.
    public func handle(_ suggestion: KeyboardKit.Autocomplete.Suggestion)

    /// Handle a certain keyboard action gesture.
    public func handle(_ gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

    /// Handle a certain keyboard action drag gesture.
    public func handleDrag(on action: KeyboardKit.KeyboardAction, from startLocation: CGPoint, to currentLocation: CGPoint)

    /// Trigger feedback for a certain action gesture.
    public func triggerFeedback(for gesture: KeyboardKit.Keyboard.Gesture, on action: KeyboardKit.KeyboardAction)

    /// Trigger the provided audio feedback.
    public func triggerAudioFeedback(_ feedback: KeyboardKit.Feedback.Audio)

    /// Trigger the provided haptic feedback.
    public func triggerHapticFeedback(_ feedback: KeyboardKit.Feedback.Haptic)
}

/// This type has auto-persisted keyboard settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `keyboard` prefix. You
/// can use ``Foundation/UserDefaults/keyboardSettings`` for
/// convenience when defining a user defaults value.
///
/// You can use ``setupStore(for:)`` to set up the store for
/// a ``KeyboardApp`` or use the parameter-based variant for
/// granular control. Make sure to first set up an App Group
/// for the app and keyboard and add its ID to the app value.
///
/// > Important: `@AppStorage` properties will use the store
/// that is available when they're first accessed. Make sure
/// to set up any custom store BEFORE using these properties,
/// otherwise the properties will use the original store.
public struct KeyboardSettings {

    /// Create a custom settings instance.
    ///
    /// - Parameters:
    ///   - onAutocapitalizationEnabledChanged: The action to trigger when autocapitalization is changed.
    public init(onAutocapitalizationEnabledChanged: @escaping () -> Void = {})

    /// The settings key prefix to use.
    public static var settingsPrefix: String { get }

    /// A list of explicitly added locale identifiers.
    @AppStorage public var addedLocales: [KeyboardKit.Keyboard.AddedLocale] { get nonmutating set }

    public var $addedLocales: Binding<[KeyboardKit.Keyboard.AddedLocale]> { get }

    /// The input toolbar type to use, if any.
    @AppStorage public var inputToolbarType: KeyboardKit.KeyboardSettings.InputToolbarType { get nonmutating set }

    public var $inputToolbarType: Binding<KeyboardKit.KeyboardSettings.InputToolbarType> { get }

    /// The input toolbar chars to show for ``KeyboardSettings/InputToolbarType/characters``.
    @AppStorage public var inputToolbarCharacters: String { get nonmutating set }

    public var $inputToolbarCharacters: Binding<String> { get }

    /// The ``inputToolbarCharacters`` max count.
    @AppStorage public var inputToolbarCharactersMaxCount: Int { get nonmutating set }

    public var $inputToolbarCharactersMaxCount: Binding<Int> { get }

    /// Whether auto-capitalization is enabled.
    @AppStorage public var isAutocapitalizationEnabled: Bool { get nonmutating set }

    public var $isAutocapitalizationEnabled: Binding<Bool> { get }

    /// Whether to auto-collapse the keyboard when an external keyboard is connected.
    @AppStorage public var isAutoCollapseKeyboardEnabled: Bool { get nonmutating set }

    public var $isAutoCollapseKeyboardEnabled: Binding<Bool> { get }

    /// Whether double-tap on shift should apply caps-lock.
    @AppStorage public var isDoubleTapOnShiftToCapsLockEnabled: Bool { get nonmutating set }

    public var $isDoubleTapOnShiftToCapsLockEnabled: Binding<Bool> { get }

    /// Whether double-tap on space should close a sentence.
    @AppStorage public var isDoubleTapOnSpacebarToCloseSentenceEnabled: Bool { get nonmutating set }

    public var $isDoubleTapOnSpacebarToCloseSentenceEnabled: Binding<Bool> { get }

    /// Whether secondary swipe down actions should be added.
    ///
    /// We must check both the setting as well as the device
    /// type to see if we should add secondary swipe actions.
    @AppStorage public var isSwipeDownActionsEnabled: Bool { get nonmutating set }

    public var $isSwipeDownActionsEnabled: Binding<Bool> { get }

    /// The ``Keyboard/DockEdge`` to use, if any.
    @AppStorage public var keyboardDockEdge: KeyboardKit.Keyboard.DockEdge? { get nonmutating set }

    public var $keyboardDockEdge: Binding<KeyboardKit.Keyboard.DockEdge?> { get }

    /// The current layout type identifier, set by ``KeyboardContext/keyboardLayoutType``.
    @AppStorage public var keyboardLayoutTypeIdentifier: KeyboardKit.Keyboard.LayoutType.ID? { get }

    public var $keyboardLayoutTypeIdentifier: Binding<KeyboardKit.Keyboard.LayoutType.ID?> { get }

    /// The current locale identifier, set by ``KeyboardContext/locale``.
    @AppStorage public var localeIdentifier: String { get }

    public var $localeIdentifier: Binding<String> { get }

    /// A leading ``Keyboard/SpacebarMenuType`` to use, if any.
    @AppStorage public var spacebarMenuLeading: KeyboardKit.Keyboard.SpacebarMenuType? { get nonmutating set }

    public var $spacebarMenuLeading: Binding<KeyboardKit.Keyboard.SpacebarMenuType?> { get }

    /// A trailing ``Keyboard/SpacebarMenuType`` to use, if any.
    @AppStorage public var spacebarMenuTrailing: KeyboardKit.Keyboard.SpacebarMenuType? { get nonmutating set }

    public var $spacebarMenuTrailing: Binding<KeyboardKit.Keyboard.SpacebarMenuType?> { get }

    /// The ``Keyboard/SpacebarLongPressBehavior`` to use.
    @AppStorage public var spacebarLongPressBehavior: KeyboardKit.Keyboard.SpacebarLongPressBehavior { get nonmutating set }

    public var $spacebarLongPressBehavior: Binding<KeyboardKit.Keyboard.SpacebarLongPressBehavior> { get }
}

extension KeyboardSettings {

    /// Add a new locale to ``addedLocales``.
    ///
    /// If you don't provide a `layoutType`, the next layout
    /// type in the ``Foundation/Locale/supportedLayoutTypes``
    /// will be used. If you provide a `layoutType`, it must
    /// be in the ``remainingLayoutTypes(for:)``
    public func addLocale(_ locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType? = nil) throws

    /// Whether a certain locale can be added.
    public func canAddLocale(_ locale: Locale) throws -> Bool

    /// Whether a certain locale & layout type can be added.
    public func canAddLocale(_ locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType) throws -> Bool

    /// Whether a certain locale can be added once.
    public func canAddLocaleOnce(_ locale: Locale) throws -> Bool

    /// Find all remaining layout types for a certain locale.
    public func remainingLayoutTypes(for locale: Locale) throws -> [KeyboardKit.Keyboard.LayoutType]
}

extension KeyboardSettings {

    /// This enum defines various input toolbar modes, which
    /// can be used to set how an input toolbar is displayed.
    ///
    /// The reason why this type is used in settings instead
    /// of ``Keyboard/InputToolbarDisplayMode`` is that this
    /// type can be persisted, while the display mode cannot.
    public enum InputToolbarType : String, KeyboardKit.KeyboardModel {

        /// Display a toolbar based on the native behavior.
        case automatic

        /// Display a fixed set of characters from settings.
        case characters

        /// Display contextual numbers or symbols.
        case numbers

        /// Display the most recently used emojis.
        case recentEmojis

        /// Remove the input toolbar altogether.
        case none

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }

    /// The input toolbar display mode.
    ///
    /// This value is derived from ``inputToolbarType``, and
    /// uses ``inputToolbarCharacters`` for input characters.
    public var inputToolbarDisplayMode: KeyboardKit.Keyboard.InputToolbarDisplayMode { get }
}

extension KeyboardSettings {

    /// The store that will be used by library settings.
    public static var store: UserDefaults

    /// The key prefix that will be used by library settings.
    public static var storeKeyPrefix: String

    /// The default key prefix that will be used by library settings.
    public static let storeKeyPrefixDefault: String

    /// Whether the ``store`` is synced with an App Group.
    public static var storeIsAppGroupSynced: Bool { get }

    /// Reset the standard settings store.
    public static func resetStore()

    /// Set up a custom settings store.
    ///
    /// - Parameters:
    ///   - store: The store instance to use.
    ///   - keyPrefix: A custom prefix to use for all store keys, if any.
    public static func setupStore(_ store: UserDefaults, keyPrefix: String? = nil)

    /// Set up a custom settings store.
    ///
    /// - Parameters:
    ///   - store: The store instance to use.
    ///   - keyPrefix: A custom prefix to use for all store keys, if any.
    ///   - isAppGroupSynced: Whether the store syncs with an App Group.
    public static func setupStore(_ store: UserDefaults, keyPrefix: String? = nil, isAppGroupSynced: Bool)

    /// Set up a custom settings store for the provided `app`.
    ///
    /// This will set up App Group syncing if the app has an
    /// ``KeyboardApp/appGroupId`` specified.
    public static func setupStore(for app: KeyboardKit.KeyboardApp)

    /// Set up a custom settings store for a given App Group.
    ///
    /// - Parameters:
    ///   - appGroup: The ID of the App Group to use.
    ///   - keyPrefix: A custom prefix to use for all store keys, if any.
    public static func setupStore(forAppGroup appGroup: String, keyPrefix: String? = nil)

    /// Get the store key prefix for a certain namespace.
    public static func storeKeyPrefix(for namespace: String) -> String
}

extension KeyboardSettings {

    /// Get and set the current locale.
    public var locale: Locale

    /// Get and set the current keyboard layout type, if any.
    public var keyboardLayoutType: KeyboardKit.Keyboard.LayoutType?
}

extension KeyboardSettings {

    /// Whether settings has any added locales.
    public var hasAddedLocales: Bool { get }

    /// Whether settings has multiple added locales.
    public var hasMultipleAddedLocales: Bool { get }

    /// Whether a certain locale is added to ``addedLocales``.
    public func firstAddedLocale(for locale: Locale) -> KeyboardKit.Keyboard.AddedLocale?

    /// Whether a certain locale is added to ``addedLocales``.
    public func firstAddedLocale(for locale: Locale, layoutType type: KeyboardKit.Keyboard.LayoutType? = nil) -> KeyboardKit.Keyboard.AddedLocale?

    /// Whether a locale has been added to ``addedLocales``.
    public func hasAddedLocale(_ locale: KeyboardKit.Keyboard.AddedLocale) -> Bool

    /// Whether a locale has been added to ``addedLocales``.
    public func hasAddedLocale(_ locale: Locale) -> Bool

    /// Whether a locale with a certain layout type has been
    /// added to ``addedLocales``.
    public func hasAddedLocale(_ locale: Locale, withLayoutType type: KeyboardKit.Keyboard.LayoutType) -> Bool
}

extension KeyboardSettings.InputToolbarType : RawRepresentable {
}

/// This namespace contains keyboard status-related types.
///
/// These types can be used to display and handle the status
/// of a keyboard, such as if the keyboard or Full Access is
/// enabled. You can use the keyboard status views to link a
/// user to system settings, where both can be enabled.
public struct KeyboardStatus {
}

extension KeyboardStatus {

    /// This label can be used to show the currently enabled
    /// state for a keyboard.
    ///
    /// While you can use it with any bool, the intended use
    /// is to show if a keyboard extension is enabled, if is
    /// has full access etc.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardStatusLabelStyle(_:)``.
    @MainActor @preconcurrency public struct Label : View {

        /// Create an enabled label.
        ///
        /// - Parameters:
        ///   - isEnabled: Whether or not the state is enabled.
        ///   - enabledText: The text to show when the state is enabled.
        ///   - disabledText: The text to show when the state is disabled.
        @MainActor @preconcurrency public init(isEnabled: Bool, enabledText: String, disabledText: String)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardStatus {

    /// This type can style a ``KeyboardStatus/Section``.
    ///
    /// Use ``SwiftUICore/View/keyboardStatusSectionStyle(_:)``
    /// to apply a custom style to any view.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct SectionStyle {

        /// Create a custom home screen.
        ///
        /// - Parameters:
        ///  - displayMode: The keyboard status display mode, by default `.always`.
        ///  - fullAccess: The full access status display mode, by default `.always`.
        ///  - systemSettingsIcon: The system settings link icon, by default `.gearshape`.
        ///  - systemSettingsLink: The system settings link display mode, by default `.always`.
        public init(displayMode: KeyboardKit.Keyboard.DisplayMode = .always, fullAccess: KeyboardKit.Keyboard.DisplayMode = .always, systemSettingsIcon: Image? = .init(systemName: "gearshape"), systemSettingsLink: KeyboardKit.Keyboard.DisplayMode = .ifNeeded)

        public var displayMode: KeyboardKit.Keyboard.DisplayMode

        public var fullAccess: KeyboardKit.Keyboard.DisplayMode

        public var systemSettingsIcon: Image?

        public var systemSettingsLink: KeyboardKit.Keyboard.DisplayMode
    }
}

extension KeyboardStatus {

    /// This style can be used to customize the localization
    /// of the ``KeyboardStatus/Section`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/keyboardStatusSectionLocalization(_:)``.
    ///
    /// You can use the ``standard`` value or your own value.
    public struct SectionLocalization {

        /// Create a custom home screen localization.
        ///
        /// - Parameters:
        ///  - sectionTitle: The title of the status section.
        ///  - keyboardEnabledTitle: The text to show when the keyboard is enabled.
        ///  - keyboardDisabledTitle: The text to show when the keyboard is disabled.
        ///  - fullAccessEnabledTitle: The text to show when Full Access is enabled.
        ///  - fullAccessDisabledTitle: The text to show when Full Access is disabled.
        ///  - systemSettingsLinkTitle: The app settings link title.
        ///  - sectionFooter: The footer of the status section.
        public init(sectionTitle: String = "Keyboard Status", keyboardEnabledTitle: String = "Keyboard Enabled", keyboardDisabledTitle: String = "Keyboard Disabled", fullAccessEnabledTitle: String = "Full Access Enabled", fullAccessDisabledTitle: String = "Full Access Disabled", systemSettingsLinkTitle: String = "System Settings", sectionFooter: String? = nil)

        public var sectionTitle: String

        public var keyboardEnabledTitle: String

        public var keyboardDisabledTitle: String

        public var fullAccessEnabledTitle: String

        public var fullAccessDisabledTitle: String

        public var systemSettingsLinkTitle: String

        public var sectionFooter: String?
    }
}

extension KeyboardStatus {

    /// This section can be used to show keyboard status for
    /// a certain keyboard bundle ID.
    ///
    /// The section will by default show if the keyboard has
    /// been enabled in System Settings, and how Full Access
    /// has been configured for the keyboard extension.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardStatusSectionStyle(_:)`` and localize each
    /// text with ``keyboardStatusSectionLocalization(_:)``.
    @MainActor @preconcurrency public struct Section : View {

        /// Create a keyboard app home screen.
        ///
        /// - Parameters:
        ///  - keyboardBundleId: The keyboard bundle ID, supports wildcards.
        @MainActor @preconcurrency public init(keyboardBundleId: String)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardStatus {

    /// This style can be used to modify the visual style of
    /// the ``KeyboardStatus/Label`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/keyboardStatusLabelStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct LabelStyle {

        /// Create a custom keyboard state label style.
        ///
        /// - Parameters:
        ///   - enabledIcon: The enabled icon view.
        ///   - enabledIconStyle: The enabled icon style, by default `.green`.
        ///   - enabledTextStyle: The enabled text style, if any.
        ///   - disabledIcon: The enabled icon view.
        ///   - disabledIconStyle: The disabled icon style, by default `.orange`.
        ///   - disabledTextStyle: The disabled text style, if any.
        public init<EnabledIcon, DisabledIcon>(enabledIcon: EnabledIcon, enabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: .green, font: nil), enabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: nil, font: nil), disabledIcon: DisabledIcon, disabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: .orange, font: nil), disabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: nil, font: nil)) where EnabledIcon : View, DisabledIcon : View

        /// Create a custom keyboard state label style.
        ///
        /// - Parameters:
        ///   - enabledIconStyle: The enabled icon style, by default `.green`.
        ///   - enabledTextStyle: The enabled text style, if any.
        ///   - disabledIconStyle: The disabled icon style, by default `.orange`.
        ///   - disabledTextStyle: The disabled text style, if any.
        public init(enabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: .green, font: nil), enabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: nil, font: nil), disabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: .orange, font: nil), disabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle = .init(color: nil, font: nil))

        /// This style defines the style of a label component.
        public struct ComponentStyle : Equatable {

            public init(color: Color?, font: Font?)

            public var color: Color?

            public var font: Font?

            /// Returns a Boolean value indicating whether two values are equal.
            ///
            /// Equality is the inverse of inequality. For any values `a` and `b`,
            /// `a == b` implies that `a != b` is `false`.
            ///
            /// - Parameters:
            ///   - lhs: A value to compare.
            ///   - rhs: Another value to compare.
            public static func == (a: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle, b: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle) -> Bool
        }

        /// The enabled icon view.
        public var enabledIcon: AnyView

        /// The enabled icon style.
        public var enabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle

        /// The enabled text style.
        public var enabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle

        /// The disabled icon view.
        public var disabledIcon: AnyView

        /// The disabled icon style.
        public var disabledIconStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle

        /// The disabled text style.
        public var disabledTextStyle: KeyboardKit.KeyboardStatus.LabelStyle.ComponentStyle
    }
}

extension KeyboardStatus.Label : Sendable {
}

extension KeyboardStatus.SectionStyle {

    /// A standard keyboard status section Style.
    public static var standard: KeyboardKit.KeyboardStatus.SectionStyle { get }
}

extension KeyboardStatus.SectionLocalization {

    /// A standard keyboard status section localization.
    public static var standard: KeyboardKit.KeyboardStatus.SectionLocalization { get }
}

extension KeyboardStatus.Section : Sendable {
}

extension KeyboardStatus.LabelStyle {

    /// The standard status label style.
    public static var standard: KeyboardKit.KeyboardStatus.LabelStyle { get }
}

/// This class has observable, keyboard status-related state.
///
/// This class can be used to check if a keyboard is enabled
/// in System Settings, if Full Access is enabled, etc.
///
/// This type supports bundle ID wildcards, which means that
/// it can inspect multiple keyboards at once, for instance:
///
/// ```
/// @StateObject
/// var status = KeyboardStatusContext(bundleId: "com.myapp.*")
/// ```
///
/// This context is not added to the input controller, since
/// it's intended to be used in the main app, not a keyboard.
public class KeyboardStatusContext : ObservableObject {

    /// Create an instance of this observable state class.
    ///
    /// Make sure to use the `bundleId` of the keyboard, not
    /// the main application.
    ///
    /// - Parameters:
    ///   - bundleId: The bundle ID of the keyboard extension.
    ///   - notificationCenter: The notification center to use to observe changes.
    public init(bundleId: String, notificationCenter: NotificationCenter = .default)

    final public let bundleId: String

    /// Whether the keyboard extension is actively used.
    @Published public var isKeyboardActive: Bool

    public var $isKeyboardActive: Published<Bool>.Publisher

    /// Whether the keyboard extension has been enabled.
    @Published public var isKeyboardEnabled: Bool

    public var $isKeyboardEnabled: Published<Bool>.Publisher

    /// Refresh the observable state.
    public func refresh()

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension KeyboardStatusContext {

    /// Whether Full Access is enabled in System Settings.
    public var isFullAccessEnabled: Bool { get }
}

/// This namespace contains style-related types.
public struct KeyboardStyle {
}

/// This view can be used to type within keyboard extensions.
///
/// This view automatically registers and unregisters as the
/// main ``KeyboardContext/textInputProxy`` when it gets and
/// loses focus. You can control focus with `@FocusState`.
///
/// Apply a ``SwiftUICore/View/focused(_:doneButton:)`` view
/// modifier to the view to it show a "done" button when the
/// text input is focused. You can also provide an `onSubmit`
/// function that is triggered when the return key is tapped,
/// if `resignOnReturn` is `true`.
///
/// The view is disabled if Full Access isn't enabled, since
/// the underlying view crashes when Full Access is disabled.
///
/// > Important: This requires that your license unlocks the
/// ``ProFeature/textInput`` feature.
@MainActor @preconcurrency public struct KeyboardTextField : UIViewRepresentable {

    /// Create a keyboard text field instance.
    ///
    /// - Parameters:
    ///   - placeholder: A placeholder to show when `text` is empty, by default `nil`.
    ///   - text: The text binding to affect.
    ///   - keyboardContext: The keyboard context to affect.
    ///   - resignOnReturn: Whether to resign focus when return is pressed, by default `true`.
    ///   - config: A native view configuration, if any.
    ///   - onSubmit: A function to trigger when return is pressed, if any.
    @MainActor @preconcurrency public init(_ placeholder: String? = nil, text: Binding<String>, keyboardContext: KeyboardKit.KeyboardContext, resignOnReturn: Bool = true, config: @escaping KeyboardKit.KeyboardTextField.Configuration = { _ in }, onSubmit: @escaping () -> Void = {})

    /// This typealias represents a text field config block.
    public typealias Configuration = (UITextField) -> Void

    @MainActor public let keyboardContext: KeyboardKit.KeyboardContext

    /// Creates the custom instance that you use to communicate changes from
    /// your view to other parts of your SwiftUI interface.
    ///
    /// Implement this method if changes to your view might affect other parts
    /// of your app. In your implementation, create a custom Swift instance that
    /// can communicate with other parts of your interface. For example, you
    /// might provide an instance that binds its variables to SwiftUI
    /// properties, causing the two to remain synchronized. If your view doesn't
    /// interact with other parts of your app, providing a coordinator is
    /// unnecessary.
    ///
    /// SwiftUI calls this method before calling the
    /// ``UIViewRepresentable/makeUIView(context:)`` method. The system provides
    /// your coordinator either directly or as part of a context structure when
    /// calling the other methods of your representable instance.
    @MainActor @preconcurrency public func makeCoordinator() -> KeyboardKit.KeyboardTextField.Coordinator

    /// Creates the view object and configures its initial state.
    ///
    /// You must implement this method and use it to create your view object.
    /// Configure the view using your app's current data and contents of the
    /// `context` parameter. The system calls this method only once, when it
    /// creates your view for the first time. For all subsequent updates, the
    /// system calls the ``UIViewRepresentable/updateUIView(_:context:)``
    /// method.
    ///
    /// - Parameter context: A context structure containing information about
    ///   the current state of the system.
    ///
    /// - Returns: Your UIKit view configured with the provided information.
    @MainActor @preconcurrency public func makeUIView(context: KeyboardKit.KeyboardTextField.Context) -> UITextField

    @MainActor @preconcurrency public func configureUIView(_ view: UITextField)

    /// Updates the state of the specified view with new information from
    /// SwiftUI.
    ///
    /// When the state of your app changes, SwiftUI updates the portions of your
    /// interface affected by those changes. SwiftUI calls this method for any
    /// changes affecting the corresponding UIKit view. Use this method to
    /// update the configuration of your view to match the new state information
    /// provided in the `context` parameter.
    ///
    /// - Parameters:
    ///   - uiView: Your custom view object.
    ///   - context: A context structure containing information about the current
    ///     state of the system.
    @MainActor @preconcurrency public func updateUIView(_ view: UITextField, context: KeyboardKit.KeyboardTextField.Context)

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = Never

    /// The type of view to present.
    public typealias UIViewType = UITextField
}

extension KeyboardTextField {

    /// A type to coordinate with the view.
    @MainActor @objc public class Coordinator : NSObject {

        @MainActor @objc public func textFieldDidChange(_ textField: UITextField)

        @objc deinit
    }
}

extension KeyboardTextField : Sendable {
}

extension KeyboardTextField.Coordinator : Sendable {
}

/// This view can be used to type within keyboard extensions.
///
/// This view automatically registers and unregisters as the
/// main ``KeyboardContext/textInputProxy`` when it gets and
/// loses focus. You can control focus with `@FocusState`.
///
/// Apply a ``SwiftUICore/View/focused(_:doneButton:)`` view
/// modifier to the view to it show a "done" button when the
/// text input is focused. You can also provide an `onSubmit`
/// function that is triggered when the return key is tapped,
/// if `resignOnReturn` is `true`.
///
/// The view is disabled if Full Access isn't enabled, since
/// the underlying view crashes when Full Access is disabled.
///
/// > Important: This requires that your license unlocks the
/// ``ProFeature/textInput`` feature. 
@MainActor @preconcurrency public struct KeyboardTextView : UIViewRepresentable {

    /// Create a keyboard text view instance.
    ///
    /// - Parameters:
    ///   - text: The text binding to affect.
    ///   - keyboardContext: The keyboard context to affect.
    ///   - resignOnReturn: Whether to resign focus when return is pressed, by default `false`.
    ///   - config: A native view configuration, if any.
    ///   - onSubmit: A function to trigger when return is pressed, if any.
    @MainActor @preconcurrency public init(text: Binding<String>, keyboardContext: KeyboardKit.KeyboardContext, resignOnReturn: Bool = false, config: @escaping KeyboardKit.KeyboardTextView.Configuration = { _ in }, onSubmit: @escaping () -> Void = {})

    /// This typealias represents a text view config block.
    public typealias Configuration = (UITextView) -> Void

    @MainActor public let keyboardContext: KeyboardKit.KeyboardContext

    /// Creates the custom instance that you use to communicate changes from
    /// your view to other parts of your SwiftUI interface.
    ///
    /// Implement this method if changes to your view might affect other parts
    /// of your app. In your implementation, create a custom Swift instance that
    /// can communicate with other parts of your interface. For example, you
    /// might provide an instance that binds its variables to SwiftUI
    /// properties, causing the two to remain synchronized. If your view doesn't
    /// interact with other parts of your app, providing a coordinator is
    /// unnecessary.
    ///
    /// SwiftUI calls this method before calling the
    /// ``UIViewRepresentable/makeUIView(context:)`` method. The system provides
    /// your coordinator either directly or as part of a context structure when
    /// calling the other methods of your representable instance.
    @MainActor @preconcurrency public func makeCoordinator() -> KeyboardKit.KeyboardTextView.Coordinator

    /// Creates the view object and configures its initial state.
    ///
    /// You must implement this method and use it to create your view object.
    /// Configure the view using your app's current data and contents of the
    /// `context` parameter. The system calls this method only once, when it
    /// creates your view for the first time. For all subsequent updates, the
    /// system calls the ``UIViewRepresentable/updateUIView(_:context:)``
    /// method.
    ///
    /// - Parameter context: A context structure containing information about
    ///   the current state of the system.
    ///
    /// - Returns: Your UIKit view configured with the provided information.
    @MainActor @preconcurrency public func makeUIView(context: KeyboardKit.KeyboardTextView.Context) -> UITextView

    @MainActor @preconcurrency public func configureUIView(_ view: UITextView)

    /// Updates the state of the specified view with new information from
    /// SwiftUI.
    ///
    /// When the state of your app changes, SwiftUI updates the portions of your
    /// interface affected by those changes. SwiftUI calls this method for any
    /// changes affecting the corresponding UIKit view. Use this method to
    /// update the configuration of your view to match the new state information
    /// provided in the `context` parameter.
    ///
    /// - Parameters:
    ///   - uiView: Your custom view object.
    ///   - context: A context structure containing information about the current
    ///     state of the system.
    @MainActor @preconcurrency public func updateUIView(_ view: UITextView, context: KeyboardKit.KeyboardTextView.Context)

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = Never

    /// The type of view to present.
    public typealias UIViewType = UITextView
}

extension KeyboardTextView {

    /// A type to coordinate with the view.
    @MainActor @objc @preconcurrency public class Coordinator : NSObject, UITextViewDelegate {

        @MainActor @preconcurrency public func textViewDidChange(_ textView: UITextView)

        @objc deinit
    }
}

extension KeyboardTextView : Sendable {
}

extension KeyboardTextView.Coordinator : Sendable {
}

/// This struct defines a keyboard-specific theme, which can
/// be used to define a bunch of styles at once.
///
/// A theme can be copied, tweaked, styled, etc. This struct
/// implements the ``KeyboardThemeCopyable`` protocol to let
/// it share copy logic with its style variations.
///
/// KeyboardKit Pro has many pre-defined themes & styles, as
/// well as a theme-based style service.
///
/// The ``SwiftUICore/View/keyboardTheme(_:)`` view modifier
/// can be used to apply a theme, which will apply all style
/// values that the theme defines.
///
/// > Important: This requires that your license unlocks
///  the ``ProFeature/themes`` feature.
public struct KeyboardTheme : KeyboardKit.KeyboardThemeCopyable, Codable, Equatable, Identifiable, Sendable {

    /// The unique theme ID.
    public var id: UUID

    /// The name of the theme.
    public var name: String

    /// The name of a collection to which the theme belongs.
    public var collectionName: String

    /// The name of the author, if any.
    public var author: KeyboardKit.KeyboardTheme.Author?

    /// The keyboard background style, if any.
    public var backgroundStyle: KeyboardKit.Keyboard.Background?

    /// The keyboard foreground color, if any.
    public var foregroundColor: Color?

    /// The button styles to apply to certain button types.
    public var buttonStyles: [KeyboardKit.KeyboardTheme.ButtonType : KeyboardKit.Keyboard.ButtonStyle]

    /// The button pressed overlay color, if any.
    public var buttonPressedOverlayColor: Color?

    /// The style to apply to autocomplete toolbars, if any.
    public var autocompleteToolbarStyle: KeyboardKit.Autocomplete.ToolbarStyle?

    /// The style to apply to keyboard callouts, if any.
    public var calloutStyle: KeyboardKit.Callouts.CalloutStyle?

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.KeyboardTheme, b: KeyboardKit.KeyboardTheme) -> Bool

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = UUID

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension KeyboardTheme {

    /// The standard `Blueprint` theme.
    public static var blueprint: KeyboardKit.KeyboardTheme { get throws }

    /// Create a theme for the provided style.
    public static func blueprint(_ style: KeyboardKit.KeyboardTheme.BlueprintStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/blueprint`` style variation.
    ///
    /// Blueprint style variations have a blue background by
    /// default, and a single ting color.
    public struct BlueprintStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, background: Color, tint: Color, calloutShadowColor: Color? = nil, calloutShadowRadius: Double? = nil)

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var background: Color

        public var tint: Color

        public var calloutShadowColor: Color?

        public var calloutShadowRadius: Double?

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/blueprint`` themes.
    public static var predefinedBlueprint: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// This struct can be used to group themes together.
    public struct Collection : Codable, Equatable, Identifiable {

        /// Create a theme collection.
        ///
        /// - Parameters:
        ///   - name: The name of the collection.
        ///   - themes: The themes in the collection.
        public init(name: String, themes: [KeyboardKit.KeyboardTheme])

        /// The collection ID,
        public var id: String { get }

        /// The name of the collection.
        public let name: String

        /// The themes in the collection.
        public let themes: [KeyboardKit.KeyboardTheme]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardTheme.Collection, b: KeyboardKit.KeyboardTheme.Collection) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardTheme {

    /// This initializer is a non-throwing internal fallback.
    public init(internalName: String)

    /// Create a theme with explicit styles.
    ///
    /// If you don't provide an explicit ID, this theme will
    /// get a new unique ID. Make sure to use an explicit ID
    /// if you want to have full control over your theme.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/themes`` feature.
    ///
    /// - Parameters:
    ///   - id: An optional, explicit ID.
    ///   - name: The name of the theme.
    ///   - collectionName: The name of the collection to which the theme belongs.
    ///   - author: The author of the theme, by default `nil`.
    ///   - backgroundStyle: The keyboard background style, by default `nil`.
    ///   - foregroundColor: The keyboard foreground color, by default `nil`.
    ///   - buttonStyles: The button styles to apply to certain button types, by default empty.
    ///   - buttonPressedOverlayColor: The button pressed overlay color, by default `nil`.
    ///   - autocompleteToolbarStyle: The style to apply to autocomplete toolbars, by default `nil`.
    ///   - calloutStyle: The style to apply to callouts, by default `nil`.
    public init(id: UUID? = nil, name: String, collectionName: String, author: KeyboardKit.KeyboardTheme.Author? = nil, backgroundStyle: KeyboardKit.Keyboard.Background? = nil, foregroundColor: Color? = nil, buttonStyles: [KeyboardKit.KeyboardTheme.ButtonType : KeyboardKit.Keyboard.ButtonStyle] = [:], buttonPressedOverlayColor: Color? = nil, autocompleteToolbarStyle: KeyboardKit.Autocomplete.ToolbarStyle? = nil, calloutStyle: KeyboardKit.Callouts.CalloutStyle? = nil) throws

    /// Create a theme with a raw style properties.
    ///
    /// If you don't provide an explicit ID, this theme will
    /// get a new unique ID. Make sure to use an explicit ID
    /// if you want to have full control over your theme.
    public init(id: UUID? = nil, name: String, collectionName: String, author: KeyboardKit.KeyboardTheme.Author? = nil, backgroundStyle: KeyboardKit.Keyboard.Background? = nil, foregroundColor: Color? = nil, inputForegroundColor: Color? = nil, inputBackgroundColor: Color? = nil, systemForegroundColor: Color? = nil, systemBackgroundColor: Color? = nil, primaryForegroundColor: Color? = nil, primaryBackgroundColor: Color? = nil, buttonCornerRadius: CGFloat? = nil, buttonBorderColor: Color? = nil, buttonBorderSize: CGFloat? = nil, buttonPressedOverlayColor: Color? = nil, buttonShadowColor: Color? = nil, buttonShadowSize: CGFloat? = nil, calloutForegroundColor: Color? = nil, calloutBackgroundColor: Color? = nil, calloutSelectedForegroundColor: Color? = nil, calloutSelectedBackgroundColor: Color? = nil, calloutSelectedCornerRadius: CGFloat? = nil, autocompleteForegroundColor: Color? = nil, autocorrectForegroundColor: Color? = nil, autocorrectBackgroundColor: Color? = nil, autocorrectBackgroundCornerRadius: CGFloat? = nil) throws
}

extension KeyboardTheme {

    /// Get all predefined themes.
    public static var predefined: [KeyboardKit.KeyboardTheme] { get }

    /// Whether or not the theme is predefined.
    public var isPredefined: Bool { get }

    /// A color shorthand to simplify defining theme colors.
    public static func rgb(_ r: Int, _ g: Int, _ b: Int) -> Color

    /// Create a copy of the theme, without its author.
    ///
    /// If you don't provide an ID, the new theme will get a
    /// randomly generated, unique ID.
    ///
    /// - Parameters:
    ///   - id: An optional, explicit ID.
    ///   - newName: An optional new name.
    public func copyWithoutAuthor(newId id: UUID? = nil, newName: String? = nil) -> KeyboardKit.KeyboardTheme
}

extension KeyboardTheme {

    /// This view can be used to list theme collections in a
    /// vertical list, with horizontally scrolling shelves.
    ///
    /// You can use ``KeyboardTheme/ShelfItem`` as item view,
    /// to render a theme button preview.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardThemeShelfStyle(_:)``.
    ///
    /// > Note: This view renders a vertical stack without a
    /// scroll view, for maximum versatility. You must add a
    /// scroll view around it if you want it to scroll.
    @MainActor @preconcurrency public struct Shelf<TitleView, ItemView> : View where TitleView : View, ItemView : View {

        /// Create a view that groups the provided themes in
        /// default grouped collections.
        @MainActor @preconcurrency public init(themes: [KeyboardKit.KeyboardTheme], @ViewBuilder title: @escaping (KeyboardKit.KeyboardTheme.Collection) -> TitleView, @ViewBuilder item: @escaping (KeyboardKit.KeyboardTheme) -> ItemView)

        /// Create a view with keyboard theme collections.
        @MainActor @preconcurrency public init(collections: [KeyboardKit.KeyboardTheme.Collection], @ViewBuilder title: @escaping (KeyboardKit.KeyboardTheme.Collection) -> TitleView, @ViewBuilder item: @escaping (KeyboardKit.KeyboardTheme) -> ItemView)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }

    /// This is a regular ``KeyboardTheme/Shelf`` title view.
    @MainActor @preconcurrency public struct ShelfTitle : View {

        @MainActor @preconcurrency public init(_ title: String)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardTheme {

    /// The standard `Standard` theme.
    public static var standard: KeyboardKit.KeyboardTheme { get }

    /// Create an `Standard` theme for the provided style.
    public static func standard(_ style: KeyboardKit.KeyboardTheme.StandardStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/standard`` style variation.
    ///
    /// Standard style variations are meant to mimic the iOS
    /// native keyboard style, but can apply a discrete tint.
    ///
    /// Note that this style has multiple ID properties, one
    /// for each theme that uses it. It's used to avoid that
    /// multiple themes get the same ID.
    public struct StandardStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, minimalId: UUID? = nil, swiftyId: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, tint: Color)

        /// The unique theme component ID.
        public var id: UUID

        public var minimalId: UUID

        public var swiftyId: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var tint: Color

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/standard`` themes.
    public static var predefinedStandard: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// The standard `Minimal` theme.
    public static var minimal: KeyboardKit.KeyboardTheme { get throws }

    /// Create a `Minimal` theme for a standard style.
    ///
    /// This will first apply the standard theme style, then
    /// remove the shapes from all keys.
    public static func minimal(_ style: KeyboardKit.KeyboardTheme.StandardStyle) throws -> KeyboardKit.KeyboardTheme

    /// Create a `Minimal` theme for the provided style.
    public static func minimal(_ style: KeyboardKit.KeyboardTheme.MinimalStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/minimal`` style variation.
    ///
    /// Minimal style variations use colors and gradients to
    /// create minimal keyboards without key shapes.
    public struct MinimalStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, background: KeyboardKit.Keyboard.Background = .standard, buttonForegroundColor: Color? = nil, calloutForegroundColor: Color? = nil, calloutBackgroundColor: Color? = nil, autocorrectBackgroundColor: Color? = nil)

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var background: KeyboardKit.Keyboard.Background

        public var buttonForegroundColor: Color?

        public var calloutForegroundColor: Color?

        public var calloutBackgroundColor: Color?

        public var autocorrectBackgroundColor: Color?

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/minimal`` themes.
    public static var predefinedMinimal: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// The standard `Candy Shop` theme.
    public static var candyShop: KeyboardKit.KeyboardTheme { get throws }

    /// Create a `Candy Shop` theme for the provided style.
    public static func candyShop(_ style: KeyboardKit.KeyboardTheme.CandyShopStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/candyShop`` style variation.
    ///
    /// Candy shop style variations should use bright pastel
    /// colors and round shapes.
    public struct CandyShopStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, background: KeyboardKit.Keyboard.Background, inputForegroundColor: Color?, inputBackgroundColor: Color?, systemForegroundColor: Color?, systemBackgroundColor: Color?, primaryForegroundColor: Color?, primaryBackgroundColor: Color?, buttonShadowColor: Color?, autocorrectBackgroundColor: Color? = nil)

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var background: KeyboardKit.Keyboard.Background

        public var inputForegroundColor: Color?

        public var inputBackgroundColor: Color?

        public var systemForegroundColor: Color?

        public var systemBackgroundColor: Color?

        public var primaryForegroundColor: Color?

        public var primaryBackgroundColor: Color?

        public var buttonShadowColor: Color?

        public var autocorrectBackgroundColor: Color?

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/candyShop`` themes.
    public static var predefinedCandyShop: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// The standard `Swifty` theme.
    public static var swifty: KeyboardKit.KeyboardTheme { get throws }

    /// Create a `Swifty` theme for a standard style.
    ///
    /// This will first apply the standard theme style, then
    /// remove the button shapes for the system keys.
    public static func swifty(_ style: KeyboardKit.KeyboardTheme.StandardStyle) throws -> KeyboardKit.KeyboardTheme
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/swifty`` themes.
    public static var predefinedSwifty: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// This style can be used to modify the visual style of
    /// the ``KeyboardTheme/Shelf`` component.
    ///
    /// You can apply this view style with the view modifier
    /// ``SwiftUICore/View/keyboardThemeShelfStyle(_:)``.
    ///
    /// You can use the ``standard`` style or your own style.
    public struct ShelfStyle {

        /// Create a keyboard theme shelf view style.
        ///
        /// - Parameters:
        ///   - listSpacing: The spacing between shelves, by default `30`.
        ///   - shelfSpacing: The spacing between shelf items, by default `20`.
        ///   - itemPadding: The item button padding, by default `10`.
        ///   - itemMinHeight: The min height to apply to items, by default `80`.
        ///   - itemCornerRadius: The corner radius to apply to items, by default `12`.
        public init(listSpacing: Double = 30, shelfSpacing: Double = 20, itemPadding: Double = 10, itemMinHeight: Double = 80, itemCornerRadius: Double = 12)

        /// The spacing between shelves.
        public var listSpacing: Double

        /// The spacing between shelf items.
        public var shelfSpacing: Double

        /// The item button padding.
        public var itemPadding: Double

        /// The min height to apply to items.
        public var itemMinHeight: Double

        /// The corner radius to apply to items.
        public var itemCornerRadius: Double
    }
}

extension KeyboardTheme {

    /// The standard `Tron` theme.
    public static var tron: KeyboardKit.KeyboardTheme { get throws }

    /// Create a `Tron` theme for the provided style.
    public static func tron(_ style: KeyboardKit.KeyboardTheme.TronStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/tron`` style variation.
    ///
    /// Tron style variations have black backgrounds and use
    /// a single colors that it applies as a signature color.
    public struct TronStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, tint: Color)

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var tint: Color

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/tron`` themes.
    public static var predefinedTron: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// This struct defines a theme author.
    public struct Author : KeyboardKit.KeyboardModel {

        /// Create a theme author value.
        public init(name: String, about: String? = nil, url: String? = nil)

        /// The name of the author.
        public var name: String

        /// Some information about the author, if any.
        public var about: String?

        /// The URL to the author website/socials, if any.
        public var url: String?

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardTheme.Author, b: KeyboardKit.KeyboardTheme.Author) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This enum defines various button types.
    public enum ButtonType : KeyboardKit.KeyboardModel, CaseIterable {

        /// Input buttons are the light ones that enter text.
        case input

        /// System buttons are darker and trigger actions.
        case system

        /// Primary buttons are the prominent return buttons.
        case primary

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.KeyboardTheme.ButtonType, b: KeyboardKit.KeyboardTheme.ButtonType) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.KeyboardTheme.ButtonType]

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.KeyboardTheme.ButtonType] { get }

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension KeyboardTheme {

    /// This view can be used as a default shelf item within
    /// a ``KeyboardTheme/Shelf``.
    ///
    /// You can style this component with the style modifier
    /// ``keyboardThemeShelfStyle(_:)``.
    @MainActor @preconcurrency public struct ShelfItem : View {

        /// Create a shelf item for the provded theme.
        @MainActor @preconcurrency public init(theme: KeyboardKit.KeyboardTheme)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardTheme {

    /// The standard `Neon` theme.
    public static var neon: KeyboardKit.KeyboardTheme { get throws }

    /// Create an `Neon` theme for the provided style.
    public static func neon(_ style: KeyboardKit.KeyboardTheme.NeonStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/neon`` style variation.
    ///
    /// Neon style variations should be dark and use vibrant
    /// button border colors.
    public struct NeonStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, buttonBorderColor: Color = rgb(105, 194, 106), buttonShadowColor: Color = rgb(236, 54, 250), systemBackground: Color = rgb(140, 140, 250), primaryBackground: Color = rgb(140, 140, 250))

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var buttonBorderColor: Color

        public var buttonShadowColor: Color

        public var systemBackground: Color

        public var primaryBackground: Color

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/neon`` themes.
    public static var predefinedNeon: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// Create an `Colorful` theme for the provided style.
    public static func colorful(_ style: KeyboardKit.KeyboardTheme.ColorfulStyle) throws -> KeyboardKit.KeyboardTheme

    /// A ``KeyboardTheme/colorful(_:)`` style variation.
    ///
    /// Colorful style variations can apply a dominant color
    /// and vary key opacities based on it.
    public struct ColorfulStyle : KeyboardKit.KeyboardThemeStyleVariation {

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, color: Color)

        public init(id: UUID? = nil, name: String, author: KeyboardKit.KeyboardTheme.Author? = .keyboardKit, background: KeyboardKit.Keyboard.Background, foregroundColor: Color = .white, buttonColor: Color = .white, inputOpacity: CGFloat = 0.2, systemOpacity: CGFloat = 0.3, primaryOpacity: CGFloat = 0.4, autocompleteOpacity: CGFloat = 0.2, calloutBackgroundColor: Color? = nil)

        /// The unique theme component ID.
        public var id: UUID

        /// The name of the theme component.
        public var name: String

        public var author: KeyboardKit.KeyboardTheme.Author?

        public var background: KeyboardKit.Keyboard.Background

        public var foregroundColor: Color

        public var buttonColor: Color

        public var inputOpacity: CGFloat

        public var systemOpacity: CGFloat

        public var primaryOpacity: CGFloat

        public var autocompleteOpacity: CGFloat

        public var calloutBackgroundColor: Color?

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = UUID
    }
}

extension KeyboardTheme {

    /// Get all ``KeyboardTheme/colorful(_:)`` themes.
    public static var predefinedColorful: [KeyboardKit.KeyboardTheme] { get throws }
}

extension KeyboardTheme {

    /// An emoji keyboard style that is based on this theme.
    public var emojiKeyboardStyle: KeyboardKit.EmojiKeyboard.Style { get }
}

extension KeyboardTheme {

    /// The keyboard view style to apply.
    public var keyboardViewStyle: KeyboardKit.KeyboardViewStyle? { get }

    /// The button style to use for the provided action.
    public func keyboardButtonStyle(for action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext, isPressed: Bool) -> KeyboardKit.Keyboard.ButtonStyle
}

extension KeyboardTheme {

    /// This type can be used to configure the themes listed
    /// in a ``KeyboardTheme/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/themeSettingsScreenStyle(_:)``.
    public struct SettingsScreenStyle {

        /// Create a custom theme screen style.
        ///
        /// - Parameters:
        ///   - itemSize: The size of the picker items, by default `125`.
        ///   - checkmarkAlignment: The checkmark alignment, by default `.topLeading`.
        ///   - checkmarkColor: The checkmark color, by default `.green`.
        public init(itemSize: Double = 125, checkmarkAlignment: Alignment = .topLeading, checkmarkColor: Color = .green)

        /// The size of the picker items.
        public var itemSize: Double

        /// The checkmark alignment.
        public var checkmarkAlignment: Alignment

        /// The checkmark color.
        public var checkmarkColor: Color
    }
}

extension KeyboardTheme {

    /// This type can be used to configure the localizations
    /// of a ``KeyboardTheme/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/themeSettingsScreenLocalization(_:)``.
    public struct SettingsScreenLocalization {

        public init(title: String = "Pick a theme")

        public var title: String
    }
}

extension KeyboardTheme {

    /// This type can be used to configure the themes listed
    /// in a ``KeyboardTheme/SettingsScreen``.
    ///
    /// You can apply a custom value using the view modifier
    /// ``SwiftUICore/View/themeSettingsScreenThemes(_:)``.
    public struct SettingsScreenThemes {

        public init(themes: [KeyboardKit.KeyboardTheme] = .predefined)

        public var themes: [KeyboardKit.KeyboardTheme]
    }
}

extension KeyboardTheme {

    /// This screen can be used as the theme settings screen
    /// in your app or keyboard extension.
    ///
    /// This screen lists all themes in a vertical list with
    /// horizontal shelves.
    ///
    /// The screen can be modified with these view modifiers:
    ///
    /// * ``themeSettingsScreenLocalization(_:)``
    /// * ``themeSettingsScreenStyle(_:)``
    /// * ``themeSettingsScreenThemes(_:)``
    ///
    /// > Important: This requires that your license unlocks
    ///  the ``ProFeature/settingsScreens`` feature.
    @MainActor @preconcurrency public struct SettingsScreen : View {

        /// Create a keyboard app locale screen.
        @MainActor @preconcurrency public init()

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        public typealias Body = some View
    }
}

extension KeyboardTheme.BlueprintStyle {

    /// Get all ``KeyboardTheme/blueprint`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.BlueprintStyle]

    /// This style is neutral blue.
    public static let standard: KeyboardKit.KeyboardTheme.BlueprintStyle

    /// This style is a dark blueprint.
    public static let dark: KeyboardKit.KeyboardTheme.BlueprintStyle
}

extension KeyboardTheme.Shelf : Sendable {
}

extension KeyboardTheme.ShelfTitle : Sendable {
}

extension KeyboardTheme.StandardStyle {

    /// Get all ``KeyboardTheme/standard`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.StandardStyle]

    /// This style has no background tint.
    public static let standard: KeyboardKit.KeyboardTheme.StandardStyle

    /// A blue standard style.
    public static let blue: KeyboardKit.KeyboardTheme.StandardStyle

    /// A green standard style.
    public static let green: KeyboardKit.KeyboardTheme.StandardStyle

    /// A pink standard style.
    public static let pink: KeyboardKit.KeyboardTheme.StandardStyle

    /// A purple standard style.
    public static let purple: KeyboardKit.KeyboardTheme.StandardStyle

    /// A yellow standard style.
    public static let yellow: KeyboardKit.KeyboardTheme.StandardStyle
}

extension KeyboardTheme.MinimalStyle {

    /// Get all ``KeyboardTheme/minimal`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.MinimalStyle]

    /// This style uses a dark blue to black color scheme.
    public static let midnight: KeyboardKit.KeyboardTheme.MinimalStyle

    /// This style uses a red to orange color scheme.
    public static let sunset: KeyboardKit.KeyboardTheme.MinimalStyle
}

extension KeyboardTheme.CandyShopStyle {

    /// Get all ``KeyboardTheme/candyShop`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.CandyShopStyle]

    /// A pink and blue cotton candy-inspired style.
    public static let cottonCandy: KeyboardKit.KeyboardTheme.CandyShopStyle

    /// A cupcake frosting-inspired candy style.
    public static let cuppyCake: KeyboardKit.KeyboardTheme.CandyShopStyle
}

extension KeyboardTheme.ShelfStyle {

    public static var standard: KeyboardKit.KeyboardTheme.ShelfStyle { get }
}

extension KeyboardTheme.TronStyle {

    /// Get all ``KeyboardTheme/tron`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.TronStyle]

    /// This style is neutral blue.
    public static let standard: KeyboardKit.KeyboardTheme.TronStyle

    /// This style is purple.
    public static let fCon: KeyboardKit.KeyboardTheme.TronStyle

    /// This style is green.
    public static let military: KeyboardKit.KeyboardTheme.TronStyle

    /// This style is white.
    public static let neutral: KeyboardKit.KeyboardTheme.TronStyle

    /// This style is red.
    public static let sark: KeyboardKit.KeyboardTheme.TronStyle

    /// This style is yellow.
    public static let virus: KeyboardKit.KeyboardTheme.TronStyle
}

extension KeyboardTheme.Author {

    public static let keyboardKit: KeyboardKit.KeyboardTheme.Author
}

extension KeyboardTheme.Author {

    /// Whether or not the author has any about info.
    public var hasAboutInfo: Bool { get }

    /// Whether or not the author has a non-empty name.
    public var hasName: Bool { get }

    /// The author's display name, with a fallback value.
    public func displayName(fallback: String) -> String
}

extension KeyboardTheme.ShelfItem : Sendable {
}

extension KeyboardTheme.NeonStyle {

    /// Get all ``KeyboardTheme/neon`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.NeonStyle]

    /// This is a dark metropol-at-night inspired style.
    public static let night: KeyboardKit.KeyboardTheme.NeonStyle
}

extension KeyboardTheme.ColorfulStyle {

    /// Get all ``KeyboardTheme/colorful(_:)`` styles.
    public static let predefined: [KeyboardKit.KeyboardTheme.ColorfulStyle]

    public static let black: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let blue: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let brown: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let cyan: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let green: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let indigo: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let mint: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let orange: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let pink: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let purple: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let red: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let teal: KeyboardKit.KeyboardTheme.ColorfulStyle

    public static let yellow: KeyboardKit.KeyboardTheme.ColorfulStyle
}

extension KeyboardTheme.SettingsScreenStyle {

    public static var standard: KeyboardKit.KeyboardTheme.SettingsScreenStyle { get }
}

extension KeyboardTheme.SettingsScreenLocalization {

    public static var standard: KeyboardKit.KeyboardTheme.SettingsScreenLocalization { get }
}

extension KeyboardTheme.SettingsScreenThemes {

    public static var standard: KeyboardKit.KeyboardTheme.SettingsScreenThemes { get }
}

extension KeyboardTheme.SettingsScreen : Sendable {
}

/// This class has observable states and persistent settings
/// for keyboard-specific themes.
///
/// KeyboardKit will create an instance of this context when
/// the keyboard launches and inject it into the environment,
/// as described in <doc:Getting-Started-Article>.
///
/// The ``settings`` property can be used to handle settings,
/// like the current ``KeyboardThemeSettings/theme``.
final public class KeyboardThemeContext : ObservableObject {

    /// Create a keyboard theme context instance.
    public init()

    /// Theme-specific, auto-persisted settings.
    @Published final public var settings: KeyboardKit.KeyboardThemeSettings

    final public var $settings: Published<KeyboardKit.KeyboardThemeSettings>.Publisher

    /// The last theme changed date, if any.
    @Published final public var themeChanged: TimeInterval

    final public var $themeChanged: Published<TimeInterval>.Publisher

    /// The type of publisher that emits before the object has changed.
    public typealias ObjectWillChangePublisher = ObservableObjectPublisher

    @objc deinit
}

extension KeyboardThemeContext {

    /// The current ``settings`` theme.
    final public var currentTheme: KeyboardKit.KeyboardTheme?

    /// A current ``settings`` theme binding.
    final public var currentThemeBinding: Binding<KeyboardKit.KeyboardTheme?> { get }

    /// Reset the current ``KeyboardThemeSettings/theme``.
    final public func resetCurrentTheme()

    /// Set the current ``KeyboardThemeSettings/theme``.
    final public func setCurrentTheme(_ theme: KeyboardKit.KeyboardTheme?)
}

extension KeyboardThemeContext {

    public static var preview: KeyboardKit.KeyboardThemeContext { get }
}

/// This protocol is implemented by pro theme components, to
/// make it possible to create mutable copies.
public protocol KeyboardThemeCopyable : Identifiable where Self.ID == UUID {

    /// The unique theme component ID.
    override var id: UUID { get set }

    /// The name of the theme component.
    var name: String { get set }
}

extension KeyboardThemeCopyable {

    /// Create a copy of this theme component.
    ///
    /// If you don't provide an ID, the new value will get a
    /// random generated, unique ID.
    ///
    /// - Parameters:
    ///   - id: An optional, explicit ID.
    ///   - newName: An optional new name.
    public func copy(newId id: UUID? = nil, newName: String? = nil) -> Self
}

/// This type has auto-persisted theme settings.
///
/// All properties for this type are automatically stored in
/// ``KeyboardSettings/store`` with a `themes` prefix.
///
/// Use the ``theme`` property to get the selected theme. It
/// will persist values using the ``themeValue`` property.
///
/// Due to an observation issue that caused SwiftUI views to
/// not update when a ``theme`` was changed, the context can
/// also be used to change the theme for now.
public struct KeyboardThemeSettings {

    /// Create a custom settings instance.
    public init()

    /// The settings key prefix to use for the namespace.
    public static var settingsPrefix: String { get }

    /// The current theme, if any.
    public var theme: KeyboardKit.KeyboardTheme? { get }

    /// The current theme storage value, if any.
    @AppStorage public var themeValue: KeyboardKit.Keyboard.StorageValue<KeyboardKit.KeyboardTheme?> { get nonmutating set }

    public var $themeValue: Binding<KeyboardKit.Keyboard.StorageValue<KeyboardKit.KeyboardTheme?>> { get }
}

extension KeyboardThemeSettings {

    public static var preview: KeyboardKit.KeyboardThemeSettings { get }
}

/// This protocol can be implemented by theme-specific style
/// variations, to ensure that a style creates unique copies.
public protocol KeyboardThemeStyleVariation : KeyboardKit.KeyboardThemeCopyable {
}

/// This view renders a view that mimics the native keyboard.
///
/// This view will by default render the standard layout for
/// the current ``KeyboardContext``, but you can pass in any
/// custom ``KeyboardLayout``, by either make customizations
/// to the ``KeyboardLayout/standard(for:)`` keyboard layout,
/// or by creating a completely custom layout.
///
/// To customize the standard layout, just create a standard
/// one with ``KeyboardLayout/standard(for:)`` and modify it
/// in any way you want, using its mutating modifiers.
///
/// See the <doc:Essentials-KeyboardView> article for a more
/// extensive guide on how to use and customize this view.
@MainActor @preconcurrency public struct KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar> : View where ButtonContent : View, ButtonView : View, CollapsedView : View, EmojiKeyboardView : View, Toolbar : View {

    /// Create a keyboard based on state and services.
    ///
    /// - Parameters:
    ///   - layout: A custom keyboard layout, if any.
    ///   - services: The keyboard services to use.
    ///   - buttonContent: The content view to use for buttons.
    ///   - buttonView: The button view to use for an buttons.
    ///   - collapsedView: The collapsed view to use.
    ///   - emojiKeyboard: The emoji keyboard to use.
    ///   - toolbar: The toolbar view to add above the keyboard.
    @MainActor @preconcurrency public init(layout: KeyboardKit.KeyboardLayout? = nil, services: KeyboardKit.Keyboard.Services, @ViewBuilder buttonContent: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonContentBuilder, @ViewBuilder buttonView: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonViewBuilder, @ViewBuilder collapsedView: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.CollapsedViewBuilder, @ViewBuilder emojiKeyboard: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.EmojiKeyboardBuilder, @ViewBuilder toolbar: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ToolbarBuilder)

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension KeyboardView {

    /// This typealias defines a button content builder.
    public typealias ButtonContentBuilder = (KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonContentParams) -> ButtonContent

    /// This typealias defines button content builder params.
    public typealias ButtonContentParams = (item: KeyboardKit.KeyboardLayout.Item, view: KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.StandardButtonContent)

    /// The standard button content view type.
    public typealias StandardButtonContent = KeyboardKit.Keyboard.ButtonContent

    /// This typealias defines a button view builder.
    public typealias ButtonViewBuilder = (KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonViewParams) -> ButtonView

    /// This typealias defines button view builder params.
    public typealias ButtonViewParams = (item: KeyboardKit.KeyboardLayout.Item, view: KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.StandardButtonView)

    /// The standard button view type.
    public typealias StandardButtonView = KeyboardKit.KeyboardViewItem<ButtonContent>
}

extension KeyboardView where ButtonContent == KeyboardKit.Keyboard.ButtonContent, ButtonView == KeyboardKit.KeyboardViewItem<KeyboardKit.Keyboard.ButtonContent>, CollapsedView == KeyboardKit.Keyboard.CollapsedView<EmptyView>, EmojiKeyboardView == KeyboardKit.EmojiKeyboard, Toolbar == KeyboardKit.Autocomplete.Toolbar<KeyboardKit.Autocomplete.ToolbarItem, KeyboardKit.Autocomplete.ToolbarSeparator> {

    /// Create a keyboard based on state and services, using
    /// standard button and component views.
    ///
    /// - Parameters:
    ///   - layout: A custom keyboard layout, if any.
    ///   - services: The keyboard services to use.
    @MainActor @preconcurrency public init(layout: KeyboardKit.KeyboardLayout? = nil, services: KeyboardKit.Keyboard.Services)

    /// Create a keyboard based on state and services, using
    /// custom button and standard component views.
    ///
    /// - Parameters:
    ///   - layout: A custom keyboard layout, if any.
    ///   - services: The keyboard services to use.
    ///   - buttonContent: The content view to use for buttons.
    ///   - buttonView: The button view to use for an buttons.
    @MainActor @preconcurrency public init(layout: KeyboardKit.KeyboardLayout? = nil, services: KeyboardKit.Keyboard.Services, @ViewBuilder buttonContent: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonContentBuilder, @ViewBuilder buttonView: @escaping KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ButtonViewBuilder)
}

extension KeyboardView {

    /// This typealias defines a collapsed view builder.
    public typealias CollapsedViewBuilder = (KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.CollapsedViewParams) -> CollapsedView

    /// This struct defines collapsed view builder params.
    public typealias CollapsedViewParams = (binding: Binding<Bool>, view: KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.StandardCollapsedView)

    /// The standard collapsed view.
    public typealias StandardCollapsedView = KeyboardKit.Keyboard.CollapsedView<EmptyView>

    /// This typealias defines an emoji keyboard builder.
    public typealias EmojiKeyboardBuilder = (KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.EmojiKeyboardParams) -> EmojiKeyboardView

    /// This typealias defines emoji keyboard builder params.
    public typealias EmojiKeyboardParams = (style: KeyboardKit.EmojiKeyboard.Style, view: KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.StandardEmojiKeyboard)

    /// The standard emoji keyboard view.
    public typealias StandardEmojiKeyboard = KeyboardKit.EmojiKeyboard

    /// This typealias defines a toolbar builder.
    public typealias ToolbarBuilder = (KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.ToolbarParams) -> Toolbar

    /// This typealias defines toolbar builder params.
    public typealias ToolbarParams = (autocompleteAction: (KeyboardKit.Autocomplete.Suggestion) -> Void, style: KeyboardKit.Autocomplete.ToolbarStyle, view: KeyboardKit.KeyboardView<ButtonContent, ButtonView, CollapsedView, EmojiKeyboardView, Toolbar>.StandardToolbar)

    /// The standard toolbar view.
    public typealias StandardToolbar = KeyboardKit.Autocomplete.Toolbar<KeyboardKit.Autocomplete.ToolbarItem, KeyboardKit.Autocomplete.ToolbarSeparator>
}

extension KeyboardView : Sendable {
}

/// This view renders a ``KeyboardView`` item view.
///
/// Unlike ``Keyboard/Button`` this view applies more insets
/// and configurations to make it work in a ``KeyboardView``.
///
/// You can style the component with the style view modifier
/// ``keyboardButtonStyle(builder:)``.
@MainActor @preconcurrency public struct KeyboardViewItem<Content> : View where Content : View {

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension KeyboardViewItem : Sendable {
}

/// This view can be used to preview a ``KeyboardView`` with
/// various actions, themes and services.
///
/// The view renders a full keyboard and is therefore not as
/// performant as e.g. ``Keyboard/ButtonPreview``. Make sure
/// to only render a single preview at a time.
///
/// You can set the ``KeyboardViewPreview/toolbar`` property
/// to adjust which toolbar to use.
///
/// > Important: This requires that your license unlocks the
/// ``ProFeature/proPreviews`` feature.
@MainActor @preconcurrency public struct KeyboardViewPreview : View {

    /// Create a keyboard view preview.
    ///
    /// - Parameters:
    ///   - returnKeyType: The primary button type, by default `.return`.
    ///   - autocompleteSuggestions: The suggestions to show, by default `.preview`.
    @MainActor @preconcurrency public init(returnKeyType: KeyboardKit.Keyboard.ReturnKeyType? = nil, autocompleteSuggestions: [KeyboardKit.Autocomplete.Suggestion]? = nil)

    public typealias ThemeBuilder = () throws -> KeyboardKit.KeyboardTheme

    /// The content and behavior of the view.
    ///
    /// When you implement a custom view, you must implement a computed
    /// `body` property to provide the content for your view. Return a view
    /// that's composed of built-in views that SwiftUI provides, plus other
    /// composite views that you've already defined:
    ///
    ///     struct MyView: View {
    ///         var body: some View {
    ///             Text("Hello, World!")
    ///         }
    ///     }
    ///
    /// For more information about composing views and a view hierarchy,
    /// see <doc:Declaring-a-Custom-View>.
    @MainActor @preconcurrency public var body: some View { get }

    /// The type of view representing the body of this view.
    ///
    /// When you create a custom view, Swift infers this type from your
    /// implementation of the required ``View/body-swift.property`` property.
    public typealias Body = some View
}

extension KeyboardViewPreview {

    /// Set this to replace the toolbar shown in the preview.
    @MainActor @preconcurrency public static var toolbar: KeyboardKit.KeyboardViewPreview.ToolbarReplacement?

    /// A typealias used for toolbar replacement.
    public typealias ToolbarReplacement = (KeyboardKit.Autocomplete.ToolbarStyle) -> AnyView
}

extension KeyboardViewPreview : Sendable {
}

/// This style can be used to style a ``KeyboardView``.
///
/// The static ``KeyboardViewStyle/standard`` can be used to
/// create a standard keyboard view style.
///
/// Use the ``SwiftUICore/View/keyboardViewStyle(_:)`` style
/// modifier to apply a custom style.
///
/// Note that while ``KeyboardContext`` allows us to disable
/// the liquid glass *keyboard* design, the style still uses
/// the true value to determine the corner radius, since the
/// keyboard system frame can't be changed.
public struct KeyboardViewStyle : Equatable, Sendable {

    /// Create a custom keyboard view style.
    ///
    /// You can use the ``SwiftUICore/EdgeInsets`` extension
    /// ``SwiftUICore/EdgeInsets/standardKeyboardEdgeInsets(for:)``
    /// to get standard edge insets for any keyboard context.
    ///
    /// - Parameters:
    ///   - background: The background style, if any.
    ///   - backgroundCornerRadiusTop: The top background corner radius, if any.
    ///   - backgroundCornerRadiusBottom: The bottom background corner radius, if any.
    ///   - foregroundColor: The foreground color, if any.
    ///   - edgeInsets: The custom edge insets, if any.
    public init(background: KeyboardKit.Keyboard.Background? = nil, backgroundCornerRadiusTop: Double? = nil, backgroundCornerRadiusBottom: Double? = nil, foregroundColor: Color? = nil, edgeInsets: EdgeInsets? = nil)

    /// The background style.
    public var background: KeyboardKit.Keyboard.Background?

    /// The top background corner radius.
    public var backgroundCornerRadiusTop: Double?

    /// The bottom background corner radius.
    public var backgroundCornerRadiusBottom: Double?

    /// The foreground color.
    public var foregroundColor: Color?

    /// The custom edge insets.
    public var edgeInsets: EdgeInsets?

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.KeyboardViewStyle, b: KeyboardKit.KeyboardViewStyle) -> Bool
}

extension KeyboardViewStyle {

    /// Create a standard keyboard view style.
    public static var standard: KeyboardKit.KeyboardViewStyle { get }
}

/// This struct represents a software license and also works
/// as a namespace for license-related types.
///
/// A license can specify various information and conditions,
/// such as customer, expiration date, tier, the bundles and
/// platforms it can be used with, which features it unlocks,
/// etc. You can also define additional info.
///
/// The various dates let you tailor your license conditions.
///
/// * The ``activationDate`` is when the license was first activated.
/// * The ``billingDate`` is when the next invoice should be sent.
/// * The ``renewalDate`` is when the license should be renewed.
/// * The ``expirationDate`` is when the license will stop working.
///
/// The ``renewalDate`` is the date that you can communicate
/// as the ``expirationDate`` to your customers, but you can
/// use a more generous internal ``expirationDate`` to allow
/// more time before the license actually expires.
///
/// This type has many validation functions that can be used
/// to validate if it is valid, if it is valid for a certain
/// bundle, environment, platform, date, etc.
public struct License : Codable, Equatable, Hashable, Identifiable, Sendable {

    /// Create a new license.
    ///
    /// - Parameters:
    ///   - licenseKey: The unique license key.
    ///   - name: The name of the licensed product, if any.
    ///   - source: The source of the license, if any.
    ///   - customer: The license customer, if any, if any.
    ///   - tier: The license tier, by default `.standard`.
    ///   - activationDate: The date when the license was first activated, if any.
    ///   - billingDate: The date when the next invoide should be issued, if any.
    ///   - renewalDate: The date when the license should be renewed, if any.
    ///   - expirationDate: The date when the license expires, if any.
    ///   - expirationProductVersion: The product version whe the license expires, if any.
    ///   - renewalPrice: The license renewal price, if any.
    ///   - bundleIds: The bundle IDs for which the license is valid, by default `empty`.
    ///   - platforms: The platforms for which the license is valid, by default ``Platform/all``.
    ///   - environments: The environments for which the license is valid, by default ``Environment/all``.
    ///   - features: The features license gives access to, by default `empty`.
    ///   - featureIds: The feature IDs the license gives access to, by default `empty`.
    ///   - additionalInfo: Additional customer information, if any.
    public init(licenseKey: String, name: String? = nil, source: KeyboardKit.License.Source? = nil, customer: KeyboardKit.License.Customer? = nil, tier: KeyboardKit.License.Tier = .standard, activationDate: Date? = nil, billingDate: Date? = nil, renewalDate: Date? = nil, expirationDate: Date? = nil, expirationProductVersion: Double? = nil, renewalPrice: Double? = nil, bundleIds: [String] = [], platforms: [KeyboardKit.License.Platform] = .all, environments: [KeyboardKit.License.Environment] = .all, features: [any KeyboardKit.LicenseFeature] = [], featureIds: [String] = [], additionalInfo: [String : String] = [:])

    /// The unique license id.
    public var id: String { get }

    /// The bundle IDs for which the license is valid, if any.
    public var bundleIds: [String]

    /// The license customer, if any.
    public var customer: KeyboardKit.License.Customer?

    /// The date when the license was activated, if any.
    public var activationDate: Date?

    /// The date when the license should be billed, if any.
    public var billingDate: Date?

    /// The date when the license should be renewed, if any.
    public var renewalDate: Date?

    /// The date when the license expires, if any.
    public var expirationDate: Date?

    /// The max supported product version, if any
    public var expirationProductVersion: Double?

    /// The feature IDs the license gives access to.
    public var featureIds: [String]

    /// The unique license key
    public var licenseKey: String

    /// The name of the license, app etc.
    public var name: String?

    /// The platforms for which the license is valid.
    public var platforms: [KeyboardKit.License.Platform]

    /// The environments for which the license is valid.
    public var environments: [KeyboardKit.License.Environment]

    /// The license renewal price, if any.
    public var renewalPrice: Double?

    /// The source of the license.
    public var source: KeyboardKit.License.Source?

    /// The license tier.
    public var tier: KeyboardKit.License.Tier

    /// Additional license information, if any.
    public var additionalInfo: [String : String]

    /// Returns a Boolean value indicating whether two values are equal.
    ///
    /// Equality is the inverse of inequality. For any values `a` and `b`,
    /// `a == b` implies that `a != b` is `false`.
    ///
    /// - Parameters:
    ///   - lhs: A value to compare.
    ///   - rhs: Another value to compare.
    public static func == (a: KeyboardKit.License, b: KeyboardKit.License) -> Bool

    /// A type representing the stable identity of the entity associated with
    /// an instance.
    public typealias ID = String

    /// Encodes this value into the given encoder.
    ///
    /// If the value fails to encode anything, `encoder` will encode an empty
    /// keyed container in its place.
    ///
    /// This function throws an error if any values are invalid for the given
    /// encoder's format.
    ///
    /// - Parameter encoder: The encoder to write data to.
    public func encode(to encoder: any Encoder) throws

    /// Hashes the essential components of this value by feeding them into the
    /// given hasher.
    ///
    /// Implement this method to conform to the `Hashable` protocol. The
    /// components used for hashing must be the same as the components compared
    /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
    /// with each of these components.
    ///
    /// - Important: In your implementation of `hash(into:)`,
    ///   don't call `finalize()` on the `hasher` instance provided,
    ///   or replace it with a different instance.
    ///   Doing so may become a compile-time error in the future.
    ///
    /// - Parameter hasher: The hasher to use when combining the components
    ///   of this instance.
    public func hash(into hasher: inout Hasher)

    /// The hash value.
    ///
    /// Hash values are not guaranteed to be equal across different executions of
    /// your program. Do not save hash values to use during a future execution.
    ///
    /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
    ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
    ///   The compiler provides an implementation for `hashValue` for you.
    public var hashValue: Int { get }

    /// Creates a new instance by decoding from the given decoder.
    ///
    /// This initializer throws an error if reading from the decoder fails, or
    /// if the data read is corrupted or otherwise invalid.
    ///
    /// - Parameter decoder: The decoder to read data from.
    public init(from decoder: any Decoder) throws
}

extension License {

    /// This struct represents a license customer.
    ///
    /// This type is optional, but can provide your licenses
    /// with more information, group licenses, etc.
    public struct Customer : Codable, Equatable, Hashable, Sendable {

        /// Create a new license customer instance.
        ///
        /// - Parameters:
        ///   - name: The name of the customer.
        ///   - fullName: The full customer name.
        ///   - contact: The customer's contact person, if any.
        ///   - address: The customer address, if any.
        ///   - email: The customer e-mail, if any.
        ///   - phone: The customer phone number, if any.
        ///   - website: The customer website, if any.
        ///   - additionalInfo: Additional customer information, if any.
        public init(name: String, fullName: String? = nil, contact: String? = nil, address: String? = nil, email: String? = nil, phone: String? = nil, website: String? = nil, additionalInfo: [String : String] = [:])

        /// The name of the customer.
        public let name: String

        /// The full, legal name of the customer.
        public let fullName: String?

        /// The customer's contact person, if any.
        public let contact: String?

        /// The customer address, if any.
        public let address: String?

        /// The customer e-mail, if any.
        public let email: String?

        /// The customer phone number, if any.
        public let phone: String?

        /// The customer website, if any.
        public let website: String?

        /// Additional customer information, if any.
        public let additionalInfo: [String : String]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.Customer, b: KeyboardKit.License.Customer) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension License {

    /// Get the license that is stored in a ``LicenseStore``.
    public static func current(in store: KeyboardKit.LicenseStore) -> KeyboardKit.License?
}

extension License {

    /// The currently registered license, if any.
    public static var current: KeyboardKit.License? { get }

    /// Whether or not a registered license exists.
    public static var hasCurrent: Bool { get }
}

extension License {

    /// This enum defines various license sources from which
    /// licenses can be read or fetched.
    ///
    /// This enum is not used within the library, but can be
    /// used for configurations. See the demo for examples.
    public enum Source : CaseIterable, Codable, Hashable, Identifiable, Sendable {

        case api

        case binary

        case file

        case gumroad

        case licensekit

        case other(name: String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.Source, b: KeyboardKit.License.Source) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.License.Source]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension License {

    /// This enum defines various runtime platforms, such as
    /// ``iOS``, ``macOS``, ``tvOS``, etc.
    ///
    /// This means that your license model can use different
    /// prices for the number of platforms that are included.
    public enum Platform : CaseIterable, Codable, Equatable, Hashable, Identifiable, Sendable {

        case iOS

        case macOS

        case tvOS

        case watchOS

        case visionOS

        case custom(name: String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.Platform, b: KeyboardKit.License.Platform) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.License.Platform]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension License {

    /// This struct represents a tier group that can be used
    /// to group tiers that go well together.
    public struct TierGroup : Codable, Equatable, Hashable, Identifiable, Sendable {

        public init(name: String, tiers: [KeyboardKit.License.Tier])

        /// The unique group ID.
        public var id: String { get }

        /// The name of the group.
        public let name: String

        /// The tiers in the group.
        public let tiers: [KeyboardKit.License.Tier]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.TierGroup, b: KeyboardKit.License.TierGroup) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This struct represents a license tier, which defines
    /// the level of service that a license provides.
    ///
    /// A license tier has a unique id, a name and a numeric
    /// level that indicates the level of service for a tier,
    /// relative to other tiers in the same license model.
    ///
    /// A ``TierGroup`` can be used to group a list of tiers
    /// that go well together.
    public struct Tier : Codable, Equatable, Hashable, Identifiable, Sendable {

        /// Create a custom license tier.
        ///
        /// - Parameters:
        ///   - name: The name of the tier.
        ///   - level: The level of service that the tier provides.
        public init(name: String, level: Int)

        /// Resolve a tier from ``allPredefined`` based on a
        /// case-insensitive name match.
        ///
        /// This can be used to resolve tiers when parsing a
        /// license from file, fetching it from an API, etc.
        ///
        /// - Parameters:
        ///   - name: The name of the tier.
        public init?(name: String)

        /// The unique tier ID.
        public var id: String { get }

        /// The name of the tier.
        public let name: String

        /// The level of service that the tier provides.
        public let level: Int

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.Tier, b: KeyboardKit.License.Tier) -> Bool

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension License {

    /// This enum defines various runtime environments, such
    /// as ``test``, ``staging``, ``production``, etc.
    ///
    /// This means that your license model can use different
    /// prices for the number of supported environments.
    ///
    /// You can define your own custom environments with the
    /// ``custom(name:)`` case.
    public enum Environment : CaseIterable, Codable, Equatable, Hashable, Identifiable, Sendable {

        case test

        case staging

        case production

        case custom(name: String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.Environment, b: KeyboardKit.License.Environment) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.License.Environment]

        /// A type representing the stable identity of the entity associated with
        /// an instance.
        public typealias ID = String

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension License {

    /// Whether the license has a certain feature.
    public func hasFeature(_ feature: any KeyboardKit.LicenseFeature) -> Bool

    /// Whether the license matches a certain search query.
    public func matches(_ query: String) -> Bool
}

extension License {

    /// Try to register a KeyboardKit Pro license, using the
    /// provided license key or reading a local license file.
    ///
    /// The provided locales will be capped to the number of
    /// locales that is supported by the license.
    ///
    /// - Parameters:
    ///   - licenseKey: The license key to register, if any.
    ///   - locales: The locales to unlock, by default `nil`.
    ///
    /// - Returns: The matching, validated license, if any.
    public static func register(withLicenseKey licenseKey: String? = nil, locales: [Locale]? = nil) async throws -> KeyboardKit.License?

    /// Unregister the current license, if any.
    public static func unregister()
}

extension License {

    /// All locales that are currently registered.
    ///
    /// This is needed since ``License`` has no property for
    /// this, and extensions cannot define stored properties.
    public static var currentLocales: [Locale]? { get }

    /// All locales that are currently registered.
    public var locales: [Locale] { get }

    /// The max number of locales the license can have.
    public var localeCap: Int { get }

    public var nonLocaleFeatureIds: [String] { get }
}

extension License {

    /// This enum defines license validation errors that can
    /// be thrown when a license key is validated.
    public enum ValidationError : Error, Equatable, LocalizedError {

        /// The license does not support a certain bundle.
        case invalidBundle(String)

        /// The license does not support a certain environment.
        case invalidEnvironment(KeyboardKit.License.Environment)

        /// The license file information was invalid.
        case invalidFileName(String, fileExtension: String, inBundle: Bundle)

        /// The license key did not match an existing license.
        case invalidLicenseKey(String, message: String? = nil)

        /// The license does not support a certain platform.
        case invalidPlatform(KeyboardKit.License.Platform)

        /// The license does not support a certain product version.
        case invalidProductVersion(Double)

        /// The license was validated using an invalid proxy.
        case invalidProxy

        /// The license was validated using an invalid API URL.
        case invalidUrl(String)

        /// The license has expired.
        case licenseExpired(Date)

        /// The license requires a certain feature.
        case licenseFeatureRequired(String)

        /// The license requires a certain feature or tier.
        case licenseFeatureOrTierRequired(String, KeyboardKit.License.Tier)

        /// There is no license registered, when one is required.
        case licenseMissing

        /// The license requires a certain tier.
        case licenseTierRequired(KeyboardKit.License.Tier)

        /// The number of managed licenses are too large.
        case tooManyLicenses(count: Int, allowed: Int)

        /// This error can be thrown for custom errors.
        case other(reason: String)

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.License.ValidationError, b: KeyboardKit.License.ValidationError) -> Bool
    }
}

extension License {

    /// Days until the license expires.
    ///
    /// This uses the ``expirationDate`` and does not regard
    /// any grace periods.
    public var daysUntilExpiration: Int? { get }

    /// Whether the license has expired.
    ///
    /// This uses the ``expirationDate`` and does not regard
    /// any grace periods.
    public var hasExpired: Bool { get }
}

extension License {

    /// Validate the provided license, if any.
    ///
    /// This function can be used to first validate that the
    /// provided license isn't nil, then validate it for the
    /// provided bundle, date, and platform.
    ///
    /// The function will then use the `additionalValidation`
    /// blocks you provide to perform any custom validations
    /// that you may want to perform.
    ///
    /// Note that providing a validation criteria will cause
    /// the license validation to fail if a license does not
    /// specify that information. For instance, passing in a
    /// product version will cause the validation to require
    /// that the license defines a ``License/maxSupportedProductVersion``.
    ///
    /// - Parameters:
    ///   - license: The license to validate, if any.
    ///   - bundleId: The bundle ID to validate, by default the ID of the main bundle.
    ///   - date: The date to validate, by default `.now`.
    ///   - platform: The platform to validate, by default ``Platform/current``.
    ///   - environment: The environment to validate, if any.
    ///   - productVersion: The product version to validate, if any.
    ///   - additionalValidation: A list of additional validation steps, if any.
    public static func validate(_ license: KeyboardKit.License?, bundleId: String? = nil, date: Date? = nil, platform: KeyboardKit.License.Platform? = nil, environment: KeyboardKit.License.Environment? = nil, productVersion: Double? = nil, additionalValidation: (KeyboardKit.License) throws -> Void = { _ in }) throws
}

extension License {

    /// Validate the license for a certain bundle, date, and
    /// platform, which is the most basic license validation
    /// that you should perform.
    ///
    /// Note that providing a validation criteria will cause
    /// the license validation to fail if a license does not
    /// specify that information. For instance, passing in a
    /// product version will cause the validation to require
    /// that the license defines a ``License/maxSupportedProductVersion``. 
    ///
    /// - Parameters:
    ///   - bundleId: The bundle ID to validate, by default the ID of the main bundle.
    ///   - date: The date to validate, by default `.now`.
    ///   - platform: The platform to validate, by default ``Platform/current``.
    ///   - environment: The environment to validate, if any.
    ///   - productVersion: The product version to validate, if any.
    public func validate(bundleId: String? = nil, date: Date? = nil, platform: KeyboardKit.License.Platform? = nil, environment: KeyboardKit.License.Environment? = nil, productVersion: Double? = nil) throws

    /// Validate the license for a certain bundle.
    ///
    /// - Parameters:
    ///   - bundle: The bundle to validate, by default `.main`.
    public func validateBundle(_ bundle: Bundle = .main) throws

    /// Validate the license for a certain bundle ID.
    ///
    /// - Parameters:
    ///   - bundleId: The bundle ID to validate.
    public func validateBundleId(_ bundleId: String) throws

    /// Validate if a bundle ID is valid for another license
    /// defined bundle ID, for both exact & wildcard matches.
    ///
    /// - Parameters:
    ///   - bundleId: The bundle ID to validate.
    ///   - licenseBundleId: A bundle ID that is defined in a license.
    public func validateBundleId(_ bundleId: String, for licenseBundleId: String) -> Bool

    /// Validate the license for a certain environment.
    ///
    /// - Parameters:
    ///   - environment: The environment to validate.
    public func validateEnvironment(_ environment: KeyboardKit.License.Environment) throws

    /// Validate the license for a certain date.
    ///
    /// - Parameters:
    ///   - date: The date to validate, by default the current one.
    public func validateExpirationDate(_ date: Date = .now) throws

    /// Validate the license for a certain product version.
    ///
    /// - Parameters:
    ///   - productVersion: The product version to validate.
    public func validateExpirationProductVersion(_ productVersion: Double) throws

    /// Validate the license for a certain feature.
    ///
    /// - Parameters:
    ///   - feature: The feature to validate.
    public func validateFeature(_ feature: any KeyboardKit.LicenseFeature) throws

    /// Validate the license for a certain lifeature or tier.
    ///
    /// - Parameters:
    ///   - feature: The feature to validate.
    ///   - tier: The tier to validate.
    public func validateFeature(_ feature: any KeyboardKit.LicenseFeature, orMeets tier: KeyboardKit.License.Tier) throws

    /// Validate the license for the current platform.
    public func validatePlatform() throws

    /// Validate the license for a certain platform.
    ///
    /// - Parameters:
    ///   - platform: The platform to validate.
    public func validatePlatform(_ platform: KeyboardKit.License.Platform) throws

    /// Validate the license meets a certain tier.
    ///
    /// - Parameters:
    ///   - tier: The tier to validate.
    public func validateTier(_ tier: KeyboardKit.License.Tier) throws
}

extension License {

    /// This `String` typealias represents a license key.
    public typealias Key = String
}

extension License {

    /// This error can be thrown when handling license files.
    public enum EncryptedFileError : String, LocalizedError {

        case invalidLicenseString

        case invalidPayload

        case invalidPrivateKey

        case invalidSignatureData

        case invalidSignature

        case missingFile

        /// Creates a new instance with the specified raw value.
        ///
        /// If there is no value of the type that corresponds with the specified raw
        /// value, this initializer returns `nil`. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     print(PaperSize(rawValue: "Legal"))
        ///     // Prints "Optional(PaperSize.Legal)"
        ///
        ///     print(PaperSize(rawValue: "Tabloid"))
        ///     // Prints "nil"
        ///
        /// - Parameter rawValue: The raw value to use for the new instance.
        public init?(rawValue: String)

        /// The raw type that can be used to represent all values of the conforming
        /// type.
        ///
        /// Every distinct value of the conforming type has a corresponding unique
        /// value of the `RawValue` type, but there may be values of the `RawValue`
        /// type that don't have a corresponding value of the conforming type.
        public typealias RawValue = String

        /// The corresponding value of the raw type.
        ///
        /// A new instance initialized with `rawValue` will be equivalent to this
        /// instance. For example:
        ///
        ///     enum PaperSize: String {
        ///         case A4, A5, Letter, Legal
        ///     }
        ///
        ///     let selectedSize = PaperSize.Letter
        ///     print(selectedSize.rawValue)
        ///     // Prints "Letter"
        ///
        ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
        ///     // Prints "true"
        public var rawValue: String { get }
    }
}

extension License.Source {

    /// The unique source identifier.
    public var id: String { get }

    public var name: String { get }
}

extension License.Source {

    /// A collection of all values of this type.
    public static var allCases: [KeyboardKit.License.Source] { get }
}

extension License.Platform {

    /// The unique platform identifier.
    public var id: String { get }

    /// Get the current platform.
    public static var current: KeyboardKit.License.Platform { get }

    /// The unique platform name.
    public var name: String { get }
}

extension License.Platform {

    /// Get all standard platforms.
    public static var all: [KeyboardKit.License.Platform] { get }

    /// Get all standard environments.
    public static var allCases: [KeyboardKit.License.Platform] { get }
}

extension License.TierGroup {

    /// A value-based group with `basic`, `silver` & `gold`.
    public static var value: KeyboardKit.License.TierGroup { get }

    /// A size-based group with `small`, `medium` & `large`.
    public static var size: KeyboardKit.License.TierGroup { get }

    /// A business-based group with `indie`, `startup`, etc.
    public static var business: KeyboardKit.License.TierGroup { get }
}

extension License.Tier {

    public static var allPredefined: [KeyboardKit.License.Tier] { get }
}

extension License.Tier {

    /// A standard tier with level of `0`.
    public static var standard: KeyboardKit.License.Tier { get }

    /// A trial tier with level `-1`.
    public static var trial: KeyboardKit.License.Tier { get }
}

extension License.Tier {

    /// A basic tier with level of `0`.
    public static var basic: KeyboardKit.License.Tier { get }

    /// A silver tier with level of `1`.
    public static var silver: KeyboardKit.License.Tier { get }

    /// A gold tier with level of `2`.
    public static var gold: KeyboardKit.License.Tier { get }
}

extension License.Tier {

    /// A small tier with level of `0`.
    public static var small: KeyboardKit.License.Tier { get }

    /// A medium tier with level of `1`.
    public static var medium: KeyboardKit.License.Tier { get }

    /// A large tier with level of `2`.
    public static var large: KeyboardKit.License.Tier { get }
}

extension License.Tier {

    /// An indie tier with level of `0`.
    public static var indie: KeyboardKit.License.Tier { get }

    /// A startup tier with level of `1`.
    public static var startup: KeyboardKit.License.Tier { get }

    /// An business tier with level of `2`.
    public static var business: KeyboardKit.License.Tier { get }

    /// An enterprise tier with level of `3`.
    public static var enterprise: KeyboardKit.License.Tier { get }
}

extension License.Tier {

    /// Check if the tier matches another tier's level.
    public func matches(_ tier: KeyboardKit.License.Tier) -> Bool

    /// Check if the tier meets another tier's level.
    public func meets(_ tier: KeyboardKit.License.Tier) -> Bool
}

extension License.Tier {

    /// ``ProFeature``s that require this tier must be added
    /// exclusively to a license.
    public static var custom: KeyboardKit.License.Tier { get }
}

extension License.Environment {

    /// The unique environment identifier.
    public var id: String { get }

    /// The unique environment name.
    public var name: String { get }
}

extension License.Environment {

    /// Get all standard environments.
    public static var all: [KeyboardKit.License.Environment] { get }

    public static func newFunc(value: Bool)

    /// Get all standard environments.
    public static var allCases: [KeyboardKit.License.Environment] { get }
}

extension License.ValidationError {

    public static func licenseFeatureOrTierRequired(_ feature: KeyboardKit.ProFeature, _ tier: KeyboardKit.License.Tier) -> KeyboardKit.License.ValidationError
}

extension License.ValidationError {

    /// The error display name, without associated values.
    public var displayName: String { get }

    /// The error description.
    public var errorDescription: String? { get }

    /// Whether this is an ``licenseExpired(_:)`` error.
    public var isExpired: Bool { get }

    /// Whether this is an ``invalidBundle(_:)`` error.
    public var isInvalidBundle: Bool { get }

    /// Whether this is an ``invalidEnvironment(_:)`` error.
    public var isInvalidEnvironment: Bool { get }

    /// Whether this is an ``invalidLicenseKey(_:message:)`` error.
    public var isInvalidLicenseKey: Bool { get }

    /// Whether this is an ``invalidPlatform(_:)`` error.
    public var isInvalidPlatform: Bool { get }

    /// Whether this is an ``invalidProductVersion(_:)`` error.
    public var isInvalidProductVersion: Bool { get }

    /// Whether this is an ``licenseFeatureRequired(_:)`` error.
    public var islicenseFeatureRequired: Bool { get }

    /// Whether this is an ``licenseTierRequired(_:)`` error.
    public var isLicenseTierRequired: KeyboardKit.License.Tier? { get }
}

extension License.EncryptedFileError : Equatable {
}

extension License.EncryptedFileError : Hashable {
}

extension License.EncryptedFileError : RawRepresentable {
}

/// This protocol can be implemented by any type that should
/// be used as a license-based feature.
///
/// For instance, some enums could want to only make certain
/// parts protected by a license.
///
/// Just return `nil` to disable the license feature.
public protocol LicenseFeature {

    /// The unique license feature ID, if any.
    ///
    /// This is optional, since enum-based features may have
    /// cases that shouldn't be handled as a license feature.
    var featureId: String? { get }

    /// The tier that implicitly unlocks the feature, if any.
    ///
    /// This will be renamed to `isUnlockedByTier`.
    var unlockedByTier: KeyboardKit.License.Tier? { get }
}

extension LicenseFeature {

    public static var vietnameseInputEngine: KeyboardKit.Vietnamese.Feature { get }
}

extension LicenseFeature {

    /// This is an alias for the `unlockedByTier` property.
    public var isUnlockedByTier: KeyboardKit.License.Tier? { get }
}

/// This license file generator can generate encrypted files
/// that can later be decrypted with encrypted file services.
///
/// > Important: This class requires a ``License/Tier-swift.struct/business`` license.
final public class LicenseFileGenerator : Sendable {

    /// Create a license file generator.
    ///
    /// Use ``generateUniqueEncryptionKey()`` to generate an
    /// encryption key that is specific to your app/SDK.
    ///
    /// - Parameters:
    ///   - license: Your LicenseKit license.
    ///   - privateKey: Your private, unique encryption key.
    public convenience init(license: KeyboardKit.License, privateKey: String) throws

    @objc deinit
}

extension LicenseFileGenerator {

    /// Generate a unique, private encryption key, which can
    /// be used to encrypt and decrypt license files.
    public static func generateUniqueEncryptionKey() -> String
}

/// This namespace contains integration-related types.
public struct LicenseIntegrations {
}

extension LicenseIntegrations {

    /// This is a namespace for Gumroad integrations.
    public struct Gumroad {
    }
}

extension LicenseIntegrations {

    /// This type can be used to configure API integrations.
    ///
    /// You can add static configuration extensions for each
    /// specific API integration.
    public struct ApiConfiguration {

        /// Create an API configuration.
        ///
        /// - Parameters:
        ///   - url: The URL to use for a license key.
        ///   - httpMethod: The HTTP method to use for a license key, by default `"GET"`.
        ///   - httpHeaders: The HTTP headers to use for a license key, by default `[:]`.
        ///   - httpFormData: The HTTP form data to use for a license key, by default `nil`.
        ///   - httpPostData: The HTTP post data to use for a license key, by default `nil`.
        public init(url: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.UrlBuilder, httpMethod: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpMethodBuilder = { _ in "GET" }, httpHeaders: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpHeadersBuilder = { _ in [:] }, httpFormData: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpFormDataBuilder = { _ in nil }, httpPostData: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpPostDataBuilder = { _ in nil })

        public typealias HttpHeadersBuilder = (_ licenseKey: String) -> [String : String]

        public typealias HttpMethodBuilder = (_ licenseKey: String) -> String

        public typealias HttpFormDataBuilder = (_ licenseKey: String) -> [String : String]?

        public typealias HttpPostDataBuilder = (_ licenseKey: String) -> Data?

        public typealias UrlBuilder = (_ licenseKey: String) -> String

        /// The URL to use for a license key.
        public let url: KeyboardKit.LicenseIntegrations.ApiConfiguration.UrlBuilder

        /// The HTTP method to use for a license key.
        public let httpMethod: KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpMethodBuilder

        /// The HTTP headers to use for a license key.
        public let httpHeaders: KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpHeadersBuilder

        /// The HTTP form data to use for a license key, if any.
        public let httpFormData: KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpFormDataBuilder

        /// The HTTP post data to use for a license key, if any.
        public let httpPostData: KeyboardKit.LicenseIntegrations.ApiConfiguration.HttpPostDataBuilder
    }
}

extension LicenseIntegrations.Gumroad {

    /// This type represents a Gumroad verification result.
    public struct Response : Codable {

        public let uses: Int?

        public let purchase: KeyboardKit.LicenseIntegrations.Gumroad.Purchase?

        public let success: Bool

        public let message: String?

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This type represents a Gumroad purchase.
    public struct Purchase : Codable {

        public var license_key: String { get }

        public var seller_id: String? { get }

        public var product_id: String? { get }

        public var product_name: String? { get }

        public var permalink: String? { get }

        public var product_permalink: String? { get }

        public var short_product_id: String? { get }

        public var email: String? { get }

        public var price: Double? { get }

        public var gumroad_fee: Double? { get }

        public var currency: String? { get }

        public var quantity: Int? { get }

        public var discover_fee_charged: Bool? { get }

        public var can_contact: Bool? { get }

        public var referrer: String? { get }

        public var card: KeyboardKit.LicenseIntegrations.Gumroad.PurchaseCard? { get }

        public var order_number: Int? { get }

        public var sale_id: String? { get }

        public var sale_timestamp: String? { get }

        public var full_name: String? { get }

        public var purchaser_id: String? { get }

        public var ip_country: String? { get }

        public var is_gift_receiver_purchase: Bool? { get }

        public var is_multiseat_license: Bool? { get }

        public var refunded: Bool? { get }

        public var disputed: Bool? { get }

        public var dispute_won: Bool? { get }

        public var id: String? { get }

        public var created_at: String? { get }

        public var variants: String? { get }

        public var chargebacked: Bool? { get }

        public var subscription_ended_at: String? { get }

        public var subscription_cancelled_at: String? { get }

        public var subscription_failed_at: String? { get }

        public var custom_fields: [String]? { get }

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This type represents a Gumroad purchase card.
    public struct PurchaseCard : Codable {

        public let visual: String?

        public let type: String?

        public let bin: String?

        public let expiry_month: Int?

        public let expiry_year: Int?

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension LicenseIntegrations.ApiConfiguration {

    /// A standard API configuration.
    public static func standard(for url: @escaping KeyboardKit.LicenseIntegrations.ApiConfiguration.UrlBuilder) -> KeyboardKit.LicenseIntegrations.ApiConfiguration
}

extension LicenseIntegrations.Gumroad.Purchase {

    /// Get a standard license from the purchase.
    public var license: KeyboardKit.License { get }

    /// Get a standard license customer from the purchase.
    public var customer: KeyboardKit.License.Customer? { get }

    /// Get a custom fields from the purchase.
    public var customFields: [String : String]? { get }

    /// Try parsing the activation date from the purchase.
    public var activationDate: Date? { get }

    /// Try parsing the expiration date from the purchase.
    public var expirationDate: Date? { get }

    /// Try parsing the tier from the purchase.
    public var tier: KeyboardKit.License.Tier { get }
}

/// This actor can be used to store a single, active license.
///
/// This store can be used by apps and libraries that should
/// only have a single active license at a time.
final public class LicenseStore : @unchecked Sendable {

    /// Create a store.
    public init()

    /// The currently registered license, if any.
    final public var license: KeyboardKit.License? { get }

    /// A shared license store.
    public static let shared: KeyboardKit.LicenseStore

    /// Whether or not the actor has a registered license.
    final public var hasLicense: Bool { get }

    /// Remove the currently stored license, if any.
    final public func removeLicense()

    /// Store the provided license.
    final public func storeLicense(_ license: KeyboardKit.License)

    @objc deinit
}

public protocol Localizable {

    /// The `Localizable.strings` key to use when localizing.
    var localizationKey: String { get }
}

extension Localizable {

    /// The localized name for a certain locale.
    ///
    /// - Parameters:
    ///   - locale: The locale to use, by default `.current`.
    public func localizedName(in locale: Locale = .current) -> String
}

extension Localizable {

    /// The localized text for a certain key, locale.
    ///
    /// - Parameters:
    ///   - key:
    ///   - locale: The locale to use, by default `.current`.
    public static func localizedText(for key: String, in locale: Locale = .current) -> String

    /// The localized text for a certain key, locale.
    ///
    /// - Parameters:
    ///   - key:
    ///   - locale: The locale to use, by default `.current`.
    public func localizedText(for key: String, in locale: Locale = .current) -> String
}

extension Localizable {

    /// The localized name for the current locale and bundle.
    public var localizedName: String { get }

    /// The localized name for a certain locale and bundle.
    ///
    /// - Parameters:
    ///   - locale: The locale to use, by default `.current`.
    ///   - bundle: The bundle that contains the localized content.
    public func localizedName(in locale: Locale = .current, bundle: Bundle) -> String
}

extension Localizable {

    /// The localized text for a certain key, locale and bundle.
    ///
    /// - Parameters:
    ///   - key:
    ///   - locale: The locale to use, by default `.current`.
    ///   - bundle: The bundle that contains the localized content.
    public static func localizedText(for key: String, in locale: Locale = .current, bundle: Bundle) -> String

    /// The localized text for a certain key, locale and bundle.
    ///
    /// - Parameters:
    ///   - key:
    ///   - locale: The locale to use, by default `.current`.
    ///   - bundle: The bundle that contains the localized content.
    public func localizedText(for key: String, in locale: Locale = .current, bundle: Bundle) -> String
}

/// This protocol is implemented by locale-specific services.
///
/// The protocol simplifies aggregating services and resolve
/// a service instance for a certain locale.
public protocol LocalizedService {

    /// The unique locale key to use when looking up values.
    var localeKey: String { get }
}

/// This enum specifies all available pro features and which
/// tier each feature requires to be implicitly unlocked.
///
/// Please see the ``unlockedByTier`` property to inspect to
/// understand the tier you need to unlock a certain feature.
public enum ProFeature : KeyboardKit.LicenseFeature {

    case autocomplete

    case clipboardAddons

    case dictation

    case emojiAddons

    case externalKeyboard

    case fonts

    case fullDocumentContext

    case hostApplication

    case inputSets

    case keyboardLayoutTypes

    case locale(Locale)

    case proPreviews

    case remoteAutocomplete

    case settingsScreens

    case textInput

    case themes

    /// The unique license feature ID, if any.
    ///
    /// This is optional, since enum-based features may have
    /// cases that shouldn't be handled as a license feature.
    public var featureId: String? { get }

    /// The tier that implicitly unlocks the feature, if any.
    ///
    /// This will be renamed to `isUnlockedByTier`.
    public var unlockedByTier: KeyboardKit.License.Tier? { get }
}

/// This namespace contains proxy-related types.
public struct Proxy {
}

extension Proxy {

    /// This configuration can be used to set up how to read
    /// the full document context of a `UITextDocumentProxy` .
    ///
    /// The ``sleepInterval`` sets how long to wait with any
    /// subsequent operations when the text cursor has moved.
    /// It's needed because a proxy needs to pause before it
    /// can access more text. A too short delay will cause a
    /// read to just return empty text, which will cause the
    /// operation to abort, while too long delays will cause
    /// the read operation to take longer.
    ///
    /// The standard `0.04` sleep interval is tested to work
    /// on older devices, like iPhone 7, while newer devices
    /// can use a much shorter interval like `0.02`. You can
    /// use the predefined ``standard`` and ``fast`` configs.
    ///
    /// The ``endOfProxyAttempts`` determines how many times
    /// to attempt to move the text cursor when reaching the
    /// beginning or the end of the proxy. This is needed to
    /// handle texts that have many line breaks, since these
    /// line breaks can otherwise cause the operation to end.
    ///
    /// The standard `10` attempts will make a proxy attempt
    /// to move the cursor that many times, but it will fail
    /// if there are more paragraphs. Using more attempts is
    /// however taking longer to complete.
    public struct FullDocumentReaderConfiguration {

        /// Create a full document configuration.
        ///
        /// - Parameters:
        ///   - sleepInterval: How long to sleep after moving the cursor, by default `0.04` seconds.
        ///   - endOfProxyAttempts: The number of times to look for more content, by default `10` times.
        public init(sleepInterval: TimeInterval = 0.04, endOfProxyAttempts: Int = 10)

        /// How long to sleep after moving the cursor.
        public var sleepInterval: TimeInterval

        /// The number of times to look for more content.
        public var endOfProxyAttempts: Int
    }

    /// This error is thrown by ``UIKit/UITextDocumentProxy``
    /// if it fails to read the full document context.
    public enum FullDocumentReaderError : Error {

        /// The operation was unable to start since there is
        /// an already active operation in progress.
        case anotherReadOperationInProgress

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Proxy.FullDocumentReaderError, b: KeyboardKit.Proxy.FullDocumentReaderError) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }

    /// This type is returned by ``UIKit/UITextDocumentProxy``
    /// after reading the full document context.
    public struct FullDocumentReaderResult {

        /// The full document context.
        public var fullDocumentContext: String { get }

        /// The full document context before the input cursor.
        public var fullDocumentContextBeforeInput: String

        /// The full document context after the input cursor.
        public var fullDocumentContextAfterInput: String
    }
}

extension Proxy {

    /// This enum defines the available edges of a selection.
    public enum SelectionEdge {

        case leading

        case trailing

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Proxy.SelectionEdge, b: KeyboardKit.Proxy.SelectionEdge) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }
}

extension Proxy.FullDocumentReaderConfiguration {

    /// A standard full document reader configuration, which
    /// behaves well on slower devices.
    public static var standard: KeyboardKit.Proxy.FullDocumentReaderConfiguration { get }

    /// A fast full document reader configuration, which may
    /// produce incorrect results on slower devices.
    public static var fast: KeyboardKit.Proxy.FullDocumentReaderConfiguration { get }
}

extension Proxy.FullDocumentReaderError : Equatable {
}

extension Proxy.FullDocumentReaderError : Hashable {
}

extension Proxy.SelectionEdge : Equatable {
}

extension Proxy.SelectionEdge : Hashable {
}

/// This protocol can be implemented by any type that can be
/// used to open URLs from anywhere.
///
/// While you must implement ``openUrl(_:)`` it can call the
/// ``openUrlDefault(_:)`` function internally.
public protocol UrlOpener : UIResponder {

    /// Try to open a URL.
    func openUrl(_ url: URL?)
}

extension UrlOpener {

    /// Try to open a URL with the default logic.
    ///
    /// This is currently only available when `UIApplication`
    /// is available. Other platforms must add their own way
    /// of opening URLs.
    public func openUrlDefault(_ url: URL?)
}

/// This is a namespace for Vietnamese types.
public struct Vietnamese {
}

extension Vietnamese {

    /// This type is used to represent Vietnamese diacritics.
    public struct Diacritic : Codable, Equatable, Hashable, Sendable {

        /// Create a custom diacritic value.
        ///
        /// - Parameters:
        ///   - char: The character to display.
        ///   - replacements: All possible replacements.
        public init(char: String, replacements: [String : String])

        /// The character to display.
        public let char: String

        /// All possible replacements.
        public let replacements: [String : String]

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Vietnamese.Diacritic, b: KeyboardKit.Vietnamese.Diacritic) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }

    /// This type describes how to apply a diacritic.
    public struct DiacriticInsertionResult : Codable, Equatable, Hashable, Sendable {

        /// Create a custom insertion result.
        ///
        /// - Parameters:
        ///   - deleteBackwardsCount: The number of backwards deletion to apply.
        ///   - textInsertion: The text to insert after deleting backwards.
        public init(deleteBackwardsCount: Int, textInsertion: String)

        /// The number of backwards deletion to apply.
        public let deleteBackwardsCount: Int

        /// The text to insert after deleting backwards.
        public let textInsertion: String

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Vietnamese.DiacriticInsertionResult, b: KeyboardKit.Vietnamese.DiacriticInsertionResult) -> Bool

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Vietnamese {

    public enum Feature : KeyboardKit.LicenseFeature, Sendable {

        case inputEngine

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Vietnamese.Feature, b: KeyboardKit.Vietnamese.Feature) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }
}

extension Vietnamese {

    /// This enum defines available Vietnamese input methods.
    public enum InputMethod : CaseIterable, Codable, Equatable, Hashable, Sendable {

        /// TELEX uses letters that are not likely to appear
        /// at the end of words to apply diacritics.
        case telex

        /// VIQR repurposes some punctuation mark characters
        /// to apply diacritics.
        case viqr

        /// VNI repurposes some number & function characters
        /// to apply diacritics.
        case vni

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: KeyboardKit.Vietnamese.InputMethod, b: KeyboardKit.Vietnamese.InputMethod) -> Bool

        /// A type that can represent a collection of all values of this type.
        public typealias AllCases = [KeyboardKit.Vietnamese.InputMethod]

        /// A collection of all values of this type.
        nonisolated public static var allCases: [KeyboardKit.Vietnamese.InputMethod] { get }

        /// Encodes this value into the given encoder.
        ///
        /// If the value fails to encode anything, `encoder` will encode an empty
        /// keyed container in its place.
        ///
        /// This function throws an error if any values are invalid for the given
        /// encoder's format.
        ///
        /// - Parameter encoder: The encoder to write data to.
        public func encode(to encoder: any Encoder) throws

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }

        /// Creates a new instance by decoding from the given decoder.
        ///
        /// This initializer throws an error if reading from the decoder fails, or
        /// if the data read is corrupted or otherwise invalid.
        ///
        /// - Parameter decoder: The decoder to read data from.
        public init(from decoder: any Decoder) throws
    }
}

extension Vietnamese {

    public static let allDiacriticVariantsForA: String

    public static let allDiacriticVariantsForD: String

    public static let allDiacriticVariantsForE: String

    public static let allDiacriticVariantsForI: String

    public static let allDiacriticVariantsForO: String

    public static let allDiacriticVariantsForU: String

    public static let allDiacriticVariantsForY: String

    public static let allDiacriticVowelVariants: String
}

extension Vietnamese.Diacritic {

    /// A Vietnamese `sắc` acute, e.g. `a => á`.
    public static func sac(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    /// A Vietnamese `huyền` grave, e.g. `a => à`.
    public static func huyen(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    /// A Vietnamese `hỏi` hook, e.g. `a => ả`.
    public static func hoi(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `ngã` tilde, e.g. `a => ã`.
    public static func nga(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `nặng` dot, e.g. `a => ạ`.
    public static func nang(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `mũ` roof, e.g. `a => â`.
    public static func mu(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `móc` tail, e.g. `o => ơ` and `u => ư`.
    public static func moc(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `trăng` bowl, e.g. `a => ă`.
    public static func trang(_ char: String) -> KeyboardKit.Vietnamese.Diacritic

    ///
    /// A Vietnamese `đ`, e.g. `d => đ`.
    public static func d(_ char: String) -> KeyboardKit.Vietnamese.Diacritic
}

extension Vietnamese.Diacritic {

    /// The sorted keys to check when performing insertions.
    public var sortedReplacementKeys: [String] { get }

    /// Determine how the diacritic should be applied a text.
    public func insertionResult(whenAppendedTo text: String) -> KeyboardKit.Vietnamese.DiacriticInsertionResult
}

extension Vietnamese.Diacritic {

    /// A keyboard action that applies the diacritic.
    public var keyboardAction: KeyboardKit.KeyboardAction { get }

    /// A keyboard-specific diacritic value.
    public var keyboardDiacritic: KeyboardKit.Keyboard.Diacritic { get }
}

extension Vietnamese.DiacriticInsertionResult {

    public static func noMatch(for char: String) -> KeyboardKit.Vietnamese.DiacriticInsertionResult
}

extension Vietnamese.Feature {

    /// The unique license feature ID, if any.
    ///
    /// This is optional, since enum-based features may have
    /// cases that shouldn't be handled as a license feature.
    public var featureId: String? { get }

    /// The tier that implicitly unlocks the feature, if any.
    ///
    /// This will be renamed to `isUnlockedByTier`.
    public var unlockedByTier: KeyboardKit.License.Tier? { get }
}

extension Vietnamese.Feature : Equatable {
}

extension Vietnamese.Feature : Hashable {
}

/// This Vietnamese typing engine can be used to resolve any
/// diacritic replacements when typing in Vietnamese.
///
/// Use the ``diacriticReplacement(for:whenAppendedTo:with:)``
/// function to resolve diacritic replacements for a certain
/// character when it's being appended to a custom text with
/// a certain Vietnamese ``Vietnamese/InputMethod``.
public class VietnameseInputEngine {

    /// Check if a certain character should be replaced with
    /// a ``Vietnamese/Diacritic`` when appended to a string
    /// with a certain ``Vietnamese/InputMethod``.
    public func diacriticInsertionResult(for char: String, whenAppendedTo text: String, with inputMethod: KeyboardKit.Vietnamese.InputMethod) -> KeyboardKit.Vietnamese.DiacriticInsertionResult

    /// Check if a certain character should be replaced with
    /// a ``Vietnamese/Diacritic`` when appended to a string
    /// with a certain ``Vietnamese/InputMethod``.
    public func diacriticReplacement(for char: String, whenAppendedTo text: String, with inputMethod: KeyboardKit.Vietnamese.InputMethod) -> KeyboardKit.Vietnamese.Diacritic?

    /// Get the final input result when appending characters
    /// to a text with a certain ``Vietnamese/InputMethod``.
    public func inputResult(for text: String, whenAppending characters: String, with inputMethod: KeyboardKit.Vietnamese.InputMethod) -> String

    @objc deinit
}

extension VietnameseInputEngine {

    public convenience init() throws
}

extension Array where Element == [KeyboardKit.KeyboardLayout.Item] {

    /// The total height of the layout.
    public var totalHeight: Double { get }

    /// The width to apply to input keys given a total width.
    public func inputWidth(for totalWidth: Double) -> Double
}

extension Array where Element == KeyboardKit.KeyboardLayout.Item {

    /// Whether or not the row has an input key.
    public var hasInputKey: Bool { get }

    /// The row suggested input width, based on the its keys.
    public func suggestedInputWidth(for totalWidth: CGFloat) -> CGFloat?
}

extension EnvironmentValues {

    public var emojiKeyboardSizes: KeyboardKit.EmojiKeyboard.Sizes
}

extension View {

    @MainActor @preconcurrency public func emojiKeyboardSizes(_ sizes: KeyboardKit.EmojiKeyboard.Sizes) -> some View

}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/blueprint`` themes.
    public static var predefinedBlueprint: [KeyboardKit.KeyboardTheme] { get throws }
}

extension EnvironmentValues {

    public var emojiKeyboardConfiguration: KeyboardKit.EmojiKeyboard.Configuration
}

extension View {

    @MainActor @preconcurrency public func emojiKeyboardConfiguration(_ config: KeyboardKit.EmojiKeyboard.Configuration) -> some View

}

extension EnvironmentValues {

    public var emojiKeyboardStyle: KeyboardKit.EmojiKeyboard.Style
}

extension View {

    @MainActor @preconcurrency public func emojiKeyboardStyle(_ style: KeyboardKit.EmojiKeyboard.Style) -> some View

}

extension Locale {

    /// The full name of this locale in its own language.
    public var localizedName: String? { get }

    /// The short display name of this locale.
    ///
    /// This is the langauge code if any, or the identifier.
    public var shortDisplayName: String { get }

    /// The full name of this locale in another locale.
    public func localizedName(in locale: Locale) -> String?

    /// The language name of this locale in its own language.
    public var localizedLanguageName: String? { get }

    /// The language name of this locale in another locale.
    public func localizedLanguageName(in locale: Locale) -> String?
}

extension View {

    /// Apply a custom screen visibility.
    @MainActor @preconcurrency public func localeSettingsScreenVisibility(_ value: KeyboardKit.Keyboard.LocaleSettingsScreenVisibility) -> some View

}

extension EnvironmentValues {

    /// A custom screen visibility.
    public var localeSettingsScreenVisibility: KeyboardKit.Keyboard.LocaleSettingsScreenVisibility
}

extension String {

    /// Whether the string contains an emoji.
    public var containsEmoji: Bool { get }

    /// Whether the string only contains emojis.
    public var containsOnlyEmojis: Bool { get }

    /// Extract all emoji characters from the string.
    public var emojis: [Character] { get }

    /// Extract all emoji scalars from the string.
    public var emojiScalars: [UnicodeScalar] { get }

    /// Extract all emojis in the string.
    public var emojiString: String { get }

    /// Whether the string is a single emoji.
    public var isSingleEmoji: Bool { get }
}

extension View {

    /// Apply a ``Autocomplete/ToolbarSeparatorStyle``.
    @MainActor @preconcurrency public func autocompleteToolbarSeparatorStyle(_ style: KeyboardKit.Autocomplete.ToolbarSeparatorStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Autocomplete/ToolbarSeparatorStyle``.
    public var autocompleteToolbarSeparatorStyle: KeyboardKit.Autocomplete.ToolbarSeparatorStyle
}

extension View {

    /// Apply a ``Autocomplete/ToolbarStyle``.
    @MainActor @preconcurrency public func autocompleteToolbarStyle(_ style: KeyboardKit.Autocomplete.ToolbarStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Autocomplete/ToolbarStyle``.
    public var autocompleteToolbarStyle: KeyboardKit.Autocomplete.ToolbarStyle
}

extension CGSize {

    /// The large iPad portrait threshold size.
    public static let iPadLargeScreen: CGSize

    /// The large iPad landscape threshold size.
    public static let iPadLargeScreenLandscape: CGSize

    /// The large iPhone portrait threshold size.
    public static let iPhoneLargeScreen: CGSize

    /// The large iPhone landscape threshold size.
    public static let iPhoneLargeScreenLandscape: CGSize

    /// Flip the size's height and width.
    public func flipped() -> CGSize

    /// Whether a size is at least a size in any orientation.
    public func isAtLeastScreenSize(_ size: CGSize) -> Bool

    /// Whether the size matches a size in any orientation.
    ///
    /// To handle small variations when new devices come out,
    /// this function automatically adds 50 points tolerance.
    public func isScreenSize(_ size: CGSize, withTolerance points: CGFloat = 50) -> Bool
}

extension Locale {

    /// Get the character direction for the locale.
    public var characterDirection: Locale.LanguageDirection { get }

    /// Whether the ``lineDirection`` is `.bottomToTop`.
    public var isBottomToTop: Bool { get }

    /// Whether the ``lineDirection``  is `.topToBottom`.
    public var isTopToBottom: Bool { get }

    /// Whether the ``characterDirection``  is `.leftToRight`.
    public var isLeftToRight: Bool { get }

    /// Whether the ``characterDirection``  is `.rightToLeft`.
    public var isRightToLeft: Bool { get }

    /// Get the line direction for the locale.
    public var lineDirection: Locale.LanguageDirection { get }
}

extension UIInputViewController {

    /// Whether this controller is KeyboardKit enabled.
    @MainActor @preconcurrency public var isKeyboardKitEnabledController: Bool { get }
}

extension UITextDocumentProxy {

    /// Whether the proxy's input cursor is at a new line.
    @MainActor @preconcurrency public var isCursorAtNewLine: Bool { get }

    /// Whether the proxy's input cursor is at the beginning
    /// of a sentence, with or without trailing whitespaces.
    @MainActor @preconcurrency public var isCursorAtNewSentence: Bool { get }

    /// Whether the proxy's input cursor is at the beginning
    /// of a sentence, with trailing whitespaces.
    @MainActor @preconcurrency public var isCursorAtNewSentenceWithTrailingWhitespace: Bool { get }

    /// The sentence just before the input cursor.
    @MainActor @preconcurrency public var sentenceBeforeInput: String? { get }

    /// A list of western sentence delimiters.
    ///
    /// Set `String.sentenceDelimiters` to modify this value.
    @MainActor @preconcurrency public var sentenceDelimiters: [String] { get }

    /// End the current sentence by removing trailing spaces,
    /// then injecting a dot and a space.
    ///
    /// - Parameters:
    ///   - text: The text to end the sentence with, by default `. `.
    @MainActor @preconcurrency public func endSentence(withText text: String = ". ")
}

extension Locale {

    /// Whether the locale matches a query in a locale.
    public func matches(query: String, in locale: Locale = .current) -> Bool
}

extension Collection where Self.Element == Locale {

    /// Filter out all locales that matches a certain query.
    public func matching(query: String, in locale: Locale = .current) -> [Locale]
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func fontSettingsScreenLocalization(_ value: KeyboardKit.Fonts.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var fontSettingsScreenLocalization: KeyboardKit.Fonts.SettingsScreenLocalization
}

extension Collection where Self.Element == KeyboardKit.License.Source {

    /// Get all available environments.
    public static var all: [KeyboardKit.License.Source] { get }
}

extension UITextDocumentProxy {

    /// Whether the last trailing quotation before the input
    /// cursor is an alternate quotation begin delimiter.
    @MainActor @preconcurrency public func hasUnclosedAlternateQuotationBeforeInput(for locale: Locale) -> Bool

    /// Whether the last trailing quotation before the input
    /// cursor is a quotation begin delimiter.
    @MainActor @preconcurrency public func hasUnclosedQuotationBeforeInput(for locale: Locale) -> Bool

    /// Whether the provided text has quotations that should
    /// be replaced with something else for a certain locale.
    @MainActor @preconcurrency public func preferredQuotationReplacement(whenInserting text: String, for locale: Locale) -> String?
}

extension String {

    public func applyingDiacriticResult(_ result: KeyboardKit.Vietnamese.DiacriticInsertionResult) -> String
}

extension UITextDocumentProxy {

    /// Get the document context (the text) before and after
    /// the input cursor.
    ///
    /// This property does not return the full document text.
    /// A keyboard extension has limited document access and
    /// can stop looking for more text at any time.
    ///
    /// KeyboardKit Pro unlocks ways to read the entire text
    /// from a document. See the <doc:Proxy-Article> article
    /// for more information.
    @MainActor @preconcurrency public var documentContext: String? { get }
}

extension View {

    /// Apply a keyboard layout item size to the view.
    ///
    /// The `edgeInsets` can be used to add intrinsic insets
    /// within the interactable button area.
    ///
    /// - Parameters:
    ///   - item: The layout item to apply.
    ///   - rowWidth: The total row width (often screen width).
    ///   - inputWidth: The width in points of an input item.
    @MainActor @preconcurrency public func keyboardLayoutItemSize(for item: KeyboardKit.KeyboardLayout.Item, rowWidth: CGFloat, inputWidth: CGFloat) -> some View

}

extension View {

    /// Apply a ``Autocomplete/ToolbarItemStyle``.
    @MainActor @preconcurrency public func autocompleteToolbarItemStyle(_ style: KeyboardKit.Autocomplete.ToolbarItemStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Autocomplete/ToolbarItemStyle``.
    public var autocompleteToolbarItemStyle: KeyboardKit.Autocomplete.ToolbarItemStyle
}

extension Locale {

    /// Whether this locale prefers to replace alternate end
    /// quotation delimiters with begin ones.
    public var prefersAlternateQuotationReplacement: Bool { get }

    /// Whether the locale prefers swipe down actions.
    ///
    /// `TODO`: This should be supported with some UI tweaks.
    public var prefersSecondarySwipeDownActions: Bool { get }
}

extension UITextDocumentProxy {

    /// Whether the proxy has an autocomplete inserted space.
    @MainActor @preconcurrency public var hasAutocompleteInsertedSpace: Bool { get }

    /// Whether the proxy has an autocomplete removed space.
    @MainActor @preconcurrency public var hasAutocompleteRemovedSpace: Bool { get }

    /// Replace the current word in the texts document proxy
    /// with a suggestion, then try to insert a space.
    ///
    /// If a space is automatically inserted, the proxy will
    /// be set to an `autoInserted` state.
    @MainActor @preconcurrency public func insertAutocompleteSuggestion(_ suggestion: KeyboardKit.Autocomplete.Suggestion, tryInsertSpace: Bool = true)

    /// Try to insert a space into the proxy after inserting
    /// an autocompete suggestion.
    ///
    /// Call this function instead of just inserting a space
    /// to apply the correct autocomplete state to the proxy.
    @MainActor @preconcurrency public func tryInsertSpaceAfterAutocomplete()

    /// Try to re-insert a space that was previously removed
    /// by `tryRemoveAutocompleteInsertedSpace`.
    @MainActor @preconcurrency public func tryReinsertAutocompleteRemovedSpace()

    /// Try to remove any space before the text input, if it
    /// was inserted by `insertAutocompleteSuggestion`.
    ///
    /// If a space is automatically removed, this proxy will
    /// be set to an `autoRemoved` state.
    @MainActor @preconcurrency public func tryRemoveAutocompleteInsertedSpace()
}

extension Collection where Self.Element == KeyboardKit.License.Platform {

    /// Get all standard platforms.
    public static var all: [KeyboardKit.License.Platform] { get }
}

extension UITextDocumentProxy {

    /// Delete backwards a certain range.
    @MainActor @preconcurrency public func deleteBackward(range: KeyboardKit.Keyboard.BackspaceRange)

    /// Delete backwards a certain number of times.
    @MainActor @preconcurrency public func deleteBackward(times: Int)
}

extension View {

    /// Apply a leading ``Keyboard/SpacebarMenuType``.
    @MainActor @preconcurrency public func keyboardSpacebarMenuLeading(_ type: KeyboardKit.Keyboard.SpacebarMenuType?) -> some View


    /// Apply a trailing ``Keyboard/SpacebarMenuType``.
    @MainActor @preconcurrency public func keyboardSpacebarMenuTrailing(_ type: KeyboardKit.Keyboard.SpacebarMenuType?) -> some View

}

extension EnvironmentValues {

    /// Apply a leading ``Keyboard/SpacebarMenuType``.
    public var keyboardSpacebarMenuLeading: KeyboardKit.Keyboard.SpacebarMenuType?

    /// Apply a trailing ``Keyboard/SpacebarMenuType``.
    public var keyboardSpacebarMenuTrailing: KeyboardKit.Keyboard.SpacebarMenuType?
}

extension RangeReplaceableCollection where Self.Element == KeyboardKit.KeyboardLayout.Item, Self.Index == Int {

    /// Get the index of a certain `action`.
    public func index(of action: KeyboardKit.KeyboardAction) -> Self.Index?

    /// Insert a layout `item` before a certain `action`.
    public mutating func insert(_ item: Self.Element, before action: KeyboardKit.KeyboardAction)

    /// Insert an layout `item` after a certain `action`.
    public mutating func insert(_ item: Self.Element, after action: KeyboardKit.KeyboardAction)

    /// Remove a certain `action`.
    public mutating func remove(_ action: KeyboardKit.KeyboardAction)

    /// Remove all items that meet a certain `condition`.
    public mutating func remove(where condition: (Self.Element) -> Bool)

    /// Replace a certain `action` with a replacement `item`.
    public mutating func replace(_ action: KeyboardKit.KeyboardAction, with item: Self.Element)
}

extension Array where Element : RangeReplaceableCollection, Element.Element == KeyboardKit.KeyboardLayout.Item, Element.Index == Int {

    /// Get the row at a certain index.
    public func row(at rowIndex: Int) -> Element?

    /// Insert an `item` before an `action` in all rows.
    public mutating func insert(_ item: Element.Element, before action: KeyboardKit.KeyboardAction)

    /// Insert an `item` before an `action` in a certain row.
    public mutating func insert(_ item: Element.Element, before action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Insert an `item` after an `action` in all rows.
    public mutating func insert(_ item: Element.Element, after action: KeyboardKit.KeyboardAction)

    /// Insert an `item` after an `action` in a certain row.
    public mutating func insert(_ item: Element.Element, after action: KeyboardKit.KeyboardAction, inRow rowIndex: Int)

    /// Remove a certain `action` from all rows.
    public mutating func remove(_ action: KeyboardKit.KeyboardAction)

    /// Remove a certain `action` from a certain row.
    public mutating func remove(_ action: KeyboardKit.KeyboardAction, fromRow rowIndex: Int)

    /// Remove all items that meet a `condition` in all rows.
    public mutating func remove(where condition: (Element.Element) -> Bool)

    /// Remove all items that meet a `condition` from a row.
    public mutating func remove(where condition: (Element.Element) -> Bool, fromRow rowIndex: Int)

    /// Replace an `action` with another `item` in all rows.
    public mutating func replace(_ action: KeyboardKit.KeyboardAction, with replacement: Element.Element)

    /// Replace an action with another item in a certain row.
    public mutating func replace(_ action: KeyboardKit.KeyboardAction, with replacement: Element.Element, inRow rowIndex: Int)
}

extension Collection where Self.Element == KeyboardKit.Emoji {

    public func matching(_ query: String, in locale: Locale = .current) -> [KeyboardKit.Emoji]
}

extension UTType {

    /// A custom uniform type for an ``Emoji``.
    ///
    /// The explicit type ID is `com.emojikit.emoji`.
    public static var emoji: UTType { get }

    /// A custom uniform type for an ``EmojiCategory``.
    ///
    /// The explicit type ID is `com.emojikit.emojicategory`.
    public static var emojiCategory: UTType { get }
}

extension Collection where Self.Element == KeyboardKit.License.Tier {

    public static var allPredefined: [KeyboardKit.License.Tier] { get }
}

extension UITextDocumentProxy where Self == KeyboardKit.KeyboardPreviews.TextDocumentProxy {

    @MainActor @preconcurrency public static var preview: any UITextDocumentProxy { get }
}

extension String {

    /// A carriage return character.
    public static let carriageReturn: String

    /// A new line character.
    public static let newline: String

    /// A space character.
    public static let space: String

    /// A tab character.
    public static let tab: String

    /// A zero width space character used in some RTL languages.
    public static let zeroWidthSpace: String
}

extension String {

    /// A list of currently known alphabetic accent switches.
    public static let alphabeticAccentSwitches: [String]

    /// A list of currently known autocorrect triggers.
    public static let autocorrectTriggers: [String]

    /// A list of currently known sentence delimiters.
    public static let sentenceDelimiters: [String]

    /// A list of currently known whitespaces and newlines.
    public static let whitespacesAndNewlines: [String]

    /// A list of currently known whitespaces and newlines.
    public static let whitespacesAndNewlinesString: String

    /// A list of currently known word delimiters.
    public static let wordDelimiters: [String]
}

extension Collection where Self.Element == String {

    /// A list of currently known alphabetic accent switches.
    public static var alphabeticAccentSwitches: [String] { get }

    /// A list of currently known autocorrect trigger.
    public static var autocorrectTriggers: [String] { get }

    /// A list of currently known sentence delimiters.
    public static var sentenceDelimiters: [String] { get }

    /// A list of currently known word delimiters.
    public static var wordDelimiters: [String] { get }
}

extension String {

    /// Whether this is a known alphabetic accent switch.
    public var isAlphabeticAccentSwitch: Bool { get }

    /// Whether this is a known autocorrect trigger.
    public var isAutocorrectTrigger: Bool { get }

    /// Whether this is a known sentence delimiter.
    public var isSentenceDelimiter: Bool { get }

    /// Whether this is a known word delimiter.
    public var isWordDelimiter: Bool { get }
}

extension Color : Codable {

    /// Initialize a color value from a decoder.
    public init(from decoder: any Decoder) throws

    /**
         Encode the color, using an encoder.
    
         Note that encoding dynamic colors that support features
         like dark mode, high contrast etc. will cause the color
         value that is encoded to only contain the current color
         information.
         */
    public func encode(to encoder: any Encoder) throws
}

extension Collection where Self.Element == KeyboardKit.License {

    /// Filter the collection on a certain search query.
    public func matching(_ query: String) -> [Self.Element]

    /// Sort the collection by name.
    public func sorted() -> [Self.Element]
}

extension Collection where Self.Element == KeyboardKit.License.Environment {

    /// Get all available environments.
    public static var all: [KeyboardKit.License.Environment] { get }
}

extension Locale {

    /// This error is thrown when you try to resolve callout
    /// actions for an unsupported locale.
    public enum KeyboardCalloutActionError : LocalizedError {

        case unsupportedLocale

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: Locale.KeyboardCalloutActionError, b: Locale.KeyboardCalloutActionError) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }

    /// The standard callout actions to use for the locale.
    public var calloutActions: KeyboardKit.Callouts.Actions? { get throws }
}

extension Bundle {

    /// Get whether or not the bundle is an extension.
    public var isExtension: Bool { get }
}

extension View {

    @MainActor @preconcurrency public func scrollGestureState(_ state: KeyboardKit.GestureButtonScrollState) -> some View

}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme.Collection {

    /// Curate the collection by placing some themes first.
    ///
    /// - Parameters:
    ///   - featured: The themes to place first, by default standard, swifty, minimal and colorful.
    public func curated(featured: [String] = ["Standard", "Swifty", "Minimal", "Colorful"]) -> [Self.Element]
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all predefined themes.
    public static var predefined: [KeyboardKit.KeyboardTheme] { get }

    /// Group the collection into named theme collections.
    public func grouped() -> [KeyboardKit.KeyboardTheme.Collection]

    /// Group the collection into named theme collections.
    public func groupedByCollection() -> [KeyboardKit.KeyboardTheme.Collection]
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/standard`` themes.
    public static var predefinedStandard: [KeyboardKit.KeyboardTheme] { get throws }
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/minimal`` themes.
    public static var predefinedMinimal: [KeyboardKit.KeyboardTheme] { get throws }
}

extension Dictionary where Key == Locale, Value == [String : String] {

    /// Create case permutations for each replacement in the
    /// dictionary.
    public func withCasePermutations() -> [Key : Value]
}

extension Collection where Self.Element == KeyboardKit.KeyboardAction {

    /// The standard callout actions to use for the provided
    /// keyboard action and context.
    public static func standardCalloutActions(for action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext) -> [KeyboardKit.KeyboardAction]?
}

extension EnvironmentValues {

    /// Apply a ``Callouts/CalloutStyle``.
    public var keyboardCalloutStyle: KeyboardKit.Callouts.CalloutStyle
}

extension View {

    /// Apply a ``Callouts/CalloutStyle``.
    @MainActor @preconcurrency public func keyboardCalloutStyle(_ style: KeyboardKit.Callouts.CalloutStyle) -> some View

}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func clipboardClipsScreenLocalization(_ value: KeyboardKit.Clipboard.ClipsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var clipboardClipsScreenLocalization: KeyboardKit.Clipboard.ClipsScreenLocalization
}

extension EnvironmentValues {

    /// This value can be used to customize callout actions.
    public var keyboardCalloutActionsBuilder: KeyboardKit.Callouts.ActionsBuilder
}

extension View {

    /// This view modifier can be used to set custom callout
    /// actions to show when long pressing a key.
    ///
    /// The builder is called with an input parameters value
    /// that has contextual information and that can be used
    /// to create a standard value that can be customized as
    /// you like before returning the final result.
    @MainActor @preconcurrency public func keyboardCalloutActions(_ builder: @escaping KeyboardKit.Callouts.ActionsBuilder) -> some View

}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/candyShop`` themes.
    public static var predefinedCandyShop: [KeyboardKit.KeyboardTheme] { get throws }
}

extension View {

    /// Apply a ``KeyboardStatus/SectionStyle``.
    @MainActor @preconcurrency public func keyboardStatusSectionStyle(_ value: KeyboardKit.KeyboardStatus.SectionStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardStatus/SectionStyle``.
    public var keyboardStatusSectionStyle: KeyboardKit.KeyboardStatus.SectionStyle
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/swifty`` themes.
    public static var predefinedSwifty: [KeyboardKit.KeyboardTheme] { get throws }
}

extension View {

    /// Apply a ``KeyboardTheme/ShelfStyle``.
    @MainActor @preconcurrency public func keyboardThemeShelfStyle(_ style: KeyboardKit.KeyboardTheme.ShelfStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardTheme/ShelfStyle``.
    public var keyboardThemeShelfStyle: KeyboardKit.KeyboardTheme.ShelfStyle
}

extension Locale {

    /// This error is thrown when resolving keyboard layouts
    /// for unsupported locales.
    public enum KeyboardLayoutError : LocalizedError {

        case unsupportedLocale

        /// Returns a Boolean value indicating whether two values are equal.
        ///
        /// Equality is the inverse of inequality. For any values `a` and `b`,
        /// `a == b` implies that `a != b` is `false`.
        ///
        /// - Parameters:
        ///   - lhs: A value to compare.
        ///   - rhs: Another value to compare.
        public static func == (a: Locale.KeyboardLayoutError, b: Locale.KeyboardLayoutError) -> Bool

        /// Hashes the essential components of this value by feeding them into the
        /// given hasher.
        ///
        /// Implement this method to conform to the `Hashable` protocol. The
        /// components used for hashing must be the same as the components compared
        /// in your type's `==` operator implementation. Call `hasher.combine(_:)`
        /// with each of these components.
        ///
        /// - Important: In your implementation of `hash(into:)`,
        ///   don't call `finalize()` on the `hasher` instance provided,
        ///   or replace it with a different instance.
        ///   Doing so may become a compile-time error in the future.
        ///
        /// - Parameter hasher: The hasher to use when combining the components
        ///   of this instance.
        public func hash(into hasher: inout Hasher)

        /// The hash value.
        ///
        /// Hash values are not guaranteed to be equal across different executions of
        /// your program. Do not save hash values to use during a future execution.
        ///
        /// - Important: `hashValue` is deprecated as a `Hashable` requirement. To
        ///   conform to `Hashable`, implement the `hash(into:)` requirement instead.
        ///   The compiler provides an implementation for `hashValue` for you.
        public var hashValue: Int { get }
    }

    /// The standard layout to use for the provided context.
    public func keyboardLayout(for context: KeyboardKit.KeyboardContext) throws -> KeyboardKit.KeyboardLayout
}

extension View {

    /// Apply a ``KeyboardStatus/SectionLocalization``.
    @MainActor @preconcurrency public func keyboardStatusSectionLocalization(_ value: KeyboardKit.KeyboardStatus.SectionLocalization) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardStatus/SectionLocalization``.
    public var keyboardStatusSectionLocalization: KeyboardKit.KeyboardStatus.SectionLocalization
}

/// This extension makes `Dictionary` able to store `Codable`
/// types, by serializing the collection to JSON.
///
/// > Important: JSON encoding may cause important type data
/// to disappear. For instance, JSON encoding a `Color` will
/// not include any information about alternate color values
/// for light and dark mode, high constrasts, etc.
extension Dictionary : @retroactive RawRepresentable where Key : Decodable, Key : Encodable, Value : Decodable, Value : Encodable {

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String
}

/// This extension makes `Array` able to store Codable types,
/// by serializing the collection to JSON.
///
/// > Important: JSON encoding may cause important type data
/// to disappear. For instance, JSON encoding a `Color` will
/// not include any information about alternate color values
/// for light and dark mode, high constrasts, etc.
extension Array : @retroactive RawRepresentable where Element : Decodable, Element : Encodable {

    /// Creates a new instance with the specified raw value.
    ///
    /// If there is no value of the type that corresponds with the specified raw
    /// value, this initializer returns `nil`. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     print(PaperSize(rawValue: "Legal"))
    ///     // Prints "Optional(PaperSize.Legal)"
    ///
    ///     print(PaperSize(rawValue: "Tabloid"))
    ///     // Prints "nil"
    ///
    /// - Parameter rawValue: The raw value to use for the new instance.
    public init?(rawValue: String)

    /// The corresponding value of the raw type.
    ///
    /// A new instance initialized with `rawValue` will be equivalent to this
    /// instance. For example:
    ///
    ///     enum PaperSize: String {
    ///         case A4, A5, Letter, Legal
    ///     }
    ///
    ///     let selectedSize = PaperSize.Letter
    ///     print(selectedSize.rawValue)
    ///     // Prints "Letter"
    ///
    ///     print(selectedSize == PaperSize(rawValue: selectedSize.rawValue)!)
    ///     // Prints "true"
    public var rawValue: String { get }

    /// The raw type that can be used to represent all values of the conforming
    /// type.
    ///
    /// Every distinct value of the conforming type has a corresponding unique
    /// value of the `RawValue` type, but there may be values of the `RawValue`
    /// type that don't have a corresponding value of the conforming type.
    public typealias RawValue = String
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/tron`` themes.
    public static var predefinedTron: [KeyboardKit.KeyboardTheme] { get throws }
}

extension Array where Element == KeyboardKit.KeyboardLayout.Item {

    /// Whether the row has a key for a certain action.
    public func hasKey(for action: KeyboardKit.KeyboardAction) -> Bool

    /// Whether the row has a certain switcher.
    public func hasKeyboardSwitcher(_ type: KeyboardKit.Keyboard.KeyboardType) -> Bool
}

extension Array where Element == [KeyboardKit.KeyboardLayout.Item] {

    /// Whether the rows has a key for a certain action.
    public func hasKey(for action: KeyboardKit.KeyboardAction) -> Bool

    /// Whether the rows have a certain switcher.
    public func hasKeyboardSwitcher(_ type: KeyboardKit.Keyboard.KeyboardType) -> Bool
}

extension Locale : KeyboardKit.LicenseFeature {

    /// The unique feature ID for the keyboard locale.
    public var featureId: String? { get }

    /// The tier that unlocks the feature, if any.
    public var unlockedByTier: KeyboardKit.License.Tier? { get }
}

extension Collection where Self.Element == KeyboardKit.Autocomplete.Suggestion {

    /// Check if the collection contains a suggestion with a
    /// certain `word`.
    public func contains(_ word: String) -> Bool

    /// Adjust the collection to either keep or remove items
    /// that are autocorrecting.
    public func withAutocorrectEnabled(_ isEnabled: Bool) -> [Self.Element]
}

extension View {

    /// Apply a custom keyboard localization.
    @MainActor @preconcurrency public func clipboardKeyboardLocalization(_ value: KeyboardKit.Clipboard.KeyboardLocalization) -> some View

}

@available(iOS 15.0, macOS 12.0, tvOS 15.0, watchOS 9.0, *)
extension View {

    /// Apply a `FocusState` with a `doneButton` that slides
    /// in and out as the view gets and loses focus.
    @MainActor @preconcurrency public func focused<DoneButton>(_ value: FocusState<Bool>.Binding, @ViewBuilder doneButton: @escaping () -> DoneButton) -> some View where DoneButton : View

}

extension View {

    /// This view modifier can be used to convert views into
    /// keyboard button that triggers a certain action.
    ///
    /// This view will apply correct styles for the provided
    /// action and style, and will apply correct gestures to
    /// make sure that the action handler is used correctly.
    ///
    /// The `edgeInsets` init parameter can be used to apply
    /// intrinsic insets within the interactable button area.
    ///
    /// The reason why this view modifier exists, is that it
    /// is used by both the keyboard button and the keyboard
    /// view item. We should consolidate these two views, to
    /// make the architecture less confusing.
    ///
    /// - Parameters:
    ///   - action: The keyboard action to trigger.
    ///   - style: The keyboard style to apply.
    ///   - actionHandler: The keyboard action handler to use.
    ///   - repeatTimer: The repeat gesture timer to use, if any.
    ///   - keyboardContext: The keyboard context to use.
    ///   - calloutContext: The callout context to affect, if any.
    ///   - additionalTapArea: An additional tap area in points, if any.
    ///   - edgeInsets: The edge insets to apply to the interactable area, if any.
    ///   - isPressed: An optional binding that can observe the button pressed state.
    ///   - isGestureAutoCancellable: Whether an aborted gesture will auto-cancel itself, by default `false`.
    ///   - scrollState: The scroll state to use, if any.
    ///   - releaseOutsideTolerance: The button sized percentage outside the button that still counts as a release, by default `1`.
    @MainActor @preconcurrency public func keyboardButton(for action: KeyboardKit.KeyboardAction, style: KeyboardKit.Keyboard.ButtonStyle, actionHandler: any KeyboardKit.KeyboardActionHandler, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, keyboardContext: KeyboardKit.KeyboardContext, calloutContext: KeyboardKit.CalloutContext?, additionalTapArea: Double? = nil, edgeInsets: EdgeInsets = .init(), isPressed: Binding<Bool> = .constant(false), isGestureAutoCancellable: Bool? = nil, scrollState: KeyboardKit.GestureButtonScrollState? = nil, releaseOutsideTolerance: Double? = nil, secondaryActionState: Binding<KeyboardKit.Keyboard.SwipeDownActionState?>? = nil) -> some View


    @MainActor @preconcurrency public func font(for style: KeyboardKit.Keyboard.ButtonStyle, action: KeyboardKit.KeyboardAction, context: KeyboardKit.KeyboardContext) -> Font

    /// Apply keyboard accessibility for the provided action.
    @MainActor @preconcurrency public func keyboardButtonAccessibility(for action: KeyboardKit.KeyboardAction) -> some View

}

extension View {

    public typealias KeyboardGestureAction = () -> Void

    public typealias KeyboardDragGestureAction = (_ startLocation: CGPoint, _ location: CGPoint) -> Void

    /// Apply keyboard button gestures that use the provided
    /// `actionHandler` to trigger gesture actions.
    ///
    /// The ``KeyboardAction/nextKeyboard`` action type will
    /// trigger system events instead of this action handler.
    ///
    /// - Parameters:
    ///   - action: The keyboard action to trigger.
    ///   - actionHandler: The keyboard action handler to use.
    ///   - repeatTimer: The repeat gesture timer to use, if any.
    ///   - calloutContext: The callout context to affect, if any.
    ///   - isPressed: An optional binding that can be used to observe the button pressed state.
    ///   - isGestureAutoCancellable: Whether an aborted gesture will auto-cancel itself, by default `false`.
    ///   - scrollState: The scroll state to use, if any.
    ///   - releaseOutsideTolerance: A custom percentage of the button size outside its bounds to count as a release, if any.
    ///   - secondaryActionState: An optional secondary action state.
    @MainActor @preconcurrency public func keyboardButtonGestures(for action: KeyboardKit.KeyboardAction, actionHandler: any KeyboardKit.KeyboardActionHandler, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, calloutContext: KeyboardKit.CalloutContext?, isPressed: Binding<Bool> = .constant(false), isGestureAutoCancellable: Bool? = nil, scrollState: KeyboardKit.GestureButtonScrollState? = nil, releaseOutsideTolerance: Double = 1, secondaryActionState: Binding<KeyboardKit.Keyboard.SwipeDownActionState?>? = nil) -> some View


    /// Apply keyboard button gestures that will trigger the
    /// provided actions.
    ///
    /// The ``KeyboardAction/nextKeyboard`` action type will
    /// trigger system events instead of these actions.
    ///
    /// - Parameters:
    ///   - action: The keyboard action to trigger, if any.
    ///   - repeatTimer: The repeat gesture timer to use, if any.
    ///   - calloutContext: The callout context to affect, if any.
    ///   - isPressed: An optional binding that can be used to observe the button pressed state, if any.
    ///   - isGestureAutoCancellable: Whether an aborted gesture will auto-cancel itself, by default `false`.
    ///   - scrollState: The scroll state to use, if any.
    ///   - releaseOutsideTolerance: A custom percentage of the button size outside its bounds to count as a release, if any.
    ///   - secondaryActionState: An optional secondary action state.
    ///   - doubleTapAction: The action to trigger when the button is double tapped, if any.
    ///   - longPressAction: The action to trigger when the button is long pressed, if any.
    ///   - pressAction: The action to trigger when the button is pressed, if any.
    ///   - releaseAction: The action to trigger when the button is released, regardless of where the gesture ends, if any.
    ///   - repeatAction: The action to trigger when the button is pressed and held, if any.
    ///   - dragAction: The action to trigger when the button is dragged, if any.
    ///   - endAction: The action to trigger when the button gesture ends.
    @MainActor @preconcurrency public func keyboardButtonGestures(action: KeyboardKit.KeyboardAction? = nil, repeatTimer: KeyboardKit.GestureButtonTimer? = nil, calloutContext: KeyboardKit.CalloutContext? = nil, isPressed: Binding<Bool> = .constant(false), isGestureAutoCancellable: Bool? = nil, scrollState: KeyboardKit.GestureButtonScrollState? = nil, releaseOutsideTolerance: Double? = nil, secondaryActionState: Binding<KeyboardKit.Keyboard.SwipeDownActionState?>? = nil, doubleTapAction: Self.KeyboardGestureAction? = nil, longPressAction: Self.KeyboardGestureAction? = nil, pressAction: Self.KeyboardGestureAction? = nil, releaseAction: Self.KeyboardGestureAction? = nil, repeatAction: Self.KeyboardGestureAction? = nil, dragAction: Self.KeyboardDragGestureAction? = nil, endAction: Self.KeyboardGestureAction? = nil) -> some View

}

extension View {

    /// Apply a ``Keyboard/SpacebarLongPressBehavior`` value.
    ///
    /// You can inject this value if you want to use it in a
    /// custom way. ``KeyboardView`` will not use this value.
    @MainActor @preconcurrency public func keyboardSpacebarLongPressBehavior(_ value: KeyboardKit.Keyboard.SpacebarLongPressBehavior?) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Keyboard/SpacebarLongPressBehavior`` value.
    ///
    /// You can inject this value if you want to use it in a
    /// custom way. ``KeyboardView`` will not use this value.
    public var keyboardSpacebarLongPressBehavior: KeyboardKit.Keyboard.SpacebarLongPressBehavior?
}

extension URL {

    /// This URL will deep link to the app's custom settings
    /// in System Settings.
    ///
    /// The URL's behavior is inconsistent. It should always
    /// link to the app's custom settings in System Settings,
    /// but can sometimes just link to the root instead. The
    /// reason for this behavior is currently unknown.
    ///
    /// To improve this link behavior, add an empty Settings
    /// Bundle to your app. This will causes the app to more
    /// consistently open the correct System Settings screen.
    ///
    /// Maybe `appSettings` would be a better name, but this
    /// name was used to reduce the risk of naming conflicts
    /// with other URL extensions.
    public static var systemSettings: URL? { get }
}

extension UIKeyboardType {

    /// The ``Keyboard/KeyboardType`` this type represents.
    public var keyboardType: KeyboardKit.Keyboard.KeyboardType? { get }
}

extension Collection where Self.Element == KeyboardKit.Keyboard.AddedLocale {

    /// A list of all supported locales.
    public static var keyboardKitSupported: [KeyboardKit.Keyboard.AddedLocale] { get }

    /// Get the first locale for a certain locale and layout.
    public func first(of locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType?) -> Self.Element?

    /// Get the first locale for a certain locale and layout.
    public func firstIndex(of locale: Locale, layoutType: KeyboardKit.Keyboard.LayoutType?) -> Self.Index?
}

extension View {

    /// Apply a ``Keyboard/ToolbarStyle``.
    @MainActor @preconcurrency public func keyboardToolbarStyle(_ style: KeyboardKit.Keyboard.ToolbarStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Keyboard/ToolbarStyle``.
    public var keyboardToolbarStyle: KeyboardKit.Keyboard.ToolbarStyle
}

extension UIReturnKeyType {

    /// The ``Keyboard/ReturnKeyType`` this type represents.
    public var keyboardType: KeyboardKit.Keyboard.ReturnKeyType { get }
}

extension UITextAutocapitalizationType {

    /// Map this type to a ``Keyboard/AutocapitalizationType``.
    public var keyboardType: KeyboardKit.Keyboard.AutocapitalizationType { get }
}

extension Collection where Self.Element == KeyboardKit.Fonts.UnicodeFont {

    public func sorted() -> [KeyboardKit.Fonts.UnicodeFont]
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func autocompleteSettingsScreenLocalization(_ value: KeyboardKit.Autocomplete.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var autocompleteSettingsScreenLocalization: KeyboardKit.Autocomplete.SettingsScreenLocalization
}

extension View {

    /// Apply an ``Keyboard/InputToolbarDisplayMode`` value.
    ///
    /// ``KeyboardView`` will use this value, if any, or the
    /// ``KeyboardSettings/inputToolbarDisplayMode``.
    @MainActor @preconcurrency public func keyboardInputToolbarDisplayMode(_ value: KeyboardKit.Keyboard.InputToolbarDisplayMode) -> some View

}

extension EnvironmentValues {

    /// Apply an ``Keyboard/InputToolbarDisplayMode`` value.
    ///
    /// ``KeyboardView`` will use this value, if any, or the
    /// ``KeyboardSettings/inputToolbarDisplayMode``.
    public var keyboardInputToolbarDisplayMode: KeyboardKit.Keyboard.InputToolbarDisplayMode?
}

extension View {

    /// Inject all observable values from the provided state
    /// into the view hierarchy.
    ///
    /// > Important: This view modifier should be added last,
    /// since both views and other modifiers may rely on the
    /// injected state.
    @MainActor @preconcurrency public func keyboardState(_ state: KeyboardKit.Keyboard.State) -> some View

}

extension EnvironmentValues {

    /// The keyboard view background visibility, by default `.automatic`.
    public var keyboardViewBackground: Visibility

    /// The keyboard view style, by default ``KeyboardViewStyle/standard``.
    public var keyboardViewStyle: KeyboardKit.KeyboardViewStyle
}

extension EnvironmentValues {

    public var keyboardViewIsEmbedded: Bool
}

extension View {

    /// Apply a custom keyboard view background visibility.
    @MainActor @preconcurrency public func keyboardViewBackground(_ visibility: Visibility) -> some View


    /// Apply a custom ``KeyboardViewStyle``.
    @MainActor @preconcurrency public func keyboardViewStyle(_ style: KeyboardKit.KeyboardViewStyle) -> some View

}

extension View {

    /// Apply a ``Keyboard/DockEdge`` value.
    @MainActor @preconcurrency public func keyboardDockEdge(_ value: KeyboardKit.Keyboard.DockEdge?) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Keyboard/DockEdge`` value.
    public var keyboardDockEdge: KeyboardKit.Keyboard.DockEdge?
}

extension UserDefaults {

    /// This is an alias for the ``KeyboardSettings/store``.
    public static var keyboardSettings: UserDefaults { get }
}

extension ButtonStyle where Self == KeyboardKit.Keyboard.PlainButtonStyle {

    /// This button style can be used for plain buttons that
    /// are used in keyboard extensions.
    ///
    /// This is needed because the transparent background is
    /// not responding to touches, which this style fixes.
    @MainActor @preconcurrency public static var plainKeyboard: KeyboardKit.Keyboard.PlainButtonStyle { get }

    /// This button style can be used for plain buttons that
    /// are used in keyboard extensions.
    ///
    /// This is needed because the transparent background is
    /// not responding to touches, which this style fixes.
    @MainActor @preconcurrency public static func plainKeyboard(pressedOpacity: Double? = nil) -> Self
}

extension EnvironmentValues {

    /// This value can be used to apply a fixed style.
    ///
    /// > Note: The value returns a red button by default to
    /// make it easy to see when a style hasn't been applied.
    public var keyboardButtonStyle: KeyboardKit.Keyboard.ButtonStyle

    /// This value can be used to apply a dynamic style.
    ///
    /// > Note: The builder returns `nil` by default to make
    /// it possible to check if there is an injected builder.
    public var keyboardButtonStyleBuilder: KeyboardKit.Keyboard.ButtonStyleBuilder
}

extension View {

    /// Apply a custom ``Keyboard/ButtonStyle``.
    @MainActor @preconcurrency public func keyboardButtonStyle(_ style: KeyboardKit.Keyboard.ButtonStyle) -> some View


    /// Apply a custom ``Keyboard/ButtonStyle`` builder that
    /// can vary the button style of any action.
    ///
    /// The builder is called with an input parameters value
    /// that has contextual information and that can be used
    /// to create a standard value that can be customized as
    /// you like before returning the final result.
    @MainActor @preconcurrency public func keyboardButtonStyle(builder: @escaping KeyboardKit.Keyboard.ButtonStyleBuilder) -> some View

}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/neon`` themes.
    public static var predefinedNeon: [KeyboardKit.KeyboardTheme] { get throws }
}

extension Collection where Self.Element == KeyboardKit.KeyboardTheme {

    /// Get all ``KeyboardTheme/colorful(_:)`` themes.
    public static var predefinedColorful: [KeyboardKit.KeyboardTheme] { get throws }
}

extension UITextDocumentProxy {

    /// Whether the proxy is currently reading the full text
    /// from the current document.
    @MainActor @preconcurrency public var isReadingFullDocumentContext: Bool

    /// Get the full document context from the proxy.
    ///
    /// This will read the document by moving the cursor, as
    /// defined by the provided configuration.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/fullDocumentContext`` feature.
    ///
    /// - Parameters:
    ///   - config: The configuration to use, by default ``Proxy/FullDocumentReaderConfiguration/standard``.
    @MainActor @preconcurrency public func fullDocumentContext(config: KeyboardKit.Proxy.FullDocumentReaderConfiguration = .standard) async throws -> KeyboardKit.Proxy.FullDocumentReaderResult

    /// Get the full document context before the text cursor.
    ///
    /// This will read the document by moving the cursor, as
    /// defined by the provided configuration.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/fullDocumentContext`` feature.
    ///
    /// - Parameters:
    ///   - config: The configuration to use, by default ``Proxy/FullDocumentReaderConfiguration/standard``.
    @MainActor @preconcurrency public func fullDocumentContextBeforeInput(config: KeyboardKit.Proxy.FullDocumentReaderConfiguration = .standard) async throws -> String

    /// Get the full document context after the text cursor.
    ///
    /// This will read the document by moving the cursor, as
    /// defined by the provided configuration.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/fullDocumentContext`` feature.
    ///
    /// - Parameters:
    ///   - config: The configuration to use, by default ``Proxy/FullDocumentReaderConfiguration/standard``.
    @MainActor @preconcurrency public func fullDocumentContextAfterInput(config: KeyboardKit.Proxy.FullDocumentReaderConfiguration = .standard) async throws -> String
}

extension UITextDocumentProxy {

    /// Move the text input cursor a certain number of steps.
    @MainActor @preconcurrency public func moveTextInputCursor(steps: Int)

    /// Move the text input cursor to the beginning.
    @MainActor @preconcurrency public func moveTextInputCursorToBeginning()

    /// Move the text input cursor to the end.
    @MainActor @preconcurrency public func moveTextInputCursorToEnd()
}

extension String {

    /// Check if the last character is a sentence delimiter.
    public var hasSentenceDelimiterSuffix: Bool { get }

    /// Check if the last sentence is ended, with or without
    /// trailing whitespace.
    public var isLastSentenceEnded: Bool { get }

    /// Check if the last sentence is ended, with a trailing
    /// whitespace sequence.
    public var isLastSentenceEndedWithTrailingWhitespace: Bool { get }

    /// Get the trimmed content of the last sentence, if any.
    public var lastSentence: String? { get }
}

extension Array where Element == KeyboardKit.KeyboardLayout.Item {

    /// Get a leading character margin action for the row, if any
    public var leadingCharacterMarginAction: KeyboardKit.KeyboardAction { get }

    /// Get a trailing character margin action for the row, if any
    public var trailingCharacterMarginAction: KeyboardKit.KeyboardAction { get }

    /// Get a matching character margin action for an action.
    public func characterMarginAction(for item: KeyboardKit.KeyboardLayout.Item?) -> KeyboardKit.KeyboardAction
}

extension Array where Element == KeyboardKit.EmojiCategory {

    /// Get the category after the provided category.
    public func category(after category: Element) -> Element?

    /// Get the category before the provided category.
    public func category(before category: Element) -> Element?
}

extension Collection where Self.Element == KeyboardKit.EmojiCategory {

    /// Get an ordered list of all standard categories, with
    /// no ``EmojiCategory/frequent`` emojis firstmost.
    public static var standard: [KeyboardKit.EmojiCategory] { get }

    /// Get the first category with a certain ID.
    public func category(withId id: KeyboardKit.EmojiCategory.ID?) -> Self.Element?

    /// Get the first category with a certain emoji.
    public func category(withEmoji emoji: KeyboardKit.Emoji?) -> Self.Element?
}

extension String {

    /// Split the string into a list of characters.
    public var chars: [String] { get }

    /// Split the string into a list of character actions.
    public var charActions: [KeyboardKit.KeyboardAction] { get }

    /// Split the string into a list of character actions.
    public func charActions(removeSpaces: Bool) -> [KeyboardKit.KeyboardAction]
}

extension String {

    /// Whether or not the string is in its lowercased form.
    public var isLowercasedWithUppercaseVariant: Bool { get }

    /// Whether or not the string is in its uppercased form.
    public var isUppercasedWithLowercaseVariant: Bool { get }
}

extension Alert {

    public static func nextWordPredictionConfirmation(settings: KeyboardKit.AutocompleteSettings, title: String, message: String, confirmText: String) -> Alert
}

extension SubmitLabel {

    public static var allCases: [SubmitLabel] { get }
}

extension Array where Element == KeyboardKit.KeyboardAction {

    /// Map a string's characters to a keyboard action row.
    public init(characters: String)

    /// Map a string array to a keyboard action row.
    public init(characters: [String])

    /// Get a matching leading character margin action.
    public var leadingCharacterMarginAction: KeyboardKit.KeyboardAction { get }

    /// Get a matching character margin action.
    public var trailingCharacterMarginAction: KeyboardKit.KeyboardAction { get }

    /// Get a matching character margin action for an action.
    public func characterMarginAction(for action: KeyboardKit.KeyboardAction?) -> KeyboardKit.KeyboardAction
}

extension Array where Element == [KeyboardKit.KeyboardAction] {

    /// Map a string array array to keyboard action rows.
    public init(characters: [[String]])
}

extension Array where Element == KeyboardKit.KeyboardLayout.InputSet.ItemRow {

    /// An internal typealias for cleaner code.
    public typealias Item = KeyboardKit.KeyboardLayout.InputSet.Item

    /// Get all input characters for a case and device type.
    public func characters(for case: KeyboardKit.Keyboard.KeyboardCase, device: KeyboardKit.DeviceType) -> [[String]]

    /// Get all input items for a case and device type.
    public func items(for device: KeyboardKit.DeviceType) -> [[Array<Element>.Item]]
}

extension Bundle {

    /// A non-optional property that can always be used when
    /// validating a license for a certain bundle.
    public var licenseValidationId: String { get }
}

extension UITextField {

    /// Set the left text field image.
    @MainActor @preconcurrency public func setLeftImage(systemName name: String)

    /// Set the left text field image.
    @MainActor @preconcurrency public func setLeftImage(_ image: UIImage?)

    /// Set the right text field image.
    @MainActor @preconcurrency public func setRightImage(systemName name: String)

    /// Set the right text field image.
    @MainActor @preconcurrency public func setRightImage(_ image: UIImage?)
}

extension EnvironmentValues {

    /// This value will inject a config into the environment.
    public var gestureButtonConfiguration: KeyboardKit.GestureButtonConfiguration
}

extension View {

    /// Apply a gesture button config to the view hierarchy.
    @MainActor @preconcurrency public func gestureButtonConfiguration(_ config: KeyboardKit.GestureButtonConfiguration) -> some View

}

extension Font.Weight {

    /// Get the keyboard font weight for the weight.
    public var keyboardWeight: KeyboardKit.KeyboardFont.FontWeight { get }
}

extension Locale {

    /// A list of layout types that the locale supports.
    ///
    /// If a locale has multiple layout types, it can switch
    /// between all of them, otherwise the locale should use
    /// its default layout service.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/keyboardLayoutTypes`` feature.
    public var supportedLayoutTypes: [KeyboardKit.Keyboard.LayoutType] { get throws }
}

extension Collection where Self.Element == KeyboardKit.Keyboard.AddedLocale {

    /// Get a list of the added layouts for a certain locale.
    public func addedLayoutTypes(for locale: Locale) -> [KeyboardKit.Keyboard.LayoutType]

    /// Get a list of layouts that a locale can pick between.
    public func layoutTypePickerItems(for locale: Locale, current: KeyboardKit.Keyboard.LayoutType?) -> [KeyboardKit.Keyboard.LayoutType]
}

extension View {

    /// Apply a custom screen style.
    @MainActor @preconcurrency public func clipboardClipsScreenStyle(_ value: KeyboardKit.Clipboard.ClipsScreenStyle) -> some View

}

extension EnvironmentValues {

    /// A custom screen style.
    public var clipboardClipsScreenStyle: KeyboardKit.Clipboard.ClipsScreenStyle
}

extension View {

    /// Customize the keyboard button gesture configuration.
    @MainActor @preconcurrency public func keyboardButtonGestureConfiguration(_ config: KeyboardKit.GestureButtonConfiguration) -> some View

}

extension View {

    /// This modifier can be applied to any view, to make it
    /// honor `.autocorrectionDisabled()`.
    @MainActor @preconcurrency public func autocorrectionDisabled(with context: KeyboardKit.AutocompleteContext) -> some View

}

extension View {

    /// Apply a ``KeyboardStatus/LabelStyle``.
    @MainActor @preconcurrency public func keyboardStatusLabelStyle(_ style: KeyboardKit.KeyboardStatus.LabelStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardStatus/LabelStyle``.
    public var keyboardStatusLabelStyle: KeyboardKit.KeyboardStatus.LabelStyle
}

extension Collection where Self.Element == KeyboardKit.KeyboardAction {

    /// A list of URL domain-related actions.
    public static var urlDomainActions: [KeyboardKit.KeyboardAction] { get }
}

extension Locale {

    /// This modifier can apply a locale context menu to any
    /// view or keyboard button.
    ///
    /// Use ``SwiftUICore/View/localeContextMenu(for:tapAction:)``
    /// to add a context menu that uses the provided locales
    /// or ``KeyboardContext/enabledLocales``.
    ///
    /// This menu will only be enabled if there are at least
    /// two locales. It will sort and localize the list with
    /// the ``KeyboardContext/localePresentationLocale``.
    ///
    /// The ``KeyboardView`` automatically applies this menu
    /// to all keys that trigger ``KeyboardAction/nextLocale``.
    @MainActor @preconcurrency public struct ContextMenu : ViewModifier {

        /// Create a locale context menu.
        ///
        /// - Parameters:
        ///   - locales: An custom list of locales, if any.
        ///   - tapAction: The action to trigger when a locale is tapped.
        @MainActor @preconcurrency public init(for locales: [Locale]? = nil, tapAction: @escaping () -> Void)

        /// Gets the current body of the caller.
        ///
        /// `content` is a proxy for the view that will have the modifier
        /// represented by `Self` applied to it.
        @MainActor @preconcurrency public func body(content: Locale.ContextMenu.Content) -> some View


        /// The type of view representing the body.
        @available(iOS 8.0, tvOS 9.0, watchOS 2.0, macOS 10.10, *)
        public typealias Body = some View
    }
}

extension View {

    /// Apply a ``Foundation/Locale/ContextMenu`` that lists
    /// a custom list of locales, or all locales that are in
    /// the environment injected ``KeyboardContext``.
    ///
    /// - Parameters:
    ///   - locales: An custom list of locales, if any.
    ///   - tapAction: The action to trigger when a locale is tapped.
    @MainActor @preconcurrency public func localeContextMenu(for locales: [Locale]? = nil, tapAction: @escaping () -> Void) -> some View

}

extension UITextDocumentProxy {

    /// Insert a diacritic and perform matching replacements.
    @MainActor @preconcurrency public func insertDiacritic(_ diacritic: KeyboardKit.Keyboard.Diacritic)
}

extension UIKeyboardType {

    /// The ``Keyboard/InputType`` this type should use.
    public var keyboardInputType: KeyboardKit.Keyboard.InputType { get }
}

extension Collection where Self.Element == KeyboardKit.Autocomplete.Suggestion {

    public static var preview: [KeyboardKit.Autocomplete.Suggestion] { get }
}

extension EnvironmentValues {

    /// This value can be used to inject a theme.
    ///
    /// > Note: The value returns a red button by default to
    /// make it easy to see when a style hasn't been applied.
    public var keyboardTheme: KeyboardKit.KeyboardTheme?
}

extension View {

    /// Apply a keyboard theme to the provided view.
    ///
    /// This takes an optional theme value, since themes are
    /// throwing. You should call this function using `try?`.
    @MainActor @preconcurrency public func keyboardTheme(_ theme: KeyboardKit.KeyboardTheme?) -> some View

}

extension Image {

    public static let keyboardEmoji: Image

    public static let keyboard: Image

    public static let keyboardArrowUp: Image

    public static let keyboardArrowDown: Image

    public static let keyboardArrowLeft: Image

    public static let keyboardArrowRight: Image

    public static let keyboardAudioFeedbackDisabled: Image

    public static let keyboardAudioFeedbackEnabled: Image

    public static let keyboardAutocomplete: Image

    public static let keyboardBackspace: Image

    public static let keyboardBackspaceRtl: Image

    public static let keyboardBrightnessDown: Image

    public static let keyboardBrightnessUp: Image

    public static let keyboardClipboard: Image

    public static let keyboardCommand: Image

    public static let keyboardControl: Image

    public static let keyboardDictation: Image

    public static let keyboardDismiss: Image

    public static let keyboardDone: Image

    public static let keyboardEmail: Image

    public static let keyboardEmojiSymbol: Image

    public static let keyboardFeedback: Image

    public static let keyboardFont: Image

    public static let keyboardGlobe: Image

    public static let keyboardGo: Image

    public static let keyboardGoRtl: Image

    public static let keyboardHapticFeedbackDisabled: Image

    public static let keyboardHapticFeedbackEnabled: Image

    public static let keyboardImages: Image

    public static let keyboardInputCursor: Image

    public static let keyboardJoin: Image

    public static let keyboardJoinRtl: Image

    public static let keyboardNewline: Image

    public static let keyboardNewlineRtl: Image

    public static let keyboardNext: Image

    public static let keyboardNextRtl: Image

    public static let keyboardNone: Image

    public static let keyboardOption: Image

    public static let keyboardRedo: Image

    public static let keyboardSearch: Image

    public static let keyboardSettings: Image

    public static let keyboardShiftCapslockActive: Image

    public static let keyboardShiftCapslockInactive: Image

    public static let keyboardShiftLowercased: Image

    public static let keyboardShiftUppercased: Image

    public static let keyboardSpeaker: Image

    public static let keyboardSpeakerDown: Image

    public static let keyboardSpeakerUp: Image

    public static let keyboardTab: Image

    public static let keyboardTabRtl: Image

    public static let keyboardTheme: Image

    public static let keyboardUndo: Image

    public static let keyboardUrl: Image

    public static let keyboardZeroWidthSpace: Image

    public static let keyboardDockEdgeNone: Image

    public static let keyboardDockEdgeLeading: Image

    public static let keyboardDockEdgeTrailing: Image

    public static let keyboardInputToolbarAutomatic: Image

    public static let keyboardInputToolbarNone: Image

    public static let keyboardInputToolbarCharacters: Image

    public static let keyboardInputToolbarNumbers: Image

    public static let keyboardInputToolbarRecentEmojis: Image

    public static func keyboardAudioFeedback(enabled: Bool) -> Image

    public static func keyboardBackspace(for locale: Locale) -> Image

    public static func keyboardDockEdge(_ edge: KeyboardKit.Keyboard.DockEdge?) -> Image

    public static func keyboardGo(for locale: Locale) -> Image

    public static func keyboardHapticFeedback(enabled: Bool) -> Image

    public static func keyboardInputToolbarType(_ type: KeyboardKit.KeyboardSettings.InputToolbarType, automaticIcon: Image? = .keyboardInputToolbarAutomatic) -> Image?

    public static func keyboardJoin(for locale: Locale) -> Image

    public static func keyboardNewline(for locale: Locale) -> Image

    public static func keyboardNext(for locale: Locale) -> Image

    public static func keyboardSpacebarLongPress(_ behavior: KeyboardKit.Keyboard.SpacebarLongPressBehavior?) -> Image?

    public static func keyboardSpacebarMenu(_ value: KeyboardKit.Keyboard.SpacebarMenuType?, fallback: Image? = .keyboardNone) -> Image?

    public static func keyboardTab(for locale: Locale) -> Image

    public static func keyboardShift(_ casing: KeyboardKit.Keyboard.KeyboardCase) -> Image
}

extension Color {

    /// This color can be used instead of `clear` to make it
    /// register touches & gestures.
    public static var clearInteractable: Color { get }

    /// The standard keyboard background color.
    public static let keyboardBackground: Color

    /// The standard keyboard background color for dark appearance.
    public static let keyboardBackgroundForDarkAppearance: Color

    /// The standard keyboard button background color.
    public static let keyboardButtonBackground: Color

    /// The standard keyboard button background color, adjusted for the iOS color scheme bug.
    public static let keyboardButtonBackgroundForColorSchemeBug: Color

    /// The standard keyboard button background color for dark appearance.
    public static let keyboardButtonBackgroundForDarkAppearance: Color

    /// The standard keyboard button foreground color.
    public static let keyboardButtonForeground: Color

    /// The standard keyboard button foreground color for dark appearance.
    public static let keyboardButtonForegroundForDarkAppearance: Color

    /// The standard keyboard button shadow color.
    public static let keyboardButtonShadow: Color

    /// The standard dark keyboard button background color.
    public static let keyboardDarkButtonBackground: Color

    /// The standard dark keyboard button background color for the iOS color scheme bug.
    public static let keyboardDarkButtonBackgroundForColorSchemeBug: Color

    /// The standard dark keyboard button background color for dark apperance.
    public static let keyboardDarkButtonBackgroundForDarkAppearance: Color

    /// The standard dark keyboard button foreground color.
    public static var keyboardDarkButtonForeground: Color { get }

    /// The standard dark keyboard button foreground color for dark apperance.
    public static var keyboardDarkButtonForegroundForDarkAppearance: Color { get }
}

extension Color {

    /// The standard keyboard button background color.
    public static func keyboardBackground(for colorScheme: ColorScheme) -> Color

    /// The standard keyboard button background color.
    public static func keyboardButtonBackground(for colorScheme: ColorScheme) -> Color

    /// The standard keyboard button background color.
    ///
    /// This color is adjusted for display with Liquid Glass.
    public static func keyboardButtonBackgroundLiquid(for colorScheme: ColorScheme) -> Color

    /// The standard keyboard button foreground color.
    public static func keyboardButtonForeground(for colorScheme: ColorScheme) -> Color

    /// The standard keyboard button shadow color.
    public static func keyboardButtonShadow(for colorScheme: ColorScheme) -> Color

    /// The standard dark keyboard button background color.
    public static func keyboardDarkButtonBackground(for colorScheme: ColorScheme) -> Color

    /// The standard dark keyboard button foreground color.
    public static func keyboardDarkButtonForeground(for colorScheme: ColorScheme) -> Color
}

extension OpenURLAction {

    /// Try open a certain host application.
    ///
    /// You can check ``KeyboardHostApplication/canBeOpened`` in
    /// advance to see if the app will be opened.
    @MainActor @preconcurrency public func tryOpen(_ app: KeyboardKit.KeyboardHostApplication)
}

extension Locale {

    /// This view can be used when listing a locale.
    @MainActor @preconcurrency public struct ListItem : View {

        @MainActor @preconcurrency public init(locale: Locale, displayLocale: Locale = .current, dragHandle: Bool = false, subtitle: String? = nil, layoutTypeDescription: String? = nil)

        /// The content and behavior of the view.
        ///
        /// When you implement a custom view, you must implement a computed
        /// `body` property to provide the content for your view. Return a view
        /// that's composed of built-in views that SwiftUI provides, plus other
        /// composite views that you've already defined:
        ///
        ///     struct MyView: View {
        ///         var body: some View {
        ///             Text("Hello, World!")
        ///         }
        ///     }
        ///
        /// For more information about composing views and a view hierarchy,
        /// see <doc:Declaring-a-Custom-View>.
        @MainActor @preconcurrency public var body: some View { get }

        /// The type of view representing the body of this view.
        ///
        /// When you create a custom view, Swift infers this type from your
        /// implementation of the required ``View/body-swift.property`` property.
        @available(iOS 8.0, tvOS 9.0, watchOS 2.0, macOS 10.10, *)
        public typealias Body = some View
    }
}

extension View {

    /// Apply a custom screen visibility.
    @MainActor @preconcurrency public func autocompleteSettingsScreenVisibility(_ value: KeyboardKit.Autocomplete.SettingsScreenVisibility) -> some View

}

extension EnvironmentValues {

    /// A custom screen visibility.
    public var autocompleteSettingsScreenVisibility: KeyboardKit.Autocomplete.SettingsScreenVisibility
}

extension Array where Element == KeyboardKit.Emoji {

    /// Get the emoji at a certain index, if any.
    public func emoji(at index: Int) -> KeyboardKit.Emoji?

    /// The first index of a certain emoji, if any.
    public func firstIndex(of emoji: KeyboardKit.Emoji) -> Int?
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func dictationSettingsScreenLocalization(_ value: KeyboardKit.Dictation.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var dictationSettingsScreenLocalization: KeyboardKit.Dictation.SettingsScreenLocalization
}

extension String {

    /// Check if the last character is a word delimiter.
    public var hasWordDelimiterSuffix: Bool { get }

    /// Get the word or fragment at the start of the string.
    public var wordFragmentAtStart: String { get }

    /// Get the word or fragment at the end of the string.
    public var wordFragmentAtEnd: String { get }

    /// Get the word at a certain index in the string.
    public func word(at index: Int) -> String?

    /// Get the word fragment before a certain string index.
    public func wordFragment(before index: Int) -> String

    /// Get the word fragment after a certain string index.
    public func wordFragment(after index: Int) -> String
}

extension SFSpeechRecognizerAuthorizationStatus {

    /// Get a dictation-mapped status.
    public var keyboardDictationStatus: KeyboardKit.Dictation.AuthorizationStatus { get }
}

extension Locale {

    /// This locale-based dictionary can store item types to
    /// let you resolve them based on locale.
    ///
    /// When resolving items, this dictionary will first try
    /// to find value for the locale's `identifier` then the
    /// locale's `languageCode`.
    public struct Dictionary<ItemType> {

        /// Create a dictionary with locale entries.
        public init(_ dict: [Locale : ItemType])

        /// Create a dictionary with locale entries.
        public init(_ dict: [Locale.Dictionary<ItemType>.LocaleIdentifier : ItemType] = [:])

        /// This alias indicates the dictionary key type.
        public typealias LocaleIdentifier = String

        /// The locale/valye dicitionary.
        public var dictionary: [Locale.Dictionary<ItemType>.LocaleIdentifier : ItemType]
    }
}

extension UITextDocumentProxy {

    /// The word at the cursor location, if any.
    @MainActor @preconcurrency public var currentWord: String? { get }

    /// The word segment before the cursor location, if any.
    @MainActor @preconcurrency public var currentWordPreCursorPart: String? { get }

    /// The word segment after the cursor location, if any.
    @MainActor @preconcurrency public var currentWordPostCursorPart: String? { get }

    /// Whether the proxy has a current word.
    @MainActor @preconcurrency public var hasCurrentWord: Bool { get }

    /// Whether the cursor is at the beginning of a word.
    @MainActor @preconcurrency public var isCursorAtNewWord: Bool { get }

    /// Whether the cursor is at the end of the current word.
    @MainActor @preconcurrency public var isCursorAtTheEndOfTheCurrentWord: Bool { get }

    /// The last ended word right before the cursor, if any.
    @MainActor @preconcurrency public var wordBeforeInput: String? { get }

    /// Replace the current word with a text.
    @MainActor @preconcurrency public func replaceCurrentWord(with replacement: String)

    /// Replace the current word pre cursor part with a text.
    @MainActor @preconcurrency public func replaceCurrentWordPreCursorPart(with replacement: String, additionalDeletionCount: Int = 0)
}

extension UITextDocumentProxy {

    @MainActor @preconcurrency public var wordDelimiters: [String] { get }
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func clipboardSettingsScreenLocalization(_ value: KeyboardKit.Clipboard.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var clipboardSettingsScreenLocalization: KeyboardKit.Clipboard.SettingsScreenLocalization
}

extension View {

    /// Apply a custom set of fonts to list in font settings.
    @MainActor @preconcurrency public func fontSettingsScreenFonts(_ value: [KeyboardKit.Fonts.UnicodeFont]) -> some View

}

extension EnvironmentValues {

    /// A custom set of fonts to list in font settings.
    public var fontSettingsScreenFonts: [KeyboardKit.Fonts.UnicodeFont]
}

extension Collection where Self.Element == KeyboardKit.Emoji {

    /// Get all available emojis from all categories.
    ///
    /// This property only returns emojis that are available
    /// the current runtime. 
    public static var all: [KeyboardKit.Emoji] { get }
}

extension Locale {

    /// A list of KeyboardKit supported locales.
    ///
    /// KeyboardKit Pro unlocks localized secondary callouts,
    /// layouts and services for all supported locales.
    public static var keyboardKitSupported: [Locale] { get }
}

extension Collection where Self.Element == Locale {

    public static var keyboardKitSupported: [Locale] { get }
}

extension Locale {

    public static var english: Locale { get }

    public static var albanian: Locale { get }

    public static var arabic: Locale { get }

    public static var armenian: Locale { get }

    public static var azerbaijani: Locale { get }

    public static var belarusian: Locale { get }

    public static var bengali: Locale { get }

    public static var bulgarian: Locale { get }

    public static var catalan: Locale { get }

    public static var cherokee: Locale { get }

    public static var chuvash: Locale { get }

    public static var croatian: Locale { get }

    public static var czech: Locale { get }

    public static var danish: Locale { get }

    public static var dutch: Locale { get }

    public static var dutch_belgium: Locale { get }

    public static var english_australia: Locale { get }

    public static var english_canada: Locale { get }

    public static var english_gb: Locale { get }

    public static var english_us: Locale { get }

    public static var estonian: Locale { get }

    public static var faroese: Locale { get }

    public static var filipino: Locale { get }

    public static var finnish: Locale { get }

    public static var french: Locale { get }

    public static var french_canada: Locale { get }

    public static var french_belgium: Locale { get }

    public static var french_switzerland: Locale { get }

    public static var georgian: Locale { get }

    public static var german: Locale { get }

    public static var german_austria: Locale { get }

    public static var german_switzerland: Locale { get }

    public static var greek: Locale { get }

    public static var hawaiian: Locale { get }

    public static var hebrew: Locale { get }

    public static var hungarian: Locale { get }

    public static var icelandic: Locale { get }

    public static var inari_sami: Locale { get }

    public static var indonesian: Locale { get }

    public static var irish: Locale { get }

    public static var italian: Locale { get }

    public static var kazakh: Locale { get }

    public static var kurdish_sorani: Locale { get }

    public static var kurdish_sorani_arabic: Locale { get }

    public static var kurdish_sorani_pc: Locale { get }

    public static var latvian: Locale { get }

    public static var lithuanian: Locale { get }

    public static var macedonian: Locale { get }

    public static var malay: Locale { get }

    public static var maltese: Locale { get }

    public static var mongolian: Locale { get }

    public static var northern_sami: Locale { get }

    public static var norwegian: Locale { get }

    public static var norwegian_nynorsk: Locale { get }

    public static var persian: Locale { get }

    public static var polish: Locale { get }

    public static var portuguese: Locale { get }

    public static var portuguese_brazil: Locale { get }

    public static var romanian: Locale { get }

    public static var russian: Locale { get }

    public static var serbian: Locale { get }

    public static var serbian_latin: Locale { get }

    public static var shughni_tajik: Locale { get }

    public static var slovak: Locale { get }

    public static var slovenian: Locale { get }

    public static var spanish: Locale { get }

    public static var spanish_latinAmerica: Locale { get }

    public static var spanish_mexico: Locale { get }

    public static var swedish: Locale { get }

    public static var swahili: Locale { get }

    public static var turkish: Locale { get }

    public static var ukrainian: Locale { get }

    public static var uzbek: Locale { get }

    public static var vietnamese: Locale { get }

    public static var welsh: Locale { get }
}

extension Locale {

    /// The KeyboardKit-specific name.
    public var keyboardKitName: String { get }
}

extension Locale {

    /// The locale-specific flag emoji.
    ///
    /// This will use a white placeholder flag if the locale
    /// doesn't have a proper emoji flag.
    public var flag: String { get }
}

extension Character {

    /// Whether the character is a an emoji.
    ///
    /// This property will manually add later version emojis
    /// to the check, since they don't
    public var isEmoji: Bool { get }

    /// Whether the character is a multi-scalar emoji.
    public var isCombinedEmoji: Bool { get }

    /// Whether the character is an emoji which was released
    /// in version 15 or later.
    public var isVersion15OrLaterEmoji: Bool { get }

    /// Whether the character is a one-scalar emoji.
    public var isSimpleEmoji: Bool { get }
}

extension View {

    /// Prepare the view with preview environments.
    @MainActor @preconcurrency public func keyboardPreview(keyboardContext: KeyboardKit.KeyboardContext = .preview) -> some View

}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func feedbackSettingsScreenLocalization(_ value: KeyboardKit.Feedback.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var feedbackSettingsScreenLocalization: KeyboardKit.Feedback.SettingsScreenLocalization
}

extension View {

    /// Apply a ``KeyboardApp/HomeScreenVisibility``.
    @MainActor @preconcurrency public func keyboardAppHomeScreenVisibility(_ value: KeyboardKit.KeyboardApp.HomeScreenVisibility) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardApp/HomeScreenVisibility``.
    public var keyboardAppHomeScreenVisibility: KeyboardKit.KeyboardApp.HomeScreenVisibility
}

extension View {

    /// Apply a ``Dictation/BarVisualizerStyle``.
    @MainActor @preconcurrency public func dictationBarVisualizerStyle(_ style: KeyboardKit.Dictation.BarVisualizerStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Dictation/BarVisualizerStyle`` collection.
    public var dictationBarVisualizerStyle: KeyboardKit.Dictation.BarVisualizerStyle
}

extension View {

    /// Apply this view modifier to any view in your app, to
    /// make it start dictation and show a provided `overlay`
    /// when it's launched with a deep link that matches the
    /// ``KeyboardContext/app``.
    @MainActor @preconcurrency public func keyboardDictation<Overlay>(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, service: (any KeyboardKit.DictationService)? = nil, openUrl: OpenURLAction, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer, @ViewBuilder overlay: () -> Overlay) -> some View where Overlay : View


    /// Apply this view modifier to any view in your app, to
    /// make it start in-app dictation if dictation has been
    /// started by the keyboard.
    @MainActor @preconcurrency public func keyboardDictationIfStartedOnAppear(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, service: (any KeyboardKit.DictationService)? = nil, openUrl: OpenURLAction, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer) -> some View


    /// Apply this view modifier to any view in your app, to
    /// make it start dictation if it's launched with a deep
    /// link that matches the ``KeyboardContext/app``.
    @MainActor @preconcurrency public func keyboardDictationOnDeepLink(dictationContext: KeyboardKit.DictationContext, keyboardContext: KeyboardKit.KeyboardContext, service: (any KeyboardKit.DictationService)? = nil, openUrl: OpenURLAction, speechRecognizer: any KeyboardKit.DictationSpeechRecognizer) -> some View


    /// Apply this view modifier to any view in your app, to
    /// show an overlay while ``DictationContext/isDictating``
    /// is `true`.
    @MainActor @preconcurrency public func keyboardDictationOverlay<Overlay>(dictationContext: KeyboardKit.DictationContext, @ViewBuilder overlay: () -> Overlay) -> some View where Overlay : View

}

extension View {

    /// Apply a ``Dictation/ScreenStyle``.
    @MainActor @preconcurrency public func dictationScreenStyle(_ style: KeyboardKit.Dictation.ScreenStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``Dictation/ScreenStyle``.
    public var dictationScreenStyle: KeyboardKit.Dictation.ScreenStyle
}

extension View {

    /// Apply a ``KeyboardTheme/SettingsScreenStyle``.
    @MainActor @preconcurrency public func themeSettingsScreenStyle(_ value: KeyboardKit.KeyboardTheme.SettingsScreenStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardTheme/SettingsScreenStyle``.
    public var themeSettingsScreenStyle: KeyboardKit.KeyboardTheme.SettingsScreenStyle
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func themeSettingsScreenLocalization(_ value: KeyboardKit.KeyboardTheme.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var themeSettingsScreenLocalization: KeyboardKit.KeyboardTheme.SettingsScreenLocalization
}

extension View {

    /// Apply a ``KeyboardApp/HomeScreenStyle``.
    @MainActor @preconcurrency public func keyboardAppHomeScreenStyle(_ style: KeyboardKit.KeyboardApp.HomeScreenStyle) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardApp/HomeScreenStyle``.
    public var keyboardAppHomeScreenStyle: KeyboardKit.KeyboardApp.HomeScreenStyle
}

extension View {

    /// Apply a ``KeyboardTheme/SettingsScreenThemes`` collection.
    @MainActor @preconcurrency public func themeSettingsScreenThemes(_ value: KeyboardKit.KeyboardTheme.SettingsScreenThemes) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardTheme/SettingsScreenThemes`` collection.
    public var themeSettingsScreenThemes: KeyboardKit.KeyboardTheme.SettingsScreenThemes
}

extension View {

    /// Apply a ``KeyboardApp/HomeScreenLocalization``.
    @MainActor @preconcurrency public func keyboardAppHomeScreenLocalization(_ value: KeyboardKit.KeyboardApp.HomeScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// Apply a ``KeyboardApp/HomeScreenLocalization``.
    public var keyboardAppHomeScreenLocalization: KeyboardKit.KeyboardApp.HomeScreenLocalization
}

extension View {

    /// Apply a custom screen visibility.
    @MainActor @preconcurrency public func keyboardSettingsScreenVisibility(_ value: KeyboardKit.Keyboard.SettingsScreenVisibility) -> some View

}

extension EnvironmentValues {

    /// A custom screen visibility.
    public var keyboardSettingsScreenVisibility: KeyboardKit.Keyboard.SettingsScreenVisibility
}

extension Image {

    /// An emoji category icon for the provided category.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/emojiAddons`` feature.
    public static func emojiCategory(_ category: KeyboardKit.EmojiCategory) throws -> Image

    /// An emoji category search icon.
    ///
    /// > Important: This requires that your license unlocks
    /// the ``ProFeature/emojiAddons`` feature.
    public static var emojiCategorySearch: Image { get throws }
}

extension UIKeyboardType {

    /// Whether or not this type prefers autocomplete.
    ///
    /// Use both ``Keyboard/KeyboardType`` & `UIKeyboardType`
    /// to compute this, since many native keyboard types do
    /// not map to a corresponding ``Keyboard/KeyboardType``.
    ///
    /// > Important: Since KeyboardKit by default always has
    /// an autocomplete toolbar, the property is used to set
    /// if *autocorrect* is contextually enabled.
    public var prefersAutocomplete: Bool { get }
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func keyboardSettingsScreenLocalization(_ value: KeyboardKit.Keyboard.SettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var keyboardSettingsScreenLocalization: KeyboardKit.Keyboard.SettingsScreenLocalization
}

extension View {

    /// Apply a custom screen localization.
    @MainActor @preconcurrency public func localeSettingsScreenLocalization(_ value: KeyboardKit.Keyboard.LocaleSettingsScreenLocalization) -> some View

}

extension EnvironmentValues {

    /// A custom screen localization.
    public var localeSettingsScreenLocalization: KeyboardKit.Keyboard.LocaleSettingsScreenLocalization
}

extension URLRequest {

    public enum URLStringError : Error {

        case invalidUrl(String)
    }
}

extension View {

    /// Setup the view as a keyboard callout container.
    ///
    /// - Parameters:
    ///   - context: The callout context to use.
    @MainActor @preconcurrency public func keyboardCalloutContainer(with context: KeyboardKit.CalloutContext) -> some View


    /// Setup the view as a keyboard callout container.
    ///
    /// - Parameters:
    ///   - calloutContext: The callout context to use.
    @MainActor @preconcurrency public func keyboardCalloutContainer(calloutContext: KeyboardKit.CalloutContext) -> some View

}

extension String {

    /// Check if the string contains any unclosed quotations.
    public func hasUnclosedAlternateQuotation(for locale: Locale) -> Bool

    /// Check if the string contains any unclosed quotations.
    public func hasUnclosedQuotation(for locale: Locale) -> Bool

    /// Check if the string contains any unclosed quotations.
    public func hasUnclosedQuotation(beginDelimiter begin: String, endDelimiter end: String) -> Bool

    /// Check if a certain text that is about to be appended
    /// to the string should be replaced with something else.
    public func preferredQuotationReplacement(whenAppending text: String, for locale: Locale) -> String?

    /// Wrap the string in quotation delimiters.
    public func quoted(for locale: Locale) -> String

    /// Wrap the string in alternate quotation delimiters.
    public func alternateQuoted(for locale: Locale) -> String
}

extension Collection where Self.Element : Equatable {

    /// Insert a certain an element first in the collection.
    public func insertingFirst(_ element: Self.Element) -> [Self.Element]

    /// Remove a certain an element from the collection.
    public func removing(_ element: Self.Element) -> [Self.Element]
}

extension Collection where Self.Element == Locale {

    /// Sort the collection by the localized item name, then
    /// optionally place a locale first.
    ///
    /// - Parameters:
    ///   - first: The locale to place first, by default `nil`.
    public func sorted(insertFirst first: Self.Element? = nil) -> [Self.Element]

    /// Sort the collection by the localized item name for a
    /// certain locale, then optionally place a locale first.
    ///
    /// - Parameters:
    ///   - locale: The locale to use to get the localized name.
    ///   - first: The locale to place first, by default `nil`.
    public func sorted(in locale: Locale?, insertFirst first: Self.Element? = nil) -> [Self.Element]

    /// Sort the collection by a sort comparator, then place
    /// a locale first in the sorted result.
    ///
    /// - Parameters:
    ///   - comparator: The sort comparator to use.
    ///   - first: The locale to place first, by default `nil`.
    public func sorted(by comparator: (Self.Element, Self.Element) -> Bool, insertFirst first: Self.Element?) -> [Self.Element]
}

extension UITextDocumentProxy {

    /// Deselect the currently selected text, by placing the
    /// input cursor at the leading or trailing edge.
    ///
    /// - Parameters:
    ///   - edge: The leading or trailing edge, by default `leading`.
    @MainActor @preconcurrency public func deselectSelectedText(placingInputCursorAtEdge edge: KeyboardKit.Proxy.SelectionEdge = .leading)
}

extension Locale {

    /// Try to create a locale from a fuzzy name.
    public init?(fuzzyName: String)
}

